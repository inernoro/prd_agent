# Agent Dashboard 设计文档

> **版本**: v1.0
> **创建日期**: 2026-02-03
> **状态**: 实现中
> **作者**: Claude Code

## 1. 概述

### 1.1 背景

为 "Agent 体验者" 角色打造独立的后台操作页面，设计灵感来源于 macOS 15 控制中心（Control Center）。核心理念是：

- **图标为王**：以大图标网格呈现各 Agent 入口
- **快速切换**：全局快捷键 `Cmd/Ctrl + K` 随时唤起迷你切换面板
- **沉浸体验**：液态玻璃视觉风格，与现有设计语言统一

### 1.2 目标用户

- Agent 体验者角色
- 需要频繁在多个 Agent 之间切换的用户
- 希望快速访问常用功能的管理员

### 1.3 设计目标

| 目标 | 描述 |
|------|------|
| 快速访问 | 任意页面 `Cmd+K` 打开切换器，1 秒内切换 Agent |
| 视觉一致 | 复用液态玻璃设计系统，保持品牌统一 |
| 信息密度 | 在有限空间展示关键统计数据 |
| 键盘友好 | 完整的键盘导航支持 |

---

## 2. 功能架构

### 2.1 双形态设计

```
┌─────────────────────────────────────────────────────────────┐
│                    Agent Dashboard 系统                      │
├─────────────────────────┬───────────────────────────────────┤
│   快捷切换浮层           │   完整控制台页面                    │
│   (Agent Switcher)      │   (Agent Dashboard Page)          │
├─────────────────────────┼───────────────────────────────────┤
│ • 全局快捷键 Cmd+K      │ • 独立路由 /agent-dashboard       │
│ • 轻量级浮层            │ • 完整功能展示                     │
│ • 任意页面可用          │ • 详细统计数据                     │
│ • 快速导航为主          │ • 快捷操作入口                     │
│ • 最近访问记录          │ • 使用趋势图表                     │
└─────────────────────────┴───────────────────────────────────┘
```

### 2.2 Agent 清单

| Agent | appKey | 图标 | 主色调 | 路由 | 统计指标 |
|-------|--------|------|--------|------|----------|
| PRD Agent | `prd-agent` | MessagesSquare | 蓝色 #3B82F6 | `/prd-agent` | 会话数量 |
| 视觉 Agent | `visual-agent` | Image | 紫色 #8B5CF6 | `/visual-agent` | 画布数量 |
| 文学 Agent | `literary-agent` | PenLine | 绿色 #22C55E | `/literary-agent` | 项目数量 |
| 缺陷 Agent | `defect-agent` | Bug | 橙色 #F97316 | `/defect-agent` | 缺陷数量 |

---

## 3. 交互设计

### 3.1 快捷键映射

| 快捷键 | 场景 | 功能 |
|--------|------|------|
| `Cmd/Ctrl + K` | 全局 | 打开/关闭 Agent 切换器 |
| `Esc` | 切换器打开时 | 关闭切换器 |
| `↑` / `↓` | 切换器打开时 | 上下移动选中项 |
| `←` / `→` | 切换器打开时 | 左右移动选中项（网格模式） |
| `Enter` | 切换器打开时 | 打开选中的 Agent |
| `1` - `4` | 切换器打开时 | 快速跳转到对应 Agent |

### 3.2 状态流转

```
┌─────────┐     Cmd+K      ┌─────────┐
│  关闭   │ ─────────────→ │  打开   │
│ (idle)  │ ←───────────── │ (open)  │
└─────────┘    Esc/Cmd+K   └─────────┘
                              │
                              │ Enter / Click
                              ↓
                         ┌─────────┐
                         │  导航   │
                         │(navigate)│
                         └─────────┘
                              │
                              │ 路由跳转完成
                              ↓
                         ┌─────────┐
                         │  关闭   │
                         └─────────┘
```

### 3.3 最近访问记录

- 记录用户最近访问的 Agent 及其具体页面
- 最多保留 10 条记录
- 本地持久化存储 (localStorage)
- 显示时间相对描述（如 "3分钟前"）

---

## 4. 视觉设计

### 4.1 快捷切换浮层

```
╭──────────────────────────────────────────────────────────────╮
│                                                              │
│   ┌────────────────────────────────────────────────────┐    │
│   │  🔍 搜索 Agent 或快速操作...              ⌘K 关闭  │    │
│   └────────────────────────────────────────────────────┘    │
│                                                              │
│   ╭────────────╮ ╭────────────╮ ╭────────────╮ ╭──────────╮ │
│   │            │ │            │ │            │ │          │ │
│   │     📝     │ │     🎨     │ │     ✍️     │ │    🐛    │ │
│   │            │ │            │ │            │ │          │ │
│   │  PRD Agent │ │ 视觉 Agent │ │ 文学 Agent │ │缺陷 Agent│ │
│   │   12 会话  │ │   8 画布   │ │   5 项目   │ │  23 缺陷 │ │
│   ╰────────────╯ ╰────────────╯ ╰────────────╯ ╰──────────╯ │
│                                                              │
│   ─────────────────── 最近访问 ───────────────────          │
│                                                              │
│   📝  PRD Agent / 产品需求文档分析              3 分钟前    │
│   🎨  视觉 Agent / 首页 Banner 设计            15 分钟前    │
│   🐛  缺陷 Agent / #DEF-0023                    1 小时前    │
│                                                              │
│                                                              │
│   [ESC 关闭]              [↑↓ 选择]           [Enter 打开]  │
│                                                              │
╰──────────────────────────────────────────────────────────────╯
```

**样式规格**:

| 属性 | 值 |
|------|-----|
| 宽度 | 560px (固定) |
| 最大高度 | 480px |
| 圆角 | 24px |
| 背景 | 液态玻璃渐变 |
| 模糊 | backdrop-filter: blur(40px) saturate(180%) |
| 边框 | 1px solid rgba(255,255,255,0.14) |
| 阴影 | 多层阴影（见代码实现） |

### 4.2 Agent 卡片样式

```
╭──────────────────╮
│                  │
│       🎨        │  ← 图标: 32x32, 圆角容器 16px
│                  │
│    视觉 Agent    │  ← 标题: 14px, font-semibold
│     8 画布       │  ← 统计: 12px, text-muted
│                  │
╰──────────────────╯

悬浮态:
- transform: scale(1.03)
- 边框高亮
- 阴影加深
```

**卡片颜色方案**:

```typescript
const AGENT_COLORS = {
  'prd-agent': {
    bg: 'rgba(59, 130, 246, 0.08)',
    border: 'rgba(59, 130, 246, 0.2)',
    iconBg: 'rgba(59, 130, 246, 0.15)',
    text: '#60A5FA',
  },
  'visual-agent': {
    bg: 'rgba(139, 92, 246, 0.08)',
    border: 'rgba(139, 92, 246, 0.2)',
    iconBg: 'rgba(139, 92, 246, 0.15)',
    text: '#A78BFA',
  },
  'literary-agent': {
    bg: 'rgba(34, 197, 94, 0.08)',
    border: 'rgba(34, 197, 94, 0.2)',
    iconBg: 'rgba(34, 197, 94, 0.15)',
    text: '#4ADE80',
  },
  'defect-agent': {
    bg: 'rgba(249, 115, 22, 0.08)',
    border: 'rgba(249, 115, 22, 0.2)',
    iconBg: 'rgba(249, 115, 22, 0.15)',
    text: '#FB923C',
  },
};
```

### 4.3 完整页面布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  Agent Control Center                              [设置] [帮助]    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ╭─────────────╮ ╭─────────────╮ ╭─────────────╮ ╭─────────────╮  │
│   │             │ │             │ │             │ │             │  │
│   │     📝      │ │     🎨      │ │     ✍️      │ │     🐛      │  │
│   │             │ │             │ │             │ │             │  │
│   │  PRD Agent  │ │ 视觉 Agent  │ │ 文学 Agent  │ │ 缺陷 Agent  │  │
│   │             │ │             │ │             │ │             │  │
│   │  ┌───────┐  │ │  ┌───────┐  │ │  ┌───────┐  │ │  ┌───────┐  │  │
│   │  │💬 12  │  │ │  │🖼️ 8   │  │ │  │📄 5   │  │ │  │🎫 23  │  │  │
│   │  │ 会话  │  │ │  │ 画布  │  │ │  │ 项目  │  │ │  │ 缺陷  │  │  │
│   │  └───────┘  │ │  └───────┘  │ │  └───────┘  │ │  └───────┘  │  │
│   │             │ │             │ │             │ │             │  │
│   │  [进入 →]   │ │  [进入 →]   │ │  [进入 →]   │ │  [进入 →]   │  │
│   ╰─────────────╯ ╰─────────────╯ ╰─────────────╯ ╰─────────────╯  │
│                                                                     │
│   ╭───────────────────────────────╮ ╭─────────────────────────────╮│
│   │        📈 使用统计             │ │      ⚡ 快捷操作            ││
│   │                               │ │                             ││
│   │   PRD ████████████░░ 45%     │ │  [+ 新建 PRD 会话]          ││
│   │   视觉 ██████░░░░░░░░ 30%     │ │  [+ 新建画布]              ││
│   │   文学 ████░░░░░░░░░░ 15%     │ │  [🛒 打开配置市场]          ││
│   │   缺陷 ██░░░░░░░░░░░░ 10%     │ │  [📊 查看调用日志]          ││
│   │                               │ │  [⚙️ 模型管理]              ││
│   ╰───────────────────────────────╯ ╰─────────────────────────────╯│
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 5. 技术实现

### 5.1 文件结构

```
prd-admin/src/
├── pages/
│   └── agent-dashboard/
│       ├── AgentDashboardPage.tsx      # 完整页面
│       ├── AgentCard.tsx               # Agent 卡片
│       ├── QuickActions.tsx            # 快捷操作
│       └── UsageStats.tsx              # 使用统计
│
├── components/
│   └── agent-switcher/
│       ├── AgentSwitcher.tsx           # 浮层主组件
│       ├── AgentSwitcherProvider.tsx   # Provider + 快捷键
│       └── index.ts                    # 导出
│
├── stores/
│   └── agentSwitcherStore.ts           # Zustand store
│
└── services/
    └── contracts/
        └── agentDashboard.ts           # API 类型定义
```

### 5.2 状态管理

```typescript
// stores/agentSwitcherStore.ts
interface RecentVisit {
  agentKey: string;
  agentName: string;
  title: string;
  path: string;
  timestamp: number;
}

interface AgentSwitcherState {
  isOpen: boolean;
  selectedIndex: number;
  recentVisits: RecentVisit[];

  open: () => void;
  close: () => void;
  toggle: () => void;
  setSelectedIndex: (index: number) => void;
  addRecentVisit: (visit: Omit<RecentVisit, 'timestamp'>) => void;
}
```

### 5.3 快捷键实现

```typescript
// AgentSwitcherProvider.tsx
useEffect(() => {
  const handleKeyDown = (e: KeyboardEvent) => {
    // Cmd/Ctrl + K 切换
    if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
      e.preventDefault();
      toggle();
      return;
    }

    if (!isOpen) return;

    // Esc 关闭
    if (e.key === 'Escape') {
      e.preventDefault();
      close();
      return;
    }

    // 方向键导航
    if (e.key === 'ArrowUp' || e.key === 'ArrowDown') {
      e.preventDefault();
      // 更新 selectedIndex
    }

    // Enter 确认
    if (e.key === 'Enter') {
      e.preventDefault();
      // 导航到选中项
    }

    // 数字键快速跳转
    if (['1', '2', '3', '4'].includes(e.key)) {
      e.preventDefault();
      // 直接导航到对应 Agent
    }
  };

  window.addEventListener('keydown', handleKeyDown);
  return () => window.removeEventListener('keydown', handleKeyDown);
}, [isOpen, selectedIndex]);
```

### 5.4 API 契约

```typescript
// services/contracts/agentDashboard.ts
export interface AgentSummary {
  appKey: string;
  displayName: string;
  count: number;
  recentActivity?: string;
}

export interface AgentDashboardSummaryResponse {
  agents: AgentSummary[];
  totalInteractions: number;
  periodDays: number;
}
```

### 5.5 组件层次

```
App
├── AgentSwitcherProvider          # 全局 Provider
│   └── AgentSwitcher              # 浮层（Portal 到 body）
│       ├── SearchInput
│       ├── AgentGrid
│       │   └── AgentQuickItem × 4
│       └── RecentVisitsList
│           └── RecentVisitItem × n
│
└── Routes
    └── /agent-dashboard
        └── AgentDashboardPage
            ├── PageHeader
            ├── AgentCardsGrid
            │   └── AgentCard × 4
            ├── UsageStats
            └── QuickActions
```

---

## 6. 权限设计

### 6.1 权限定义

| 权限 Key | 描述 | 默认角色 |
|----------|------|---------|
| `agent-dashboard.read` | 访问 Agent 控制台 | Agent 体验者、管理员 |

### 6.2 菜单注册

```csharp
// 后端菜单目录
new AdminMenuItem
{
    AppKey = "agent-dashboard",
    Label = "Agent 控制台",
    Icon = "Grid2X2",
    Path = "/agent-dashboard",
    Permission = "agent-dashboard.read",
    SortOrder = 5
}
```

---

## 7. 性能考量

### 7.1 优化策略

| 策略 | 实现 |
|------|------|
| 懒加载 | 浮层内容仅在打开时渲染 |
| 防抖搜索 | 搜索输入 300ms 防抖 |
| 虚拟列表 | 最近访问超过 20 条时启用 |
| 预加载 | 悬浮 Agent 卡片时预取路由 |

### 7.2 动画性能

- 使用 `transform` 和 `opacity` 进行动画（GPU 加速）
- 避免触发 layout/paint
- 浮层打开/关闭使用 `will-change: transform, opacity`

---

## 8. 可访问性

### 8.1 ARIA 属性

```jsx
<div
  role="dialog"
  aria-modal="true"
  aria-label="Agent 快捷切换"
>
  <input
    role="combobox"
    aria-expanded={isOpen}
    aria-controls="agent-list"
    aria-activedescendant={`agent-${selectedIndex}`}
  />
  <ul id="agent-list" role="listbox">
    <li role="option" aria-selected={selected}>
      ...
    </li>
  </ul>
</div>
```

### 8.2 焦点管理

- 打开时自动聚焦搜索框
- Tab 键在可交互元素间循环
- 关闭时恢复之前的焦点

---

## 9. 后续扩展

### 9.1 Phase 2 规划

- [ ] 搜索功能：支持模糊搜索 Agent 名称和最近访问
- [ ] 自定义布局：用户可调整 Agent 顺序
- [ ] 快捷操作：在浮层中直接新建会话
- [ ] 主题感知：根据当前 Agent 自动调整强调色

### 9.2 可能的功能点

- Agent 收藏/置顶
- 自定义快捷键映射
- Agent 使用提醒/通知
- 跨 Agent 全局搜索

---

## 10. 变更记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-02-03 | 初始设计文档 |
