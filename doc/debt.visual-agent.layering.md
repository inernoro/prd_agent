# 视觉创作 · AI 分层 · 验收债务台账

> **版本**：v1.0 | **日期**：2026-08-11 | **状态**：开发中

**一句话**：AI 分层这条链路上「我声称做了、但当前没有机械判据能证明」的每一条，连同它为什么证不了、要什么才证得了。
**谁该读**：接手 AI 分层的工程师；下一轮想问「这块到底验没验过」的人。
**读完能做什么**：一眼分清哪些是有冒烟守着的、哪些只是我推导的，并知道下一步该补哪条判据。

---

## 为什么要有这份台账

「已验证」和「我认为它是对的」之间隔着一条真实的判据。这两者混在交付消息里，
下一个人（包括我自己）就会把后者当成前者，在一个从没被证明过的结论上继续盖楼。

本台账只记**验收债务**：功能已交付，但缺少能变红的判据。工程实现层面的欠账
（想做没做的功能）不进这里。判据分档与坏形状的定义见文末「实现来源」列的判据纪律规则，
自测优先的要求见根目录的 Agent 约定第 8.1 条。

判据状态只有三档，不许有第四档：

| 档位 | 含义 |
|---|---|
| 已守 | 有能变红的自动判据（冒烟或单测），且做过红绿闭环 |
| 弱守 | 有判据但证明力不足（样本恒成立、只测了形状没测行为） |
| 未守 | 没有任何自动判据，当前只靠人工观察或逻辑推导 |

---

## 台账

| # | 项 | 判据状态 | 为什么证不了 | 要什么才证得了 | 当前兜底 |
|---|---|---|---|---|---|
| 1 | 自然语言拆法真的影响拆分结果 | 未守（等外部输入） | 「说了把人物和风景分开，模型就按这个拆」取决于上游模型，没有确定性可断言。冒烟只证明了输入框存在、意图拼进 prompt、参数送达 | 一组带标注答案的样本图 + 一个「拆出来的部件语义是否符合意图」的判定器（人工标注或视觉模型打分），跑通过率而不是单次断言 | 面板显示本次实际用的模型；层数与拆法都摆在用户面前，拆得不对可以立刻重拆 |
| 2 | 兼容多个分层模型 | 未守（等外部输入） | 逻辑模型池当前只有一个上游。架构上「加模型即可多上游」成立，但没有第二个上游跑过，等于没验 | 接入第二个分层上游（可能需要新的协议转换器），然后跑同一份冒烟两遍、比较两组产物 | 面板显示本组由哪个模型拆分，换模型重拆后两组并排可比较 |
| 3 | 层数「自动」档 | 未守（刻意不做） | 上游层数是必填参数，做一个假的「自动」等于编造能力 | 上游支持可变层数，或我们改为「多拆几层再按语义合并」 | 层数降级为「最多拆 N 层」的可选提示；凑数产生的空层/实色层被标注并默认隐藏 |
| 4 | 双主题下的分层界面 | **已守** | — | — | 判据：冒烟「浅色主题下图层面板的文字读得清（对比度 ≥ 3:1）」。不判「好不好看」（主观），只判「读不读得清」（可算）。实测 16.41:1 |
| 5 | 编组/解组的持久化 | **已守** | — | — | 判据：冒烟「刷新后编组还在」+「解组后刷新，Frame 不会复活」；单测「解组之后刷新，Frame 不许复活」+「旧数据仍靠 layerGroupId 补出 Frame」。补这条时抓到真缺陷，见下方历史 |
| 6 | 通用 Frame 导出的 PSD 正确性 | **已守** | — | — | 判据：冒烟「多选导出的 PSD 能反读且是真分层」+「每层各自带包围盒」。实测 9 层、画布 3312x1024、有界 9/9。Frame 头部导出与多选导出走同一个函数，因此这条同时守住两个入口 |
| 7 | 多选 ZIP 导出 | **已守** | — | — | 判据：冒烟「多选 ZIP 能下载且里面不止一张图」，按 ZIP 条目头计数，实测 9 个条目 |
| 8 | 画布落盘频控改期重试 | **已守** | — | — | 判据：把落盘时序抽成纯函数后单测直接打时序，「撞上频控时返回还要等多久，而不是让调用方放弃这一次」。红绿闭环已做 |
| 9 | 拆出来的部件边界符合人的语义 | 未守（等外部输入） | 2026-08-12 用户实测两例：请求 3 层时 logo 与衣服上的 logo 被并成一层；请求 4 层时人物的手被切开。层数是上游必填参数，模型必须凑够 N 份——图里自然部件不足 N 个时，多出来的那一份只能靠切开某个完整物体来凑，切在哪由上游决定。MAP 侧能做的只有「把意图原样送到」，这条已接通并有守卫，但「送到之后模型听不听」无法断言 | 同第 1 条：带标注答案的样本图 + 语义判定器，按通过率评估；换个角度也可以接第二个上游（第 2 条）横向比较 | 意图输入框（用户可以直接说「logo 单独一层」「不要切开人物」）；面板显示本组用的层数与模型，拆得不对可立刻改层数重拆，两份并排比较 |
| 10 | 拆分途中拖走 Frame，图层跟着落到新位置 | **已守** | — | — | 判据：单测「拖走之后到达的图层落在新位置」+「锚点消失后保留最后位置」（红绿闭环已做）；接线守卫盯住三处落位都读实时原点、钉死开跑时的坐标只许当种子；冒烟端到端「拆分途中拖走 Frame，整组当场跟着走、跑完后没有一块退回原位」，并同时断言「拖走之后确实还有图层继续到达」避免空跑 |
| 12 | 拆分途中关掉页签，占位卡能自愈 | 未守（熔断记账，真实缺口） | `decomposeImageToLayers` 内部拿到了 runId 却没往外给，图层占位卡身上也没有 `runId`。页签在提交后关掉，占位卡按无 runId 恢复，看门狗退回工作区对账；而对账按「画布元素 id == run.TargetCanvasKey」找，分层这条 run 的 TargetCanvasKey 是**原图**的 key，对不上任何一张占位卡，于是恒回 `no-run`，占位卡永久停在生成中，已经产出的图层资产也接不回画布 | 两条路选一：① 把 runId 透出来并落到该组每张占位卡上，看门狗按 runId 直接查；② 后端给分层加一条专用对账键（不复用单图的 TargetCanvasKey 语义）。任一条都要配一个「关页签再打开」的端到端判据，否则改完仍无法证明 | 用户可删掉停住的占位卡重新拆；图层资产本身没丢，只是没接回画布 |
| 13 | 混合编组的 Frame 不该冒充 AI 分层组 | 未守（熔断记账，真实缺口） | Frame 的 `kind` 只要成员里**有一个** `layerRole === 'layer'` 就判成 `ai-layers`。用户把某个图层和原图、无关图片或另一次分层的图层一起 Cmd+G，头部就把「整组导出 PSD」换成图层面板按钮，而面板只按第一个成员的 layerGroupId 选层，其余成员被静默排除 | 判据改成「全体相关成员同属一个 layerGroup 才算 ai-layers」，混合编组一律退回通用 Frame；并补一条守卫覆盖「混合编组不得出现图层面板入口」 | 通用 Frame 的整组 PSD 导出走的是同一个写层实现，功能本身不缺；缺的是这一屏给的入口不对 |
| 14 | MAP 首次登录撞用户名竞态时能重试 | 未守（熔断记账，本表外系统） | 首次 SSO 建号先查 `preferredUsername` 可用性再插入，两步之间另一个账号可以抢走这个名字。唯一索引抛出的重复键异常与「同一外部身份并发建号」是同一种，而 catch 只按外部身份反查，查不到就往外抛——于是 SSO 建号直接失败，尽管确定性兜底名 `map-{hash}` 还空着 | 在重复键分支里确认「本外部身份确实没建成账号」之后，用 `fallbackUsername` 重试一次建号；配一条并发用例（两个请求抢同一个 preferredUsername，断言两边都拿到可用账号） | 用户可换个名字重登；该竞态需要恰好同名并发，日常触发概率低 |
| 15 | 快捷编辑后图层的显隐/不透明度/次序不丢 | 未守（熔断记账，真实缺口） | 对某个图层跑快捷编辑，产物只继承编组身份（frameId / layerGroupId / layerIndex），没带 `layerHidden` `layerOpacity` `layerZ`。`selectExportableLayers` 又按 layerIndex 取最新那版，于是面板与导出里这一层悄悄变回「可见、全不透明、按序号排」，用户之前调的显隐和次序白调 | 建产物时把三个呈现字段一并继承；配一条单测「编辑一个隐藏且调过次序的图层，产物仍隐藏、次序不变」 | 用户可在面板里重新调一次；编辑本身不丢图 |
| 16 | 分层整条调用失败时按请求层数计账 | 未守（熔断记账，计数不准） | 分层改成「一次调用要 N 层」之后，失败分支仍按单图逻辑只 `Failed += 1`、只写 imageIndex 0 的错误项。四层失败会留下 `Total=4, Failed=1` 和三个没人认领的槽位，`runDone` 与轮询响应里的计数因此对不上 | 失败分支按 `ResolveLayerCount` 把 N 个槽位全标失败、逐个写错误项；配一条「四层请求整条失败后 Failed==4」的用例 | 已核实**不影响终态**：完成态只看 `Failed > 0`，run 仍会落到 Failed，前端轮询正常退出并拿到错误文案。只是计数不准 |
| 17 | 分层产物不重复上传资产 | 未守（熔断记账，冗余） | Worker 的 `PersistLayeringImagesAsync` 已经给每个图层建好 `ImageAsset` 并在 `imageDone` 里带上 id / sha / url，前端拿到后又把同一张图下载并重新上传一次。工作区加载会返回全部资产记录，于是每个原始图层在「资产视角」（如移动端时间线）里各重复一份，元数据提取与写入也白跑一遍 | 把 Worker 已持久化的资产透传到收集结果，前端直接用，不再二次上传；判据是「一次分层后该组的资产记录数 == 图层数」 | 画布与导出用的都是正确那份，功能不受影响；代价是存储与视图里的重复 |
| 18 | 改口令后不吃掉 SSO 的短时效 | 未守（熔断记账，本表外系统） | `MapSsoLifetimeMinutes` 把联邦会话压到很短（如 15 分钟）时，走新的账号安全流程设置或修改口令会重新签发 token 却不传 lifetime，于是短时效令牌被换成正常的多天令牌，而 `fed_session` 标记还在——等于绕过了这条 SSO 时效配置 | 重签时保留原联邦会话的剩余时效（或按配置重新取短时效）；配一条「短时效 SSO 会话改完口令后仍然短时效」的用例 | 仅在显式配置了 `MapSsoLifetimeMinutes` 时才有影响；默认配置下两者时效一致 |
| 19 | 裁剪落位一定落盘（刷新后不退回满幅） | 弱守（判据已换成相机无关口径，根因未定） | 第 10 轮冒烟刷新后，第二组第 4 层退回「锚点位置 + 满幅」——正是它裁剪之前的形状，同组另两层的裁剪都留住了。debounce 1200ms + 冷却 800ms 合计约 2 秒，而脚本在静止后又等了 6 秒，所以**不是**单纯的 debounce 竞态。两个候选根因：(a) 落盘请求失败，但 `lastSavedJsonRef` 在发请求**之前**就乐观置位了，此后若画布不再变化就永远没人重试；(b) 该层的裁剪落得比脚本判定的「静止」更晚——同一轮里「收敛」判据认定稳定时，那一组四层其实都还是满幅，6 秒后才陆续裁好，说明「连续两次尺寸一致」这个静止判据本身会误报 | 先定性再修：给冒烟挂上失败请求日志，看刷新前那一刻画布保存有没有 4xx/5xx。若是 (a)，把 `lastSavedJsonRef` 改成落盘成功才置位并补一次重试；若是 (b)，静止判据要改成「所有 done 层都已有裁剪产物」而不是「尺寸两次相同」 | 用户可见的后果是那一层刷新后变回大方块，再拆一次或手动拖动都会重新触发落盘；产物本身没丢，导出用的是满幅原件、不受影响 |
| 11 | 图层面板顶部展示 `{模型} · {平台}` | 未守（本轮刻意不做，记为后续） | 面板现在只在底部说明「本组走『image-layering』能力路由，具体模型由网关决定」，没有把 runStart 事件里的 `platformId` 留下来、也没在顶部展示。按 `ai-model-visibility` 的字面要求这是缺口 | 把 runStart 的 `modelId` 与 `platformId` 一起接住并透传到面板 header；同时要和 `capability-is-not-model` 对齐——分层是「能力路由」，MAP 侧不感知真实上游，所以展示口径要先定清楚是「能力 + 网关选中的上游」还是别的说法，不能把能力标识当模型名（本 PR 已为此加过守卫） | 面板底部如实说明走的是能力路由；`layerModel` 已随组落盘，换模型重拆后两组可并排比较 |

---

## 已经守住的（不进台账，列此备查）

端到端冒烟共 48 条判据，覆盖：副本落在原图右侧且原图仍在、重拆不删旧结果、
拆分途中拖走 Frame 后图层跟着落位、
部件裁成最小非透明外接矩形（含**取覆盖率最低那层**验证面积比）、裁剪落位收敛、刷新前后尺寸一致、
单层 PNG 是裁剪版、AI 分层与多选两条导出路的 PSD 反读、多选 ZIP 条目数、拆法输入气泡自动聚焦、
全选与编组/解组及二者的刷新存活、拖 Frame 头部整组移动、多选浮条、浅色主题对比度、全程无 401/403。

---

## 维护约定

1. 每次交付 AI 分层相关改动，**先更新本表**再报完成：新增的「说了但没判据」必须落一行。
2. 判据补上后把该行改成「已守」并写明判据名，**不要删行**——删了就看不出这条曾经欠过。
3. 「弱守」升「已守」必须做红绿闭环：把修复临时撤掉，确认判据变红。
4. 台账只增不隐：如果某条判据被发现是恒成立的假证据，降级回「弱守/未守」并写明为什么。

## 历史

**判据自己造假一次**：裁剪那条判据的第一版是恒成立的——挑了覆盖率 100% 的背景层去验裁剪，
它的最小矩形本来就等于整幅，裁没裁都会通过。发现后改成挑覆盖率最低那层并按面积比判。

**补判据时抓到真缺陷一次**：给「编组/解组持久化」补判据的过程中，冒烟报出「解组后刷新，
Frame 又有 2 组」。根因是读回时无条件拿产物血缘去补编组标识，于是「解组」在下一次刷新后
被静默撤销。修法是给新写出的记录盖一个迁移标记：有标记时缺失就是真的解过组，没标记才当旧数据补。
这条缺陷在补判据之前一直存在，没有任何人发现——**这就是为什么弱守要优先于未守**。

**判据太松放跑了两个现象一轮**：「部件裁成最小矩形」当时判的是「各部件尺寸不全相同」，
这条对「框比内容大一圈」完全不敏感。用户圈图指出文字层的框比字大一大圈、还有一层看不见的
空盒子占着位置，根因都是求包围盒用的 alpha 阈值太松（8，约 3% 不透明度），
模型返回的那层几乎看不见的雾就把框撑到了整幅。改成按「实墨」（alpha ≥ 64）求框 +
按实墨覆盖率判空层，两个现象一起消失。判据也补了：单测直接对比「松阈值撑到整幅 vs
实墨阈值贴着墨走」。

---

## 实现来源

- 端到端冒烟：`scripts/smoke/visual-layering.mjs`（一条命令跑完，退出码 0/1/2）
- 判据纪律：`.claude/rules/predicate-and-wiring-discipline.md`
- 循环怎么跑：`acceptance-debt-loop` 技能
