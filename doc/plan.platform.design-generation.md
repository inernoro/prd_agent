# 知识驱动设计生成体系 · 计划

> **版本**：v1.21 | **日期**：2026-09-08 | **状态**：本批可靠性修复通过限定独立复审与相关回归；待部署验真，模型累计预算与冻结语料证据待核实

**一句话**：用一套可恢复、可追溯、可替换执行器的无头生成内核，同时支撑网页托管、知识库、HTML PPT 与网页微调四个入口。
**谁该读**：产品负责人、网页托管与知识库负责人、HTML PPT 负责人、CDS 与 Agent 运行时负责人。
**读完能做什么**：判断四个 MVP 是否真正共用同一条数据链、哪些执行器已可用，以及离可发布还差什么。

---

## 实时状态板

负责人：主实现轨道。状态按完成条件更新，不以代码量或历史测试数量估算百分比。“已实现”不等于“已验收”；历史构建与服务探针不能替代最终候选的真实用户路径。

| 阶段 | 状态 | 未完成项 | 下一步 | 完成条件 |
|------|------|----------|--------|----------|
| 需求与边界 | 已确认 | 无 | 保持四个 MVP 边界不扩张 | 产品目标与授权范围一致 |
| 统一生成内核 | 修复待独立复审 | 可靠性修复尚未完成非实现者复核 | 复核 A01-A18，关闭已证实缺陷 | 租约、事件、来源、发布与取消合同通过回归和独立复审 |
| 四个入口投射 | 已实现待 CDS 验证 | 最终候选真实长任务尚未验收 | 运行生成、微调、HTML PPT 发布及刷新恢复 | 四条用户路径均产出可追溯、可发布的结果 |
| 自动化验证 | 待候选版本校验 | 历史测试结果不能证明当前候选全绿 | 跑适用编译、测试及 CDS 远端校验 | 校验结果与最终候选版本对应，失败项明确关闭 |
| Provider 插件合同 | 待独立复审与真机验证 | CDS 运行时注册、隔离与限额仍需验真 | 检查替换边界和会话资源策略 | MAP 配置权威，未就绪执行器不可用，会话隔离有真实证据 |
| CDS 与真人路径验收 | 待执行 | 最终候选部署、匿名配对、双主题视觉和密封盲验尚未完成 | 核实控制面兼容性后部署取证 | 公网入口、核心流程、独立评分和盲验均通过 |

## OpenDesign 真实链路任务台账

本轮目标是在 CDS 测试环境完成共享 Workspace Session、MAP 权威 LLMGW 会话配置、对象存储物化与产物清单提交，并以匿名双盲、独立视觉评分和对抗式审查证明效果。验收标准由独立 Agent 在实现前密封；实现轨道只能看到摘要哈希，最终只接收通过或不通过结论。

| 编号 | 要做什么 | 细化步骤 | 当前问题 | 下一步准备做什么 | 为什么做 | 状态 |
|------|----------|----------|----------|------------------|----------|------|
| T00 | 建立持续目标 | 1 | 无 | 保持目标到最终盲验结束 | 防止把骨架误报为完整交付 | 已完成 |
| T01 | 确认 CDS 测试分支可用 | 4 | 最终候选部署状态待重新核验 | 检查公网入口、资源与容器停止原因 | 确保测试对象真实可用 | 待候选部署验证 |
| T02 | 一次性盘点与确认资源 | 5 | 无 | 按三条非敏感现有知识加一条合成知识执行；真实模型调用不超过三十六次 | 避免执行中断和隐性数据授权 | 已完成 |
| T03 | 密封盲验清单 | 4 | 标准不得向实现轨道泄露 | 最终由全新独立验收 Agent 接收匿名证据并独占解密，只返回通过或不通过与失败编号 | 防止按答案定向实现 | 已密封，待最终验收 |
| T04 | 冻结匿名基线语料 | 5 | 当前证据不足以确认四份冻结语料及其配对运行完整 | 核对输入快照与运行关联，不向评分者揭示执行器身份 | 保证 MAP 基线与候选输入一致且不越权 | 待核实证据 |
| T05 | 实现 CDS Workspace Session | 12 | 字节、inode、并发与失败回收代码闸通过，真实节点待验 | 部署后核对硬限额与自动清理事实 | 让 OpenDesign 可安全进入多租户测试 | 已实现待真机验证 |
| T06 | 实现对象存储物化器 | 7 | 本地合同测试通过，真实对象存储待验 | 在 CDS 下载内容寻址输入包并核对文件哈希 | 消除大文本往返并收紧数据面 | 已实现待真机验证 |
| T07 | 部署 OpenDesign 无头运行时 | 7 | 能力探针不能证明真实生成成功 | 核对固定镜像并运行真实任务 | 把 Provider 合同变成真实执行能力 | 待最终候选复验 |
| T08 | 接通 MAP 权威 LLMGW | 10 | 真实票据仅在出口代理、认证头重写和固定输出预算已通过代码闸，待真实审计 | 运行候选任务并检查 LLMGW 调用归属 | 保持单一配置源和完整调用记录 | 已实现待调用审计 |
| T09 | 实现 Workspace Committer | 7 | MAP 与 CDS 的 run、基线、哈希、清单合同已对齐 | 在真实任务验证幂等、冲突、取消和超限 | 用原子提交替代同步复制 | 已实现待真机验证 |
| T10 | 投射四个 MVP | 8 | 无已知代码阻断，尚待 CDS 真实任务验真 | 运行四条真实用户路径并核对统一运行与发布记录 | 证明架构不是单页面样例 | 已实现待端到端验收 |
| T11 | 补齐交互与恢复 | 7 | 阶段、每三秒用时、取消、恢复已接；工作区实时画面留后续 | 在真实长任务检查持续变化和最终草稿 | 避免等待黑盒与静止加载 | 已实现待交互验收 |
| T12 | 首批提交与 CDS 部署 | 6 | WIP SHA 已到 CDS，但最终候选尚未提交 | 复审通过后只推一次最终候选，等待同一 SHA 五服务就绪 | 给后续审查提供可运行证据 | 进行中 |
| T13 | 对抗式工程审查 | 8 | 代码复核不能替代最终用户路径与效果验收 | 将本批通过结论绑定到候选，再进入 CDS 验真 | 主动暴露实现者盲区 | 本批限定复审通过 |
| T14 | 集中修复与复测 | 15 | 真实环境证据尚缺 | 部署候选并验证真实故障恢复 | 把审查发现转换成可验证的产品修复 | 本批相关回归通过，待 CDS 验证 |
| T15 | 匿名视觉评分 | 7 | 本地尺寸与交互修复完成，尚无新部署截图 | 用新 SHA 在桌面、390×844、双主题和真实长任务中复测 | 判断相对真实 AI 产品是否达到可用标准 | 待部署复验 |
| T16 | 最终双盲验收 | 5 | 密封清单已重建，证据包尚未完成 | 新 Agent 独占解密，主轨道只接收通过或不通过及失败编号 | 避免实现者影响最终结论 | 待执行 |
| T17 | 架构图、MVP 与交接 | 6 | 依赖部署、对抗、视觉和盲验结论 | 汇总架构、发布门、已知边界、预览深链和交接清单 | 形成可维护而非一次性演示的成果 | 进行中 |

## 对抗问题台账

独立审查基线为 `13afa920d`，审查者未参与实现且不能读取密封验收标准。后端审查 P0 为零，随后产品集成审查发现一个 P0；所有 P0、P1 必须关闭后才进入视觉与最终盲验。状态“修复待复测”只代表代码已改，不能替代审查者复核与 CDS 运行证据。

| 编号 | 级别 | 问题 | 当前处理 | 下一步 | 状态 |
|------|------|------|----------|--------|------|
| A01 | P1 | 活跃租约可通过省略 owner 绕过围栏 | 生命周期统一验证 owner、期限与恢复观测值，Mongo CAS 同步约束 | 在最终复审复核负面测试 | 已修复待复审 |
| A02 | P1 | 发布绑定信任文件自报哈希且来源关系不完整 | 重哈希真实 revision 字节并核对 Run、站点、版本、执行器和 manifest | 在最终复审复核篡改与串 Run 测试 | 已修复待复审 |
| A03 | P1 | HTML PPT 绑定合成版本而非真实不可变版本 | 创建并绑定最终发布字节对应的真实 HostedSiteRevision | CDS 发布后核对预览、回退和哈希 | 已修复待部署 |
| A04 | P1 | 工作区已提交但 Done 丢失时可信结果可能被标失败 | result-ready 以精确 owner、lease、版本和无产物 CAS 重新排队，直接复用可信结果 | CDS 断点场景复验 | 已修复待部署 |
| A05 | P1 | HTML PPT 发布跨集合双写不幂等 | 持久 publish intent、租约 CAS、确定性站点与退避 dead-letter | CDS 并发和重启复验 | 已修复待部署 |
| A06 | P1 | Redis 或即时队列故障造成请求报错但后台执行 | Mongo queued 为权威意图，Redis 与即时队列退化为可恢复投影 | CDS 人为断流复验 | 已修复待部署 |
| A07 | P1 | 生成端点缺少功能权限 | 复用网页托管读写权限元数据并通过中间件回归 | CDS 非授权用户复验 | 已修复待部署 |
| A08 | P1 | 旧版发布记录占满窗口导致 v2 恢复饥饿 | 从未绑定 v2 Run 反查版本 | 复查最终 SHA 启动日志 | 已修复待部署 |
| A09 | 回归 | 新集合未进入数据同步范围 | 版本随站点同步，实例运行与删除任务明确排除 | 已通过 64 项范围守卫 | 已关闭 |
| A10 | P2 | 隔离运行时透传内部错误与 LLMGW 响应体 | 对外仅返回稳定领域错误，内部详情只进服务端日志 | CDS 恶意上游响应复验 | 已修复待部署 |
| A11 | P0 | MAP 与 OpenDesign 网页生成公开 SSE 合同不一致，后台完成时前端可能拿不到站点 | 两种运行时统一 phase/thinking/delta/done/error/cancelled，Redis 断流回退 Mongo 终态 | CDS 断流和刷新恢复复验 | 已修复待部署 |
| A12 | P1 | 网页生成与“帮我修改”缺少用户主动取消闭环 | 生成和编辑共用取消协调器；UI 显式停止，断开浏览器不取消服务端任务 | CDS 运行中停止复验 | 已修复待部署 |
| A13 | P2 | HTML PPT 的 SSE 与 FormData 裸请求未遵守可配置 API base URL | 所有裸请求统一走 resolveApiUrl，并修正对应静态闸门 | 分域环境复验 | 已修复待部署 |
| A14 | P0 | 已发布 revision 存在但 Run 尚未回写 Produced 身份时，恢复绑定被安全校验自身阻断 | 绑定事务允许从可信 SourceRunId revision 原子认领 Produced 身份，仍拒绝部分身份或目标站点不一致 | 新对抗者复核 generate/edit 恢复 | 已修复待复审 |
| A15 | P1 | Worker 把 Redis 投影失败当成生成失败，创建端容错没有覆盖执行中断流 | 元数据从 Mongo 构建，投影失败只停本轮投影；阶段和产物继续权威持久化 | CDS 验证断流 | 本地复测及限定复审通过 |
| A16 | P1 | 站点落库但 Produced 未回写时，result-ready 重放重复建站 | 按 Run 确定站点身份，重放核验真实对象哈希；补偿与执行租约互斥 | CDS 验证换租约重放且不重复上传 | 本地复测及限定复审通过 |
| A17 | P1 | 普通基线抢占版本导致 HTML PPT 或多文件网页的生成来源绑定失败 | 相同基线在验证站点、任务及实际对象字节后原子补齐来源和文件清单，冲突拒绝 | CDS 验证查看历史后仍可恢复发布 | 本地复测及限定复审通过 |
| A18 | 回归 | 同毫秒续租字节未改变时，ModifiedCount 为零被误判丢失租约 | 续租改用匹配数判定有效归属 | 保持有效归属与错误归属回归 | 本地复测及限定复审通过 |
| A19 | P1 | 补偿进程退出后过期清理租约永久阻断提交恢复 | 持久区分上传账本与已启动补偿；双租约过期后先完成清理再恢复可信结果 | CDS 验证补偿中断及失败重试不误删正常上传结果 | 本地复测及限定复审通过 |

## 执行顺序与资源边界

先完成可靠性修复的独立复审及适用校验，再提交候选；部署前核实 CDS 控制面对受信输入哈希和公共产物清单的兼容性，必要时按授权更新。随后验证公网入口与四条真实路径，完成匿名配对、独立视觉评分及最终密封盲验。所有发布结论必须对应最终候选，不能以旧版本的运行记录替代。

资源一次性确认的默认值为：选取三条非敏感现有知识和一条合成知识；不额外提供品牌资产；真实模型调用上限三十六次；允许在指定 CDS 分支预览域名使用浏览器已保存的 MAP 管理员登录态完成验收；允许上述非敏感知识经项目 LLMGW 发送给其已配置上游。任何长期凭据都不得交给 OpenDesign，密钥值不得写入台账、日志、产物或验收证据。

真实调用前按 MAP 任务与 LLMGW 日志核对累计预算；当前会话列表为空不代表历史调用为零。验收样本、截图、运行日志及评分报告归验收知识库，本计划只维护任务状态、完成条件和已知边界。

当前预算核验未完成：MAP 代理预占计数不等于实际发送上游的次数，不能用于推定剩余额度。实际调用日志需要 LLMGW 独立授权会话读取；核清前不发起新模型请求，不自动扩大已授权总预算。

## 目标产物

首个可发布版本不是四套独立功能，而是一个内核和四个投射：

1. 网页托管可选择知识、补两句话并生成可保存网页，生成中直接看到页面生长。
2. 知识库当前文档可选择“生成网页”或“生成 HTML PPT”，目标页自动带入来源。
3. HTML PPT 保留成熟的大纲、生成、微调与发布能力，同时记录知识来源与统一任务身份。
4. 已托管网页可继续“帮我修改”，生成草稿、发布新版本、预览历史并回退。

## 范围边界

- OpenDesign 是可替换的设计 Provider，不是 MAP 的任务中心、知识库、版本库或发布系统。
- 隔离容器属于 CDS Remote Agent 基础设施，可以单独项目化；MAP 不管理 Docker 生命周期。
- 首版默认使用 MAP LLM Gateway 执行网页生成与微调，复用现有 HTML PPT 执行链；OpenDesign 主容器只拿随机占位密钥，真实模型票据仅由 CDS 出口代理持有。
- OpenDesign、Codex、Claude 只有在 CDS 中存在真实运行时、健康检查通过且配置启用时才显示为可用。
- 首版不复制外部智能体实现，不宣称未部署的执行器已经接通。
- 首版不重写 HTML PPT 已有成熟链路，只补统一来源、任务与发布回写契约。
- OpenDesign 网页修改首版只支持声明式、自包含单页 HTML；脚本、表单、外链资产和 ZIP 多文件站点在任务创建前拒绝。

## 验收目标

| 编号 | 用户旅程 | 必须发生的结果 | 证据强度 |
|------|----------|----------------|----------|
| AC-001 | 网页托管引用知识生成 | 选择知识并输入短要求后，页面持续生长，完成后成为可再次编辑的托管网页 | 预览环境真人路径 |
| AC-002 | 知识库直达网页 | 当前文档一键进入网页生成，来源已带入且产物记录来源快照 | 预览环境真人路径与数据记录 |
| AC-003 | 知识库直达 HTML PPT | 当前文档一键进入 HTML PPT，来源已带入，可生成并发布到网页托管 | 预览环境真人路径与发布记录 |
| AC-004 | 已托管网页微调 | 修改生成草稿，发布后形成新版本，历史版本可预览并回退 | 预览环境真人路径 |
| AC-005 | 执行器事实展示 | 未部署的 OpenDesign、Codex、Claude 显示不可用原因，不能被选择或假装执行 | 接口与界面双证据 |
| AC-006 | 刷新恢复 | 生成中或完成后刷新，仍能按同一任务恢复阶段、产物和来源 | 集成测试与预览环境 |

## 发布门

- 四个入口使用同一知识引用结构与任务标识，来源删除后仍保留生成时快照；网页微调刷新后可按站点找回未完成任务。
- MAP 进程内模型调用经过 LLM Gateway；外部 Provider 只经 CDS 托管运行时，二者都使用服务器权威 Run、Worker 与可续接事件流。
- 等待超过两秒时，主视觉展示正在生长的网页或幻灯片，而不是静止加载文案。
- 生成产物在隔离预览中运行，不能读取 MAP 的认证存储或私有接口。
- CDS 动态容器全部就绪，公开预览域名完成四条真实路径验收。
- Provider 能力目录必须显示执行归属、隔离要求和真实可用性；会话级资源尚未强制时不得启用 OpenDesign。

## 已知后续

- 首版候选链提供持续阶段和用时变化，尚未把 OpenDesign 工作区中的临时 HTML 作为实时画面传回；最终原子提交不受影响，后续可增加只读临时快照。
- 会话并发配额当前由单个 CDS 进程原子管理；多副本部署前必须迁移到共享配额事实源。
- Workspace 输入包包含知识快照并作为运行审计证据随 Run 保留；首版尚未建立独立保留期、引用计数和自动清理策略。进入生产前必须与知识快照、站点删除和合规保留规则一起确定，不能由对象存储自行永久增长。
- 本次升级前已创建且没有冻结 CDS 连接标识的 OpenDesign 在途任务会安全失败并要求重发；部署窗口需先排空旧任务，不能承诺跨版本续跑。
- 受限知识引用当前保存来源快照并把工作区相对链接降为不可导航标记；发布闸门已校验来源外数值和敏感事实，面向读者的引用查看器、claim 级映射和同值指标语义比对留待后续。
- 多知识条目组合、跨库检索和向量召回不阻塞首版，首版以用户明确选择的知识快照为准。
- 新 CDS 节点不会提前宣称 OpenDesign 可用；它会在后台安装固定衍生镜像，并在 Docker、OpenCode CLI、资源策略和健康探针全部通过后启用。
- 其他外部执行器的授权、资源配额和模板许可证清单在启用前单独验收。

## 关联文档

- [知识驱动设计生成体系](./design.platform.design-generation.md)
- [网页托管与分享](./design.web-hosting.md)
- [MD 转 PPT 对话工作台收尾](./plan.md-to-ppt.chat-redesign.md)
- [知识库技术债务台账](./debt.knowledge-base.md)
