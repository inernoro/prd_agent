# 知识驱动设计生成体系 · 计划

> **版本**：v1.17 | **日期**：2026-09-08 | **状态**：新提交正在 CDS 构建并接受独立对抗复审，尚未执行最终盲验

**一句话**：用一套可恢复、可追溯、可替换执行器的无头生成内核，同时支撑网页托管、知识库、HTML PPT 与网页微调四个入口。
**谁该读**：产品负责人、网页托管与知识库负责人、HTML PPT 负责人、CDS 与 Agent 运行时负责人。
**读完能做什么**：判断四个 MVP 是否真正共用同一条数据链、哪些执行器已可用，以及离可发布还差什么。

---

## 实时状态板

最后更新：2026-09-08 | 更新人：Codex 主轨道 | 距离可发布：四个入口的 v2 接线已提交，CDS 已取得目标镜像并正在收敛源码容器；部署诊断发现旧版发布记录可饿死 v2 恢复、数据同步漏分类，均已修复并通过定向测试。剩余为修复提交部署、真实长任务、双主题视觉评分、独立对抗收口与全新密封盲验

| 阶段 | 进度 | 状态 | 阻塞项 | 下一步 | 证据 |
|------|------|------|--------|--------|------|
| 需求与边界 | 100% | 已确认 | 无 | 保持四个 MVP 边界不扩张 | 本计划与用户确认记录 |
| 现状链路取证 | 100% | 已完成 | 无 | 保持调用边界稳定 | 设计文档与源码对照 |
| 统一生成内核 | 100% | 已提交待复审 | 无已知代码阻断 | 根据独立审查结果补强并复测 | Mongo 权威事件、租约围栏、受信 manifest、Plan 绑定、发布幂等及统一公开 revision 已通过定向测试 |
| 四个入口投射 | 100% | 已提交待部署验真 | 真实长任务尚未重跑 | 验证生成即绑定、编辑草稿后发布绑定及中断恢复 | HTML PPT、网页生成与网页微调均接入 v2；知识库继续通过既有入口投射 |
| 自动化验证 | 88% | 进行中 | 全仓 API 含需外部密钥和独立 Mongo 的真实集成用例，本机不能无条件全绿 | 保持相关模块全绿，并在 CDS 走真实服务验证 | prd-admin 2440/2440、CDS 125/125、生命周期 63/63、数据同步守卫 64/64 |
| Provider 插件合同 | 92% | 独立对抗复审中 | 第二份产品集成审查受智能体线程上限等待 | 先收后端复审，再启动独立产品审查 | 首轮 P1/P2/P3 已关闭；当前审查者未参与实现且不能读取密封标准 |
| CDS 与真人路径验收 | 50% | 新 SHA 构建中 | API 与 Admin 源码容器尚在 starting，真实长任务与双主题截图缺失 | 等构建自然收敛后推送第二修复提交，再走真实用户路径 | CI 镜像目标 SHA 正确，三个 LLMGW 服务已运行；旧记录恢复告警已定位 |

## OpenDesign 真实链路任务台账

本轮目标是在 CDS 测试环境完成共享 Workspace Session、MAP 权威 LLMGW 会话配置、对象存储物化与产物清单提交，并以匿名双盲、独立视觉评分和对抗式审查证明效果。验收标准由独立 Agent 在实现前密封；实现轨道只能看到摘要哈希，最终只接收通过或不通过结论。

| 编号 | 要做什么 | 细化步骤 | 当前问题 | 下一步准备做什么 | 为什么做 | 状态 |
|------|----------|----------|----------|------------------|----------|------|
| T00 | 建立持续目标 | 1 | 无 | 保持目标到最终盲验结束 | 防止把骨架误报为完整交付 | 已完成 |
| T01 | 恢复 CDS 测试分支 | 4 | 上次容器异常退出原因未被平台完整记录 | 纳入首轮部署后的稳定性复测 | 先恢复真实可验证入口 | 已完成 |
| T02 | 一次性盘点与确认资源 | 5 | 无 | 按三条非敏感现有知识加一条合成知识执行；真实模型调用不超过三十六次 | 避免执行中断和隐性数据授权 | 已完成 |
| T03 | 密封盲验清单 | 4 | 标准不得向实现轨道泄露 | 最终向同一 Agent 提交匿名证据，只接收通过或不通过与失败编号 | 防止按答案定向实现 | 已完成 |
| T04 | 冻结匿名基线语料 | 5 | 无 | 最终复评继续使用同一冻结输入，不向评分者揭示执行器身份 | 保证 MAP 基线与候选输入一致且不越权 | 已完成 |
| T05 | 实现 CDS Workspace Session | 12 | 字节、inode、并发与失败回收代码闸通过，真实节点待验 | 部署后核对硬限额与自动清理事实 | 让 OpenDesign 可安全进入多租户测试 | 已完成 |
| T06 | 实现对象存储物化器 | 7 | 本地合同测试通过，真实对象存储待验 | 在 CDS 下载内容寻址输入包并核对文件哈希 | 消除大文本往返并收紧数据面 | 已完成 |
| T07 | 部署 OpenDesign 无头运行时 | 7 | 无 | 保持固定摘要镜像与能力探针 | 把 Provider 合同变成真实执行能力 | 已完成 |
| T08 | 接通 MAP 权威 LLMGW | 10 | 真实票据仅在出口代理、认证头重写和固定输出预算已通过代码闸，待真实审计 | 运行候选任务并检查 LLMGW 调用归属 | 保持单一配置源和完整调用记录 | 已完成 |
| T09 | 实现 Workspace Committer | 7 | MAP 与 CDS 的 run、基线、哈希、清单合同已对齐 | 在真实任务验证幂等、冲突、取消和超限 | 用原子提交替代同步复制 | 已完成 |
| T10 | 投射四个 MVP | 8 | 无已知代码阻断，尚待 CDS 真实任务验真 | 运行四条真实用户路径并核对统一运行与发布记录 | 证明架构不是单页面样例 | 已完成 |
| T11 | 补齐交互与恢复 | 7 | 阶段、每三秒用时、取消、恢复已接；工作区实时画面留后续 | 在真实长任务检查持续变化和最终草稿 | 避免等待黑盒与静止加载 | 已完成 |
| T12 | 首批提交与 CDS 部署 | 6 | 自动部署与手工部署重叠，源码容器仍在收敛 | 不重复触发，等当前部署终态后再推送修复提交 | 给后续审查提供可运行证据 | 进行中 |
| T13 | 对抗式工程审查 | 8 | 后端与产品集成两轮独立审查已收口；产品审查发现一个实时闭环 P0 | 先修复 P0，再逐项处置取消能力与跨域 API 基址 | 主动暴露实现者盲区 | 已完成 |
| T14 | 集中修复与复测 | 15 | 已发现旧记录恢复饥饿与数据同步漏分类 | 提交修复后复查启动日志与相关测试 | 把审查和运行时发现转换成可验证的产品修复 | 进行中 |
| T15 | 匿名视觉评分 | 7 | 本地尺寸与交互修复完成，尚无新部署截图 | 用新 SHA 在桌面、390×844、双主题和真实长任务中复测 | 判断相对真实 AI 产品是否达到可用标准 | 待部署复验 |
| T16 | 最终双盲验收 | 5 | 密封清单已重建，证据包尚未完成 | 新 Agent 独占解密，主轨道只接收通过或不通过及失败编号 | 避免实现者影响最终结论 | 待执行 |
| T17 | 架构图、MVP 与交接 | 6 | 依赖部署、对抗、视觉和盲验结论 | 汇总架构、发布门、已知边界、预览深链和交接清单 | 形成可维护而非一次性演示的成果 | 进行中 |

## 对抗问题台账

独立审查基线为 `13afa920d`，审查者未参与实现且不能读取密封验收标准。后端审查 P0 为零，随后产品集成审查发现一个 P0；所有 P0、P1 必须关闭后才进入视觉与最终盲验。状态“修复待复测”只代表代码已改，不能替代审查者复核与 CDS 运行证据。

| 编号 | 级别 | 问题 | 当前处理 | 下一步 | 状态 |
|------|------|------|----------|--------|------|
| A01 | P1 | 活跃租约可通过省略 owner 绕过围栏 | 生命周期轨道统一验证函数与 Mongo CAS | 补缺 owner、错 owner、过期 owner 负面测试 | 修复中 |
| A02 | P1 | 发布绑定信任文件自报哈希且来源关系不完整 | 生命周期轨道改为重哈希真实字节并核对 Run、站点、版本、执行器 | 运行篡改内容与串 Run 测试 | 修复中 |
| A03 | P1 | HTML PPT 绑定合成版本而非真实不可变版本 | HTML PPT 轨道创建并绑定最终发布字节对应的真实 Revision | 核对预览、回退和哈希一致性 | 修复中 |
| A04 | P1 | 工作区已提交但 Done 丢失时可信结果可能被标失败 | 将 result-ready 作为可续跑事实，恢复器补建站点或草稿 | 加断点恢复测试 | 待实现 |
| A05 | P1 | HTML PPT 发布跨集合双写不幂等 | 发布前持久化 intent 与 CAS，并以稳定来源认领站点 | 跑并发与崩溃边界测试 | 修复中 |
| A06 | P1 | Redis 或即时队列故障造成请求报错但后台执行 | Mongo queued 作为权威入队意图，兼容投影失败仍返回唯一 Run；旧流回退 Mongo | 编译并运行 Redis、队列、SSE 故障测试 | 修复待复测 |
| A07 | P1 | 生成端点缺少功能权限 | 复用网页托管读写权限元数据 | 跑权限合同与中间件回归 | 修复待复测 |
| A08 | P1 | 旧版发布记录占满窗口导致 v2 恢复饥饿 | 改从未绑定 v2 Run 反查版本 | 复查 CDS 启动日志不再刷旧记录 | 修复待部署 |
| A09 | 回归 | 新集合未进入数据同步范围 | 版本随站点同步，实例运行与删除任务明确排除 | 已通过 64 项范围守卫 | 已关闭 |
| A10 | P2 | 隔离运行时透传内部错误与 LLMGW 响应体 | 返回稳定领域错误，内部详情仅写服务端日志 | 跑恶意上游响应体测试 | 修复待复测 |
| A11 | P0 | 网页生成前端按旧 Redis 字段消费统一 v2 SSE，MAP 默认任务又未进入 v2，导致后台完成但前台拿不到已保存站点 | 已完成证据定位，尚未修复 | 统一 MAP 与 OpenDesign 公开事件合同，Mongo 终态兜底并补两条端到端合同测试 | 待实现 |
| A12 | P1 | 网页生成与“帮我修改”缺少用户主动取消闭环 | 已确认后端无取消端点、前端仅中断订阅 | 增加幂等取消意图、Worker 停止 CDS 会话及两个 UI 的停止入口 | 待实现 |
| A13 | P2 | HTML PPT 的 SSE 与 FormData 裸请求未遵守可配置 API base URL | 已确认当前同域代理可用、分域部署会失败 | 抽取共享 URL 构造并补带路径前缀的合同测试 | 待实现 |

## 断电前恢复点

2026-09-08 已将当前工作作为 WIP 恢复点提交并推送。HTML PPT 发布协调器当前约完成 65%，尚缺服务注册、并发 runId 收敛、测试更新与完整编译；生命周期安全修复代码已落盘，Core 与 Infrastructure 构建通过，但联合测试被 HTML PPT 半完成代码的编译错误阻塞。恢复后第一步必须先完成协调器注册并消除编译错误，再处理 A11，禁止基于此恢复点部署或宣称可验收。

资源一次性确认的默认值为：选取三条非敏感现有知识和一条合成知识；不额外提供品牌资产；真实模型调用上限三十六次；允许在指定 CDS 分支预览域名使用浏览器已保存的 MAP 管理员登录态完成验收；允许上述非敏感知识经项目 LLMGW 发送给其已配置上游。任何长期凭据都不得交给 OpenDesign，密钥值不得写入台账、日志、产物或验收证据。

## CDS 验收结果

| 验收项 | 结果 | 关键证据 |
|--------|------|----------|
| AC-001 / AC-002 | 通过 | 知识库条目生成托管网页，任务完成后保存站点、版本和来源哈希 |
| AC-003 | 通过 | 两页 HTML PPT 并行生成，产物 17439 字符、降级页数为 0，发布后统一任务回写站点 |
| AC-004 | 通过 | 网页微调先生成草稿，再显式发布；回退新增可审计版本，历史共三版 |
| AC-005 | 通过 | 网页生成与微调入口均展示 CDS 动态 Provider 事实；OpenDesign 薄适配器在会话资源未强制时保持不可用；6 项定向测试与 CDS 真实界面通过 |
| AC-006 | 通过 | 网页生成、HTML PPT 与网页微调均可按同一运行标识恢复；微调在刷新或进度流中断后会继续读取阶段并打开最终草稿 |

最终分支复验确认移动端生成弹窗不再横向溢出，触屏站点卡具有操作入口；真实分享预览中“帮我修改”位于“向我提问”上方，窄桌面和 CDS 制品条均不会遮挡。修改抽屉复用同一网页修改内核，没有形成第二套编辑与版本状态。

## 目标产物

首个可发布版本不是四套独立功能，而是一个内核和四个投射：

1. 网页托管可选择知识、补两句话并生成可保存网页，生成中直接看到页面生长。
2. 知识库当前文档可选择“生成网页”或“生成 HTML PPT”，目标页自动带入来源。
3. HTML PPT 保留成熟的大纲、生成、微调与发布能力，同时记录知识来源与统一任务身份。
4. 已托管网页可继续“帮我修改”，生成草稿、发布新版本、预览历史并回退。

## 范围边界

- OpenDesign 是可替换的设计 Provider，不是 MAP 的任务中心、知识库、版本库或发布系统。
- 隔离容器属于 CDS Remote Agent 基础设施，可以单独项目化；MAP 不管理 Docker 生命周期。
- 首版默认使用 MAP LLM Gateway 执行网页生成与微调，复用现有 HTML PPT 执行链；OpenDesign 主容器只拿随机占位密钥，真实模型票据仅由 CDS 出口代理持有。
- OpenDesign、Codex、Claude 只有在 CDS 中存在真实运行时、健康检查通过且配置启用时才显示为可用。
- 首版不复制外部智能体实现，不宣称未部署的执行器已经接通。
- 首版不重写 HTML PPT 已有成熟链路，只补统一来源、任务与发布回写契约。
- OpenDesign 网页修改首版只支持声明式、自包含单页 HTML；脚本、表单、外链资产和 ZIP 多文件站点在任务创建前拒绝。

## 验收目标

| 编号 | 用户旅程 | 必须发生的结果 | 证据强度 |
|------|----------|----------------|----------|
| AC-001 | 网页托管引用知识生成 | 选择知识并输入短要求后，页面持续生长，完成后成为可再次编辑的托管网页 | 预览环境真人路径 |
| AC-002 | 知识库直达网页 | 当前文档一键进入网页生成，来源已带入且产物记录来源快照 | 预览环境真人路径与数据记录 |
| AC-003 | 知识库直达 HTML PPT | 当前文档一键进入 HTML PPT，来源已带入，可生成并发布到网页托管 | 预览环境真人路径与发布记录 |
| AC-004 | 已托管网页微调 | 修改生成草稿，发布后形成新版本，历史版本可预览并回退 | 预览环境真人路径 |
| AC-005 | 执行器事实展示 | 未部署的 OpenDesign、Codex、Claude 显示不可用原因，不能被选择或假装执行 | 接口与界面双证据 |
| AC-006 | 刷新恢复 | 生成中或完成后刷新，仍能按同一任务恢复阶段、产物和来源 | 集成测试与预览环境 |

## 发布门

- 四个入口使用同一知识引用结构与任务标识，来源删除后仍保留生成时快照；网页微调刷新后可按站点找回未完成任务。
- MAP 进程内模型调用经过 LLM Gateway；外部 Provider 只经 CDS 托管运行时，二者都使用服务器权威 Run、Worker 与可续接事件流。
- 等待超过两秒时，主视觉展示正在生长的网页或幻灯片，而不是静止加载文案。
- 生成产物在隔离预览中运行，不能读取 MAP 的认证存储或私有接口。
- CDS 动态容器全部就绪，公开预览域名完成四条真实路径验收。
- Provider 能力目录必须显示执行归属、隔离要求和真实可用性；会话级资源尚未强制时不得启用 OpenDesign。

## 已知后续

- 首版候选链提供持续阶段和用时变化，尚未把 OpenDesign 工作区中的临时 HTML 作为实时画面传回；最终原子提交不受影响，后续可增加只读临时快照。
- 会话并发配额当前由单个 CDS 进程原子管理；多副本部署前必须迁移到共享配额事实源。
- 本次升级前已创建且没有冻结 CDS 连接标识的 OpenDesign 在途任务会安全失败并要求重发；部署窗口需先排空旧任务，不能承诺跨版本续跑。
- 受限知识引用当前保存来源快照并把工作区相对链接降为不可导航标记；发布闸门已校验来源外数值和敏感事实，面向读者的引用查看器、claim 级映射和同值指标语义比对留待后续。
- 多知识条目组合、跨库检索和向量召回不阻塞首版，首版以用户明确选择的知识快照为准。
- 新 CDS 节点不会提前宣称 OpenDesign 可用；它会在后台安装固定衍生镜像，并在 Docker、OpenCode CLI、资源策略和健康探针全部通过后启用。
- 其他外部执行器的授权、资源配额和模板许可证清单在启用前单独验收。

## 关联文档

- [知识驱动设计生成体系](./design.platform.design-generation.md)
- [网页托管与分享](./design.web-hosting.md)
- [MD 转 PPT 对话工作台收尾](./plan.md-to-ppt.chat-redesign.md)
- [知识库技术债务台账](./debt.knowledge-base.md)
