# 知识驱动设计生成体系 · 计划

> **版本**：v1.32 | **日期**：2026-09-09 | **状态**：知识导入及生成来源绑定通过；PPT 翻页就绪、调用归属与 OpenDesign 修复反馈集中修复，产物质量及整体验收未完成

**一句话**：用一套可恢复、可追溯、可替换执行器的无头生成内核，同时支撑网页托管、知识库、HTML PPT 与网页微调四个入口。
**谁该读**：产品负责人、网页托管与知识库负责人、HTML PPT 负责人、CDS 与 Agent 运行时负责人。
**读完能做什么**：判断四个 MVP 是否真正共用同一条数据链、哪些执行器已可用，以及离可发布还差什么。

---

## 实时状态板

负责人：主实现轨道。状态按完成条件更新，不以代码量或历史测试数量估算百分比。“已实现”不等于“已验收”；历史构建与服务探针不能替代最终候选的真实用户路径。

本台账只保留产品目标、有效决策、真实缺陷、验收结论和下一步依赖。临时会话记录与重复执行日志不进入产品文档，也不得自动转为系统需求；业务审计、代码备份与密封验收证据独立保留。

### 当前批次：先验收网页托管主链

本批只使用已有授权合成网页验证主链，不重新生成整组对照样本。保留原列表与卡片布局，仅在既有操作菜单增加“帮我修改”；版本、发布与回退留在编辑面板中。未完成的 OpenDesign 启动修复独立保留，不混入本批发布。

| 顺序 | 要做什么 | 当前问题与状态 | 下一步 | 完成判据 |
|------|----------|----------------|--------|----------|
| B01 | 还原列表与卡片 | 已通过：本地及 CDS 桌面、390 像素触屏双主题复核，原菜单可进入修改 | 保留对应界面证据，不再扩张工具条 | 原工具条、右栏与卡片密度恢复，无新增常驻大按钮 |
| B02 | 正确读取及呈现版本记录 | 已定位一处入口对象缺失；错误契约和单条提示已部署，本地失败恢复与 CDS 正常历史通过，缺失文件未恢复 | 对缺失站点补部署后错误复验并核对真实备份来源 | 不提示检查修改输入，不重复弹窗，不建立空白基线 |
| B03 | 生成隔离修改草稿 | 已通过：指定颜色改变，文字事实未变，线上指针及公开文件均不变；用时约 219 秒仍需改善 | 后续优化流式处理等待，不重复本次生成 | 草稿形成，线上内容和当前版本均不改变 |
| B04 | 预览与发布草稿 | 已通过：实际点击发布，公开文件与草稿预览逐字节一致 | 保留对应版本及审计证据 | 预览内容正确，发布后真实访问内容更新 |
| B05 | 历史预览与回退 | 已通过：历史预览未变，回退恢复原始公开文件，原版及修改版均保留；版本界面双主题与触屏复核通过 | 保留对应版本及界面证据 | 原内容恢复，新增回退版本且全部历史保留 |
| B06 | 集中校验与部署 | 已通过：161afd3db 已推送及部署；前端 2453 项、API 4938 项通过及 4 项跳过，CI 与 CDS 冒烟全绿，真实主链通过 | 单列剩余体验与基础设施告警，不再为本批反复构建 | 结论绑定本批版本，不以本地合成响应冒充线上验收 |
| B07 | 恢复完整体系验收 | OpenDesign 尚无本轮有效产物 | 主链通过后依次验证 OpenDesign、知识库、HTML PPT、匿名评分和最终盲验 | 四个 MVP 与独立验收全部完成 |

### 后续批次：恢复 OpenDesign 与改善等待

| 顺序 | 要做什么 | 细化步骤 | 当前问题与状态 | 下一步与原因 | 完成判据 |
|------|----------|----------|----------------|--------------|----------|
| C01 | 同会话异步创建恢复 | 4 | 响应正文中断与落盘前提前就绪均已修；真实任务已越过容器就绪及消息派发，但后续出口探活失败 | 保留同一任务失败证据，后续出口问题独立处理，不重复创建恢复会话 | 同一请求身份从创建到就绪，只在持久化成功后派发，失败可追踪 |
| C02 | 降低增量事件等待 | 3 | 同一合成网页、同一改色要求，真实任务从创建到观察到草稿约 22 秒，前次约 219 秒；文字事实及结果哈希保持一致 | 保留单样本测量边界，不将一次改善推广为全部任务性能保证 | 真实任务耗时改善，顺序、取消与最终产物不退化 |
| C03 | 发布完成状态一致 | 3 | 已通过：真实草稿 95%、发布及回退后 100%；公开字节、历史预览和旧版本保留均经独立接续核对 | 保留同一任务证据；恢复失败继续由组件负面回归覆盖 | 仅发布成功显示完整完成，刷新恢复后一致 |
| C04 | 一次集中部署与真实复验 | 5 | 8ee461d53 已统一部署；两条 CI、公网三层冒烟通过，五个运行镜像内部版本及控制面提交已独立核对 | 用已补齐的入口和同一合成知识继续真实生成与发布，不换资料、不把部署成功等同产品闭环 | 公网入口可用，OpenDesign 真实产物及网关审计闭环 |
| C05 | 继续其余 MVP 与盲验 | 6 | 依赖 C04 有效产物，密封清单未开启 | 同输入同模型比较、独立视觉评分，最后密封验收 | 四个 MVP 及用户要求的独立验收均通过 |
| C06 | 完整知识选择 | 3 | 已通过：两个真实网页入口均能选择最近列表外的指定知识，来源身份一致；我的与团队范围、选择取消及双主题通过 | 保留修正版证据；本结论不替代生成、发布和完整权限矩阵验收 | 用户能选择有权访问的目标知识，不依赖资料碰巧处在最近列表 |
| C07 | 知识库上下文入口配对 | 4 | MAP 基线已保存；同输入 OpenDesign 实际使用相同 GPT-4.1，经四轮质量修复后触及 15 分钟期限，没有产物；独立审查确认修复提示丢失具体违规数值 | 补受控位置及数值反馈，再以原质量闸验证；不延长期限或放宽标准，最终输出是否合法仍需真实证据 | 第二 MVP 真实闭环，为匿名视觉比较提供合法同输入产物 |
| C08 | HTML PPT 新知识上下文隔离 | 4 | 25cc4c457 真实首帧导入门禁、带字等待、来源哈希、大纲确认及生成后刷新绑定通过；现有作品保留，未发布 | 生成与导入缺陷不混判；翻页就绪和降级质量单列 C10，继续同一作品恢复验收 | 新知识进入独立任务，刷新恢复当前任务，旧异步结果不串入新稿，快速操作不丢来源 |
| C09 | 出口代理健康检查 | 3 | 已通过真实启动：OpenDesign 进入模型调用，终态后专属容器、网络、卷与目录清理已独立核对；后续失败属于质量修复超时 | 保持隔离及 Key 门禁，后续质量失败归 C07，不重新改出口探针 | 代理真实健康时可启动，不健康时不派发模型请求，资源可清理 |
| C10 | PPT 就绪、审计与产物质量 | 5 | 真实四页稿中三页经两次事实校验拒绝后使用本地兜底；刷新时导航提前就绪导致首击丢失，大纲调用缺少显式任务归属 | 先修就绪与归属，集中校验部署；事实校验正确性和版式质量独立取证，不能将完成状态等同四页模型成功 | 首次允许翻页有效，全部模型调用归属可查，产物质量及真实发布通过 |

| 阶段 | 状态 | 未完成项 | 下一步 | 完成条件 |
|------|------|----------|--------|----------|
| 需求与边界 | 已确认 | 无 | 保持四个 MVP 边界不扩张 | 产品目标与授权范围一致 |
| 统一生成内核 | 本批限定复审通过 | 全链路真实故障恢复仍需验真 | 在候选环境复核 A01-A19 的真实路径 | 租约、事件、来源、发布与取消合同通过回归和独立复审 |
| 四个入口投射 | 已实现待 CDS 验证 | 最终候选真实长任务尚未验收 | 运行生成、微调、HTML PPT 发布及刷新恢复 | 四条用户路径均产出可追溯、可发布的结果 |
| 自动化验证 | 本批相关回归通过 | 未以相关测试替代全量及端到端验收 | 保留版本对应关系，继续真实路径验真 | 校验结果与最终候选版本对应，失败项明确关闭 |
| Provider 插件合同 | 待独立复审与真机验证 | CDS 运行时注册、隔离与限额仍需验真 | 检查替换边界和会话资源策略 | MAP 配置权威，未就绪执行器不可用，会话隔离有真实证据 |
| CDS 与真人路径验收 | 网页修改主链及菜单、历史双主题复核通过 | OpenDesign、其余 MVP、匿名产物评分及密封盲验尚未完成 | 按用户授权在 Key 侧门禁内执行，不增加另一套任务次数限制 | 公网入口、核心流程、独立评分和盲验均通过 |

## OpenDesign 真实链路任务台账

本轮目标是在 CDS 测试环境完成共享 Workspace Session、MAP 权威 LLMGW 会话配置、对象存储物化与产物清单提交，并以匿名双盲、独立视觉评分和对抗式审查证明效果。验收标准由独立 Agent 在实现前密封；实现轨道只能看到摘要哈希，最终只接收通过或不通过结论。

| 编号 | 要做什么 | 细化步骤 | 当前问题 | 下一步准备做什么 | 为什么做 | 状态 |
|------|----------|----------|----------|------------------|----------|------|
| T00 | 建立持续目标 | 1 | 无 | 保持目标到最终盲验结束 | 防止把骨架误报为完整交付 | 已完成 |
| T01 | 确认 CDS 测试分支可用 | 4 | 入口可用已确认，故障注入与业务端到端不在此结论内 | 以当前候选完成四个 MVP 验收 | 确保测试对象真实可用 | 公网页面、入口资源及认证冒烟通过 |
| T02 | 一次性盘点与确认资源 | 5 | LLMGW 登录与审计已打通；按原授权选取的三篇非敏感使用指南正文可读、哈希已核对；调用仅受 Key 门禁约束 | 生成前再核对冻结哈希，门禁拒绝时停止并报告，不绕过、不改 Key 配置 | 复用既有资源与费用治理，不另建重复控制体系 | 资源已盘点，真实配对待执行 |
| T03 | 密封盲验清单 | 4 | 标准不得向实现轨道泄露 | 最终由全新独立验收 Agent 接收匿名证据并独占解密，只返回通过或不通过与失败编号 | 防止按答案定向实现 | 已密封，待最终验收 |
| T04 | 冻结匿名基线语料 | 5 | 合成输入 MAP 基线成功，新 OpenDesign 候选同模型但质量修复超时；三篇真实指南的来源身份及哈希已确定 | 保留成功基线和失败事实，以同一来源快照与简报补齐有效配对 | 保证比较输入和模型条件可比，不把失败诊断算成盲验通过 | 语料已确定，正式有效配对待补 |
| T05 | 实现 CDS Workspace Session | 12 | 字节、inode、并发与失败回收代码闸通过，真实节点待验 | 部署后核对硬限额与自动清理事实 | 让 OpenDesign 可安全进入多租户测试 | 已实现待真机验证 |
| T06 | 实现对象存储物化器 | 7 | 本地合同测试通过，真实对象存储待验 | 在 CDS 下载内容寻址输入包并核对文件哈希 | 消除大文本往返并收紧数据面 | 已实现待真机验证 |
| T07 | 部署 OpenDesign 无头运行时 | 7 | 能力探针不能证明真实生成成功 | 核对固定镜像并运行真实任务 | 把 Provider 合同变成真实执行能力 | 待最终候选复验 |
| T08 | 接通 MAP 权威 LLMGW | 10 | MAP 基线与 OpenDesign 候选的实际模型、平台和任务归属已独立核对一致；OpenDesign 调用成功但无最终产物 | 保留真实审计，完成候选产物后再进行效果配对 | 保持 MAP 配置权威和 Key 门禁，不在执行链重复设置消费上限 | 两侧调用归属通过，额外次数预算已移除，产物配对待验 |
| T09 | 实现 Workspace Committer | 7 | MAP 与 CDS 的 run、基线、哈希、清单合同已对齐 | 在真实任务验证幂等、冲突、取消和超限 | 用原子提交替代同步复制 | 已实现待真机验证 |
| T10 | 投射四个 MVP | 8 | 网页微调、知识选择和 PPT 来源导入绑定通过；OpenDesign 没有产物，PPT 翻页与降级质量仍阻断 | 保留已通过证据及现有作品，修复直接缺陷并完成其余场景 | 证明架构不是单页面样例 | 四个 MVP 整体验收未通过 |
| T11 | 补齐交互与恢复 | 7 | 阶段、每三秒用时、取消、恢复已接；工作区实时画面留后续 | 在真实长任务检查持续变化和最终草稿 | 避免等待黑盒与静止加载 | 已实现待交互验收 |
| T12 | 首批提交与 CDS 部署 | 6 | 25cc4c457 五个业务运行镜像及公网三层冒烟通过，控制面保持 8ee461d53；本批三项直接修复集中校验 | 部署前排空活动任务，更新控制面后独立核对实际版本，不以服务健康代替业务闭环 | 给后续审查提供可运行证据 | 已部署基线可用，新批次待验证 |
| T13 | 对抗式工程审查 | 8 | 代码复核不能替代最终用户路径与效果验收 | 将本批通过结论绑定到候选，再进入 CDS 验真 | 主动暴露实现者盲区 | 本批限定复审通过 |
| T14 | 集中修复与复测 | 15 | 网页修改等待实测改善至约 22 秒，发布完成进度、完整知识选择及 PPT 导入竞态关闭；翻页就绪、大纲归属、OpenDesign 修复反馈和产物质量尚未关闭 | 本批只修三项已有证据的实现缺陷，不改模型、Key 门禁或质量标准；其他质量问题独立取证 | 把真实失败转换为可验证修复，不以源码测试替代用户验收 | 完整体系未通过 |
| T15 | 匿名视觉评分 | 7 | 新候选桌面双主题等待态、390×664 真实触控入口与表单已复验；完成浮层对比度发现直接缺陷，OpenDesign 无有效产物 | 修复完成浮层并复拍；取得有效配对后由匿名审查者评分 | 判断相对真实 AI 产品是否达到可用标准 | 部分界面通过，独立产物评分未开始 |
| T16 | 最终双盲验收 | 5 | 密封清单已重建，证据包尚未完成 | 新 Agent 独占解密，主轨道只接收通过或不通过及失败编号 | 避免实现者影响最终结论 | 待执行 |
| T17 | 架构图、MVP 与交接 | 6 | 依赖部署、对抗、视觉和盲验结论 | 汇总架构、发布门、已知边界、预览深链和交接清单 | 形成可维护而非一次性演示的成果 | 进行中 |

## 对抗问题台账

独立审查基线为 `13afa920d`，审查者未参与实现且不能读取密封验收标准。后端审查 P0 为零，随后产品集成审查发现一个 P0；所有 P0、P1 必须关闭后才进入视觉与最终盲验。状态“修复待复测”只代表代码已改，不能替代审查者复核与 CDS 运行证据。

| 编号 | 级别 | 问题 | 当前处理 | 下一步 | 状态 |
|------|------|------|----------|--------|------|
| A01 | P1 | 活跃租约可通过省略 owner 绕过围栏 | 生命周期统一验证 owner、期限与恢复观测值，Mongo CAS 同步约束 | 在最终复审复核负面测试 | 已修复待复审 |
| A02 | P1 | 发布绑定信任文件自报哈希且来源关系不完整 | 重哈希真实 revision 字节并核对 Run、站点、版本、执行器和 manifest | 在最终复审复核篡改与串 Run 测试 | 已修复待复审 |
| A03 | P1 | HTML PPT 绑定合成版本而非真实不可变版本 | 创建并绑定最终发布字节对应的真实 HostedSiteRevision | CDS 发布后核对预览、回退和哈希 | 已修复待部署 |
| A04 | P1 | 工作区已提交但 Done 丢失时可信结果可能被标失败 | result-ready 以精确 owner、lease、版本和无产物 CAS 重新排队，直接复用可信结果 | CDS 断点场景复验 | 已修复待部署 |
| A05 | P1 | HTML PPT 发布跨集合双写不幂等 | 持久 publish intent、租约 CAS、确定性站点与退避 dead-letter | CDS 并发和重启复验 | 已修复待部署 |
| A06 | P1 | Redis 或即时队列故障造成请求报错但后台执行 | Mongo queued 为权威意图，Redis 与即时队列退化为可恢复投影 | CDS 人为断流复验 | 已修复待部署 |
| A07 | P1 | 生成端点缺少功能权限 | 复用网页托管读写权限元数据并通过中间件回归 | CDS 非授权用户复验 | 已修复待部署 |
| A08 | P1 | 旧版发布记录占满窗口导致 v2 恢复饥饿 | 从未绑定 v2 Run 反查版本 | 复查最终 SHA 启动日志 | 已修复待部署 |
| A09 | 回归 | 新集合未进入数据同步范围 | 版本随站点同步，实例运行与删除任务明确排除 | 已通过 64 项范围守卫 | 已关闭 |
| A10 | P2 | 隔离运行时透传内部错误与 LLMGW 响应体 | 对外仅返回稳定领域错误，内部详情只进服务端日志 | CDS 恶意上游响应复验 | 已修复待部署 |
| A11 | P0 | MAP 与 OpenDesign 网页生成公开 SSE 合同不一致，后台完成时前端可能拿不到站点 | 两种运行时统一 phase/thinking/delta/done/error/cancelled，Redis 断流回退 Mongo 终态 | CDS 断流和刷新恢复复验 | 已修复待部署 |
| A12 | P1 | 网页生成与“帮我修改”缺少用户主动取消闭环 | 生成和编辑共用取消协调器；UI 显式停止，断开浏览器不取消服务端任务 | CDS 运行中停止复验 | 已修复待部署 |
| A13 | P2 | HTML PPT 的 SSE 与 FormData 裸请求未遵守可配置 API base URL | 所有裸请求统一走 resolveApiUrl，并修正对应静态闸门 | 分域环境复验 | 已修复待部署 |
| A14 | P0 | 已发布 revision 存在但 Run 尚未回写 Produced 身份时，恢复绑定被安全校验自身阻断 | 绑定事务允许从可信 SourceRunId revision 原子认领 Produced 身份，仍拒绝部分身份或目标站点不一致 | 新对抗者复核 generate/edit 恢复 | 已修复待复审 |
| A15 | P1 | Worker 把 Redis 投影失败当成生成失败，创建端容错没有覆盖执行中断流 | 元数据从 Mongo 构建，投影失败只停本轮投影；阶段和产物继续权威持久化 | CDS 验证断流 | 本地复测及限定复审通过 |
| A16 | P1 | 站点落库但 Produced 未回写时，result-ready 重放重复建站 | 按 Run 确定站点身份，重放核验真实对象哈希；补偿与执行租约互斥 | CDS 验证换租约重放且不重复上传 | 本地复测及限定复审通过 |
| A17 | P1 | 普通基线抢占版本导致 HTML PPT 或多文件网页的生成来源绑定失败 | 相同基线在验证站点、任务及实际对象字节后原子补齐来源和文件清单，冲突拒绝 | CDS 验证查看历史后仍可恢复发布 | 本地复测及限定复审通过 |
| A18 | 回归 | 同毫秒续租字节未改变时，ModifiedCount 为零被误判丢失租约 | 续租改用匹配数判定有效归属 | 保持有效归属与错误归属回归 | 本地复测及限定复审通过 |
| A19 | P1 | 补偿进程退出后过期清理租约永久阻断提交恢复 | 持久区分上传账本与已启动补偿；双租约过期后先完成清理再恢复可信结果 | CDS 验证补偿中断及失败重试不误删正常上传结果 | 本地复测及限定复审通过 |
| A20 | P1 | 网页生成与修改受部署白名单遗漏影响，绕过独立 LLMGW 后缺失统一审计 | 两类调用固化为 HTTP 路由，网关失败不回退旧域；生成及本批修改真实审计均已独立核对 | 保留任务、网关请求与版本归属证据，不补造旧审计 | 已关闭 |
| A21 | P1 | 导出目录重建丢失属主；修复后又发现 CDS 私有临时目录与 Docker 宿主路径不一致 | 属主和共同可见数据根已部署，保留非 root 与私有临时目录保护 | 当前启动故障解决后完成真实导出、清单提交及 MAP 保存 | 合成导出通过，真实生成闭环未通过 |
| A22 | P1 | 本任务新增的每次运行次数预算与用户最新 Key 门禁决策冲突 | 删除次数拦截及配置，保留原子审计计数与租约、票据鉴权；旧记录兼容读取 | 保留历史消费审计，不修改 Key 门禁 | 已关闭：部署及旧记录读取验证通过 |
| A23 | P2 | 暗色主题生成等待态的近白文字叠在白底上，提示不可读 | 空状态使用不透明主题底色，生成网页自身底色不变 | 完成态同类问题单列 A25，不能用等待态证据替代 | 已关闭：真实双主题等待态可读 |
| A24 | P1 | 同输入配对分别使用默认调度与指定模型，无法公平比较执行器效果 | 共用 MAP 配置已部署，MAP 新样本实际使用指定 GPT-4.1；OpenDesign 尚未进入模型调用 | OpenDesign 成功后核对实际网关模型及平台，再进行匿名评分 | 模型选择合同通过，真实配对待验 |
| A25 | P2 | 生成完成浮层引用不存在的背景类，在白色网页上近白文字不可读 | 完成浮层改用已有主题阅读背景，不改 iframe；独立代码复审及前端 2,449 项测试通过 | 部署后复拍暗色与浅色完成态 | 已修复，待部署视觉复验 |
| A26 | P1 | OpenDesign 真实生成尚未完成闭环 | 异步创建及出口探活已通过真实任务；新失败发生于质量修复阶段，先后处理缺失锚点和无依据量化事实，最终超时 | 恢复可证明的诊断材料，区分生成违规与质量闸误报，不重发掩盖故障 | 启动修复通过，质量修复超时未解决 |
| A27 | P1 | MAP 微调复制系统安全包装，回收时被自身安全闸拒绝 | 仅规范化唯一可信包装；数据库行为测试及 161afd3db 真实草稿、发布与回退通过 | 保留安全负面回归与版本证据 | 已关闭 |
| A28 | P1 | 入口文件不可读时版本记录错误归类为输入问题，并重复显示通知 | 候选站点对象 HEAD 证实缺失；稳定错误与单条提示已部署，本地真实组件失败恢复及 CDS 正常历史通过，不生成空基线 | 缺失站点错误路径仍需部署后复验；缺失正文需从真实备份恢复 | 错误修复已部署，缺失文件未恢复 |
| A29 | P2 | 本任务改版工具条和卡片超出用户要求 | 按历史改动还原原布局，保留原菜单内修改入口；本地及 CDS 桌面和触屏双主题实际渲染通过 | 保留菜单与版本界面证据 | 已关闭 |
| A30 | P1 | 模型约 14 秒完成，草稿总耗时约 219 秒 | 同站点同改色要求真实草稿观察耗时约 22 秒，事实与结果哈希一致；发布及历史回退接续均通过 | 保留单次实测边界，不宣称所有任务均达到该耗时 | 已关闭 |
| A31 | P2 | 已发布界面仍显示 95% 进度，完成语义不一致 | 组件路径、限定复审及真实发布前 95% 到发布后 100% 均通过 | 保留绑定实际发布版本的证据 | 已关闭 |
| A32 | P2 | 网页生成与修改只能选择少量最近知识，指定旧知识不可选 | 两入口均通过真实授权知识选择、来源身份核对、取消及双主题复验，原列表布局保留 | 保留机器身份及修正版标注证据 | 已关闭 |
| A33 | P2 | 真实路径中 CDS 活动流发生两次连接中断 | 与网页修改业务流分开记录；另外四条外部统计脚本被安全策略拒绝，不放宽策略 | 核对活动流恢复与统计注入来源，保留原始告警证据 | 未修复，不冒充零告警验收 |
| A34 | P1 | 从新知识启动 HTML PPT 时，旧稿串入或快速发送可能丢失来源 | 25cc4c457 真实首帧禁用、导入后来源三元组、大纲确认、生成及刷新恢复均通过；本地失败重试与旧响应围栏保留 | 发布与质量问题独立列项，不重复生成来覆盖失败 | 来源导入与生成绑定已关闭，完整 MVP 未通过 |
| A35 | P1 | PPT 刷新后已显示页码并允许点击，但导航监听尚未安装，首击丢失 | 同一作品真实时序证明就绪报告早于模板监听；当前文档就绪合同已修，组件行为、反向验证及独立复审通过 | 部署后用同一作品确认首次允许点击有效、旧文档消息不污染新稿 | 修复待部署 |
| A36 | P1 | 大纲请求未显式携带任务身份，跨进程 LLMGW 审计无法按 Run 归属 | 普通与流式大纲共用显式审计请求，保持原自动路由；序列化红绿回归及编译通过 | 全量及真实大纲审计复验，不凭时间窗口伪造强关联 | 修复待部署 |
| A37 | P1 | OpenDesign 量化事实修复提示丢失具体违规数值，无法精确定位 | 复用现有校验结果提供受控文档序号及数值单位，不传原始周边文本；定向测试通过 | 集中部署后观察真实修复，不改变质量判定、模型和运行安全限制 | 修复待部署 |
| A38 | P1 | PPT 四页中三页两轮生成均被事实校验拒绝，最终使用本地兜底 | 独立确认七次页面模型请求成功，但仅一页模型结果被接受；拒绝正确性尚未核实 | 核对实际被拒内容及视觉质量，不把完成状态、网关成功或兜底页算作质量通过 | 未关闭 |

## 执行顺序与资源边界

先按 B01–B06 完成网页托管主链，集中修复、校验并部署一次；OpenDesign 未完成变更不混入本批。主链通过后，再完成运行时修复与控制面兼容验证，依次验证其余场景、匿名配对、独立视觉评分及最终密封盲验。所有发布结论必须对应最终候选，不能以旧版本的运行记录替代。

当前资源授权为：选取三条非敏感现有知识和一条合成知识；不额外提供品牌资产；用户已明确取消本任务额外模型调用次数上限，由现有 Key 侧门禁控制；允许在指定 CDS 分支预览域名使用浏览器已保存的 MAP 管理员登录态完成验收；允许上述非敏感知识经项目 LLMGW 发送给其已配置上游。任何长期凭据都不得交给 OpenDesign，密钥值不得写入台账、日志、产物或验收证据。

真实调用保留 MAP 任务与 LLMGW 日志归属；当前会话列表为空不代表历史调用为零。验收样本、截图、运行日志及评分报告归验收知识库，本计划只维护任务状态、完成条件和已知边界。

历史核验发现原三十六次授权曾被超过：五个已关联本任务的 Run 至少有八十三条成功上游 HTTP 调用记录。该数量是已核实下界，费用字段缺失，不能据此推定账单金额。用户随后明确要求以 Key 侧门禁为准、不另设次数上限，因此撤销新增任务总预算控制的前置要求，并移除本任务曾新增的每次运行次数拦截，不改写历史消费或此前超限事实。超时、并发、修复轮次与容器资源限制仍承担运行安全职责；门禁拒绝时不得换 Key 或绕过重试。

一份合成输入已通过真实知识库上传、正文及原始下载字节一致性、设计引用哈希和固定任务要求核验，并完成首对诊断运行，未改变知识库公开分享权限。来源漂移以及客户端覆盖模型、网关地址和审计归属的四项负向请求均在创建任务前被拒绝；这属于接口诊断，不替代真人验收。输入哈希不代表条目不可编辑，生成前仍须拒绝来源漂移。

按既有默认授权，从当前账号的“MAP系统和设计”知识库确定视觉创作、文学创作、视频创作三篇使用指南。正文可读、来源身份与哈希一致，内容不含实际账户、客户、人事或凭据；证据独立保留，不修改源知识或扩大分享。三篇均为旧版指南，测试须标注来源日期并限定为忠实转化使用步骤，不把历史性能或版权陈述当作当前承诺；不得用合成资料替代这些真实语料后宣称完整。

## 目标产物

首个可发布版本不是四套独立功能，而是一个内核和四个投射：

1. 网页托管可选择知识、补两句话并生成可保存网页，生成中直接看到页面生长。
2. 知识库当前文档可选择“生成网页”或“生成 HTML PPT”，目标页自动带入来源。
3. HTML PPT 保留成熟的大纲、生成、微调与发布能力，同时记录知识来源与统一任务身份。
4. 已托管网页可继续“帮我修改”，生成草稿、发布新版本、预览历史并回退。

## 范围边界

- OpenDesign 是可替换的设计 Provider，不是 MAP 的任务中心、知识库、版本库或发布系统。
- 隔离容器属于 CDS Remote Agent 基础设施，可以单独项目化；MAP 不管理 Docker 生命周期。
- 首版默认使用 MAP LLM Gateway 执行网页生成与微调，复用现有 HTML PPT 执行链；OpenDesign 主容器只拿随机占位密钥，真实模型票据仅由 CDS 出口代理持有。
- OpenDesign、Codex、Claude 只有在 CDS 中存在真实运行时、健康检查通过且配置启用时才显示为可用。
- 首版不复制外部智能体实现，不宣称未部署的执行器已经接通。
- 首版不重写 HTML PPT 已有成熟链路，只补统一来源、任务与发布回写契约。
- OpenDesign 网页修改首版只支持声明式、自包含单页 HTML；脚本、表单、外链资产和 ZIP 多文件站点在任务创建前拒绝。

## 验收目标

| 编号 | 用户旅程 | 必须发生的结果 | 证据强度 |
|------|----------|----------------|----------|
| AC-001 | 网页托管引用知识生成 | 选择知识并输入短要求后，页面持续生长，完成后成为可再次编辑的托管网页 | 预览环境真人路径 |
| AC-002 | 知识库直达网页 | 当前文档一键进入网页生成，来源已带入且产物记录来源快照 | 预览环境真人路径与数据记录 |
| AC-003 | 知识库直达 HTML PPT | 当前文档一键进入 HTML PPT，来源已带入，可生成并发布到网页托管 | 预览环境真人路径与发布记录 |
| AC-004 | 已托管网页微调 | 修改生成草稿，发布后形成新版本，历史版本可预览并回退 | 预览环境真人路径 |
| AC-005 | 执行器事实展示 | 未部署的 OpenDesign、Codex、Claude 显示不可用原因，不能被选择或假装执行 | 接口与界面双证据 |
| AC-006 | 刷新恢复 | 生成中或完成后刷新，仍能按同一任务恢复阶段、产物和来源 | 集成测试与预览环境 |

## 发布门

- 四个入口使用同一知识引用结构与任务标识，来源删除后仍保留生成时快照；网页微调刷新后可按站点找回未完成任务。
- MAP 进程内模型调用经过 LLM Gateway；外部 Provider 只经 CDS 托管运行时，二者都使用服务器权威 Run、Worker 与可续接事件流。
- 等待超过两秒时，主视觉展示正在生长的网页或幻灯片，而不是静止加载文案。
- 生成产物在隔离预览中运行，不能读取 MAP 的认证存储或私有接口。
- CDS 动态容器全部就绪，公开预览域名完成四条真实路径验收。
- Provider 能力目录必须显示执行归属、隔离要求和真实可用性；会话级资源尚未强制时不得启用 OpenDesign。

## 已知后续

- 首版候选链提供持续阶段和用时变化，尚未把 OpenDesign 工作区中的临时 HTML 作为实时画面传回；最终原子提交不受影响，后续可增加只读临时快照。
- 会话并发配额当前由单个 CDS 进程原子管理；多副本部署前必须迁移到共享配额事实源。
- Workspace 输入包包含知识快照并作为运行审计证据随 Run 保留；首版尚未建立独立保留期、引用计数和自动清理策略。进入生产前必须与知识快照、站点删除和合规保留规则一起确定，不能由对象存储自行永久增长。
- 本次升级前已创建且没有冻结 CDS 连接标识的 OpenDesign 在途任务会安全失败并要求重发；部署窗口需先排空旧任务，不能承诺跨版本续跑。
- 受限知识引用当前保存来源快照并把工作区相对链接降为不可导航标记；发布闸门已校验来源外数值和敏感事实，面向读者的引用查看器、claim 级映射和同值指标语义比对留待后续。
- 多知识条目组合、跨库检索和向量召回不阻塞首版，首版以用户明确选择的知识快照为准。
- 新 CDS 节点不会提前宣称 OpenDesign 可用；它会在后台安装固定衍生镜像，并在 Docker、OpenCode CLI、资源策略和健康探针全部通过后启用。
- 其他外部执行器的授权、资源配额和模板许可证清单在启用前单独验收。

## 关联文档

- [知识驱动设计生成体系](./design.platform.design-generation.md)
- [网页托管与分享](./design.web-hosting.md)
- [MD 转 PPT 对话工作台收尾](./plan.md-to-ppt.chat-redesign.md)
- [知识库技术债务台账](./debt.knowledge-base.md)
