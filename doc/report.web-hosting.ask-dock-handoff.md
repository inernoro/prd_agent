# 「向我提问」形变坞 · 交接清单

**一句话**：访客页的提问入口重做成同一个 DOM 节点的四态形变，开场问题改由系统读正文自动生成；两条链路都已在预览域名上走真实路径验通。
**谁该读**：接手这条分支的人、决定要不要合并的人。
**读完能做什么**：知道改了什么、哪些有证据、还剩什么没做、下一步该找谁。

版本：2026-08-29 | 分支 `claude/web-hosting-feature-description-d2gbck`

> 本文是随分支更新的活文档，不是某一天的快照。改动或验证状态变了就同步这里；
> 与 PR 描述冲突时以 PR 描述为准（那份是提交时的权威）。

## 一句话结果

访客在托管页面上问问题的入口，从「一个按钮 + 一个抽屉」变成一件会形变的东西（胶囊 → 中下玻璃长条 → 右侧对话栏 → 竖条），开场问题不再要上传者手填。**对话与开场问题两条链路都已在预览域名上验通**——开场问题这条曾长期卡在「网关意图池成员数为 0」，根因是成员资格判据读错了字段（批量导入的模型永远进不了意图池），修掉判据并放入模型后复验通过。

## 改动范围

- 基线：`origin/main`
- 全 PR 累计：173 个文件，+21801 / −1309
- 模块：网页托管主控台与分享档（按设计稿重做）、访客页与提问组件、后端提问服务与开场问题生成、网关调度与意图池判据

本文只讲提问坞与开场问题这条线；设计稿重做那半边的逐屏改动见 PR 描述。

## 使用与验收

- **入口**：访客分享页右下角胶囊（站点需开启提问）；owner 侧在站点「提问设置」抽屉里看题库来源与「重新生成」
- **操作步骤**：打开分享链接 → 点右下角胶囊 → 挑一条浮在长条上方的问题或自己敲 → 右侧展开对话
- **预览深链**（根域名取自 `cdscli --human preview-url`，路由按本次改动追加）：
  - 访客提问坞：https://web-hosting-feature-description-d2gbck-claude-prd-agent.miduo.org/s/wp/uhmrj3eXAr5b
  - owner 侧主控台（站点右栏与提问设置入口）：https://web-hosting-feature-description-d2gbck-claude-prd-agent.miduo.org/web-pages
  - 网关控制台（意图池成员，判据修复的验收面）：https://web-hosting-feature-description-d2gbck-claude-prd-agent-llmgw.miduo.org/
- **登录**：访客路径匿名可用；owner 路径需平台账号，凭据走既有安全渠道，不在本文出现

## 八维检查

| 维度 | 状态 | 证据或待办 |
|---|---|---|
| 入口与使用路径 | 已完成 | 四态几何在真机逐态量过（收起 132×40 / 长条 660×54 居中 / 对话 400×视口高 / 竖条 44×视口高），坞全程只有一个节点 |
| 文档与决策 | 已完成 | [debt.web-hosting.md](./debt.web-hosting.md) #56–#88，含一条我自己走错的弯路（误判成去 MAP 建池） |
| 契约与数据 | 已完成 | 站点上新增两个字段记「这批题是谁写的、按哪一版正文生成的」；新增一个按正文重新生成的端点（owner/editor 门）；新增一个用于开场问题的调用方标识，黄金快照已同步 |
| 测试与证据 | 已完成 | 前端 246 文件 / 2216 用例全绿、tsc 零错误；后端 `PrdAgent.Tests` 1468 全绿；判据类改动逐条红绿闭环 |
| 发布与运维 | 已完成 | 预览环境两个出口均可达；意图池已配好可调用模型 |
| 安全与隐私 | 已完成 | 未引入新凭据；提问上下文由服务端取，不接受前端传入正文；调试用隧道已关闭 |
| 已知风险 | 已记录 | 见 [debt.web-hosting.md](./debt.web-hosting.md)，未做项在 PR 描述「后续事项」逐条列出 |
| 后续行动 | 见下表 | |

## 已运行验证

| 命令或路径 | 断言 | 结果 |
|---|---|---|
| `pnpm vitest run`（全量） | 246 文件 / 2216 用例 | 全绿 |
| `pnpm tsc --noEmit` | 零类型错误 | 通过 |
| `dotnet build` | 零 `error CS` | 通过 |
| `PrdAgent.Tests` | 全量 | 1468 全绿 |
| 真机四态（HTML / Markdown / 幻灯片 / 多文件 ZIP / PDF） | 几何、单节点、角标、额度真减 | 五类一致通过 |
| 真机手机端 390×844 | 三态、满宽长条、整屏对话、让开手势条 | 通过 |
| 提问端点直打（五类站点） | 不返回 `ASK_NO_CONTENT` | 五类正文快照全部取得到 |
| 视频包装站开提问 | 后端明确拒绝并说明原因 | 按设计拒绝 |
| 重新生成端点 | 模型不可用时不盖版本戳、给可执行文案 | 通过；六种结局各自的文案逐条核过 |
| 访客真问一句看到答案 | 答案流式长出来且只依据本页正文 | 通过：答出正文原话，额度真减 |
| 开场问题自动生成 | 五类站点各真写出题 | 通过（意图池判据修复并放入模型后复验） |
| 调度跳过停用成员 | 停用后连续三次跳过该候选并正常作答 | 通过（红绿闭环：推回队首必复现原错） |

## 风险与后续

| 事项 | 影响 | 优先级 | 负责人 | 完成条件 |
|---|---|---|---|---|
| 开启提问时的后台生成没有可见进度 | owner 保存后不知道题在不在生成 | P2 | 待产品决定抽屉是否留驻 | 真人开一次提问，全程屏幕有变化且题最终出现 |
| 站点列表访客数在应用侧聚合 | 站点多、访客多时内存与传输量偏大 | P2 | 本分支后续 | 聚合下沉到库里 |
| 分享管理屏依赖全局 100 条窗口 | 账号链接过多时台账数不全 | P2 | 本分支后续 | 管理屏有独立的完整读路径 |
| 访客阅读页只有暗色一套皮肤 | 浅色偏好的人观感不一致 | P2 | 既有缺口，非本分支引入 | 补齐浅色档 |
| 我在预览环境留了 4 个 `[坞验收]` 测试站点 | 轻微数据噪音 | P3 | 本分支 | 确认不再需要后删除 |

完整台账（含每条的修法与验收标准）见 [debt.web-hosting.md](./debt.web-hosting.md)。
