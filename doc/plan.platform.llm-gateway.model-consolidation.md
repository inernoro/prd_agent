# 网关模型概念收敛

**一句话**：把「模型池 / 逻辑模型 / 模型 / Provider / Exchange」五个入口收敛成「模型」与「上游」两个，因为池和白名单本来就是同一件事写了两遍。
**谁该读**：想知道这项工程做到哪一步、卡在哪、凭什么说某一步过了的人。
**读完能做什么**：不看代码也能独立判断当前进度与可发布性，不用再来问。

> 版本：2026-09-14 起 | 设计稿：<https://claude.ai/code/artifact/97bde0cb-89b9-40ea-817d-13ceba72b99c>

---

## 看板

**最后更新**：2026-09-15 · 单轨 · **距离可发布**：阶段 1、2、3、5、6、7、8 都已完成；**断流已做完，池上零流量**，阶段 4（删路删壳）等批准。不动对外契约，每一步都能独立发独立退。

| 阶段 | 进度% | 状态 | 当前 blocker | 下一步 | 验收证据 |
|---|---|---|---|---|---|
| P0 补验上一轮 | 100 | **已验收** | 无 | 无 | 在 CDS 预览上用一把只有 `route:read` 的最小权限 key 实打五条：清单 200 且形状合 OpenAI 标准、名单外模型 404、匿名 401、无线路的模型不出现、只授权给别的 appCaller 的模型不出现且带价的出现时 `pricing.routes` 有值。实测修掉两个真 bug（scope 判反、`via` 泄露内部 id），探针数据已清理 |
| 1 白名单能当默认 | 100 | **已验收** | 无 | 无 | CDS 预览上真打五条：①基线不点名走池「图片生成默认池」；②给 image1 设默认后同一请求变成 `LogicalModel · image1`；③再把 image2 设默认，image1 被自动顶掉且解析跟着变；④点名 image1 仍解析到 image1（默认不影响点名）；⑤**取消默认后原样回落到池**——回退承诺兑现。守卫 5 条，两条做过红绿闭环 |
| 2 存量池搬过来 | 100 | **已验收** | 无 | 无 | CDS 预览上真搬：15 池 → 11 模型 / 316 线路，4 个空池按原因跳过；重跑验证「不重复建、只补能力」（建 0 / 挂已有 11 / 补能力 11）。影子比对抓出两个真缺陷并已修（见下） |
| 3 页面收敛 | 100 | **已验收** | 无 | 无 | 部署在 031c0500 上真人路径走九条全过：导航只剩「模型」「上游」；上游页两段；展开 OpenAI 看到「114 个模型 · 4 个没登记」，没登记的排最前并直说调用方找不到它；转接段与旧地址 /exchanges 都落到 Exchange 那一页且图片分层锚点在；模型页标题「模型」且挂着存量模型池入口；模型池页冻结横幅在、新建与补齐按钮 0 个；池详情里删除/编辑仍在（冻结的是新建不是修复）；白天主题。排版漂移 0 项 |
| 5 调用全貌 | 100 | **已验收** | 无 | 无 | 判据收敛到一份：权威在 Core、控制台一份镜像、前端零份，两者由 71 条行为对照逐条比对（红绿闭环跑通）。部署上打真的：面板推演与运行时 `/gw/v1/resolve` 在三个模型上逐条一致（含「只给 appCallerCode 不点名」那条路）。真人路径视觉验收 8 条全过、双主题、排版漂移 0 项。过程中抓到并修掉两个真缺陷（见下） |
| 6 判据补主语 + 画图 | 100 | **已验收** | 无 | 无 | 部署 158bce01 上逐调用方实打：default-chat 的 14 个调用方 + 点名共 15 次真解析全部与面板一致，其中 `document-store.transcribe-summary::chat` 配了专属池，面板说「走不到这里」、运行时确实落到它自己的池（gpt-4.1-mini）而不是队首（gpt-3.5-turbo）——**修复前面板会说「会落到它」，这是那句假话的实证**。真人路径视觉验收 13 条全过、双主题真的不同、排版漂移 0 项。网关测试 1671 条全过，红绿闭环跑通（撤掉一个例外调用方即 2 条变红）。过程中冒烟自己又抓出一条：点名那条路也有同样的缺陷（见下）|
| 7 减枝 | 100 | **已完成** | 无 | 无 | 「健康优先 → 顺位」这一个判断从 **5 份收到 2 份**：删掉无人调用的 `SelectBestModel`（30 行），测试里那份「模拟」拷贝与池成员排序两处改为调权威判据（那 13 条用例原本测的是它自己）。池成员 Id 传补零下标保住原有次序，**是减枝不是改行为**。顺带删掉 `ResolutionType == "DefaultPool"`（永远为假）、它喂的那个恒为 false 的 DTO 字段（前后端各一处）、`NeedsModelConfigFallback`。新增两条守卫：标签字面量必须真的会被产出；这份判据只许两处。测试 1673 + Api 全量通过（215 条红全部是缺 MongoDB / ffmpeg，20 个测试类逐个核过；其中最像改坏的 3 条另开干净 worktree 跑基线对照，**不含减枝的代码上同样全红**）。部署 5ba58366 上重跑逐调用方冒烟：15 次真解析落点与减枝前**逐条相同**，含走池那条（`document-store.transcribe-summary::chat` 仍落 gpt-4.1-mini）——池成员次序没被这一刀动过 |
| 8 断流 | 100 | **已完成** | 无 | 无 | 盘线上数据：23 个调用方里只剩 1 个还绑池（`document-store.transcribe-summary::chat`）。直接清绑定会让它从自己池里的 gpt-4.1-mini 掉到全局默认 gpt-3.5-turbo——**换药不是断流**，所以先补了「按调用方的默认」这一层，又把写入侧那条「active 调用方必须绑池」换成「必须有人接得住」。切完真解析三条对照：它不点名 → **gpt-4.1-mini**（与走池时同一条线路，ResolutionType 从 `GatewayRegistryPool` 变成 `LogicalModel`）；点名 → 同一条线路；别的调用方不点名 → gpt-3.5-turbo（没有误伤）。复查全局：**还在走池的调用方 0 个** |
| 4 删路删壳 | 0 | 未开始 | **要用户单独批准**（不可逆） | 断流已完成，池上零流量，随时可删 | 待 |

## 阶段 4 的删除清单（等逐项点头）

盘完之后有一个和直觉相反的结论：**现在几乎没有「安全可删」的东西**。池那一整套不是死代码，
它通过 MAP 后台的「测试模型池」按钮和控制台的池详情页活着——所以「代码不删只会越积越多」
这句话成立，但账不在几个孤立文件上，在下面这三层。

| 档 | 删什么 | 体量 | 现在被谁活着 | 删了会怎样 |
|---|---|---|---|---|
| 一 · 已删 | 池端点测试器（`IPoolEndpointTester` / `HttpPoolEndpointTester`） | 103 行 | 只有一条测试 | 没人受影响。已在 2026-09-15 删掉 |
| 二 · 连带砍功能 | MAP 的池调度子系统 `PrdAgent.Infrastructure/ModelPool/` | 2277 行 | 唯一外部入口是 MAP 后台「测试这个池通不通」那个按钮 | 那个按钮一起没了 |
| 二 · 连带砍功能 | 控制台池页面 + `/pools` 路由 | 2225 行 | 新建已冻结，但池详情的查看与修复还在用 | 存量池没有地方可看可修 |
| 二 · 连带砍功能 | console-api 的 13 个 `/gw/pools*` 端点 | 约 1400 行 | 页面在用；其中 `migrate-to-models` 是阶段 2 的搬迁入口 | 删了就再也搬不了存量池 |
| 三 · 真正不可逆 | 解析器里的池分支（10 个方法，`ResolvePinnedGatewayPoolMember` / `TryFallbackToGatewayDefaultPools` / `FindGatewayOwnedOrMapModelPool` 等） | 约 900 行 | **线上流量还在走** | 旧路彻底没了，回退只能靠 revert 整个提交 |

**顺序必须是「先断流、再删路、最后删壳」**，反过来做等于白忙：

1. 把还在走池的调用方逐个切到对外模型，盯影子比对直到差异归零——**这一步不删任何代码**，随时可回退；
2. 差异归零且稳定一段时间后，删第三档（解析器的池分支）。这一步不可逆；
3. 第三档一删，上面第二档的三行自动变成没人调用的死代码，那时候删是纯删。

先删页面或先删 MAP 子系统都不减少任何东西：解析器照样在跑，判据照样是两份，只是少了看它的地方。

## 四个概念的关系（结论）

| 概念 | 结论 | 依据 |
|---|---|---|
| 模型池 | **并进模型**，兜底降成行上一个「默认」标记 | 与逻辑模型逐字段对照，七项里六项一一对应（对外名、候选集、优先级权重、健康三件套、挑法、授权），唯一真差别是 `IsDefaultForType` |
| 模型（物理） | **收进上游详情**，不再是顶层入口 | `Model.PlatformId` 必填，脱离上游的模型不存在 |
| 上游 | **独立存在但降一级**，进「配置」区 | 密钥与地址是一对多：一把密钥挂一批模型，并进模型页就要重复配 N 次，轮换漏一遍即静默 401 |
| Exchange | **降成上游的一个属性**（协议要不要转换） | 它回答的是「这个上游协议标不标准」，不是另一类东西 |

## 阶段 6 抓到什么（对抗审查的真实产出）

**那句「不点名会落到它」没有主语**（已修）。判据只看模型自己（是不是本用途的默认、启用了没、有没有能接的线路），全程不问「谁在调」。而运行时那道门是 `!严格池契约 || 目录例外`——调用方一旦配了专属池，对外模型这一档**整个被跳过**，不点名的请求落在它自己的池上。冒烟之所以全绿，是因为只跑了一个调用方，而它恰好不是这种：**一个样本判绿了一句全称命题**（判据太窄，形状 1）。

修法不是补个 if：把「这个调用方认不认对外模型目录」收进共享判据，运行时与面板共用一份，面板改成逐个调用方给结论，冒烟改成逐个调用方去对。顺带把那份「即便配了专属池也仍认目录」的调用方名单从解析器里收进权威判据——它原本散在那儿，控制台要用就只能再抄一遍。

**同一个洞在「点名」那条路上也开着**（已修，由新冒烟自己抓到）。那道门罩的不只是「不点名」那一档，它罩着整张对外模型目录——配了专属池的调用方哪怕点名这个模型也走不到这里。逐调用方冒烟第一轮就红了：点名 `document-store-transcribe-summary` 时运行时回的是 `GatewayRegistryPool`，压根没有线路标识可比。上一版冒烟挑一个样本，这条抓不到。结论句因此也补了范围，点名与不点名一起说清。

**浏览器取证的判据也差点为了错误的原因通过**。为了让无头浏览器认代理的中间人 CA，第一版随手写了 `--ignore-certificate-errors=false` 想「显式关掉忽略」——chromium 只看这个开关在不在、不看值，于是全局忽略了所有证书错误，页面照样打开、断言照样绿。改成从 CA bundle 现算指纹、用 `--ignore-certificate-errors-spki-list` 只钉那几张代理 CA 的公钥，别的证书照常验。

**顺手修掉一条会反向锁死的守卫**。原来有一条断言逐字要求某句文案存在，结果这次把那句话改得更准确（补上主语）反而让守卫变红——谁修谁的 CI 红（形状 4a）。已改成断言接线而不是措辞。

## 为什么值得做

不是为了少几个页面。是**判据只剩一份**：池与逻辑模型两套熔断恢复实现各自漂移，2026-09-11 修的三个稳定性洞里有两个出在这儿——Offering 没有半开租约而池成员有，于是 Offering 熔断后永远不会自己回来。

## 影子比对抓到什么（阶段 2 的真实产出）

做法：同一个 appCaller、同一个用途，先关默认让解析走池，再开默认让解析走模型，比最终落到的物理模型。脚本见文末「实现来源」。

**一、搬过去的模型能力是空的**（已修）。建模型时传了 `NormalizeDetailed(modelType, null)`，那个函数只在已有 `image_generation` 时才展开场景能力集，传 null 得到空集合。空能力的模型在能力门一律不放行，于是搬迁报成功、调用方却调不到它，请求默默回落到池——回落本身工作正常，但产出了一个死模型。改成从池成员的能力快照取并集，一条都没有时退到用途基线能力。

**二、健康状态一律重置太绝对**（已修）。`default-generation` 池里 `chatgpt-image-latest` 优先级最高但 18 天前被单次 401 隔离，池一直跳过它；搬迁重置成健康后新路立刻选了它，新旧两路稳定不一致。改成以 24 小时为界：窗口内的不可用照搬（它说的是现在），窗口外重置（它说的是过去）。

**三、剩下的那条差异是可解释的，不是缺陷**。修完之后新路仍然选 `chatgpt-image-latest`，旧路多数时候选 `gpt-image-1`——因为那条 18 天前的判断按新判据该重置。同一轮比对里**旧路自己也选中过一次** `chatgpt-image-latest`，那是池的半开试探在重新给它机会。两条路在往同一个方向收敛：新路直接给机会，旧路通过半开慢慢给。真坏的话熔断会在 5 次失败内（401 是 1 次）重新摘掉它。

结论：**不是搬迁缺陷，可以往下走**。留一条已知边界——搬迁后头几次不点名请求可能打到一个池已经躲了很久的上游；它要是真坏，熔断会当场处理。

## 阶段 5 抓到什么（对抗审查与实打的真实产出）

**一、面板指着一条运行时根本不会走的线路**（已修）。`SkipReason` 只看了线路自己的启用与熔断，
漏了「它指向的物理模型或所属上游被停用」——而运行时按 Offering 查目标时是带 `requireEnabled` 的。
线上 `default-chat` 的队首 `chat-latest` 物理模型正是停用状态，面板照样写着「会落到它」。
判据比它该管的范围窄（形状 1）。修完之后同一个模型上 93 条线路被正确标成「上游那个模型被停用了」，
结论改指 `gpt-3.5-turbo`——与运行时解析一致。

**二、「不点名会落到谁」是这个判断的第四份拷贝**（已修）。我在端点里写的默认模型查询少了
`Enabled` 过滤、也没排序，而运行时两者都有：于是停用的默认会被报成「现在的默认是 X」，
存量有重复默认时面板与实际还会指向不同的模型。顺带补上「真有一条线路能接」这一条——
所有线路都被摘掉时运行时会解析失败并回落，而面板此前照样说「会落到它」。

**三、一次假绿**（已修）。跑镜像测试时用 grep 过滤构建输出，两条 xUnit 编译错误被滤掉了，
`--no-build` 于是拿旧 DLL 跑出「57 条全过」。把断言写坏了照样绿——正是「测试自己坏了」那一形状。
现在 71 条。

## 明确不做

- 不改任何对外契约。调用方现在按什么名字请求，之后还是那个名字；池标识继续认，直到阶段 4。
- 不动 Mongo 集合名。旧集合原地保留，新表另写，回退就是把默认标记关掉。
- 不把四步合成一个 PR。阶段 4 不可回退，必须单独拿批准。

## 已知边界

阶段 3 的池冻结**只在控制台**：`POST /gw/pools` 仍然接受创建请求。这里刻意不动后端——阶段 4 才删旧实现，提前把接口关掉等于把回退路一起关掉。判据是「界面上不再引导任何人建池」，不是「谁都建不出来」。

阶段 3 冻结的是**新建**不是**修复**：摘坏成员、让成员恢复、停用整池仍然可用。旧解析路径还在兜底，成员真坏掉时人得能进去处置；全锁死会把人关在门外，比多一个概念更糟。这一条与用户说的「只读」有出入，已在交付里点名。

阶段 4 要删的 `PoolHealthTracker` / `ModelPoolDispatcher` 那套在生产请求链路上本来就是死代码（`IModelPool.DispatchAsync` 全仓无调用方），台账见 [debt.platform.llm-gateway.md](./debt.platform.llm-gateway.md) 的 `2026-09-11-model-pool-dispatcher-is-a-shell`。

---

## 实现来源

| 这一块 | 落在哪 |
|---|---|
| 影子比对脚本 | `scripts/llmgw-pool-migration-shadow.py` |
| 推演与运行时对照冒烟 | `scripts/llmgw-call-trace-smoke.py` |
| 架构总纲 | [design.platform.llm-gateway.model-architecture.md](./design.platform.llm-gateway.model-architecture.md) |
