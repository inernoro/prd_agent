# MAP 核心业务稳定基线落地 · 计划

> **版本**：v2.0 | **日期**：2026-08-13 | **状态**：执行中

**一句话**：把核心业务稳定基线分成台账、凭据、真实旅程、视觉证据、回滚演练和长期治理六个阶段逐项关闸。
**谁该读**：负责推进 MAP 稳定性建设、安排复测资源、审核验收报告或决定是否发布的人。
**读完能做什么**：看清每个阶段的完成状态、当前阻塞和下一动作，并据此追踪到可验收、可回滚的完成状态。

## 目标状态

每条 P0/P1 功能线都有可执行的双环境真实旅程；每 48 小时自动复测；任何逃逸问题进入永久回归；conditional 或 fail 定向通知；正式发布与回滚全部通过 CDS 留痕。

## 阶段与闸门

| 阶段 | 交付内容 | 完成闸门 | 当前状态 |
|---|---|---|---|
| A 基础台账 | 功能线 JSON、面包屑、源代码映射、caseId、双环境与回滚策略 | 目录校验全绿，核心代码无未映射变更 | 已实现 |
| B 调度与凭据 | 48 小时任务、双环境独立账号密钥、合成登录、MAP 定向通知 | 手动触发一轮，账号预检通过，通知可回读 | 部分完成：本地任务已启用并完成首跑；正式通知认证需随本 PR 发布后回读 |
| C 核心真实旅程 | 录音、上传解析、视频、文学、视觉、多图和 LLMGW 最小真实产物 | CDS 全量、正式安全变体、清理为零 | 部分完成：本轮 CDS 28 通过、7 失败、1 抖动；正式环境只读核心检查通过，写入旅程未放行 |
| D 视觉与恢复 | 桌面和移动端截图、进度尺寸、刷新恢复、错误可读性 | 标准验收报告归档，关键页面双主题或适用主题通过 | 部分完成：148/148 个唯一验收位已取证，36 通过、112 需干预，整体不通过 |
| E 发布回滚演练 | CDS 固定 commit 发布、previous 回滚和公网入口复测 | ReleaseRun 和回滚证据完整，恢复时间达标 | 能力已核对：正式目标、38 条 ReleaseRun 和 previous 锚点存在；演练未执行 |
| F 长期治理 | PR 自动纳入、永久回归、抖动和成本趋势 | 连续三轮无漏测、无残留、无重复逃逸 | 待实施 |

## 执行顺序

1. 为 CDS 环境和正式环境分别配置环境托管的固定 `admin` 破窗账号，并准备 `stsmk_cds_admin`、`stsmk_prod_admin` 或等价专用账号；真实值优先写入本机钥匙串或平台 Secret Store，专用账号加入合成登录白名单。
2. 创建 Codex 桌面端本地自动化 `stable-smoke-48h`，立即手动运行一次，先完成目录、协议和入口基线。
3. 按风险顺序补真实旅程：多图视觉与 LLMGW、录音与上传解析、视觉单图、视频、文学、知识库与资产。
4. 每补一条旅程，同时补夹具、产物断言、清理和正式环境安全变体，不接受只有点击脚本的用例。
5. 运行标准视觉验收并归档报告；修复所有 P0/P1 后再把对应功能线的 `automationStatus` 提升到 `journey`。
6. 在非高峰窗口用 CDS 演练一次 previous 回滚，复测公网入口与最小真实产物。
7. 连续三轮观察失败率、flaky、耗时、成本和清理残留，达到闸门后把基线状态更新为已落地。

## 2026-08-04 执行快照

| 项目 | 当前事实 | 结论 | 下一关闭动作 |
|---|---|---|---|
| CDS 部署版本 | 业务代码已在 `4ea5ba5fe` 完成 CDS 部署和多图真实复测；后续 `21b76a0da` 只修正验收脚本断言 | 通过 | 后续测试继续同时记录业务版本与验收脚本版本 |
| ASR 网关权威 | 确定白名单值与守卫已提交，新配置导入 `4a3fa816785f` 仍待 CDS 批准；当前真实转写继续失败 | 不通过 | 批准导入并重新部署后跑结束转录、音频上传和清理三项 |
| CDS 业务矩阵 | 103 项计划中 77 项有执行证据，74 通过、3 个 ASR 失败、26 未执行；多图 14/14 已通过 | 不通过 | 批准 ASR 配置导入并复测，再补齐 26 个真实自动化步骤 |
| 正式业务矩阵 | 103 项均因正式 MAP 产品合成身份未开通而未执行 | 需干预 | CDS 闭环后通过 CDS 正式发布记录开通 `stsmk_prod` 和白名单 |
| 视觉证据 | 旧轮采集文件 124 张；新门禁实跑确认逐张均缺少主验收状态、测试类型、结论、主题、设备、方法或面包屑，合格证据为 0/148 | 未执行 | 按十模块 148 张计划重跑；单图与多图各不少于 18 张，逐张可反查路径与方法 |
| 上一版主管报告 | [旧双环境账本与 124 张采集文件](https://cds.miduo.org/reports?project=prd-agent&folder=935f3a2b07f34bf8ba4dff783b57f051&report=8454e3dd393a47bc91ddf0a13f0540c3) 已被新口径判定为视觉证据不足，不再作为视觉通过依据 | 已作废视觉结论 | 新报告必须显示 74 项通过、3 项失败、129 项未执行，并把视觉显示为 0/148 合格证据 |
| 技术附录 | [原始完整证据与技术明细](https://miduo.org/reports?project=prd-agent&folder=0deea4df39e1457eb875c6fa79d3b1d7&report=d1af281d864642fbbf57c16178f34d63) | 可审阅 | 仅在定位时查看命令、接口、日志与内部佐证 |
| CDS 自动化身份 | 项目级密钥 `fd885dcd` 已存钥匙串，验证只能读取 `prd-agent` | 通过 | 后续正式预检、发布、回滚和报告归档只用该身份 |
| 正式发布与回滚 | 受管正式目标存在；最近成功运行带 previous 锚点 | 通过（能力） | 在非高峰期完成一次真实 previous 回滚演练 |
| 本地 48 小时调度 | 计划、提示词和互斥运行器已定义；当前会话未暴露原生自动化管理接口，桌面应用也禁止自动控制 | 未执行 | 在 Codex 自动化接口可用的会话创建 `stable-smoke-48h` 并立即首跑 |
| MAP 定向通知 | 脚本和拒绝全局发送门禁通过；正式通知身份与目标用户 ID 未开通 | 未执行 | 正式合成身份开通后发送并回读一条通道测试 |
| 新增永久回归 | 非法短视频文本过去会被排队；现已在入口拒绝并给出重新粘贴动作 | 通过 | `REG-short-video-input-001` 已激活并由计划器自动纳入，证据目录 `/tmp/prd-agent-stable-smoke/stsmk-20260804-short-video-regression-active` |
| 报告分层 | 主管报告已去除技术编号，先显示发布建议、是否需主管介入、异常项、完整面包屑与可点击方法；采集文件和合格证据分开统计 | 进行中 | 完成 148 张新取证后归档新版主管报告和独立技术附录 |

## 2026-08-13 执行快照

本节是当前状态；上方 2026-08-04 内容保留为历史基线，不再代表最新结论。

| 项目 | 当前事实 | 结论 | 下一关闭动作 |
|---|---|---|---|
| 固定版本 | 稳定复测绑定业务提交 `f6c6a41ff86f732156c929e88b5473f78261744b`；后续索引启动修复与技能分发快照已在 CDS 按 PR 最新提交独立验证 | 可追溯 | 合并前继续以 PR 最新提交通过 CI、CDS 和 Code Review 为准 |
| 本地 48 小时调度 | Codex 本地自动化 `stable-smoke-48h` 已启用，执行环境为 local，工作目录为项目主工作区，按每两日 02:17 运行 | 通过 | 后续运行沿用同一功能台账、永久回归账本和报告门禁 |
| CDS 功能旅程 | 36 项中 28 通过、7 失败、1 抖动；失败集中在视频供应能力、真实 ASR、ASR 双成员池、图片双路由及真实单图和多尺寸生成 | 不通过 | 补足外部供应能力、额度和真实路由后按原 caseId 复测，禁止改成技术错误或假通过 |
| 正式环境旅程 | 只执行只读核心检查并通过；因 CDS 功能闸未关闭，没有放行正式环境写入旅程 | 条件通过 | CDS 七项失败关闭后，再由同一运行按生产计划执行限额写入 |
| 视觉验收 | 十个模块 148/148 个唯一验收位均有逐项证据；36 通过、112 需干预，含 21 个移动端取证位 | 不通过 | 逐项关闭占位状态、外部能力失败与视觉缺陷后复跑，不以截图数量代替结论 |
| 用户可读错误 | 视频失败已统一落为可理解、可恢复的用户提示；原始供应商和协议细节仅保留在诊断证据 | 通过 | 其他模块发现同类错误时追加永久回归用例 |
| 报告归档 | [本轮主管验收报告](https://cds.miduo.org/reports?project=prd-agent&folder=0bd4f98a9322453c8b1ae4921881fa60&report=2cb1514e95f1426b92b5ce460243d2c0) 已读回标题、runId、固定提交、图片和内部跳转 | 通过 | 审核人先看发布结论和需干预项，再按模块展开截图与测试方法 |
| MAP 定向通知 | 报告归档后已按定向签名通道尝试一次；生产尚未包含本 PR 新增认证代码，服务返回未授权，未降级为全局通知 | 未完成 | PR 合并并通过 CDS 留痕发布正式环境后，重新发送一次并回读目标用户收件结果 |
| 发布与回滚 | CDS 最新提交五个服务均健康，PR 可合并且 CI 全绿；正式环境未部署未合并提交 | 条件通过 | 合并后先由 CDS 固定版本发布，再执行正式验收；异常时使用 CDS previous 锚点回滚 |

本轮 runId：`stsmk-20260813-pr1350-final-r5`。本地证据目录：`/tmp/prd-agent-stable-smoke/stsmk-20260813-pr1350-final-r5`。该目录用于本机定位，主管审核以已归档报告为准。

## 失败时限

| 严重度 | 示例 | 第一动作 | 恢复目标 |
|---|---|---|---|
| P0 | 登录不可用、数据污染、主要创作全失败 | 立即暂停写入并通过 CDS 回滚 | 30 分钟内恢复核心入口，随后补永久回归 |
| P1 | 单模块失败、产物错误、清理残留 | 阻断模块，通知并指定负责人 | 当日修复并双环境复测 |
| P2 | 单端布局或非主路径问题 | 保留证据并进入下轮 | 两个工作日内处理 |

## 验收清单

- [x] 业务功能台账可机器校验，并包含面包屑与回滚动作。
- [x] PR 变更可自动映射功能线，active 回归固定纳入。
- [x] 48 小时双环境任务和固定版本报告已定义。
- [x] 通过 Codex 自动任务管理能力创建本地定时任务，并完成首次真实运行。
- [x] MAP 通知脚本拒绝全局发送，不包含 Slack 通道。
- [ ] CDS 与正式专用账号、白名单和独立密钥已配置（CDS 已完成，正式待开通）。
- [ ] 所有 P0/P1 功能线均达到 `journey`。
- [x] 完成一轮 CDS 环境完整复测和正式环境只读安全复测；正式写入因 CDS 闸失败保持阻断。
- [ ] 完成一次 CDS previous 回滚演练。
- [x] 标准验收报告已归档并读回校验通过（当前结论为失败，不代表模块闸门已关闭）。

未打勾项或任一 P0/P1 失败会使整体结论保持失败或 conditional，不得对外宣称全功能基线已经完成。截图数量达到下限仅代表证据预算达标，不代表功能通过。
