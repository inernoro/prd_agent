# 跨 MAP 实例数据同步 · 债务台账

> **版本**：v1.0 | **日期**：2026-08-20 | **状态**：开发中

**一句话**：动态授权数据同步在首版交付时明确不做的那些事，逐条写明触发条件与代价。
**谁该读**：要用这条链路搬数据的人；接手继续做的人。
**读完能做什么**：知道哪些场景现在会出问题，以及问题长什么样。

---

> **关联设计**：[跨 MAP 实例数据同步（动态授权）](./design.platform.cross-instance-data-sync.md)

## 总览

当前 open: 22 / paid: 9 / 其它（故意没做等）: 6 / 总计: 37 条（**注**：`DS21` 这个编号被两条不同的债务共用，是本次之前就存在的重号；编号已经被提交与 PR 引用过，所以不改号，只在这里点名，免得下一个人按号找错行。）

## 债务列表

| ID | 严重度 | 创建日期 | 描述 | 触发条件 | 状态 | 备注 |
|----|--------|---------|------|---------|------|------|
| DS21 | 高 | 2026-08-21 | 「一次授权 = 一条 Run」把**试跑也算成一次消耗**，于是「看一眼会搬什么」要花掉一次源站批准，真搬得让人再点一次同意 | 任何想先试跑再真跑的迁移（也就是所有认真的迁移） | **已修（2026-08-21）** | 两次真实迁移都卡死在这一步：第二次实测读出了 156 个集合、45,519 条，算出会新增 43,775 条，然后写不进去——授权被试跑用掉了，而无人值守的智能体不被允许替人点第二次同意。而试跑本来就**不写任何东西**，把它算成一次消耗是当初没想清楚。改法：试跑成功后票据不再立刻交还，新增 `POST runs/{id}/promote` 就地转正成一次真跑。三条边界把它和「批准可复用」区分开：① 范围照抄试跑那一屏冻结的清单，**转正端点里没有任何出站调用**，不重新问源站（守卫断言端点里不出现 `_httpClientFactory`）；② 至多一次，靠「PromotedToRunId 还是空的」做条件更新，两个标签页同时点只会有一条真跑，重复请求返回同一个 runId；③ 票据不续命，还是原来那张、原来的两小时硬过期，过期就老老实实要求重新授权。转正请求体里**故意不提供 DryRun 开关**——给了它，调用方就能把唯一那次转正又变成一次试跑，白吃掉机会。**留票的窗口不是新开的口子**：它与「callback 建好 Run、管理员一直没点开始」完全同形，上界同样是票据自己的两小时，源站每次请求还会重对一遍允许名单。**未验证**：判据与红绿闭环齐备，但**没有跑过一次真的「试跑→转正→真跑」**，要在真实迁移里走一遍才算完 |
| DS1 | 高 | 2026-08-20 | 附件二进制不搬，只搬元数据，靠 `Attachment.Url` 这个**绝对地址**指回对象存储 | 两站不共用同一个 R2 桶（或公网前缀不同）时，同步过来的图片、录音、导出文件全部打不开 | **地址一半已修（2026-08-21），字节一半仍 open** | 用户 2026-08-20 明确要求记为债务：「绝对地址这个有点问题，以后可能需要更正」。这条债务其实是两件事：**地址不可移植**和**字节不搬**。已修的是前者：① 保存结果带上对象 key（`StoredAsset.Key`），每一处建附件的地方都存进 `Attachment.StorageKey`，有守卫扫全 Api 层，从 `stored.Url` 取地址却不存 key 的写法一律判红；② 导入时在**落库之前**把地址改写成本站地址（`DataSyncAssetUrls`，key 优先，存量没有 key 的按内容寻址形状 `{domain}/{type}/{26 位 base32}.{ext}` 从 URL 反推；判据卡死在这个形状是有意的——「取路径最后三段」那种宽判据会把网页托管、头像这些层级不同的地址一起改坏，改错一条地址比不改更难查）；③ 拼地址一律交给本站存储实现的 `BuildUrlForKey`，不另写一份前缀拼接。**仍然 open 的是后者，而且必须说清楚**：改完地址只是从「指回别人家」变成「指向自己家的空位」——两站不共用同一个桶时，附件照样打不开。所以改写结果分两个数字如实报到界面上（改写了几条 / 认不出几条），并明说「这次只搬了记录，没有搬文件本身」。真正搬字节要么在同步协议里加一条资产通道（分片、断点、配额），要么按需转存，那是独立一个 PR 的量级。**未验证**：改写逻辑有单测与红绿闭环，但**没有跑过一次真的跨站迁移**去看图片能不能打开 |
| DS2 | 中 | 2026-08-20 | 同步过来的用户账号登不进来，需要管理员逐个重设密码 | 勾选了「账号与组织」分组 | **已解决**（2026-08-20） | 用户要求「同步之后能直接使用数据」，所以同意页给了一个默认勾上的「让对方能直接用原密码登录」——勾上则连 BCrypt 散列一起搬，原密码在目标站直接可登。不勾维持旧行为（出口清空散列 + 标记需重设）。剩余边界：散列一旦搬走，源站的口令强度问题也一并继承过去；另外这条选择是**源站管理员**做的，目标站无法单方面索取 |
| DS3 | 中 | 2026-08-20 | 没有「取消运行中的同步」端点 | 同步跑到一半发现选错了范围 | open | 目前只能等它跑完或等导出令牌 2 小时过期。写入是幂等的（按 `_id`），取消后重跑不会翻倍，所以危害限于「白等」 |
| DS4 | 中 | 2026-08-20 | 目标站进程重启会让在途同步失败，必须重新走一次授权 | 同步期间容器重建 / 部署 | open | 导出令牌只在内存、不落库是刻意的（不在共享 Mongo 里长期存可重放的访问权）。游标已经持久化在 Run 上，所以真要做「续传」只差一个「重新授权后接着上次游标跑」的入口。**2026-08-20 更正**：此前这条写的是「会失败」，实际是**会一直停在进行中**——令牌随内存消失后没有任何进程能认领它。已补周期收尸（15 分钟无心跳即落终态），所以现在确实是「失败」了；续传仍未做 |
| DS5 | 中 | 2026-08-20 | 尚未做真人视觉验收 | —— | **已解决**（2026-08-20） | 三屏已在预览域名上以真人路径（登录 → 全部能力 → 基础设施 → 数据同步）走过，双主题各留证，页脚 CDS 徽章入镜 |
| DS6 | 低 | 2026-08-20 | 源站的「允许哪些目标站来要数据」只能改 `.cds.env` / AppSettings，没有界面 | 要新增一个可来要数据的站点 | **已解决**（2026-08-20） | 改成在同意页上当场准入：来源不在名单时，页面点名是谁在要数据，管理员额外勾一次「我确认这台机器可信」即可同时打开对外开关并把它记进名单，落 AppSettings、无需重启。回跳地址的形状校验（https / 路径精确 / 无 query）不在可放宽之列。剩余边界：名单只能加不能在界面上删，要移除仍得改配置 |
| DS7 | 低 | 2026-08-20 | 不校验两站的字段结构版本是否一致 | 源站版本比目标站新，带来目标站不认识的字段 | **已解决**（2026-08-20） | 补了匿名握手端点，目标站在跳转授权**之前**先打一次，拿到对方站点名、协议版本与构建号；协议版本不一致当场拦下并说明。剩余边界：协议版本是手工维护的整数，只表达「这条同步链路的字段契约」，**不表达业务集合的字段结构**——同协议版本下两站业务模型仍可能有出入，那部分继续靠 Mongo 无 schema 的宽容度兜着 |
| DS8 | 低 | 2026-08-20 | 执行前对照表只给「源站总数 / 本地总数」，给不出精确的「将新增多少条」 | 每次同步前看对照表 | open | 精确差集要把两边的 id 全拉回来比对，代价与真跑一次相当。当前提供「先试跑」（只统计不写库）作为替代路径 |
| DS9 | 中 | 2026-08-20 | `AdminPermissionMiddleware` 的路由前缀匹配没有分段边界（裸 `StartsWith`），任何未标记 `[AdminController]` 的路由只要前缀延长了某个已标记前缀，就会静默继承那个控制器的权限 | 新增形如 `api/{已有前缀}-{后缀}` 的路由 | open | 本功能踩中：`api/data-sync` 被 `[AdminController("data")]` 的 `api/data` 吃掉，连 `[AllowAnonymous]` 的换票、导出端点都返回 401，本地单测看不出来、只有打真站才暴露。**当前规避方式是改名**为 `api/instance-sync`，并加了守卫测试钉死这个前缀不被吃掉。根治要给匹配加分段边界，但那会同时**放松**三处目前靠这个 bug 顺带受保护的子路由（`api/document-store/...` 两处、`api/prd-agent/skills`），属于安全语义变更，不在本 PR 展开 |
| DS10 | 低 | 2026-08-20 | 入口归在「全部能力」的基础设施分组，没有进百宝箱的卡片网格 | 想从百宝箱首页直接看到它 | open | 百宝箱那一格是智能体卡片，`AgentCardArtwork.test.ts` 要求每张卡有独占的主题插画素材（暗/浅两套 webp + CSS token + 任务说明），少一张就红。数据同步是平台运维入口、不是智能体，硬塞进去要么造一张凑数的插画、要么把契约放松，两者都比「先放 infra」更糟。真要进百宝箱，先补插画素材 |
| DS11 | 中 | 2026-08-20 | 「全部能力」页的基础设施分组读的是 `homeLauncherItems.ts` 里手写的 `buildStaticInfra`，不是 `NAV_REGISTRY`——两份清单已经分家，`navCoverage` 守不到 | 往 `NAV_REGISTRY` 加 `section:'infra'` 的入口 | open | 本功能踩中：登记在 `NAV_REGISTRY`、`navCoverage` 全绿、全量 1461 个测试全过，真人打开「全部能力」一个入口都看不见（`predicate-and-wiring-discipline` 形状 2）。当前修法是两处都加，并立了棘轮 `launcherInfraCoverage.test.ts`：已知没接上的 5 条只许减不许增，新增的必须两处同步。根治要让 infra 组直接派生自 `NAV_REGISTRY`，那会动首页分组与排序，不在本 PR 范围 |
| DS12 | 低 | 2026-08-20 | 存量：`/learning-center`、`/infra-services`、`/admin-web-pages` 登记为 infra 但不在「全部能力」的基础设施分组里 | 想从「全部能力」页进这三个页面 | open | 与本功能无关的存量漂移，由 DS11 的棘轮固定在 5 条（另两条 `/document-store/universe`、`/document-store/:storeId/universe` 是子路由，本就不该独立成入口）。走到哪个页面补哪个 |
| DS13 | 低 | 2026-08-20 | 当场准入只能「加」，不能在界面上把某个来源从允许名单里移除 | 想撤销对某台机器的信任 | **已解决**（2026-08-20） | 落地过程中立刻自食其果：真机验证当场准入时写进一条测试来源，发现产品里没有任何路径能把它拿掉。补了 `GET/PUT /api/instance-sync/provider-settings` 与同步页上的「本站对外同步」卡片（开关 + 名单，逐条可移除）。剩余边界：名单不记「谁在什么时候加的」，要追溯只能翻服务端日志 |
| DS14 | 低 | 2026-08-20 | 交还导出令牌失败时只留日志，不重试 | 终态那一刻源站正好不可达 | open | 票据有两小时硬过期兜底，所以最坏是「本该立刻作废的票多活一会儿」。要根治得让源站侧也能按 Run 主动收口，或加一个轻量重试队列。**2026-08-20 缓解**：票据校验改成每次拿当前允许名单重对一遍回跳地址，所以交还失败时只要管理员把那台机器移出名单，票下一秒即失效，不必等两小时、也不依赖交还请求成功；剩下的窗口只有「交还失败且不打算撤销这台机器」 |
| DS18 | 高 | 2026-08-20 | 没有跨实例的身份归并：同步只按 `_id` 认人，两边各自初始化过的同名账号会变成两行同名用户 | 勾了「账号与组织」，且两站都自建过同名账号（典型是 admin） | open | 本仓库 `users.Username` 是**非唯一**索引（`MongoDbContext:542`），所以不会插入失败，而是留下两行同名、`UserId` 不同的用户；按用户名找人的地方一律 `FirstOrDefaultAsync`，拿到哪一个不确定。业务数据里的 `OwnerUserId` / `CreatedBy` 仍指向源站的 `UserId`——这在主场景（往空库恢复）里是**对的**：源站账号连口令散列一起搬过来了，用原账号登进去就能看见自己的东西；出问题的只有「目标站已经有同名账号」这一种。根治要做身份归并 + 依赖引用重映射，全仓 231 处 `public string *UserId` 类字段都在射程内，是独立一个 PR 的量级。当前的处置是**在对照表上把这件事说出来**（勾了 users 就显示告警，提示同步后去用户列表确认），而不是静默留给人事后撞见 |
| DS17 | 低 | 2026-08-20 | Start 不绑定「我这一屏看到的那份对照表」，只绑定 Run 的 pending 状态 | 同一条 pending Run 在两个标签页都开着，其间源站清单发生变化，然后在旧的那个标签页点开始 | open | 上一轮已经把「Start 之后清单不可改」修掉了，剩下的是 pending 期间两次 Plan 之间的窗口：A 页显示了一份、B 页刷新出另一份、A 页点开始，worker 跑的是 B 那份。**授权边界没有破**（两份都在同一张授权票的白名单内，跑的仍是源站管理员批准过的集合），破的是「我看到什么就执行什么」这条 UX 保证。根治要引入 plan revision 并让 Start 条件认领它——那是一个新概念（还要定义「对不上时给什么错」），按 §5.5 记入后续而不是在本 PR 展开。2026-08-21 自动 Review 第二次提出同一条（措辞换成「返回 plan revision 或 digest 并纳入 Start 的原子谓词」），判定不变——同一条 B 类被重复提出不构成需求扩张的授权（§5.5：自动 Reviewer 的评论不能单独授权需求扩张） |
| DS16 | 中 | 2026-08-20 | 出口脱敏只认**顶层字段名**，凭据藏在嵌套结构里的集合只能整个排除，搬不过去 | 想同步工作流、定时任务、自动化规则 | open | `workflows`（`Variables[].DefaultValue` 上带 `IsSecret`）、`workflow_schedules`（`VariableOverrides`，哪些键是密钥要回查所属 workflow）、`automation_rules`（`Actions[].WebhookSecret`）三个集合因此不导出。要搬得先让脱敏器认识嵌套路径，或者更彻底——把「这个字段是凭据」标注到模型上（一个 `[Sensitive]` 特性），让脱敏与守卫都从标注派生，而不是各自维护一份会漂移的名单 |
| DS15 | 中 | 2026-08-20 | `MAP_ADMIN_FORCE_RESET` 重置管理员口令时，不会注销该账号已经发出去的登录态 | 用这个开关抢回一个疑似泄露的管理员账号 | open | 抬 tokenVersion 需要 `IAuthSessionService`，而它在 Api 层、不在 Infrastructure 的依赖面上；`DatabaseInitializer` 够不着。当前的补救路径是：用新口令登录后，在「账户设置 → 登录密码」再自助改一次密——那条路径会抬两端 tokenVersion 并清 refresh 会话。真要根治，得把会话作废抽成一个 Core 层接口 |
| DS19 | 低 | 2026-08-20 | 对外同步名单没有乐观并发控制，两个人同时改会后写覆盖先写 | 两个管理员在两个标签页/两台机器上同时增删同一份允许名单 | open | 接口收的是**整份名单**而不是增量，所以后提交的那份会把先提交的改动整个盖掉——而票据鉴权每次都读这份名单，被覆盖掉的「移除」等于撤销被悄悄取消。2026-08-20 已修掉同一个浏览器标签内的连续改动（改传「怎么改」而不是「改成什么」、对着最新的一份求值、乐观更新、失败回滚、在途禁用输入；串行化逻辑已抽成独立模块并配了行为回归与接线守卫），跨标签/跨人这一半没修：要么给设置加版本号让服务端拒绝过期写入，要么把接口从整份覆盖改成「加一条 / 删一条」的增量操作。两者都动接口契约，按 §5.5 记入后续 |
| DS20 | 低 | 2026-08-20 | 「目标站本地执行历史」目前只登记了一个字段，靠人记得往清单里加 | 以后再往 appsettings 或别的同步集合里加类似的本机账（迁移标记、初始化标记、一次性开关消费记录） | open | 新增的 PreserveFields 是显式清单，漏登记不会报错、只会在跨站同步之后静默出现「本机账被别人的账替换」。现有守卫只钉住已知的那一个，钉不住「下一个」。想机械化得让这类字段在模型上带标注（一个 `[TargetLocal]` 特性），让清单从标注派生——和 DS16 想给凭据字段加 `[Sensitive]` 是同一件事，应该一起做 |
| DS21 | 低 | 2026-08-20（2026-08-20 修正一次误判） | worker 串行执行：多条 Run 同时被启动时，后面的要等前面整条同步跑完才轮得到，而它的导出票在回跳那一刻就开始计两小时 | 两个管理员前后脚各起一条同步，且前一条很慢 | open | 后果**不是静默卡死**——排队期间票过期会被既有的过期收口落成终态并给出「导出令牌在同步开始前就过期了，请重新授权后再跑一次」，人看得到原因。差的是排队这段时间界面显示「进行中」却没有任何进度，属于预期管理缺口。要修得二选一：给 Run 加一个 queued 态（新语义类别，前端与状态机都要跟），或者给 worker 加并发上限（要定上限、要考虑同库并发写的压力）。两者都超出本 PR 目标，按 §5.5 记入后续。顺带一提：串行本身对共享 Mongo 是友好的，改并发前得先确认写压力能扛。**误判更正**：本条原先写「后果不是静默卡死，人看得到原因」——那个判断漏了一格。前一条跑满 15 分钟时，排队那条一次心跳都没打过，别的部署的收尸扫描据此把它判成无人认领并落成 failed，尽管本进程正握着它有效的导出令牌；这不是等待，是被当死人收走。已修（排队中的 Run 跟着正在执行那条一起打心跳），剩下的「排队期间界面没有进度」仍是本条记的预期管理缺口 |
| DS22 | 低 | 2026-08-20 | 待补凭据清单按**页**归属，不按**文档**归属：源站报的 `clearedFields` 是整页一份，本站据此把「这一页里带该字段的文档」全算成候选。于是一页里若同时有「密钥被清空但因 _id 已存在而跳过」的 A 和「源站本来就没配」的新插入 B，B 会被报成「需要补」，而真正被清空的 A 因为没落地反而不在清单里 | 同一页里同时出现「跳过的已存在文档」和「源站空值的新文档」，且两者共用同一个脱敏字段 | open | 后果是**建议清单里多一行**，不是数据错也不是安全洞：清单是给人照着补凭据的提示，多一行的代价是管理员去看一眼发现不用补。要根治得改协议——导出响应里按文档 id 逐条给出「这一条清空了哪些字段」，那是新的协议字段 + 版本兼容层（旧源站不报时怎么退化），按 §5.5 属于 B 类扩范围，不在本 PR 展开。当前已有的收窄：候选集只取**确实带着该字段**的文档，且最终值非空就不报，所以误报仅限上面那一种组合 |
| DS23 | 中 | 2026-08-21 | 导出不是一致性快照：逐页读取期间源站仍可写，中途新增且 _id 排在游标前的记录永远读不到，中途改过的记录读到的是旧值；分页照样走到 nextCursor=null，Run 报成功 | 源站在同步期间有写入（几万条要拉几十分钟，这期间有人在用就会命中） | open | 后果是「成功」二字名不副实——不是报错，是数据静默不完整。要真正解决只有三条路，都超出本 PR：① 绑定 Mongo 快照读（`readConcern: snapshot` 默认只保 5 分钟，撑不住几十分钟的导出，要改成分批短事务 + 断点续传）② 源站写冻结（产品上不可接受）③ 截止时间戳 + 收尾对账（要给每个集合加变更时间字段并做二次比对，是一整套新语义）。按 §5.5 属 B 类。**当前已做的**：不在界面上假装它是完整快照——执行前对照表明确写出「逐页读取、不是一致性快照、源站仍可写」，并建议在源站空闲时段执行。这不解决不完整，只是不再误导 |
| DS24 | 低 | 2026-08-21 | 首登强制改密那条路径（`AuthController.ResetPassword`）仍是「无条件写密码」+「单独一句清 MustResetPassword」两步，没有条件更新 | 管理员在用户走首登改密的这两步之间重置该账号 | open | 与本 PR 修掉的自助改密是同一族：管理员原子写下临时密码 + `MustResetPassword=true`，随后那句单独的清标记把它抹回 false，管理员发的临时密码不再强制首登改密。自助那条已经把标记并进条件更新（谁写赢密码谁拥有标记），这条没有——它连 CAS 都没有，用的是 `UpdatePasswordAsync`（无条件写）。要修得先给它一个条件更新，而首登流程的「旧散列」语义与自助改密不同（用户此刻并不出示旧密码），需要另定谓词，属于新语义。该端点不在本 PR 的 diff 里（本 PR 只往 AuthController 新增了自助改密），按 §5.5 记入后续 |
| DS25 | 低 | 2026-08-21 | 同意页的展开不绑定「渲染这一屏时的那份目录」：`scope-catalog` 由旧版本渲染、`authorize` 落到刚上线的新版本时，分组会按**新**白名单展开，可能冻进批准人从没在屏幕上见过的集合 | 源站在管理员打开同意页与点下同意之间完成一次改动了 `DataSyncScope.Groups` 的部署 | open | 与本轮修掉的「脱敏契约随票冻结」是同一族的另一半：那条管「签发之后契约不许变」，这条管「渲染之后到签发之间不许变」。根治要给目录一个 revision（对展示出去的分组 → 集合 → 脱敏字段整体取摘要），同意页回传它，对不上就拒绝并让人刷新——那是一个新概念，还要定义「对不上时给什么错、前端怎么引导重新确认」，按 §5.5 属 B 类。窗口是「GET 与 POST 之间恰好落一次改白名单的部署」，且展开只从已批准分组取、无从跨到没勾的分组 |
| DS26 | 低 | 2026-08-21 | 自助改密签发新令牌前那次「账号还启用吗」重读，与随后的推版本 + 签发之间仍有窗口：管理员恰在此刻停用，本站会推高会话版本（盖过管理员刚做的踢下线）并签出一套匹配当前版本的令牌 | 用户自助改密与管理员停用该账号在毫秒级重叠 | open | 代码里已写明这是已知边界、没假装互斥。之所以不在本 PR 收窄：**同一个暴露的宽版本是平台既有属性，与本竞态无关也一直敞着**——`Program.cs` 校验访问令牌时只认令牌版本、不查用户当前状态，所以任何一个在停用前一分钟签出的令牌都会照常被接受直到过期。把毫秒级窗口堵上、而小时级窗口原样敞着，只会让系统看起来比实际更安全。真正的修法是令牌校验时纳入用户状态（或走可撤销会话查询），那是平台级新语义，超出本 PR。按 §5.5 属 B 类 |
| DS27 | 低 | 2026-08-21 | 目标站会话恰好在回跳落地时失效（本地登录看着还在、刷新会话已被撤销）：换票请求 401，全局拦截直接登出并跳 `/login`，这一页连同手上的授权码一起被卸载 | 管理员在源站同意的这几十秒内，目标站的刷新会话过期或被踢 | open | 本轮已把「换票没成就不消耗一次性状态」做掉，页面还在时能重试。剩下的是**跨登出跳转**那一半，不做的理由是它做了也没用：授权码只活 60 秒，人重新登录再回来必然已经过期，把码存进浏览器存储只是多留一枚死凭据。真正有用的是 401 时告诉管理员「这次授权作废了，请重新登录后回到同步页重新发起」，而那需要让这一处调用退出全局 401 跳转——改的是共享的 apiClient，按 §5.5 属 B 类 |
| DS28 | 中 | 2026-08-25 | **转正冻结的是范围不是数据**：子 Run 照抄集合清单与计划清单，但内容仍由 worker 重新调源站 `/export` 拉取、游标从头开始 | 试跑到点「开始真的搬」这段时间里源站有人在写 | open（已改口，未做快照） | Codex review 第四轮 P1。界面原话是「真的搬只会照着它执行，不会重新去源站取一份新的」，那是假的——尤其勾了覆盖时，写进来的是操作者**没看过**的值，而上面那些「预计新增 / 覆盖」条数也会对不上。本次按「改契约不改行为」处理：把界面两段话改成「范围不变、数据现取、要求一致请确认这段时间源站没人写」，后端与前端的相关注释同步纠正。**要真冻结**得给源站导出加快照或版本边界（导出时记一个 resume token / 时间点，转正时按它重放），那是导出协议层的改动，牵到 DS23（导出本来就不是一致性快照）——两条应当一起做，不该在一次 review 里顺手加。 |
| DS29 | 低 | 2026-08-25 | **转正的补偿没有兜底收敛**：激活写库失败后就地补偿，补偿再失败就只剩日志，没有任何周期任务会去收拾「父记录仍占着认领、子记录仍是 pending」这个中间态 | 补偿与激活败在同一次数据库抖动上 | open（已如实报错，无收敛） | Codex review 第四轮 P1（同一处第三轮）。本次做的是**别说假话**：补偿成功与否分开回话，失败时报 `DATA_SYNC_PROMOTE_STUCK` 并把两条记录 id 给出来，不再一律回「已回滚，请再点一次」——那句话会把人骗去按一个必然撞回「已经转正过」的按钮。**没做的是自动收敛**：正解是让「认领 + 激活」原子化（同一文档内的状态机，或一次事务），或加一个周期 reconciler 把「父记录 PromotedToRunId 指着一条 pending 且超时」的组合复位。两条都属于给这条链路换实现，不是 review 修复的范围；与 `concurrency-gate-discipline` 的「周期收敛」是同一条纪律。 |
| DS30 | **高** | 2026-08-26 | **源站用本地磁盘存附件时，同步过来的每一个附件链接都指向不存在的文件，而且一句提示都没有**：本地存储的地址是相对路径 `/local-assets/...`，判据把「已经是相对路径」当成「天然可移植」直接放行，既不改写也不计入未解析；而附件卡只在「改写数 + 未解析数 > 0」时才显示，于是这种部署下那张卡整个不出现 | 源站 `LocalAssetStorage`（本地磁盘存附件），且两站不共享同一个存储 | **已解决**（2026-08-27） | 四条一起做（DS30/31/33/34 是同一张附件卡上的四个洞，分四次补会让口径改四次）。`AlreadyRelative` 这一档不再静默：判据不再把「已经是相对路径」读成「天然可移植」——那两件事只在两站共享同一份磁盘时才等价。现在逐条数出来、经 progress 与 API 送到前端，附件卡的显示条件同步放宽成三个数之和；卡上单独一段说清「源站把附件存在它自己那台机器上，这种地址改不了也用不了，点开就是 404」，并给出两条出路（复制文件目录 / 两站都换对象存储）。「已把 N 条改写成本站地址」那句改成 N>0 才说，否则本地磁盘部署会读到「已把 0 条…附件现在就能打开」。 **原始记录**：Codex review 第十一轮 P1，事实成立。这条的严重度高于它看起来的样子：**它不是「说得不够清楚」，是一句都没说**——其它两种情况（改写成功、认不出对象位置）至少还有卡片和数字，本地存储这一种是彻底静默。判据把「相对路径」读成了「可移植」，可这两件事只在两站共享同一份磁盘时才等价，而跨实例同步的前提恰恰是两台不同的机器。要做的是：① `AlreadyRelative` 计数透出到 progress；② 除非能证明两站共享存储，否则把它并进「未解析」那一档（宁可多提醒，不可静默）；③ 附件卡的显示条件跟着放宽，别再依赖前两个计数。**没有当场做的原因**：这一批（第十、十一轮）在未改动代码里连续产出 8 条新语义类别，命中 AGENTS.md §5.5 熔断；本 PR 已 9 个 review 修复提交、6600+ 行 / 57 文件。**与已知边界的关系**：「只搬记录不搬文件」本身已经在页面上讲了，但只在那张卡出现时才讲得到——这条要修的正是「卡片不出现」这半 （**Codex review 补一刀**：第一版把「不是绝对地址」当成提前退出条件放在读 key 之前，于是本地磁盘存的附件——它们同样带着 `StorageKey` / `RelativePath`——明明改得了却被判成改不了，界面还照着劝人去复制文件。真正改不了的是「没有可用的 key」，不是「地址是相对的」。现在先认 key 再看地址形状，相对那一档收窄成「相对且认不出 key」，卡片文案跟着收窄。） （**同一轮 review 的第二刀**：形状判据只认 R2/COS 那种 26 位 base32，而本地磁盘存的是完整 sha256 十六进制、64 位——顺序改对了，key 还是过不了门。两种写法各对应一个真实存储实现，是封闭两项；守卫改成真的跑一次 `LocalAssetStorage.SaveAsync` 拿它产出的 key 来验，实现改了命名就红。） （**第三刀改的是结构不是判据**：前两刀之后，「必须是绝对地址」还留在解析器里当**前提**，于是没有 key 字段的 `image_assets` 在本地磁盘源站上整批解析失败。同一个毛病第三次冒头，按 §5.5 不再加第三种接受形态，而是把这个前提删掉——取路径只管把字符串变成路径段，判形状对三种来源完全一样，分支变少不是变多；「是不是绝对地址」退回它真正该管的地方：认不出来时算哪一档。） |
| DS31 | 中 | 2026-08-26 | **目标站对象存储配了非空前缀时，改写出来的附件地址指向错误的对象路径**：`BuildUrlForKey`（R2 / COS）期望的是完整物理 key、不会替你补上自己配置的前缀；旧格式地址只带 `domain/type/id` 缺目标前缀，新的 `StorageKey` 又可能带着源站的前缀 | 源站与目标站的对象存储前缀不一致 | **已解决**（2026-08-27） | 按 `AssetKeyKind` 分两类处理。内容寻址：`StorageKey` 存的是源站**物理** key（带源站前缀），统一剥成逻辑 key，前缀由目标站的 `BuildUrlForLogicalKey` 自己套（R2 / COS 各自 override，`RegistryAssetStorage` 显式转发——包装器不转发新方法会让前缀在装饰层丢掉）。完整物理路径（首页 / 桌面端素材的 `RelativePath`）两侧原样使用、**不套前缀也不截断**，拿内容寻址那套「取末三段」处理它会把 `icon/desktop/dark/bg.mp4` 截成一个不存在的路径。两类各有前缀不同档位的测试。 **原始记录**：Codex review 第十一轮 P2，事实成立。要做的是先把逻辑 key 规范化（剥掉源站前缀），再显式套上目标站前缀，两端都要有测试覆盖「前缀不同」这一档。与 DS30 同批熔断，理由同上 |
| DS32 | 低 | 2026-08-26 | **转正的「已经转正过」这条快速返回会在子记录还是 pending 的那一瞬报 `status: running`**：认领父记录与激活子记录是两次写库，两者之间有个毫秒级窗口；第二个标签页此刻发来的请求读到 `PromotedToRunId` 已非空，于是照着返回「running」并让界面跳过去。若第一个请求的激活随后失败，它的补偿会删掉子记录、清掉认领——第二个调用者拿着一句「成功」，落在一条**已经不存在**的 Run 上 | 两个标签页同时点转正，且第一个请求的激活写库失败 | **故意没做** | Codex review P2，事实成立（`claimed.ModifiedCount == 0` 那条分支同样是这个形状）。**「至多一次」这条边界没有破**：不管几个请求进来，被创建并认领的子 Run 永远只有一条；破的是「报出去的状态」在这个窗口里比真相乐观。要修只有两条路，都要新语义：① 给转正加一个「进行中」态（新的 Run 状态或新的返回码，前端要跟着分支），② 让快速返回去读子记录的真实状态并等它落定（引入等待/轮询）。两者都是 §5.5 说的 B 类扩范围。**根治其实和 DS29 是同一件事**——让「认领 + 激活」原子化（同一文档内的状态机，或一次事务），这个窗口连同 DS29 记的那个中间态会一起消失，应当一起做，不该在 review 里顺手加半个。|
| DS33 | **高** | 2026-08-26 | **改写契约只登记了 `attachments` 一个集合，而「创作与素材」这一组实际导出四个存着绝对地址的集合**：`homepage_assets`（`Url`）、`image_assets`（`Url` / `OriginalUrl`）、`desktop_assets`（`Url`）、`asset_registry` 都不在 `DataSyncAssetUrls.UrlFields` 里，于是 `RebaseIncoming` 对它们直接返回 `Empty`——地址原样落库，**而且 `Unrecognized` 是 0**，附件卡因此一个字都不说。搬完之后目标站的首页素材、生成图、桌面端资源仍然从源站加载，等源站或它的 CDN 退役就整片打不开 | 勾了「创作与素材」，且两站资产源不同 | **已解决**（2026-08-27） | 登记 `homepage_assets` / `desktop_assets` / `image_assets` 三个集合（`asset_registry` 明确不改：它是存储操作流水，Url 是当时那一刻的事实记录，改写等于篡改历史）。**反向守卫已补**：可导出集合里凡是模型上带 `*Url` 字段的，必须落在「改写 / 想清楚了不改 / 待定」三个名单之一，一个不许没交代，且三份名单都不许留死条目。**台账原文与事实有两处出入**：① 预期「登记之后大多会落进 Unrecognized」不成立——`RelativePath` 存的正是拼地址用的那个 key，比正则可靠，能正常改写；② `desktop_asset_skins` 曾被我一并登记，被「登记的字段必须真实存在」当场查出：它只存皮肤名与开关，模型上一个地址字段都没有。 **原始记录**：Codex review P1，事实成立（四个集合的模型字段已逐个核对）。**这是 DS30 的同族、且更彻底**：DS30 是「有东西可说但卡片不显示」，这条是「连数都没数」——`UrlFields` 那张表的守卫只校验「登记的字段真实存在」，不校验「导出的资产集合都登记了」，是条单向的闸（predicate-and-wiring-discipline 形状 2：反方向那半没接）。**修法本身不难**：把四个集合的地址字段登记进去。但要连着两件事一起想清楚——① 这几个集合的地址形状与 `attachments` 不同（`RelativePath` 形如 `icon/desktop/dark/bg.mp4`），内容寻址那条正则会认不出，登记之后大多会落进 `Unrecognized`（**这是对的**：从静默变成有数可说），但那会让附件卡的数字一下子变大，文案要跟着改口，否则用户会以为同步坏了；② 守卫要补反方向——`DataSyncScope` 里任何导出的资产集合没登记进 `UrlFields` 就判红，否则下一个新增的资产集合还会静默漏掉。**与 DS30/DS31 应当一起做**：三条都是同一张改写契约上的洞，分三次各补一块只会让附件卡的口径改三次 |
| DS34 | 低 | 2026-08-26 | **撞唯一索引被剔除的那几条，附件改写数照样算在了「已改写」里**：本轮已经把改写挪到`Decide` 之后、只作用于写库集合，但插入还可能在真正落库时撞唯一索引——`SurvivingInserts` 会把撞上的那几条从 `ToInsert` 里剔掉，而计数在这之前就累加过了 | 不覆盖模式下，另一个写入方在本轮 `Decide` 与 `InsertManyAsync` 之间插了同一个 _id | **已解决**（2026-08-27） | `RebaseIncoming` 改为返回 `RebaseBatch`（合计 + 与传入下标对齐的逐文档结果），worker 在 `MongoBulkWriteException` 分支按**同一批失败下标**把三个计数回冲，再用同一批下标走 `SurvivingInserts`。守卫同时钉住回冲成立的前提：`willWrite` 必须先插入后替换拼起来，顺序一反回冲就冲到替换那一批身上（照样全绿）。 **原始记录**：Codex review P2，事实成立，是本轮那条修复的**残余**——同一族的第二层。**我这轮的说法需要收窄**：「只改这一批真会写进去的」对「目标站已经有、被判成跳过」那一档成立，对「落库时才撞索引」这一档不成立。**误差方向是偏大**，且偏大的条数恰好等于本轮的冲突数，而冲突数本身已经如实记进 `Skipped` 单独报出来了，所以不会把「没搬」说成「搬了」，只会让附件那个数字虚高几条。**改法本身很小**（把计数挪到冲突处理之后，或按 `SurvivingInserts` 的结果回冲），没有当场做只是因为本 PR 的 review 修复提交数早已越过 §5.5 熔断线；与 DS30 / DS31 / DS33 一起做，它们本来就是同一张附件卡的口径。|
| DS35 | 低 | 2026-08-27 | **「建附件时必须存下对象 key」这条判据靠文本匹配，天然认不全所有写法**：它先按构造语法找初始化块，再看块里有没有 key。三轮 review 里被连着挑出三种能绕过去的等价写法（另一个程序集里的、目标类型 new 的、块体 return 的），每次的应对都是给正则再加一种形状 | 有人用判据认不出的写法新增一处建附件的地方 | **已缓解，未根治** | 第三轮起不再加正则——按 §5.5，同一个文本判据反复被要求加格式时正解是换有限枚举。现在站点清单被逐一钉死（少一处、多一处都红），所以「悄悄丢掉一处」这条路已经封上：判据认不出新写法时表现为「实际比登记少」，会红并点名。**残余风险的根都是同一条**：判据认不出的站点，对「实际清单」和「违规名单」都是零贡献，而清单按文件聚合成计数，于是任何让计数持平的组合都能骗过它。已知两种走法：① 新增一处、用判据认不出的写法（实际数不变）；② 同一个文件里把一处改成认不出的写法并丢掉 key、同时新增一处合规的（一增一减对冲，多站点文件尤其容易，第四轮 review 指出）。两种都不响。根治要用 Roslyn 按语义找 `Attachment` 的对象创建表达式，那是把文本判据换成语法树判据，属于新机制不是新判据 |
| DS36 | 低 | 2026-08-27 | **还有 14 个可导出集合存着本站资产地址，但改写方式没定**：`watermark_font_assets` / `literary_agent_configs` / `reference_image_configs` 等按 sha 换地址（`TryBuildUrlBySha`），那是第三类 key，登记进改写契约要先想清楚它的跨站语义；`daily_tips` / `hosted_sites` / `document_stores` 则是一个集合里既有本站资产又有外部链接，得先拆开逐字段登记 | 勾了含这些集合的分组，且两站资产源不同 | open | DS33 的反向守卫已经把它们逐个列进 `PendingSurvey` 并写明原因，不再是静默漏掉；那一栏是**只许缩小**的棘轮（守卫钉住条数只减不增），新增的资产集合没法悄悄躺进来。每清掉一条就往 `UrlFields` 挪一条。没有当场做的原因：sha 寻址是新的语义类别，而本轮已经在同一张契约上落了四条修复，再扩就是 AGENTS.md §5.5 说的范围蔓延 |


## 与其他台账的关系

- [debt.cds.branch-isolation.md](./debt.cds.branch-isolation.md)：分支预览共享同一个 Mongo 库这件事的总台账。本功能写库时受它约束，
  所以执行前对照表必须显示目标数据库名。
- [debt.platform.production-release.md](./debt.platform.production-release.md)：真要把这条链路指向生产源站时，按那份台账的发布安全约束办。
