# 关键业务监控 · 计划

**一句话**：把「关键业务能不能用」做成一条自己会说话的链——业务可被自由纳入监控、面板一眼看出干没干活干了什么活哪些出了问题、出问题时主动通知到人，从被动救火转成主动发现。
**谁该读**：这条链的负责人、要把自己服务纳入监控的后端作者、想知道「现在做到哪一步」的决策者。
**读完能做什么**：30 秒答出现在到哪了、卡在哪、下一步做什么、凭什么说这步过了。

最后更新：2026-09-14 07:55 | 更新人：AI Agent（basic-error-solution 轨道）| 距离可发布：**不可发布**——面板四问已补齐并线上验过三问，但通知链一次没响过（W5）

---

## 零、这条链要回答的四个问题

用户原话（2026-09-14）：

> 从一个低级问题，反推到一个疑问：「我能否对一些关键业务做监控」……
> 首先是面板能迅速展示出哪些问题，这叫人类去看，然后展示全局；然后是通知，能够时时刻刻通知展示这些问题。
> 从被动看到问题解决问题，到主动的去发现问题，这是一个质的跨越。
>
> 实际上是面板我没有找到关键元素，既：满足我一眼知道，**他干活了，干了什么活，哪些活出现了问题**，
> 当然：我天生谨慎，我还得**看到没有出现问题的证据**，避免因为程序没有跑，而跳过了。

这四个问题就是这条链的验收口径，面板与通知都按它对齐：

| # | 问题 | 面板必须给出 | 现状 |
|---|---|---|---|
| Q1 | **他干活了吗** | 探测器上一轮什么时候跑的；停摆时必须盖过一切结论 | 缺（数据有，第一屏没用） |
| Q2 | **干了什么活** | 这条业务检查的是什么、怎么判的 | 缺（详情页有，第一屏是空话） |
| Q3 | **哪些活出问题了** | 红的置顶 + 一句能行动的结论 | 有 |
| Q4 | **没出问题的证据** | 「正常」必须带上「最近一次检查在什么时候」；逾期未检查 ≠ 正常 | **缺，且最危险** |

判定口诀：**把探测器关掉，面板会不会照样说「全部正常」？** 会 → Q1/Q4 没做到。

---

## 一、看板

| 阶段 | 进度% | 状态 | 当前 blocker | 下一步 | 验收证据 |
|---|---|---|---|---|---|
| W1 业务可被自由纳入监控 | 90 | 已验收 | 无 | 其余项目（BDE / 标识中台 / 米多星球等）在别的仓库，需各自加自检端点 | 6 条在跑，三天 359/384 样本；协议 `spec.platform.monitor-discovery.md`；CI 守卫 `test_monitor_discovery_contract.py` |
| W2 多环境并排 | 60 | 进行中 | 协议里没有 environment 字段，端点报不了自己是生产还是预览 | 协议加 `environment`，两端自检端点声明，对账透传 | 第一屏环境格子已上线（2026-09-11 真视觉验收） |
| W3 主动/被动两种观测 | 100 | 已验收 | 无 | — | `observeMode` 分档；被动零样本不算绿（`owner-board.test.ts`） |
| W4 面板 Q1/Q2/Q4（证据链） | 85 | 进行中 | 逾期/停摆两条**反面分支线上演示不了**，见下方「取证缺口」 | 要么短暂关一次监控总开关取证，要么加一条注入式的演练开关 | 线上 `498b0395`：headline「6 项业务在 1 个环境都正常，**每一条都在 3 小时 19 分内检查过**」；卡片带「检查 GET …observedValue lt 2000」与「4 分钟前检查过 · 每 5 分钟一次」；单测 41 条，三条核心判据红绿闭环 |
| W5 通知：出问题主动找到人 | 40 | 阻塞 | 铃接了但**一次没响过**；CDS 侧投递凭据是否配齐未验证 | 故意弄坏一条自检端点，端到端演一次，并固化成守卫 | 代码 + 单测在（`map-notifier.ts`），无运行时证据 |
| W6 全局面板（跨项目一屏） | 20 | 未开始 | 无 | 先做完 W4，全局面板复用同一套证据判据 | 「全部目标」那一屏有全局计数，但不回答 Q1/Q2/Q4 |
| W7 公开面板（免登录可看） | 70 | 进行中 | 自发现监控标不成公开（端点没声明 `publicVisible`），页面上只有 1 条 | 协议与 W2 一起改：端点自报是否对外 | 地址已开：`/s/<token>`，匿名取证零泄漏 |

### 取证缺口（W4，如实记账）

线上**演示不出**单条逾期，原因是设计本身而不是缺陷：编辑器要求探测间隔 ≥ 全局间隔
（实测下限 20 秒，超出会拒绝并提示「探测间隔必须在 20-86400 秒之间」），而探测循环
60 秒一轮，于是任何一条的年龄上限约等于「间隔 + 60 秒」，永远够不到「3 个间隔」这条线。

换句话说：**探针健康时，单条监控不可能逾期**——这正是这一档想表达的意思，
它只在探针没干活时才亮。代价是它没法在健康系统上被看见。

所以这一档目前的证据只有单测（含红绿闭环：把逾期改回判正常、把停摆分支短路、
把证据取成最新，三条对应用例逐条变红，恢复后全绿），没有线上观测。两条补法：

1. 短暂关一次监控总开关（约 2 分钟），拍下「探测器停摆」那一屏，再打开——影响共享实例，需人点头；
2. 加一个**只读的演练开关**：让面板按「假装上一轮是 N 分钟前」重算一次，专供取证，不改真实数据。

在补上之前，这一档按「未经线上验证」记账，不许写成「已验收」。

**同批发现的两条，已单独记账**：
- 对自发现监控写 `publicVisible` 接口返回 200 并回显新值，下一轮对账又悄悄还原（按设计端点说了算，但接口该拒绝而不是假装成功）；
- 同一个监控 PUT 用原始 fetch 一会儿 200 一会儿 401，走界面则稳定成功。

---

## 二、W4 要做什么（本轮）

### 2.1 两级「检查是否真的发生过」

| 级别 | 判据 | 说什么 |
|---|---|---|
| 全局 | 探测器上一轮距今 > 2 个轮次间隔（`prober.stalled`，已有） | 探测器停摆，**下面所有绿灯都不作数** |
| 单条 | 距上次探测 > 1.5 个该条间隔 = 迟到；> 3 个 = 逾期 | 这条多久没被检查过了 |

两级都要，因为粒度不同：探测器整个挂掉几分钟就能被全局判据发现；单条被跳过（间隔改了、认领没轮到）只有单条判据看得见。

### 2.2 「正常」这句话必须自带证据

现在：`6 项业务在 1 个环境都正常`
改成：`6 项业务正常，最近一次检查 3 分钟前`

逾期时不许说正常，headline 优先级固定为：

```
探测器停摆 > 有故障 > 有逾期未检查 > 有零样本 > 全部正常（带证据）
```

「探测器停摆」排最高，因为它污染其余全部结论——我不是知道它好，我是不知道。

### 2.3 卡片回答 Q1 + Q2

每张业务卡加两行：检查了什么（人话判据）、上次什么时候检查的 + 每隔多久检查一次。

---

## 三、不做什么（范围边界）

- 不在本轮做全局面板（W6）与通知（W5），先把证据判据做扎实，那两者都复用它。
- 不改探测调度本身（间隔、认领、去抖）。本轮只做「把已有事实端出来」。
- 不碰其他仓库的项目接入。

---

## 四、相关

- 规则 `degradation-must-alarm.md`：这条链的来源；层 1 说的「监控是触发器不是看板」正是 W5 欠的账。
- 规则 `predicate-and-wiring-discipline.md` 形状 4b（静默空跑）：Q4 是它的用户版表达——不会红的证据比没有证据更糟。
- 协议 `spec.platform.monitor-discovery.md`：W1 的 SSOT，W2/W7 要在它上面加字段。
- 债务 `debt.cds.md`：公开面板口令、存量监控 environment 回填。
