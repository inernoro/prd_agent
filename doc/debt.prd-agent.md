# prd-agent 平台杂项 · 债务台账

> **版本**：v1.9 | **日期**：2026-08-31 | **状态**：开发中

**一句话**：prd-agent 平台级债务合集，涵盖权限、头像、文档覆盖及稳定冒烟环境阻塞。
**谁该读**：接手 prd-admin 头像编辑器或后台头像生成任务的工程师；接手管理员通知推送订阅的工程师；做 entropy-cleanup D6 文档覆盖核对的人。
**读完能做什么**：知道各处已知边界缺什么、下一步补哪；尤其是第一条（changelog 覆盖）——它的登记曾经被判"已核对完毕"，后来发现遗漏，教训是「大聚合 changelog 不能只查五类就收工」。

---

## 总览

当前 open: 7 / in-progress: 0 / paid: 0 / 总计: 7

## 债务列表

| ID | 严重度 | 创建日期 | 描述 | 触发条件 | 状态 | 备注 |
|----|--------|---------|------|---------|------|------|
| 2026-08-31-image-input-repeated-validation | medium | 2026-08-31 | 生图输入在应用与网关边界均验证可读性；请求同时携带图片原始意图与上传文件时，同一内容可能重复解码，增加大图请求的处理耗时和分配压力，尚无性能测量或线上超时证据 | 多张大尺寸参考图同时进入生图流程，尤其接近现有字节和像素上限时 | open | PR #1458 第二轮审查按 B 类后续优化登记，不以移除安全检查收口。后端与网关负责人先测典型图片及上限内多图的耗时、峰值内存和解码次数，再设计请求内复用；不得假定两份表示内容必然一致，不得信任客户端自报已校验。验证直传、备用路由、蒙版、内容不一致及损坏输入仍被拒绝，且有明确性能改善后关闭 |
| 2026-08-31-generation-progress-live-layout | medium | 2026-08-31 | 生图进度条随画布缩小而不可读，已补屏幕像素布局与多尺寸浏览器回归；组件确定性等待态已验证，但灰度缺登录会话，真实生图过程尚未完成多尺寸复验 | 交付画布进度条修复时 | open | 验收负责人在灰度登录后，从首页进入视觉创作，在独立测试工作区分别生成方图、横图、竖图；检查正常图片与生成器两条路径在连续缩放、手机触控和支持的主题下文字清晰、进度不越界、结果替换等待态。不得把组件截图当作真实生图产物，证据齐备后关闭本项 |
| 2026-08-31-stable-smoke-image-input-live-gate | high | 2026-08-31 | 线上 JPEG 内容与 PNG 声明不一致逃逸。格式规范化和真实 JPEG 稳测用例已补，但 48 小时任务最近两轮均未执行；本轮使用既有专用配置复检仍缺正式环境独立允许域名，CDS 产品与网关专用身份未通过，不能声称线上已修复或巡检已恢复 | 生图修复需要灰度与正式真实产物验收、每 48 小时自动巡检时 | open | 环境管理员恢复专用身份、独立域名配置与定向通知后，先在灰度执行 JPEG 错标 PNG 的最小旅程，再执行正式环境正常 JPEG 变体；断言图片可读、主路成功、字节哈希及 MIME/扩展名一致、测试资源清理且通知可回读。用例随 VIS-003 每轮纳入，REG-image-input-001 在实环境证据齐备前保持 candidate，不绕过身份或允许域名门禁 |
| 2026-08-13-avatar-editor-no-design-doc | low | 2026-08-13 | `changelogs/2026-08-11_stable-smoke-runtime-equivalence.md`（406 行，跨 prd-api/prd-admin/llmgw/cds 多模块）里，除了已被 [design.platform.core-business-stability.md](./design.platform.core-business-stability.md) 覆盖的稳定冒烟框架部分，还包含一个完整的产品功能：点击本人头像打开沉浸式编辑器、AI 生成预览、后台任务化生成（浏览器断开不取消）、内容哈希版本化对象键防 CDN 缓存旧图、幂等重试与并发清理等——这部分已由 [doc/design.prd-agent.avatar-editor.md](./design.prd-agent.avatar-editor.md) 补齐。但同一份 changelog 里还包含至少五类未核对的安全相关内容：视频供应商结果地址的重定向公网校验、视频任务跨应用访问隔离、正式环境 RSA-PSS 签名破窗认证与防重放、破窗认证的请求体读取上限、破窗签名通知的定向投递限制 | 2026-08-14 entropy-cleanup PR #1372 review（Codex）第九轮指出：本条目 2026-08-14 曾一次性标记「五类均已核实、changelog 已登记」，但审查发现除头像编辑器外还有上述安全相关内容全仓 `doc/` 与 `.claude/rules/` 均无覆盖，此前的"五类核对"本身不完整。该 changelog 已从 `changelogs/.entropy-manifest.yml` 撤回登记，避免后续 D6 扫描永久跳过 | open | 补法：逐条核对上述五处安全内容——已有独立 design 文档覆盖的记录引用、真无文档的另开 design 章节或新增 debt 条目列出机制与已知边界；全部核对完（不是只挑好写的几条）后才能把这份 changelog 重新登记进 manifest。核对本身应该独立于头像编辑器这条，不要再和其他内容混在一次性判断里 |
| 2026-08-14-admin-avatar-upload-fixed-filename-cdn-staleness | low | 2026-08-14 | 管理员为他人上传头像走的是固定对象键（用户名+扩展名）覆盖写，不带内容哈希；本人自服务的直传与 AI 生成两条路径已改用内容哈希对象键规避 CDN 边缘节点缓存旧图的问题，管理员路径未跟进，理论上仍会出现「后台换好了、别人看到的还是老头像」 | 撰写头像编辑器设计文档时核对两条路径代码发现的既有差异，非本次改动引入 | open | 补法：管理员路径改用与自服务路径相同的内容哈希对象键方案 + 旧对象回收逻辑；改动前确认是否有下游依赖「头像文件名固定为用户名」这个约定（如需要按用户名反查文件的场景），避免改坏兼容性 |
| 2026-08-14-avatar-ai-generation-no-cross-session-recovery | low | 2026-08-14 | AI 生成头像的任务只能在提交它的那个浏览器会话里查到进度，没有跨会话找回的入口。用户提交生成后关闭浏览器，任务在服务端跑完了，但重新打开编辑器时无从得知这次生成的结果，只能再发起一次——已完成的这次生成实际上被浪费 | entropy-cleanup PR #1372 review（Codex）连续两轮指出：先是「不能宣称任何时候都能恢复进度」，随后进一步指出这与「不会浪费生成额度」的说法自相矛盾 | open | 补一个「查当前用户最近一次头像生成任务」的能力，让前端在会话内进度丢失时能兜底找回；或至少在关闭前提示用户「本次生成完成前请勿关闭页面，否则需要重新生成」 |
| 2026-08-14-admin-push-subscription-permission-gap | medium | 2026-08-14 | 站内信通知中心的列表按管理员权限过滤，非管理员看不到管理员专属通知（如网关故障告警）；但把站内信转发到外部地址的推送订阅功能没有复用同一道权限校验——具备普通看板权限、不具备管理员权限的账号仍能订阅这类管理员专属主题，把内容（含模型名、失败摘要、请求 ID、日志链接）转发到自己配置的外部 Bark/Webhook 地址，绕开了「只有管理员能看到」的边界 | entropy-cleanup PR #1372 review（Codex）第十三轮指出：撰写模型池可见性设计章节时描述「只有管理员权限能看到和处理」，但该说法只对通知中心列表成立，未核对推送订阅链路是否有同等校验 | open | 在推送订阅的创建/更新入口与实际投递环节都补上与通知中心列表一致的管理员权限校验（订阅时校验一次不够，投递时也要重新核实——权限可能在订阅之后被收回）；补齐后回来更新对应设计章节的表述 |
