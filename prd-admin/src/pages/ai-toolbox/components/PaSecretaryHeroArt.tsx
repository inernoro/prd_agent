import { PaSecretaryIcon } from './PaSecretaryIcon';

/**
 * 毒舌秘书空状态大图 — 复用 Gemini 风星芒（与侧栏/百宝箱图标一致）
 */
export function PaSecretaryHeroArt({ size = 72 }: { size?: number }) {
  return <PaSecretaryIcon size={size} />;
}
