# 偏差台账模板

> 每个画板一节。一条差异一行，处置列只允许「改掉 / 保留 / 待办」三个值。
> 「看着差不多」不是处置，写了等于这条没验。

## 画板：<画板名>（主题：深色 / 浅色，宽度：1440 / 1280）

并排图：`shots/pair/pair.<画板id>.<主题>.<宽度>.png`

| # | 位置 | 设计稿 | 实现 | 处置 | 说明 |
|---|------|--------|------|------|------|
| 1 | 列头「证据」 | 证据（后端已有，此前未显示） | 证据 | 改掉 | 文案逐字抄，括号补充是稿子刻意给的解释 |
| 2 | 顶部 KPI 行 | 5 段 + 右侧规则覆盖块 | 5 段，无规则覆盖块 | 待办 | 后端无该聚合字段，落 debt.llmgw.model-pool.md |
| 3 | 卡片圆角 | 10px | 8px | 保留 | 沿用既有 token，稿子未定义新档位 |

## 六个观察维度（逐条过，不要只挑显眼的）

1. 整块内容缺失 / 多出
2. 主次区块面积比、列宽比例
3. 竖向节奏：分组间距档位是否一致
4. 字重字号层级差
5. 状态色语义分配（严重度色不与来源/分类标签撞色）
6. 文案逐字

## 汇总

- 画板数：<设计稿 N> / 已比对 <M>
- 偏差：改掉 <a> 条，保留 <b> 条，待办 <c> 条
- 未比对画板及原因：<无 / 逐条列出>
