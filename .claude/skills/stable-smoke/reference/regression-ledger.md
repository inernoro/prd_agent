# 稳定冒烟回归台账

> 首次出现的问题在修复时追加一行。只追加，不复用 caseId，不因功能下线直接删除历史记录。

## 准入规则

问题标记“已解决”前必须具备：根因、最小夹具、CDS 环境失败证据、修复后通过证据、正式环境安全变体、所属稳定冒烟集合。

同一根因再次逃逸时，在“重复逃逸”列累计，并新增稳定性流程 P1，说明原用例为何没有拦住。

## 台账

| caseId | 模块 | 首次发现 | 问题与根因 | 最小夹具 | CDS 环境证据 | 正式环境变体 | 必跑集合 | 重复逃逸 | 状态 |
|---|---|---|---|---|---|---|---|---:|---|
| REG-multi-image-001 | 多图视觉创作 | 2026-08-02 | 专用图片模型误走聊天协议，且首次请求未重建协议请求体 | 两张几何参考图 | Image 2 两图生成完成 1/1 | 专用合成工作区最小两图任务 | MVIS-001、MVIS-008、GW-005 | 0 | active |
| REG-multi-image-002 | 多图视觉创作 | 2026-08-02 | Offering 覆盖 Endpoint 后与 Provider base path 组合成错误地址，HTML 200 被当作模型响应 | Provider base 含 `/api`，Offering Endpoint 留空与错误覆盖对照 | 默认 Endpoint 命中 `/api/v1/images` | 正式配置只读加最小真实请求 | GW-004、GW-005 | 0 | active |
| REG-visual-error-001 | 视觉创作 | 2026-08-02 | 原始上游错误直接进入用户界面，用户无法判断恢复动作 | 空输入、模型不可用、协议响应异常 | 用户文案不含 HTTP、token、Provider、协议原文 | 自然失败时检查文案和 requestId | CORE-005、CORE-006、VIS-008、MVIS-010 | 0 | active |
| REG-llmgw-auth-001 | LLMGW 身份可用性 | 2026-08-03 | 仓库 Compose 新增环境变量后，CDS 平台配置快照未自动同步，导致新代码已部署但容器未收到长期托管开关 | 固定 admin 凭据、连续两次同版本部署、部署运行的 `profileEnvKeys` | 受审导入后运行 `dr_165055155f07444a60d2d56f` 与 `dr_8a6269a009134193806decf2` 均通过；容器 authority=1、force-reset=0，旧令牌跨重启仍返回 200 | 正式发布后只验证注入键名、两次重启和登录，不输出秘密 | GW-009 | 0 | active |
| REG-file-001 | 文件上传与解析 | 2026-08-04 | 支持的文档格式解析失败后仍保存为无正文条目，形成静默假成功 | 损坏 DOCX 与零字节 TXT | CDS `stsmk-202608032054-mwv5mq` 五格式矩阵通过；损坏与空文件返回可恢复文案且清理无残留 | 正式环境只运行零字节前置拦截，不主动上传损坏文件 | FILE-004、FILE-005、FILE-010、CORE-005 | 0 | active |
| REG-video-001 | 视频创作 | 2026-08-04 | 直出任务完成后把需要上游鉴权的地址交给浏览器，导致成片存在但用户下载收到 401 | 最短 720p、无音频视频 | CDS `stsmk-202608032056-dr7pmm` 两次稳定复现；`stsmk-202608032110-cusvfn` 修复后经后端鉴权下载并返回有效视频字节 | 正式环境最低成本成片只走同源下载端点 | VIDEO-004、VIDEO-007、VIDEO-008、CORE-005 | 0 | active |
| REG-user-error-001 | 用户可读错误 | 2026-08-04 | 首页系统告警把上游英文错误、Provider 名称和管理地址原样展示，缺少普通用户可执行的恢复动作 | 额度不足错误含英文原因与管理地址 | L2 视觉验收登录首页稳定复现；修复后通知只显示结果、重试动作和管理员恢复动作，原始内容仅写脱敏日志 | 正式环境只检查通知正文不含 Provider、地址、API Key、HTTP 或协议术语 | CORE-001、CORE-005、CORE-006 | 0 | active |
| REG-tutorial-progress-001 | 登录与权限 | 2026-08-04 | 历史用户 `LearnedTips` 为 null 时点击“我已学会”返回 500，页面乐观关闭但刷新后教程再次出现 | 使用历史空进度账号完成知识库或文学创作教程 | CDS 真实浏览器取证稳定复现；修复后连续提交两次均成功且进度接口保持 learned=true | 正式环境只对稳定冒烟专用账号执行幂等完成与回读 | CORE-008 | 0 | active |
| REG-asr-routing-001 | 录音转写与 LLMGW | 2026-08-04 | CDS 只有一个高成本 ASR 成员且余额不足；网关默认池切换后，API 容器因未透传 HTTP 调用方白名单仍读取旧 MAP 池，形成配置与运行时双权威 | 3.5 秒真实演讲 M4A、主备 ASR 池、容器白名单注入检查 | `stsmk-20260804-audio-root-cause` 稳定复现 402；修复后要求 REC-003/007/012 真实转写通过，GW-006/007 验证默认池主备与健康 | 正式环境使用最低成本真实音频，验证运行日志命中 llmgw 权威池并清理 | REC-003、REC-007、REC-012、GW-006、GW-007 | 0 | active |
| REG-document-store-action-001 | 录音、文件与短视频入口 | 2026-08-04 | 新建并选中知识库后，自动触发首个操作及库内新增入口在多轮浏览器验收中未稳定出现；双击快捷录音曾成功，说明入口状态机或测试锚点存在不一致，根因待复现定位 | 新建一个 `stsmk-` 知识库，分别验证录音、文件和短视频三个首操作入口 | CDS 视觉验收报告 `d1af281d864642fbbf57c16178f34d63` 已保留入口缺失证据；尚无修复后通过证据 | 正式环境仅在问题修复并于 CDS 激活永久用例后执行只读入口检查 | COMMON-001、REC-001、PARSE-001 | 0 | candidate |
| REG-short-video-input-001 | 短视频上传与链接解析 | 2026-08-04 | 任务创建入口只检查非空，普通文字也会被当作链接排队，导致延迟失败或生成无来源骨架 | 普通文字“这不是链接”与完整公开视频链接对照 | CDS `stsmk-20260804-short-video-input-fix` 断言 400、无任务残留且文案包含重新粘贴动作，`PARSE-003` 通过 | 正式环境仅发送非法输入并断言前置拒绝，不创建任务 | PARSE-003 | 0 | active |
| REG-visual-progress-001 | 单图与多图视觉创作 | 2026-08-04 | 生成进度层使用固定画布坐标和固定宽度，结果尺寸变化后进度信息被裁切；旧视觉验收只截代表页面，没有按方图、横图、竖图和移动端分别核销 | 方图、横图、竖图各一个最小生成任务，并在桌面与真实触控移动端记录准备、生成和完成阶段 | CDS 组件边界测试已通过；真实横图、竖图与移动端连续阶段证据尚待补齐 | 正式环境按每轮轮换比例执行一个最低成本真实任务，并断言进度完整位于容器内 | VIS-004、VIS-005、VIS-009、MVIS-011、MVIS-012 | 0 | candidate |
| REG-visual-evidence-001 | 全面视觉验收 | 2026-08-04 | 旧报告按页面抽取代表图，一张图被用来暗示多个状态通过，导致失败恢复、持久化、进度变化、移动端和主题分支被漏掉 | 十模块 148 个唯一验收位，每个验收位一张唯一截图，并逐张绑定自动检查、人工视觉、严格结论、面包屑和测试方法 | CDS 已完成 148/148 张唯一证据人工回读，严格结果为 125 通过、9 不通过、7 需补证、7 需干预 | 正式环境执行同一 148 位目录；双环境合计少于 296 张或任一验收位缺失时不得判全面通过 | 全部 active 用例与视觉证据目录 | 0 | active |

| REG-image-input-001 | 视觉创作与 LLMGW | 2026-08-30 | JPEG 内容在图片编辑链路被固定命名为 PNG，声明与内容不一致；旧稳测夹具只有 PNG，最近巡检又在预检阶段阻塞未执行 | JPEG 字节配 PNG 声明，单图、多图、蒙版及上游切换；空文件与损坏图前置拒绝 | 已补本地真实图片字节和出站 multipart 契约测试；CDS 前后对照证据待专用身份恢复后补齐 | 合成 JPEG 正常上传，OpenAI 图片编辑主路成功且日志 MIME、扩展名、哈希一致，不用 fallback 代替通过 | VIS-003、GW-005 | 0 | candidate |
| REG-auth-diagnosis-001 | 跨系统授权诊断 | 2026-09-01 | CDS 项目 AI Access Key 被批量环境替换后，本机仍持有旧值；后端已知“Key 不匹配”却统一返回“未授权”，前端又把任意 401 当会话失效，导致 Agent、验收和用户看到不同表象 | 错误 AI Access Key、失效 Agent Key、撤销会话各一次；同一路径恢复复测 | CDS 已从权威项目配置同步本机 Keychain，合成票据签发、消费及 `/api/authz/me` 回读通过；结构化诊断、看板和故障注入证据随本用例持续补齐 | 正式环境不主动轮换凭据，只检查自然 401 分类、授权健康看板、非会话 401 不退出用户与签名合成登录回读 | CORE-002、CORE-003、CORE-005、CORE-006、CORE-007、CORE-009 | 0 | active |
| REG-auth-health-entry-001 | 授权健康中心入口 | 2026-09-01 | `/authorization-health` 已注册路由，但桌面首页能力搜索仍从静态 `buildStaticUtilities` 分组取数，移动抽屉沿用既有导航集合，导致页面已部署却无法从正常入口发现 | 有 `logs.read` 的管理员分别在 1440×900 桌面首页搜索“授权健康”，以及 390×664、`isMobile=true`、`touchPoints=1` 的移动端打开导航抽屉 | CDS 分支 `1700bf604b56` 稳定复现：桌面搜索 0 项并显示“无匹配”，移动抽屉无授权健康中心；失败报告 `e984ef90fd42443c8d6b4a5fe20df567` 已通过线上打开验证 | 正式环境发布后只读验证首页搜索、命令目录、桌面导航和移动抽屉均能点击进入，再回读当前结论；不创建业务数据 | CORE-001、CORE-009、AUTH-VIS-001、AUTH-VIS-005 | 0 | active |

## 新增模板

```markdown
| REG-{module}-{number} | {模块} | {YYYY-MM-DD} | {问题与根因} | {夹具} | {失败到通过证据} | {正式安全变体} | {caseId 列表} | 0 | active |
```

## 状态

- `candidate`：已发现，尚未满足永久用例准入。
- `active`：每轮稳定冒烟必跑。
- `retired`：功能已下线且有替代测试；保留记录和退役原因。
