# {功能名称} 验收清单

> **验收目标**：{一句话}
> **分支**：{git branch 名}
> **入口**：{CLI 命令 / Web URL}
> **场景类型**：CLI / Web / 混合
> **估算总时长**：{N} 分钟
> **打勾方式**：每个 checkpoint 有 ☐，跑完一项写 ✅ / ❌ / ⏭ (跳过)
> **【位置】**：{百宝箱 / 左侧导航"X"菜单 / 首页快捷入口}
> **【路径】**：{登录后首页 → 1) 点击 → 2) 点击 → 3) 到达}

---

## 🟢 Phase 0: 前置检查（{2} 分钟）

> 在开始任何操作之前，确认环境就绪。这些失败就不要继续，否则测试结果不可信。

| # | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 0.1 | {探活命令 1} | {应有的输出} | ☐ |
| 0.2 | {版本 / 依赖检查} | {版本号 / 存在性} | ☐ |
| 0.3 | {网络 / 权限 / 时钟检查} | {探活成功} | ☐ |

**如果 0.X 失败**：
- 原因 A → 诊断命令
- 原因 B → 修复步骤
- 仍然不行 → 贴 `{具体日志抓取命令}` 给开发者

---

## 🟢 Phase 1: 冷启动自检（{2} 分钟，涉及服务时必做）

> 验证目标系统是从干净状态启动的，而不是上次失败的残留。这一步在 GSD 社区统计中是**最容易省略、最容易制造假阳性**的一步。

| # | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 1.1 | {停止服务的命令} | 进程不存在 | ☐ |
| 1.2 | {清理残留状态} | {指定文件/缓存消失} | ☐ |
| 1.3 | {启动服务的命令} | 启动成功 | ☐ |
| 1.4 | {健康检查 URL/命令} | 返回 200 / ok | ☐ |

**如果 1.X 失败**：
- 原因 A → 诊断
- 原因 B → 修复
- 仍然不行 → 贴日志

**验收点 1**：{这个阶段的核心判断，例如"系统从干净状态成功启动"}

---

## 🟢 Phase 2: 核心执行（{5} 分钟）

> 这是真正要测的新功能主路径。按顺序敲命令/点按钮。

| # | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 2.1 | {触发新功能的第一步操作} | {看到的关键输出} | ☐ |
| 2.2 | {第二步} | ... | ☐ |
| 2.3 | {第三步} | ... | ☐ |
| 2.4 | {最终完成信号} | {明确的成功标志} | ☐ |

**如果 2.X 失败**：
- 原因 A → 诊断命令
- 原因 B → 诊断命令
- 仍然不行 → 贴 {具体日志范围}

**验收点 2**：{核心价值判断}

---

## 🟢 Phase 3: 验证副作用（{3} 分钟）

> 核心执行成功了，现在检查它是不是真的产生了期望的副作用。"看起来成功 ≠ 真的成功"。

| # | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 3.1 | {检查数据库落盘} | {指定字段/集合出现} | ☐ |
| 3.2 | {检查日志记录} | {关键 log 行存在} | ☐ |
| 3.3 | {检查 UI 状态同步}（Web 场景必做）| {页面显示符合} | ☐ |
| 3.4 | {F12 看 Network}（Web 场景必做）| {指定 API 返回 200}| ☐ |

**验收点 3**：{例如"数据正确落盘且 UI 能看到"}

---

## 🟢 Phase 4: 回归检查（{3} 分钟）

> 确认新改动没有搞坏老功能。这是"无痕升级"的保证。

| # | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 4.1 | {访问一个完全不相关的老页面} | 正常渲染 | ☐ |
| 4.2 | {调用一个完全不相关的老 API} | 正常返回 | ☐ |
| 4.3 | {新旧数据混合展示}（如有 fallback 字段）| 旧数据也能正确渲染 | ☐ |

**验收点 4**：{例如"老用户使用路径不受影响"}

---

## 🟢 Phase 5: 回滚演练（{2} 分钟）

> 撤销操作必须能工作。永远不要部署一个"进得去出不来"的功能。

| # | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 5.1 | {撤销命令 / 删除按钮} | 看到撤销成功提示 | ☐ |
| 5.2 | {验证状态恢复} | 恢复到操作前状态 | ☐ |
| 5.3 | {验证没有孤儿数据} | 关联数据已清理 | ☐ |

**验收点 5**：{例如"任何时候用户都能退出这个功能"}

---

## 🔴 Phase 6（可选）: 负面测试

> 主动触发失败路径，验证错误处理。不是必做，但验收严格时推荐。

| # | 操作 | 预期结果 | 状态 |
|---|---|---|---|
| 6.1 | {故意输错参数} | 友好的错误提示（不是 500）| ☐ |
| 6.2 | {故意断网} | 超时后给明确提示 | ☐ |
| 6.3 | {权限不足的账号访问} | 403 + 登录提示 | ☐ |

---

## 🧾 整体验收汇总

> 跑完所有 Phase 后填这张表，作为最终判定。

| 验收点 | Phase | 你的结论（✅/❌/⏭）|
|---|---|---|
| 前置环境就绪 | 0 | |
| 冷启动成功 | 1 | |
| 核心功能主路径跑通 | 2 | |
| 副作用全部发生 | 3 | |
| 老功能无回归 | 4 | |
| 回滚路径可用 | 5 | |
| 异常处理友好 | 6 (可选) | |

**任一 ❌**：按对应 Phase 的"失败处置"排查，还不行就贴日志
**全部 ✅**：功能正式验收通过 → 建议下一步跑 `/handoff` 生成交接文档

---

## 🆘 快速诊断命令（贴给开发者时用）

```bash
# 看进程和端口
ps aux | grep {关键字} | grep -v grep
ss -tlnp 2>/dev/null | grep ':{端口}'

# 看最近日志
{具体的 tail 命令}

# 看关键配置
cat {配置文件路径} | grep -v TOKEN | grep -v PASSWORD
```
