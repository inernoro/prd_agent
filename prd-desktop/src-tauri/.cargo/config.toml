# Cargo 配置
# 注意：自定义链接器配置已移除以保证 CI 兼容性
# 本地开发如需加速链接，可手动添加：
#   [target.x86_64-unknown-linux-gnu]
#   linker = "clang"
#   rustflags = ["-C", "link-arg=-fuse-ld=mold"]

# 优化网络请求
[net]
git-fetch-with-cli = true

# 注册表协议（加速 crates.io 访问）
[registries.crates-io]
protocol = "sparse"
