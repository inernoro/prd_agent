# Skill Template: PR Review Prism Bootstrap (Compatibility Pointer)

标准可执行 Skill 已迁移至：

- `.claude/skills/pr-prism-bootstrap/SKILL.md`

推荐在新项目中直接复制上述目录（含 `SKILL.md`），即可作为标准技能导入并执行。

若只需立即初始化，也可直接运行：

```bash
bash scripts/init-pr-prism-basis.sh
```

说明：

- 本文件保留用于兼容旧引用路径；
- 后续内容维护以标准 Skill 为准（`.claude/skills/pr-prism-bootstrap/SKILL.md`）。

