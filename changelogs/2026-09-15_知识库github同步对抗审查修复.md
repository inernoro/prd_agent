| fix | prd-api | GitHub 目录同步：远端目录被删光时跟着删掉旧子文档，含「目录整个不存在」这种最常见的形状；动手前先确认仓库与分支仍可访问，避免把权限变动当成删除 |
| fix | prd-api | 断开 GitHub 连接时先向 GitHub 收回整份授权（不只是当前令牌）再删本地密文；没收回或未确认都如实告诉用户去哪儿手动移除 |
| fix | prd-api | 连接状态接口补 usable 三态（真打一次 GitHub），不再只凭库里有没有记录就说「已连接」 |
| fix | prd-admin | 授权已被撤销时向导停在「连接」这一步并说明原因，不再跳到选仓库再撞 401 |
| fix | prd-admin | 授权已撤销后点重新连接，不再显示「当前连接仍然有效」这句自相矛盾的话 |
| fix | prd-admin | 断开连接的提示区分「GitHub 授权已撤销」与「仅本地删除」两种结果 |
| docs | doc | debt.knowledge-base 记 K-17/K-18/K-19（令牌刷新、仓库搜索跨页、OAuth scope 收敛），K-10 第六条标为已偿还 |
| fix | prd-admin | 连接可用性「没问出结论」时不再断言旧连接仍然有效（放行与断言分成两把尺子） |
| fix | prd-admin | 授权已失效时标题栏不再显示「已连接 X」（与同屏的「授权已被撤销」自相矛盾）；关于连接的措辞收敛到同一个判据模块 |
| fix | prd-api | 目录清单可能被上游截断（一次最多 1000 条且不明说）时跳过删除环节，避免把「这轮没见到」误判成「远端没有了」而删掉仍然存在的文档；跳过时父条目标红并说清原因与下一步，不演成一次成功 |
| fix | prd-api | 断开时的删除钉住被撤销的那一条连接，避免撤销等待期间另一个标签页刚接好的新连接被误删 |
| polish | prd-api | 断开失败的提示只写用户能据此行动的部分（去 GitHub 自查并移除），没撤成的内部原因留在服务端日志 |
| fix | prd-api | 断开时撤销与删除锚定同一条连接记录（此前撤销仍会重查库，换账号时会撤错人且漏撤该撤的） |
| fix | prd-api | 分支探测遇到服务停机时原样抛出取消，不再被吞成「没问出结论」而记成一次用户可见的同步失败 |
| fix | prd-admin | 断开若因并发被替换（后端保住新连接）不再谎报「已断开」，改为重读连接状态并说明发生了什么 |
| refactor | prd-api | 断开结果由布尔改为三态（已删除 / 本来就没有 / 被替换而保留），界面据此说三句不同的话，不再靠猜 |
| docs | doc | design.knowledge-base.github-sync 去掉评审编年史，不变量折进正文（doc-types「永远替换，不留历史」） |
| fix | prd-api | 删除条数为零时回读一次再定性，两个标签页同时断开不再把第二个误报成「被替换」 |
| fix | prd-admin | 连接失效或消失时一律退回「连接」这一步，不再把用户留在需要有效令牌的步骤上 |
| fix | prd-admin | 断开提示由判据一次产出，本地结果与「GitHub 授权收回了没」各说各的，不再因分支提前返回而漏掉安全提示 |
| fix | prd-api | 断开失败提示只讲 GitHub 那一侧，本地状态交给界面说，避免拼出「新连接已保留」+「连接已删除」的自相矛盾 |
| fix | prd-api | 连接状态探的是刚读到的那一条记录，不再重查库，避免「拿 B 的令牌探测、报 A 的账号信息」 |
| fix | prd-api | 列目录先把分支解析成不可变提交号再按它列，消除「两次请求之间分支变了」导致误删刚恢复文档的竞态 |
| docs | doc | debt.knowledge-base 的 K-10 去掉字段名、接口路径与控制流描述，只留边界与用户影响 |
| fix | prd-api | 存量子条目改按仓库内路径认亲（不再逐字比对含 ref 的地址），避免按提交号列目录后把老文档误判成新增并删掉原件 |
| fix | prd-api | 本轮取正文与最近修改时间也用同一个已解析的提交号，避免清单来自一个提交、正文来自另一个 |
| fix | prd-api | 分支定不住提交号时整轮中止并提示重试，不再退回按分支名列——那条退路会让「404 即远端删光」的前提在它自己身上失效 |
| fix | prd-api | 订阅地址里的 ref 允许是标签或提交号，改用对分支/标签/提交号一视同仁的接口解析（上一版只认分支，会让按标签订阅的目录从能用变成永远同步失败） |
| fix | prd-api | 列目录 404 要删光子文档之前，再探一次此刻是否仍读得到该仓库：探不通就按失败处理——定住提交号定住的是内容版本，定不住授权，中途被收回授权拿到的同样是 404 |
| polish | prd-api | 同步失败文案统一走既有的唯一出处（分得清「带授权的找不到」与「匿名访问私有仓」），不再在中止分支另写一句笼统的「网络波动」 |
| docs | doc | debt.knowledge-base 记 K-21（存量条目路径推断假定 ref 只占一段，经核对无可达路径）与 K-22（「无需撤销」与「已撤销」同档，界面当前不因此说谎） |
| fix | prd-api | 「远端删光了」改为只认正面证据：从目标逐级上溯找第一个列得出来的祖先，看它的清单里有没有这一级；上一版只再确认「仓库还读得到」，证明的不是同一件事（授权在两次请求之间抖一下，确认照样通过） |
| fix | prd-api | 解析 ref 时遇到限额耗尽（403/429）不再被压成裸状态码，限额信息随结果一起传下去——否则用户看到的是「没有读取权限」，而正确的下一步是「等到几点几分再试」 |
| refactor | prd-api | 两条请求地址收敛成纯函数，守卫改为断言它吐出来的地址而不是扫源码字面量（前者换个写法照样成立，后者一重构就假红、一写错又可能假绿） |
| refactor | prd-api | GitHub 请求失败收敛成唯一载体（构造它就必然带上限额判据），取证那次列目录撞上限额时用户看到的是「等到几点几分再试」，不再是最初那条 404 翻出来的「找不到 / 没有权限」 |
| docs | doc | debt.knowledge-base 记 K-23（断开与重连之间那个回读消不掉的窗口，后果是界面标签短暂不准） |
