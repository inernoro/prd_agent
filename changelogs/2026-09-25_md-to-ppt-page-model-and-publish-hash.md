| fix | prd-api | MD 转 PPT 页面生成先向网关确认模型名：默认运行配置里的旧模型池名字网关不认时改用网关默认对外模型并告知，用户点名的配置不认则直接说明原因，不再每页退化为兜底版式 |
| fix | prd-api | MD 转 PPT 生成与精修完成事件改为下发落库后的正文，修复刚生成的演示稿「发布为网页」「重绘本页」返回 409 内容不一致 |
| fix | prd-admin | MD 转 PPT 改用网关默认模型时弹出提示，说明原配置模型与本次实际模型 |
