| fix | prd-admin | 文学创作编辑器浅色主题配色：正文 [插图] 标记改为主题前景色（原白字压浅橙底不可读），右侧配图卡片无图时改纸面底 + 深色标题/提示词浮层，状态徽标改实心底 + 双写语义前景 |
| fix | prd-admin | 文学创作编辑器其余写死深色的控件改走主题 token：模型/位置策略选项、策略预览配图占位、Thinking 面板、图片显示尺寸浮条、[IMG] 行内代码 |
| feat | prd-admin | 文学创作配图「生成中」改用视觉创作共享的 GenDevelopLoader（画框进度 + 阶段词 + 基于历史耗时的剩余时间），正文占位与右侧卡片共用同一计时，并把真实出图耗时回写共享预估；替换原模糊光晕与静止引用块 |
| feat | prd-admin | GenDevelopLoader 新增 tone="adaptive" 跟随主题档（浅色下纸面配色），画布舞台默认恒暗档不变 |
| polish | prd-admin | 共享步骤条 WorkflowProgressBar 重做：已完成=绿圈对勾、当前=主色描边胶囊 + 实心编号、未开始=灰编号；生成中只禁点击不再整条 50% 透明（文学创作、自动化规则页同步生效） |
| polish | prd-admin | 文学创作右侧面板：生成中主按钮保持实心并显示实时进度（已识别 N 处 / 已完成 X/Y + 底边进度条）；配置条改为「生成设置」2×2 设置项（小标题 + 当前值 + 下拉箭头）+「全部配置」次按钮；配图卡片状态标签改为无边框圆点 + 文字 |
| polish | prd-admin | 文学创作 AI 输出面板：标题改「AI 输出 · 识别进度」，正文走主题正文色并放大到 12px，识别出的 [插图] 行单独高亮 |
| fix | prd-admin | 文学创作生成设置：提示词 / 风格图 / 水印 / 配图位置改为就地弹出的选项菜单（选中项打勾 + 底部「管理…」），不再点任何一项都打开整页配置 |
| polish | prd-admin | 文学创作右侧面板压高度：生成设置从 2×2 改为一行四个弹出按钮 + 行尾「全部配置」图标按钮；步骤条收紧一档；删除列表底部说明文字 |
| polish | prd-admin | 按 Apple HIG 统一编辑器菜单与按钮：新增 LiteraryQuickMenu（弹出按钮 + 平铺勾选菜单，近实心浮层底），两个模型选择同用；顶部模型胶囊去粗描边；配图卡片次要操作改无边框图标按钮 |
| polish | prd-admin | 浅色主题全站主按钮改为「深陶土实底 + 白字」（#A64B35，白字 5.71:1），替换「亮陶土底 + 深墨字」的夜间写法；首页 HERO 渐变改走专用 --hero-gradient-fg，暗色图表岛内钉回深墨字 |
| polish | prd-admin | 尺寸选择胶囊（共享 ImageSizePicker）与文学创作顶栏投稿胶囊去掉彩色描边，改实心 / 浅填充 + 正式上下箭头 |
| fix | prd-admin | 移除文学创作编辑器反复弹出的「新功能：手动指定配图位置」气泡（自动保存模型偏好会整份覆盖偏好、清掉已读标记，导致点过「知道啦」仍会再弹）；操作说明并入「配图位置」菜单 |
| polish | prd-admin | 浅色主题品牌强调色由陶土改为暖调苔绿 #3F5D45（主按钮、选中态、焦点描边、分节标题、装饰渐变、手机悬浮按钮一并切换；白字 7.33:1）；深色主题与周报专属配色不变 |
| polish | prd-admin | 共享步骤条改 Apple 写法：已完成 = 品牌色实心对勾、当前 = 品牌色描边圆圈、未开始 = 灰，不再借用「成功绿」 |
| polish | prd-admin | 头像菜单重排：消息置顶、按外观/消息/我的/工具分组，行高 32px、去掉补充说明，教程改为普通行显示等级，角标与分隔线走主题 token，桌面与手机抽屉共用 AccountMenuRow |
| fix | prd-admin | 浅色主题下看不清的琥珀色文字（暂用缓存/已降级提示）与几乎隐形的正文分隔线改用主题 token |
| test | prd-admin | 新增账号菜单可读性守卫：禁止 9/10px 字与写死 hex 颜色 |
| fix | prd-admin | AI 资讯分类色与「精选 / 实时」青色改走 --news-label-* 双主题 token，浅色下压到 700/800 档；首页资讯挂件在恒深底条上固定用亮色档 |
| fix | llmgw | CDS 预览里从 MAP 头像菜单跳「模型网关」报「did you mean to visit /llmgw/auth/map」：Vite 开发服务器 base 在 CDS 内改为根路径，正式构建仍为 /llmgw/ |
| fix | prd-admin | 步骤条标签放不下时横向滚动；文学创作风格图切换失败时提示原因（两处入口共用同一函数） |
