| fix | cds | 图的时间轴只留一个口径：前端不再把 5 秒的实时点追加进服务端数十秒的桶数组。此前抽屉开 5 分钟后，右边 60 个点占 69% 宽度只代表 5 分钟、左边 27 个点占 31% 却代表 30 分钟，X 轴横跨全宽差七倍，且开得越久越离谱 |
| fix | cds | 「够不够画图」改看真有数据的桶数（filled），不看数组长度。冷启动只有 1 个真样本时旧判据判真，画出一个从左上斜到零的大三角——那条下降沿是 null 被映射成 0 画出来的，纯属虚构 |
| feat | cds | 出图前改为图形骨架 + 诚实进度（已有 N 帧 · 约还需 X 秒出现曲线），替掉原来的虚线空框「正在读取指标历史…」 |
| feat | cds | 数据更新走数值补间（easeOutCubic 420ms），形状变化时直接切；尊重 prefers-reduced-motion |
| feat | cds | 补采磁盘 I/O：docker stats 一直在返回 BlockIO、ContainerStats 一直在解析，只有历史存储没收。并入现有吞吐卡（网络一行、磁盘一行），不新增卡片 |
| fix | cds | 图例「当前值」改走实时快照，不再取序列最后一个桶——桶是数十秒的平均值，摆在「当前值」位置上慢一拍 |
| test | cds | 新增 12 条守卫（时间轴单一口径 / series 被轮询 / 当前值走实时 / 骨架非空盒 / 帧数来自真样本 / 补间与 reduced-motion / hasPlot 看 filled / 磁盘速率差分与回绕与 null 口径），均做过红绿闭环 |
