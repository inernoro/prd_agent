| feat | prd-api | 智能体密钥新增「能力范围自动档」：不存清单，鉴权时现算「主人当前权限 ∩ 平台当前开放能力」，平台新增能力自动跟进、权限被收回立刻失效 |
| feat | prd-api | 接入台面板与授权自检改用「此刻实际拿得到什么」推导，并给出手动档「你有、但没开给它」的能力清单 |
| refactor | prd-admin | 接入弹窗从三步向导收成一屏：起名 → 一键复制配置，选能力收进高级设置（动了才钉死清单） |
| refactor | prd-admin | 接入台主页重构：第一屏改成挂着数字的判断句，能力大卡改成紧凑能力条，客户端行区分「说明标签」与「动作按钮」 |
| refactor | prd-admin | 调用记录改成「一件事一行」：按产物身份把入队与轮询折成一件事，失败行整行染色并给出原因与下一步 |
| test | prd-api | 补自动/手动能力范围守卫：Manual 必须是枚举 0 值、自动档必须取权限交集、鉴权与面板必须同源推导 |
| test | prd-admin | 补事件折叠、判断句生成、默认能力档与两档接线四组守卫 |
| docs | doc | debt.platform.md 记三条已知边界：手动档无回到自动档的界面入口、事件折叠只在已加载那一页、失败下一步按结果类别给 |
| fix | prd-admin | 调用记录折叠改判「一次动作 + 它之后的查看」：同一篇文档改两次不再折成一行（旧规则会让新的成功盖掉旧的失败、参数摘要还取自另一次编辑） |
| fix | prd-admin | 结论条已用数改取服务端权威合计：密钥当天被撤销后不再出现「判断句说调了 47 次、旁边显示 0 / 0」 |
| fix | prd-admin | 「你能给出去的工具数」按工具的 scope 数，不按能力整块数：只读用户不再被告知能给出发布类工具 |
| fix | prd-api | 密钥管理页返回「此刻真拿得到什么」：自动档密钥不再显示成零权限；四处消费方收敛到唯一判据 McpCapabilityCatalog.EffectiveScopesFor |
| fix | prd-api | 「你还能给它什么」改用 ScopeSatisfies 判：只存 `:write` 的钥匙（知识库/网页托管声明写蕴含读）不再被报成缺了读的那半 |
| fix | prd-admin | 客户端行不再把只挂 `agent.*` 开放接口 scope 的钥匙说成「一块能力也拿不到」，如实显示「另有 N 项开放接口授权」 |
| refactor | prd-admin | 移除第一屏的「今天用量」合计条：分子含已撤销密钥、分母只算还在的，凑成比值必然在某个边界说谎（三轮 Review 各挑出一种）。总量归判断句，单把钥匙的用量与上限归它自己那一行 |
| fix | prd-api | 「你还能给它什么」只报整块一点都没给的能力：只拿到读档的钥匙不再被整块报成未授权（会与同一行的已授权标签自相矛盾） |
| fix | prd-admin | 手机端客户端行改上下堆叠：并排时右侧按钮会把最后一个能力标签挤出可视区（390 宽实测桌面五块、手机只剩四块） |
| fix | prd-admin | 没有写入档的能力（海鲜市场）不再标「只能看」——那会暗示还有一档没给，而那一档不存在 |
| fix | prd-admin | 调用记录不再拿轮询的 HTTP 200 当「这件事成了」：以查看收尾又没有产物的事件如实标「还没出结果」，不给失败的 run 打绿灯 |
| fix | prd-admin | 撤掉最后一把钥匙后不再把当天活动一起抹掉（「没有过」与「现在没有了」是两件事） |
| fix | prd-admin | 续期指引改指真实位置：接入台这一页没有续期按钮，它在「海鲜市场 → 开放接口 → 密钥」 |
| fix | prd-admin | 接入台窄屏不再把整页锁死在一屏内：内部滚动改成只在宽屏生效，窄屏由外层滚（原来第二台客户端与「断开」按钮根本不渲染） |
| fix | prd-admin | 判断句不再把「还连着几台」当成「调了多少次」的主语：两个数来自不同人口（后者含当天被撤销的密钥），会把别人的账算到还在的那台头上 |
| docs | prd-admin | 窄屏守卫的注释改掉一个会误导后来人的判据：`document.scrollHeight === innerHeight` 修好前后都成立（滚动归 AppShell 的内容容器），真正的信号是那个容器有没有超出 clientHeight、以及「断开」按钮有几个 |
| fix | prd-admin | 调用记录的时间列不再只写 `HH:mm`：这份列表按条数取最近 N 次、天然跨天，昨天的 `15:01` 和今天的 `15:01` 原来长得一模一样，还紧挨着「今天一次都还没调过」那句判断 —— 不是今天的现在带上日期 |
| fix | prd-admin | 「还没出结果」的判据从「以查看收尾且多步」收敛成唯一一处 `outcomeOf`：单步的轮询、以及按结果筛选走的那条不折叠路径，原来都绕过了它，排队中的生图 run 仍显示绿色「成功」 |
| fix | prd-admin | 判断句补上最后一条会吞掉当天调用的分支（名单非空但全不可用），并把「有调用就必须说出这个数」钉成跨分支的不变量守卫 |
| fix | prd-api | 用不了的钥匙（已作废／已停用／过了宽限期）不再报出有效 scope —— 原来会给一把调不动任何东西的钥匙画满授权芯片，与同页授权自检的 toolCount=0 自相矛盾 |
| docs | doc | 债务台账新增 #28（自动档钥匙在回滚到旧版本时拿不到 scope）与 #29（「还没出结果」分不出还在跑与已失败） |
| fix | prd-api | 新加的那条守卫自己踩了同一个坑：`IndexOf("scopes = ")` 在这个文件里命中的是第一处（工具清单那段），不是 ToDto 里那一处，于是它量了一段无关代码。锚点改成 ToDto 的方法体 |
