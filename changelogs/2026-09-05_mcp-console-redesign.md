| feat | prd-api | 智能体密钥新增「能力范围自动档」：不存清单，鉴权时现算「主人当前权限 ∩ 平台当前开放能力」，平台新增能力自动跟进、权限被收回立刻失效 |
| feat | prd-api | 接入台面板与授权自检改用「此刻实际拿得到什么」推导，并给出手动档「你有、但没开给它」的能力清单 |
| refactor | prd-admin | 接入弹窗从三步向导收成一屏：起名 → 一键复制配置，选能力收进高级设置（动了才钉死清单） |
| refactor | prd-admin | 接入台主页重构：第一屏改成挂着数字的判断句，能力大卡改成紧凑能力条，客户端行区分「说明标签」与「动作按钮」 |
| refactor | prd-admin | 调用记录改成「一件事一行」：按产物身份把入队与轮询折成一件事，失败行整行染色并给出原因与下一步 |
| test | prd-api | 补自动/手动能力范围守卫：Manual 必须是枚举 0 值、自动档必须取权限交集、鉴权与面板必须同源推导 |
| test | prd-admin | 补事件折叠、判断句生成、默认能力档与两档接线四组守卫 |
| docs | doc | debt.platform.md 记三条已知边界：手动档无回到自动档的界面入口、事件折叠只在已加载那一页、失败下一步按结果类别给 |
| fix | prd-admin | 调用记录折叠改判「一次动作 + 它之后的查看」：同一篇文档改两次不再折成一行（旧规则会让新的成功盖掉旧的失败、参数摘要还取自另一次编辑） |
| fix | prd-admin | 结论条已用数改取服务端权威合计：密钥当天被撤销后不再出现「判断句说调了 47 次、旁边显示 0 / 0」 |
| fix | prd-admin | 「你能给出去的工具数」按工具的 scope 数，不按能力整块数：只读用户不再被告知能给出发布类工具 |
| fix | prd-api | 密钥管理页返回「此刻真拿得到什么」：自动档密钥不再显示成零权限；四处消费方收敛到唯一判据 McpCapabilityCatalog.EffectiveScopesFor |
| fix | prd-api | 「你还能给它什么」改用 ScopeSatisfies 判：只存 `:write` 的钥匙（知识库/网页托管声明写蕴含读）不再被报成缺了读的那半 |
| fix | prd-admin | 客户端行不再把只挂 `agent.*` 开放接口 scope 的钥匙说成「一块能力也拿不到」，如实显示「另有 N 项开放接口授权」 |
| refactor | prd-admin | 移除第一屏的「今天用量」合计条：分子含已撤销密钥、分母只算还在的，凑成比值必然在某个边界说谎（三轮 Review 各挑出一种）。总量归判断句，单把钥匙的用量与上限归它自己那一行 |
| fix | prd-api | 「你还能给它什么」只报整块一点都没给的能力：只拿到读档的钥匙不再被整块报成未授权（会与同一行的已授权标签自相矛盾） |
| fix | prd-admin | 手机端客户端行改上下堆叠：并排时右侧按钮会把最后一个能力标签挤出可视区（390 宽实测桌面五块、手机只剩四块） |
| fix | prd-admin | 没有写入档的能力（海鲜市场）不再标「只能看」——那会暗示还有一档没给，而那一档不存在 |
| fix | prd-admin | 调用记录不再拿轮询的 HTTP 200 当「这件事成了」：以查看收尾又没有产物的事件如实标「还没出结果」，不给失败的 run 打绿灯 |
| fix | prd-admin | 撤掉最后一把钥匙后不再把当天活动一起抹掉（「没有过」与「现在没有了」是两件事） |
| fix | prd-admin | 续期指引改指真实位置：接入台这一页没有续期按钮，它在「海鲜市场 → 开放接口 → 密钥」 |
| fix | prd-admin | 接入台窄屏不再把整页锁死在一屏内：内部滚动改成只在宽屏生效，窄屏由外层滚（原来第二台客户端与「断开」按钮根本不渲染） |
| fix | prd-admin | 判断句不再把「还连着几台」当成「调了多少次」的主语：两个数来自不同人口（后者含当天被撤销的密钥），会把别人的账算到还在的那台头上 |
| docs | prd-admin | 窄屏守卫的注释改掉一个会误导后来人的判据：`document.scrollHeight === innerHeight` 修好前后都成立（滚动归 AppShell 的内容容器），真正的信号是那个容器有没有超出 clientHeight、以及「断开」按钮有几个 |
| fix | prd-admin | 调用记录的时间列不再只写 `HH:mm`：这份列表按条数取最近 N 次、天然跨天，昨天的 `15:01` 和今天的 `15:01` 原来长得一模一样，还紧挨着「今天一次都还没调过」那句判断 —— 不是今天的现在带上日期 |
| fix | prd-admin | 「还没出结果」的判据从「以查看收尾且多步」收敛成唯一一处 `outcomeOf`：单步的轮询、以及按结果筛选走的那条不折叠路径，原来都绕过了它，排队中的生图 run 仍显示绿色「成功」 |
| fix | prd-admin | 判断句补上最后一条会吞掉当天调用的分支（名单非空但全不可用），并把「有调用就必须说出这个数」钉成跨分支的不变量守卫 |
| fix | prd-api | 用不了的钥匙（已作废／已停用／过了宽限期）不再报出有效 scope —— 原来会给一把调不动任何东西的钥匙画满授权芯片，与同页授权自检的 toolCount=0 自相矛盾 |
| docs | doc | 债务台账新增 #28（自动档钥匙在回滚到旧版本时拿不到 scope）与 #29（「还没出结果」分不出还在跑与已失败） |
| fix | prd-api | 新加的那条守卫自己踩了同一个坑：`IndexOf("scopes = ")` 在这个文件里命中的是第一处（工具清单那段），不是 ToDto 里那一处，于是它量了一段无关代码。锚点改成 ToDto 的方法体 |
| fix | prd-api | 只说「切成手动」不给清单时，把这把钥匙此刻拿得到的 scope 快照下来，而不是让它在返回 200 的同时失去全部工具（自动档的 Scopes 本来就是空的） |
| fix | prd-admin | 把「『今天』按 UTC 自然日算」重新放回页面上——改版时它被收进了额度编辑弹窗，而列表每一行都写着「今天」，UTC+8 的用户在早上 8 点看到它归零会以为数字错了 |
| fix | prd-api | 「用不了的钥匙不报 scope」这层判断收进 `McpCapabilityCatalog.EffectiveScopesForKey`，密钥管理页与接入台两处投影都走它——上一轮只补了其中一处，另一处仍会在写着 isActive=false 的同一行旁边列出它调不动的能力 |
| fix | prd-admin | 按结果筛选时再按**结局**过一道：服务端过的是纯 HTTP 结果，排队中的生图 run 回的正是 200，不过这道会在「成功」档里看到一行「还没出结果」 |
| fix | prd-admin | 幂等命中的重试不再算作事件的发起点——它没产生任何副作用，把它当发起会写出一个没发生过的「从发起到落地」耗时 |
| fix | prd-admin | 不可用的客户端不再一律写「已作废」——这份名单里根本不会有已吊销的钥匙，走到那一句只可能是停用或过期，两种都救得回来；新增 `unusableReason` 分别给出对应的恢复路径 |
| fix | prd-admin | 「成功」档里被语义过滤掉的条数如实说出来，并指到「全部结果」——服务端按纯 HTTP 结果分页取 50 条，极端情况下这一档会空，而页面此前一个字都不说 |
| fix | prd-api | 「你有、但没开给它」改用有效清单比对，不再读库里那份原始 scope——权限被回收后两者会分叉，而 write 蕴含 read 会让整块能力从「还能给它什么」里漏掉 |
| fix | prd-admin | 停用钥匙的文案不再写「重新启用后还能接着用」——前端根本没有任何一处会把 isActive 改回 true，那是指向一个不存在的动作（本 PR 第二次犯同一个毛病）；改成如实说明并给出真做得到的那条路 |
| docs | doc | 债务台账新增 #30（停用的密钥在界面上开不回来） |
| fix | prd-admin | 判断句不再说「全都成了」——today 的计数出自纯传输层判据，排队中甚至已失败的生图 run 在这里算成功，而同一屏的事件行会把它标成「还没出结果」；改成只说这些数字真能证明的「没被挡下、没报错」，并指路到「它干了什么」 |
| fix | prd-admin | 缺能力的提示不再写「到密钥管理里补上」——界面上根本没有改清单的入口（本 PR 第三次把用户指向不存在的动作）；只给真做得到的「重新接一台」 |
| docs | doc | 债务台账新增 #31（已发出去的手动档钥匙，界面上改不了它的清单） |
| fix | prd-admin | 「从发起到落地」只留给真落地的事件——还在排队的写成「从发起算起 Xs，还没出结果」、失败的写成「从发起到失败」；原来这一行只看多步与有无发起，跟上面那枚「还没出结果」的徽章自相矛盾 |
| fix | prd-admin | 事件行显示的时刻改用最后一步（与排序键同源）——10:00 发起、10:20 跑完的生图事件原来排在 10:10 那次上面却显示 10:00，看起来像列表没按时间排；发起时刻挪进展开详情并标「发起于」 |
| fix | prd-admin | 只入队、还没轮询的生图事件不再打绿灯——入队本身成功但图并不存在（判据只认产物，不再看最后一步是写还是读）；原来那条锁死此缺陷的用例也一并拆开 |
| fix | prd-admin | 「这一档筛没了」不再复用「还没有调用记录」那段引导语——正上方刚说「另有 N 条被滤掉」，下面就说去接一台客户端，同一屏自己打架 |
| fix | prd-api | 生图跑到一半（`finished:false`）不再把第一张成品的地址当成整件事的产物——照着它取地址，接入台会把四张里只成了一张的 run 判成「已落地」并打绿灯 |
| fix | prd-admin | 判断句里「出图 N 张」改成「发起生图 N 张」——这个数出自入队时占下的额度，不是真做出来的图，说「出图」会跟事件行的「还没出结果」当场打架 |
| docs | doc | 债务台账 #9 补记新后果：事件结局判据只认产物，而直连开放接口那条路产物列恒空，于是排队中的生图在直连记录上仍显示成绿色成功、入队与轮询也折不成一件事 |
| fix | prd-api | 切手动档的快照判据改认「给的清单是不是空的」，不再只认 null——`{"scopeMode":"manual","scopes":[]}` 与不给清单是同一个意思，而它原来会走校验分支、零个 scope 全部「通过」，然后把空清单原样存下去，接口 200、钥匙失去全部工具 |
| fix | prd-api | 跑完但没成的生图 run 不再被认出产物——`IsRunFinished` 对 Failed / Cancelled 同样返回 true，而 worker 在多张里坏了一张时会把整条 run 记成 Failed 却留着已成那几张的地址，只看 finished 的话失败的 run 照样打绿灯写「落地」；认不出来的状态仍放行，免得「打开」哪天静默消失 |
| fix | prd-admin | 接入台页头窄屏不再排成 Z 字——原来是单个 flex-wrap 加动作组 `ml-auto`，折行之后 `ml-auto` 照样把按钮顶到它那一行的最右端，于是切页组左对齐、右边空一片，下一行按钮右对齐、左边空一片；窄屏改成三行竖排，切页两档平分整行，「接入新的」吃掉整行剩余宽度 |
| fix | prd-admin | 「今天 N 次」与能力条的「看清单」不再各自孤零零占掉一整行——同一个 `ml-auto` 折行毛病；让被它们挤下去的那句说明 `w-full` 独占第二行，右对齐的元素就留在第一行末，宽屏用 `lg:order-last` 还原原来那一排 |
| fix | prd-admin | 手机上「刷新」缩成图标钮（下拉刷新本来就能做这件事，它不该和主操作一样重），主次两个按钮触控高度从 36px 提到 40px |
| fix | prd-admin | 额度条用过一点也看得见了——1/200 按真实比例是 0.5%，在一条 4px 高的轨道上等于没画，「今天用了一次」和「今天一次没用」长得一模一样；填充抽成 `quotaFillPercent` 并给最小可见宽度，轨道加高到 6px、底色换成看得出是容量而非划痕的那一档 |
| fix | prd-admin | 能力芯片之间的间距拉开到明显宽于芯片内部，「· 只能看」不再被扫读成独立的一项 |
| fix | prd-admin | 「连着的客户端」这个标题不再被旁边的说明挤成两行——flex 项默认可收缩，而那两句说明比标题长得多、抢得也多，390 宽下被压到最窄的正是标题；标题 `shrink-0` 不许被压，两句说明合成一句、窄屏独占第二行（这处退化是上一轮把「「今天」按 UTC 自然日算」加到这一行时引入的，当时判断「放标题旁比逐行写省地方」，省下的地方正是从标题这一行扣掉的） |
| fix | prd-admin | 接入台页头窄屏改成**一条**横滚控制条（标题 + 切页 + 接入新的 + 刷新图标），不再竖排三行——上一版把 Z 字改成「每行都填满」的竖排，正好撞上 `mobile-first-density` 原则 3「进内容前最多一条控制条，多条要合并/横滚、不要竖向堆」，130px 高的 chrome 把内容推出首屏；形态照规则点名的参考实现 `InsightsPanel.renderMobileSingle`，次要动作（刷新）图标化并入条尾，标题 `shrink-0 whitespace-nowrap` |
| fix | prd-admin | 窄屏只剩图标的刷新按钮补 `aria-label`——文字 span 被 `hidden` 摘出无障碍树、图标又是 `aria-hidden`，读屏用户拿不到这个按钮的名字；文案跟着 refreshing 状态走 |
| fix | prd-admin | 空态与停用钥匙的指路文案不再写「右上角」——页头收成一条横滚之后，「接入新的」在手机上既不在右上角、初始也不在屏幕上；改成「这一页顶上那行里的」，宽窄两端都成立。守卫扫源码禁止写死屏幕方位（右上角/左上角/右下角/左下角/侧栏），这是本 PR 第四次把用户指向找不到的东西，前三次是不存在的动作、这次是会随断点变的方位 |
| docs | doc | 债务台账把「已知边界（后续可补）」整节移出「已结清（供回溯）」区段——那节里 #4-#18、#22-#31 大多是「第一期不做」的未还项，压在「下列条目已解决」的引言下面会被读成已结清；顺手去掉失去上下文的孤儿编号「二、」（同名标题在文档上半部还有一个，那个才有对应的「一、」） |
| fix | prd-admin | 页头标题在手机端让位（`sr-only lg:not-sr-only`，不是 `hidden`——读屏仍念得出这一页叫什么，h1 也还在文档大纲里）——390px 装不下标题 135 + 切页 243 + 主操作 108，横滚条里最先该保住的是「能点的东西」，而用户刚从抽屉点着「智能体接入台」进来，标题是标识不是操作；让位后切页两档与「接入新的」都进首屏 |
| fix | prd-api | 生图 run 的产物判据收成白名单：状态在场就必须是 `Completed`——`IsRunFinished` 是「终态 **或** done+failed>=total」的或，而 worker 要等所有条目跑完、读回最终计数才写 Status，中间那个窗口 GetRun 会同时回「跑完了」「还在跑」和已成那几张的地址，上一版直接放行。推翻上一版「认不出来的状态一律放行」的取舍：那条防的是假设中的将来（新增状态导致丢链接），而这个窗口是现在就存在的缺陷，且误报成功比丢链接更糟——丢链接用户看得见（没得点），误报成功用户看不见（以为图出来了）。唯一保留的宽松是状态字段缺席时只靠 finished |
| fix | prd-admin | 判断句分得出「从来没接过」和「现在没连着了」——昨天用过、今天之前被撤销的钥匙会让 clients 空且 today.calls 为 0，上一版据此说「还没有客户端接进来」，而它的记录还在「它干了什么」里躺着；用现成的 recentCalls（按条数取最近 N 次、天然跨天）判断，不改后端契约 |
| fix | prd-admin | 方位守卫从只扫 `McpConsolePage.tsx` 扩到扫整个 mcp-console 目录——上一版加这条守卫的同一个提交里，`headline.ts` 还留着两处「右上角」原样没改，因为守卫只盯着我刚改的那个文件（predicate-and-wiring-discipline 形状 7：守卫自己没接上线）；现在新增文件自动进扫描范围 |
| test | prd-admin | 调用记录分组的三条用例不再在失败行上造 artifact——后端 `RecordFinishedAsync` 的 error 分支只写 ErrorMessage、不调 `ApplyArtifact`，失败行的产物列恒为空；我用后端不可能产生的数据测了分组，绿了也说明不了什么。按真实形状造之后暴露出：一次生图的入队与它那次失败的轮询会落成两件互不相关的事（实测分组数 2，应为 1）。根因在后端记录侧，属新增语义类别，本轮只用 `it.fails` 锁定期望行为（修好后会因「意外通过」而红），台账 #32 |
| fix | prd-api | overview 新增 `hasHistory`：服务端不带时间下界查一次「这个人有没有过任何调用」。面板其余数字全按 UTC 自然日切，而「从来没接过」是一句关于全部历史的话 |
| fix | prd-admin | 判断句改用 `hasHistory` 判「从来没接过」——上一版拿 overview 的 `recentCalls` 非空代替，理由写的是「它按条数取最近 N 次、天然跨天」，**那是错的**：它同样走 `TodayFilter`，今天没调用时必然为空，那个分支等于没改。跨天的那份是「它干了什么」端点（`listMcpCalls`），与这里同名不同义（台账 #33）。旧后端不回这个字段时缺省当「有过」——宁可少说一句引导，不要把真实历史说成从来没有 |
