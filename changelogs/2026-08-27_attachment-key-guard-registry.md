| test | prd-api | 「建附件必须存下对象 key」的守卫从「够不够多」改成「是不是正好这几处」：站点清单逐一钉死，少一处、多一处都红。旧的 `>= 6` 阈值挡不住「悄悄少一处」——把工厂方法从表达式体改成块体（一样合法的写法）判据就认不出它，同时去掉 key，处数从 9 掉到 8 仍然过阈值，真实回归一路绿 |
| docs | platform | debt 台账新增 DS35：这条判据靠文本匹配、天然认不全所有写法，三轮 review 挑出三种绕过写法。第三轮起不再加正则（§5.5：同一个文本判据反复被要求加格式时改有限枚举）。钉清单封住了「悄悄丢一处」，残余风险是「新增一处且用判据认不出的写法」，根治要换 Roslyn 语法树判据 |
