| feat | cds | 分支详情「关系」卡改版：缩略态改为一行流向条（入口 → 壳 → 前缀成员 → 共享基础设施 / 外部引用），前缀写在节点里，出问题的节点自己描边并挂问题数，新增结论下的事实行；修复缩略图只按宽度缩放、宽屏上被裁到只剩「入口」一枚节点的缺陷 |
| feat | cds | 关系图线型有语义：声明的关系一律实线，只有按名推断画虚线；框角与入口卡改用用户语言，图例按线型讲清六种含义；redis / mongo 徽标不再占用 bad / ok 语义色 |
| polish | cds | 关系卡并入总览面板（判断行之下、入口之上），不再常驻页签之上；加载 / 失败态用同一副骨架，卡片高度不跳；chip 错峰淡入并尊重 reduced-motion；窄于 880px 时成员列与尾列折成计数 chip |
| polish | cds | 总览判断行：已运行不满 1 分钟按秒计并标「刚就绪」，部署不满 1 分钟写「刚刚部署」；CPU 图就绪不满 5 分钟时在图里明说「正在积累」，左侧空白不是掉线 |
| test | cds | 新增 relation-flow-strip 守卫：流向条必须列全每个服务与基础设施、关系卡只在总览 relationSlot 出现、线型 dash 只由 inferred 决定、时长文案按秒计 |
| fix | cds | 关系卡 / 半屏图 / 全屏页补零服务空态：不再显示全零事实行、孤零零的入口 chip 和「体检无错误」，改为说明没有服务并给「去配置服务」入口；关系图图例每项不换行，窄抽屉里不再折成一坨 |
| feat | cds | CDS 托管 CDS：父实例注入的徽章在托管态长成「CDS 托管 CDS」深绿变体（去部署按钮、提示部署 / docker 已禁用），子实例侦测到父徽章就不再画自己的橙色 pill，底部只剩一条 |
| feat | cds | 预览实例数据镜像：父实例部署预览实例分支时把项目 / 构建配置（env 脱敏）/ 分支（含服务状态、预览地址）/ 部署 run / 活动日志 / 报告元数据 / 运行中容器近 30 分钟指标写成 `.cds/preview-mirror.json`，子实例每次启动按它播种（只读、带镜像标记、幂等替换），静态形状快照退役为兜底；序列端点在预览实例上回放镜像点位；分支卡与抽屉标「镜像 · 采集于」 |
| test | cds | 新增 widget-script-hosted-cds 与 preview-mirror 守卫：托管态三处部署入口都被闸住、镜像脱敏零泄露、幂等替换、指标锚到当下、两个部署点与序列端点 / 启动 / 实例模式端点的接线 |
| fix | cds | 总览 CPU 图「正在积累」提示的包裹层补 h-full：此前包裹层 0 高，面积图整张不可见（本地起子实例截图抓到，已加守卫） |
