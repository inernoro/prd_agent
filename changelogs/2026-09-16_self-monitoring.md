| feat | cds | CDS 用监控自发现协议监控自己：新增免登录自检端点 `GET /api/self-check`（部署 / 构建 / 页面 / 探测器 / 接入 / 宿主 / 通知 / 自身八类共 13 条监控），启动时以代码初始化建内置项目「CDS 自身」并插上端点，迁移到别的机器自己长出来 |
| feat | cds | 端点列表接口标出内置端点，内置端点不能拔（400）；面板上标「内置 · CDS 自身」并不给拔掉按钮 |
| test | cds | 自检文档喂真解析器断言 13 条全收零拒收；磁盘 / Docker / 探测器三处「量不到写哨兵不写 0」各有用例；启动引导幂等与回生；接线守卫 |
| fix | cds | 自检刚重启时不误报：探测器首轮未完成写「进程起来多久」并复用探测器自己的停摆判定，自身状态缓存未就绪在 5 分钟宽限内只 warn 不响铃——否则每次自更新都先响一次铃再自愈 |
| fix | cds | 自定义监控的探测目标带上项目名，没有分支预览的项目（内置「CDS 自身」）在面板与盲区地图上显示名字而不是 id |
| fix | cds | 「部署卡住」同时看心跳：在途部署心跳停超 10 分钟即报（CDS 重启打断的部署就是这样，按年龄要等 45 分钟）；自检文档 15 秒短缓存，13 条监控同一轮共用一份，不再各自跑 docker 与 Mongo 查询；Docker 打不通时空原因补成退出码与超时说明 |
| docs | cds | 债务台账追加：self-update 两次「重启」没换进程、self restart 能换但要两三分钟且状态看不出正在换 |
| security | cds | 自检端点不再免登录：只认「本机回环 + 进程内一次性令牌」，令牌不落盘不进日志、只发给自检端点自己的地址，从外面打一律 401 不吐一个字；探测器与发现器在进程内自动带令牌 |
| fix | cds | 启动收尸：心跳早于本进程启动时刻的在途部署一律收敛为失败（可重试），不再等 15 分钟周期收割；重启打断的部署不会再挂着 building |
| fix | cds | 自更新「重启没换进程」根因是重启前等在途部署排空（最多 5 分钟）全程不可见：排空每 10 秒推 SSE 进度，self-status 新增 restartWait（在等谁、等了多久、最多等多久），restartStatus 判定收成唯一函数，维护页摆出等待横幅 |
| fix | cdscli | self update / self restart 只在 restartStatus=completed 后才报 restarted，不再靠 healthz 200（旧进程照样 200）；incomplete 如实报失败并给出下一步 |
| security | cds | 通知通道接口（读 / 建 / 改 / 删 / 演练）改用「非作用域管理员」守卫：create-only 与带作用域的全局 Key 不再能读到 webhook 地址、Bark 密钥尾号、MAP 端点（Codex P1）；判定抽到独立模块与 projects.ts 共用一份 |
| security | cds | 项目级 Key 的存活摘要不再附带系统级通知通道（含别的项目的通道名、投递计数、最近失败原因）（Codex P1） |
| fix | cds | 自监控「通知通道」按投递台账判活：最近一次投递失败的通道不算通着，只看配齐了不够（Codex P1） |
| fix | cds | 通知通道编辑器补启用开关：草稿与请求体带 enabled，关掉即临时静音、配置保留（Codex P2） |
