| feat | llmgw | 批量导入上游模型时一步登上白名单：同时建公开模型名与上游线路，同名只加线路 |
| feat | llmgw | serving 补 GET /v1/models 与 /v1/models/{id}，OpenAI 标准清单，按 key 过滤白名单 |
| feat | llmgw | 对外清单按线路逐条报价，非美金整条不报，一条算不出价时 pricing 写 null |
| fix | llmgw | 公开模型名剥掉供应商前缀，官网与中转导入的同一模型不再变成两个条目 |
| feat | llmgw | 白名单页补「从上游批量登记」直达入口，导入结果如实回显登记了几个 |
| test | prd-api | 新增批量登记与对外清单守卫五条，测试项目引用 console-api 以做行为断言 |
| fix | prd-api | 逻辑模型 Offering 熔断后补上冷却与半开租约，不再永久出局 |
| fix | prd-api | 失败计数与健康状态改为原子自增加单调升级，并发下断路器不再迟迟不跳 |
| fix | prd-api | 隔离路径清掉半开租约与人工恢复标记，坏成员不再每轮抢占首发名额 |
| refactor | prd-api | 熔断阈值与冷却租约收敛为 GatewayCircuitBreakerPolicy 唯一判据源 |
| feat | llmgw | 新增手工恢复上游线路端点，恢复语义为授予半开资格而非直接放回健康 |
| test | prd-api | 新增熔断守卫 14 条，覆盖判据单调性、原子写口径与半开接线 |
| docs | llmgw | 台账记入调度器死配置、探针从未启用、流中断不切换、调度空壳四条已知边界 |
| feat | llmgw | 逻辑模型页重做为模型白名单：一行一模型，上游成带色线路，用量给趋势线 |
| feat | llmgw | 新增逻辑模型近 30 天用量聚合端点，按天卷起调用量与美金花费 |
| fix | llmgw | 团队授权改为给数量不平铺名字，部门多或名字长时不再撑爆列宽 |
| refactor | llmgw | 协议、优先级、权重等内部字段下沉到展开态，列表表面只留要决策的四件事 |
| test | prd-api | 新增白名单列表三条源码守卫：团队计数口径、按线路逐条报价、趋势线渐变 id 唯一 |
| feat | llmgw | 模型页新增「调用全貌」：六步推演现在发一个请求会落到谁，含只给 appCallerCode 不点名那条路 |
| feat | llmgw | 新增 GET /gw/logical-models/{id}/call-trace：目录闸、不点名去向、候选线路逐条、协议、近 30 天账本 |
| fix | llmgw | 前端不再自己判「哪条线路在扛流量」，改用服务端下发的排队名次——旧判据比运行时严，把降级但仍在承接的线路显示成「没有主」 |
| refactor | prd-api | 挑选判据抽成 GatewayRouteSelection 纯函数，ModelResolver 调它；停用与熔断不再写在 Mongo 查询条件里 |
| test | prd-api | 新增判据行为对照：同一组输入喂运行时与控制台镜像，逐条断言结果一致，红绿闭环跑通 |
| docs | doc | 新增 design.platform.llm-gateway.model-architecture：四层架构、一次调用七步、异构上游三个槽 |
| fix | llmgw | 调用全貌的默认模型查询补 Enabled 与排序，与运行时逐条对齐；「会落到它」增加「真有一条线路能接」这一条 |
| fix | llmgw | 跳过判据补「目标模型或所属上游被停用」这一档——线上队首指向的物理模型是停用的，面板照样指着它 |
| polish | llmgw | 模型行按钮列放宽，「调用全貌」「展开」不再挤成一列每行一个字；协议未显式配时说「跟着目标模型走」而不是拼一个假适配器名 |
| test | e2e | 新增调用全貌真人路径验收脚本；双主题判据改为「两张图真的不一样」，此前只断言「不是深色底」而默认本就是浅色 |
| feat | prd-api | 模型可标记为「这个用途没点名时用它」，同租户同用途最多一个 |
| feat | prd-api | 解析链路未点名模型时先找默认模型，找不到或解析不出来原样回落到模型池 |
| refactor | prd-api | 按名字与按默认两条入口共用同一个逻辑模型解析体，不再各写一份 |
| feat | llmgw | 控制台行上显示默认标记，展开态可设可取消，并说清顶掉了谁 |
| test | prd-api | 新增默认模型守卫 5 条，回落承诺与先清旧再置新的顺序各做过红绿闭环 |
| fix | llmgw | 搬迁的模型能力从池成员快照取，不再是空集合导致能力门不放行 |
| fix | llmgw | 影子比对区分「新路没生效」与「选了别的上游」，不再混成一句不一致 |
| test | prd-api | 能力采集 8 条行为断言，Value=false 不当成具备，无快照退基线能力 |
| fix | llmgw | 搬迁改为「近期不可用照搬、陈年旧账重置」，24 小时为界 |
| test | prd-api | 近期失败判据 9 条行为断言，含无失败时间与降权两种边界 |
| fix | llmgw | 搬迁遇到能力为空的已有模型会补上，只补空的不覆盖人工调过的 |
| fix | llmgw | 搬迁也守「同用途最多一个默认」，第二个降级并报出来，不再绕过 PUT 的互斥 |
| feat | llmgw | 新增搬迁影子比对脚本：同一批请求新旧两路解析必须一致，差异逐条列出 |
| test | prd-api | 守卫补：搬迁端点必须再走一遍同用途唯一默认那条规则 |
| refactor | llmgw | 上游页展开即见名下模型与登记状态，没登记的排前面并直说「调用方找不到它」 |
| refactor | llmgw | Provider 与 Exchange 合成「上游」一个入口两段，/exchanges 落到转接上游段、锚点保留 |
| refactor | llmgw | 模型池停止新建（冻结横幅指向模型页），保留摘成员/恢复/停用等修复动作 |
| refactor | llmgw | 路由导航五条收成两条（模型 / 上游），三条旧地址保留路由与页内入口 |
| refactor | llmgw | 「模型白名单」页标题改为「模型」，与导航同名 |
| test | prd-api | 新增两条跨模块守卫：上游名下模型接线、导航两条且旧地址不留死链；均做过红绿闭环 |
| chore | prd-api | 测试项目删掉两个已由 ProjectReference 提供的 Compile Link，消除 CS0436 |
| fix | llmgw | /v1/models 的 DataContext 参数补 [FromServices]，不注册它的宿主不再整张端点表构建失败 |
| test | prd-api | 新增 serving 端点绑定守卫：最小宿主上枚举 EndpointDataSource 必须建得起来 |
| polish | llmgw | 上游页删掉指向 Exchange 的那句说明（转接段就在上面），名下模型列不再折行 |
| fix | llmgw | GET /v1/models 的权限判反：列模型是读不是调用，改判 route:read |
| docs | llmgw | 新增模型概念收敛活看板，四个阶段各带 blocker、下一步与验收证据 |
| test | prd-api | 补守卫：/v1/models 必须在落到 invoke 兜底前判成 route:read |
| feat | llmgw | 新增池搬迁端点：池 Code 成公开名、成员成线路、兜底标记原样搬 |
| feat | llmgw | 搬迁默认试运行，只读旧表写新表，可重复跑不重复建 |
| test | prd-api | 搬迁判据 7 条行为断言 + 端点守卫（试运行、不动旧表、健康不搬） |
| fix | llmgw | 对外模型清单的 via 不再回落到内部 Mongo id，取上游模型名 |
| test | prd-api | 补守卫：via 不许写成 TargetId |
| docs | llmgw | 活看板记录 P0 已验收，五条实打结论与两个实测 bug |
| fix | llmgw | 调用全貌的「不点名会落到它」补上调用方主语，配了专属池的调用方不再被谎报 |
| feat | llmgw | 调用全貌面板新增判定流程图，每条岔路按当前状态点亮或灰掉 |
| docs | llmgw | 架构文档补分层框图与判定流程图两张 Mermaid |
| test | prd-api | 镜像行为对照补调用方维度，守卫覆盖流程图接线与逐调用方冒烟 |
| refactor | prd-api | 删掉只被一条测试养着的池端点测试器 |
| test | llmgw | 真人路径验收补流程图与逐调用方两条断言 |
| docs | llmgw | 看板补阶段 4 删除清单，分三档并给出「先断流再删路最后删壳」的顺序 |
| fix | llmgw | 结论那句也补主语：配了专属池的调用方点名与不点名都走不到这张目录 |
| fix | llmgw | 验收脚本改为直连预览：钉代理 CA 公钥而非忽略证书，等待判据不再用 networkidle |
| feat | prd-api | 对外模型可「指定调用方」：这些调用方不点名时走它，优先于用途默认——断流的前提 |
| feat | llmgw | 模型页可看可改指定的调用方；调用全貌逐调用方结论认两层默认 |
| test | prd-api | 守卫钉住两层顺序（认领必须查在用途默认之前）与认领唯一性的维护顺序 |
| feat | llmgw | 「active 调用方必须绑池」换成「必须有人接得住」：判据与运行时两层同序 |
| fix | llmgw | 调用全貌的「其余为什么没落到它」按真实构成说，不再写死成专属池或未放行 |
| refactor | prd-api | 删掉解析器里的模型池分支：ResolveCoreAsync 767 行降到 88 行，整个文件 3702 行降到 2297 行 |
| refactor | prd-api | 池查询换成状态查询（114 行降到 55 行）；可选模型清单只剩端出对外模型目录（89 行降到 14 行） |
| refactor | prd-api | 删掉 13 个已成死码的池方法共 649 行，以及 3 条测已删功能的用例 |
| test | prd-api | 清理钉住已删池行为的守卫；「GW-only 不得查 MAP 调用方」的不变量升级为「压根不查」 |
| docs | llmgw | InMemoryModelResolver 化石记进债务台账 |
| refactor | llmgw | 删壳第二层：console-api 删掉模型池的 11 个写入端点与 `/gw/pool-types/ensure`，连带清掉随之变孤儿的 7 个辅助函数与 12 个请求/结果 DTO，Program.cs 净减 1419 行、Dtos.cs 减 94 行 |
| refactor | llmgw | 保留三个只读/搬迁入口：`GET /gw/pool-types`、`GET /gw/pools`（调用方页的池筛选、实体详情的池展示仍在读）、`POST /gw/pools/migrate-to-models`（正式环境的存量池尚未搬迁，删了就再也搬不了） |
| refactor | llmgw | 前端删掉最后一个孤儿池接口 `removePoolModel` 与随之未用的 `ModelPool` 类型导入 |
| test | prd-api | `GatewayLegacySweepGuardTests` 改为池退场的反向守卫：钉住三个保留入口仍在、12 个已删路由不许回来（红绿闭环验证过），并清掉 `GatewayDataDomainGuardTests` 里 16 条锁死已删实现的断言 |
| chore | prd-api | 协议路由审计脚本去掉两条指向已删池写入端点的静态断言 |
| fix | llmgw | 删壳后跟上文案：首页不再说学习中心讲「模型池」、学习中心那条卡片改指 `/logical-models`（原先指向已删的 `/pools`）、排查指引改说「对外模型 / 线路」 |
| refactor | prd-api | 挑选判据从 5 份收到 2 份：删无人调用的 SelectBestModel，池成员排序与测试拷贝改为调权威判据 |
| fix | prd-api | 删掉永远为假的 ResolutionType == "DefaultPool" 及它喂的恒为 false 的 DTO 字段 |
| refactor | prd-api | 删掉无人调用的 NeedsModelConfigFallback |
| test | prd-api | 新增两条减枝守卫：解析标签字面量必须真的会被产出；挑选判据只许权威与镜像两份 |
| docs | llmgw | 架构文档补「减枝」一节，写清三条现在不砍的枝与理由 |
| fix | llmgw | 模型价格抽屉的两个接口路径自带 /gw 前缀，拼成 /gw/gw/... 后 404 |
| test | llmgw | 挂载点守卫新增判据：客户端路径不许自带 /gw 前缀 |
| fix | llmgw | 补登喂给每一道名录门：导入端点原本只查内置 38 条，刚补登的模型会被当场拒 |
| fix | llmgw | 名录补登的唯一性覆盖别名，且更新端点补上同一份查重 |
| fix | llmgw | 导入失败后重试真的能补回缺失的对外模型与线路（原来重试时整段被跳过） |
| fix | prd-api | 生图契约覆盖表按租户过滤，不再把别的租户的配置装进进程全局表 |
| fix | llmgw | 池搬迁影子比对脚本不再永久改掉线上默认（快照 + finally 恢复） |
| fix | llmgw | 教程漂移判据认「本次被删掉的页面」，不再要求给不存在的页面登记教程 |
| test | prd-api | 新增「补登喂给每一道名录门」守卫，跑过红绿闭环 |
| fix | llmgw | 批量导入盖上能力 schema 版本，不再让导入后的第一次 capability-audit 变红 |
| fix | llmgw | 批量导入遇到同名但别的用途的对外模型时拒绝挂线路，不再让生图线路被当成 chat |
| fix | llmgw | 生图同步状态按租户分行，共用网关库时不再互相覆盖 |
| test | prd-api | 名录门兑换所用例改走对外模型线路，暴露出一条真缺陷（详见提交说明） |
| fix | prd-api | 对外模型解析带上物理模型的价格：此前读方强类型缺这几个字段，价格写进去读不出来，每次调用都按「没配价」记账且无任何报错 |
| fix | llmgw | appCaller 页的模型池控件退役成只读回执：解析器已不读那几个字段，保存成功却什么都不改变，是一次静默空操作 |
| fix | llmgw | appCaller 列表不再把模型池当「当前路由」显示，删掉随之失效的「预览模型池」抽屉 |
| test | prd-api | 新增对外模型计价守卫（弱类型写、真解析器读），红绿闭环验证过 |
| fix | llmgw | /v1/models 的可用判据与运行时对齐：熔断线路、指向已停用模型/平台/兑换所的线路不再当成可用发布 |
| fix | llmgw | 价格抽屉清空全部价格时发 clearPricing，过期价格终于删得掉 |
| fix | llmgw | 调用全貌面板不再说「回落到模型池」——那条路已退场，改说当场解析失败并给下一步 |
| fix | prd-api | 加权选路只在最健康那一档内分配，降级线路不再被旋到健康线路前面；面板报的比例同范围 |
| fix | llmgw | 生图契约的模式冲突检查收敛成一份，更新端点不再是后门 |
| fix | llmgw | 创建对外模型时校验「指定调用方」唯一性，不再出现两个模型同时认领 |
| fix | llmgw | 池搬迁四处：跨用途标识撞车当场拒绝、盖能力契约版本、成员按三种标识匹配、上游名取物理文档真名 |
| fix | llmgw | 批量导入重发布按规范化名查询，换个大小写重试不再空跑 |
| test | prd-api | 新增加权跨健康档的行为用例（遍历全部 seed 落点，两份判据各断一遍） |
| ops | llmgw | chat 的默认从 `default-chat`（169 条能接线路、127 条是 OpenRouter 全量导入、第 2-8 位全是 gpt-3.5 全系与 gpt-4 初代）改指新的旗舰梯队：gpt-5.6-sol 直连 → 同模型异上游 → sol-pro → terra → 5.5 → claude-opus-5 → claude-sonnet-5，七条里没有任何 mini/nano/lite/flash/haiku 便宜档 |
| ops | llmgw | 老的 `default-chat` 原地保留为归档（描述里写清为什么），回退就是把默认指回来。数据面改动，正式环境要另做一次 |
| feat | prd-api | 生图模型契约可以配在控制台、不用改代码不用发版：新增 `GatewayImageModelConfig` 实体与 `llmgw_imagegen_model_configs` 集合，`ImageGenModelAdapterRegistry.TryMatch` 内部先查覆盖表再回落到代码内置那 19 条（纯增量：库里一行都没有时行为逐字节不变） |
| feat | prd-api | 新增 `ImageGenModelConfigSyncWorker`：每 60 秒整表原子替换覆盖表，拉取失败保留上一版快照不清空；并在启动时把代码内置那份发布进 `llmgw_imagegen_builtin_catalog`，供控制台显示与「照这条建一份」 |
| feat | llmgw | 控制台上游页新增「生图契约」一段：列表、增删改、看内置那 19 条、照内置那条建一份；界面如实写明「保存后最长 60 秒生效」 |
| test | prd-api | 新增 `ImageGenConfigOverrideGuardTests` 七条：覆盖真的赢过内置（红绿闭环验证过）、空覆盖回到内置、没人绕过唯一判定入口、实体每个字段都真的接进运行时、控制台词表与运行时常量一致、内置清单由运行时发布而非手抄、刷新器接上线且失败不清空 |
| refactor | prd-api | 「数据行 ↔ 运行时配置」的翻译从 Api 的 worker 搬进 `PrdAgent.Infrastructure/LLM/ImageGenConfigTranslation.cs`——守卫项目不引用 Api，留在 worker 里「漏接一个字段」这种静默坏法没有任何东西够得着 |
| feat | llmgw | 生图契约补同步状态回写：刷新器每轮把「几点同步的、认到了哪几个模式」写进 `llmgw_imagegen_sync_status`，控制台读它，界面逐条对着刚填的模式说「已生效」或「还没被认到」——不再只说一句「最长 60 秒」让人盯着屏幕猜 |
| feat | llmgw | 模型名录可在控制台补登：上游出新模型时登记一次立刻生效，不用改代码也不用发版 |
| feat | llmgw | 上游清单那一屏的「名录外」模型就地补登，补完重拉当场翻成「名录内」 |
| test | prd-api | 新增 9 条名录补登守卫（补登优先、同一套查找规则、别名、端点现查、写入侧拒未知用途、两处前端接线） |
| fix | llmgw | 模型名录那一段的界面文案漏了 Markdown 星号，用户会原样看到；已改用 strong |
| test | llmgw | 新增守卫：src 下全部 tsx 的界面文案里不许出现 Markdown 强调符 |
| polish | llmgw | 上游主表去掉「类型 / API URL / 并发」三列，接口细节收进「查看接口」预览与编辑折叠区 |
| test | prd-api | 新增守卫：主表不许摆接口实现细节，且三项必须仍在预览里查得到 |
| test | prd-api | 名录门兑换所用例拆成两个对外模型：旧形态那条原本与被测线路同挂一个模型、按设计放行后顶了上来，断言测不到被测项 |
| test | prd-api | pinned 用例从模型池改建对外模型 + 两条线路，补上「不点名会选到另一条」的对照物，并钉住 pinned 不读线路协议 |
| fix | prd-api | 还绑着模型池的调用方不再静默走默认：解析时点名那条已失效的绑定并说清下一步 |
| test | prd-api | 池退场留下的测试债：视觉目录、未登记调用方回落、名录门顶替三组改用对外模型表达同一批不变量 |
| ops | llmgw | default-chat 队首换成 gpt-5.6-sol（OpenAI 直连那条线路顺位 670 → 10）。这是数据面改动，落在 CDS 共享库里、不随代码走，正式环境要另做一次 |
| fix | prd-api | 调用方去向判据从三档收到两档：模型池退场后运行时不再读 `AllowedModelPoolIds`，放行的调用方一律认对外模型目录。此前面板拿这个历史字段下结论，对 `document-store.transcribe-summary::chat` 说「点名与不点名都走不到 default-chat」，真打一次点名却落到了 default-chat 的队首——结论是反的 |
| refactor | prd-api | 视觉创作那三个调用方的名单从共享判据搬回解析器：它剩下的唯一职责是模型选择器的目录展示，解析判据与控制台面板都不需要知道它，份数 2 → 1（权威 + 控制台镜像 → 只剩权威） |
| fix | llmgw | 面板同步：结论句改说「放行中的 N 个」、逐调用方 chip 去掉「走自己的专属池」、判定流程图第一个菱形从「认对外模型目录吗」改为「放行吗」、构成统计去掉已不存在的那一档 |
| test | prd-api | 行为对照测试改判新不变量「放行与否是唯一判据，换哪个调用方代码都必须同一个答案」（红绿闭环验证过）；删掉那条正在保护错误结论的用例（形状 4a：它逐字要求面板继续按池来判） |
| docs | llmgw | 架构文档同步：判定流程图去掉池岔路并把两层默认画出来、第 5 节补这次判据收敛的实证、6.5 节记「预判对了一半」 |
| fix | llmgw | 教程映射跟上页面收敛：模型池那一面退场、并入「模型」；「上游」改成一壳两段，判据认内嵌面 |
| fix | doc | 看板 H1 与 index.yml / guide.list 登记的标题对齐 |
| test | llmgw | 内嵌面新增 2 组守卫（现状必须真的走到那条分支 + 三种坏法逐个必须报错） |
| fix | llmgw | 批量导入的多模态模型判成 chat 而不是 vision：带图对话走的就是 chat 端点，判 vision 会让 gpt-4o 这类模型接不了普通文本请求 |
| fix | llmgw | /v1/models 只报显式声明 USD 的价格：缺币种此前落进报价分支，而 pricing 段 label 写死 USD |
| fix | llmgw | /v1/models/{id} 改 catch-all 路由并认百分号编码，带斜杠的 PublicId 终于取得回来 |
| fix | llmgw | 调用全貌结论补上「上游模型被停用」这第三种跳过原因，不再输出「0 条全被停用」 |
| fix | prd-api | 声明「没有尺寸概念」的生图模型不再发任何尺寸参数（此前白名单为空会兜底成 1024x1024） |
| chore | llmgw | 30 个 changelog 碎片按规则 #4 合并成一个 |
| docs | llmgw | 台账新增：对外模型删除没有保留窗口，会打断在途的异步视频任务 |
| fix | llmgw | 生图契约刷新器搬进 Infrastructure 并在 serving 也注册：此前控制台配的契约对网关的生图请求完全不生效，而界面显示「已同步」 |
| fix | llmgw | 模型编辑抽屉判据改成「动没动过」：清空价格删得掉了，只改名字也不再把上游价谎报成人工价 |
| fix | llmgw | 最大输出 token 补显式清空标志，兑现界面上「留空表示不限制」那句话 |
| fix | llmgw | /v1/models 补上场景能力判据：分层这类动作能力模型不再列给普通生图调用方（列出来一调就失败） |
| fix | prd-api | 半开探测失败时作废人工恢复通行证，否则坏线路被每个后续请求反复抢去当队首，冷却形同虚设 |
| fix | llmgw | 「算不出钱的笔数」两处都补上 stale_currency，不再报「0 笔未计价」 |
| fix | llmgw | 调用全貌面板不把缺币种的价标成 USD（上一轮只修了 /v1/models 那一处，同类没横扫） |
| security | llmgw | 兼容入口拒绝客户端自带的 pinned_platform_id / pinned_model_id：池退场后那道调用方边界没了，一把 key 只要知道内部 id 就能调任何启用的物理模型 |
| fix | llmgw | 发布门禁两条池判据换成对外模型可达性，不再让正确配置的调用方卡住发布且无处可修 |
| fix | llmgw | 池搬迁认兑换所成员：纯 Exchange 的池此前会搬成零线路，而池路由已经删了，那些流量再无处可去 |
| fix | llmgw | 生图契约同步状态按进程分行，汇总取最旧时间 + 模式交集；界面点名是哪个进程没跟上 |
| fix | prd-api | 搬迁后的对外模型记住来源池 ID 并把它当查询键：model_policy=pool 契约仍在用池 ID 点名，池退场后那些请求会整条 MODEL_NOT_FOUND |
| fix | llmgw | 「指定调用方」必须落在授权名单里，create/update 两条路径共用一份判据、按改完之后的值判 |
| fix | llmgw | 池搬迁复用已有对外模型时也搬兜底标记：默认池映射到已有模型时不设的话，池退场后不点名请求就接不住了 |
| fix | llmgw | 搬迁 dry-run 认本轮已规划的标识/默认/线路，预览不再与实际写入不符 |
| fix | prd-api | 按次计费的模型在上游回了 token 用量时仍然计价：此前会判成未计价，按次的钱算进总额但美金口径为空，这笔调用被整个排除在用量合计与预算之外 |
| fix | llmgw | 生图契约覆盖表没有租户维度，按请求密钥判租户的网关宿主因此只装平台级契约，跳过条数与原因回写同步状态并在控制台说出口 |
| fix | llmgw | 生图契约同步状态判「它还活着吗 + 装的是不是这一版」：只判状态行存在的话，停掉的同步器会拿一次陈年的成功替现在作答，而改尺寸档位时模式名不变、只比模式名改前改后都判已生效 |
| fix | llmgw | 勾了「这个模型没有选尺寸这件事」时不再提交藏起来的尺寸档位，已配过尺寸的契约此前根本改不成不选尺寸 |
| chore | llmgw | 删掉池搬迁影子比对脚本：它的「旧路」靠回落到池，而生产解析路上的池分支已在本轮删除，两腿比的是同一条路 |
| fix | prd-api | 按次计费不再叠加 token 费用：两种价都填得进去，而「都填了」多半是填错了，叠加会让每次成功调用按两套价重复收费、用量与预算一起虚高 |
| fix | llmgw | 手工新增模型也登白名单（建公开模型名 + 挂线路），与批量导入共用一份发布规则；池路由删掉之后，只进池的模型在库里看得见却调不通 |
| fix | llmgw | 取模型详情的权限判定排到按子串判 raw/images 之前：模型 id 允许带斜杠，叫 vendor/raw-model 的合法模型此前取详情要 raw:invoke |
| fix | llmgw | 认不出用途的模型不再兜底当对话模型登上白名单：名录外放行的模型能力常为空，兜底成 chat 会让它进对话默认面、被普通对话调用方按对话契约调走 |
| fix | prd-api | 对外模型解析把物理模型的最大输出上限一并搬进合成条目：此前只搬了价格，模型页上填的 MaxTokens 对走对外模型的流量一个都不生效 |
| fix | llmgw | 池搬迁把调用方的专属绑定转成模型认领：绑定写在调用方一侧而新解析器只看模型一侧，不转则池退场后这些调用方静默改用用途默认 |
| fix | llmgw | 池搬迁扫两个数据域并对 MAP 原生成员给出下一步：此前只读网关池表，MAP 原生的池一个都不会搬而报告显示全部搬完 |
| fix | llmgw | 「谁接得住不点名请求」按运行时口径判：补上健康档、目标模型与平台可用性、授权名单三道，此前只看线路的启用开关，闸门放行而请求全灭 |
| fix | llmgw | 调用方身上残留的池字段不再让这道判断整个跳过：运行时早已不读那几个字段，跳过去校验旧池会一边假绿一边误伤 |
| feat | llmgw | 模型页新增编辑抽屉，模型建完可改名改价，保存时把引用它的模型池一并同步，覆盖价保留并标注是谁何时设的 |
| feat | llmgw | 计价纳入缓存 token：按协议决定是否从输入扣减，缓存读写各自单列单价，未配缓存价按输入全价算 |
| feat | llmgw | 计价口径统一美金，非美金或未声明币种的价格判为待换算，不进用量汇总也不参与限额 |
| feat | llmgw | 价格必须带来源与观测时间，超过 30 天未复核在模型页标为陈旧 |
| feat | llmgw | 用量页新增缓存节省与缺价点名，算不出钱的调用按模型列出并给补价入口 |
| feat | prd-api | 计价算法抽成 GatewayCostCalculator 纯函数，新增成本状态枚举与十一条单测 |
| rule | prd-api | 禁内置价目表守卫升级为「价格必须带来源与时效」判据，另补模型可编辑、成本状态镜像、计价单一入口三条守卫 |
| feat | llmgw | 请求详情给出缓存读写估算、价格来源与「为什么没算出钱」的具体原因，不再笼统显示未知 |
| fix | llmgw | 对外模型清单过名录门：名录外又没放行的模型此前会被列出来，对方照单调一次必然拿到 MODEL_NOT_IN_CATALOG |
| fix | llmgw | 生图契约范围模式补齐六个边界输入并在写入侧拦住空范围：此前保存成功却什么都不约束，尺寸原样发给上游 |
| fix | llmgw | 「已导入」与「已登上白名单」分开报，未登记的模型可以再导一次补登，不再堵死服务端给的那句下一步 |
| chore | llmgw | 本 PR 的两个 changelog 碎片合并成一个，按仓库规则一个 PR 只留一份 |
| fix | prd-api | 熔断升级带上「失败数还是当初那么多」：自增与升级之间挤进一次成功时，此前会把作废的档位写回去，把刚成功的线路隔离整个冷却期 |
| fix | llmgw | 同用途唯一默认升成库级约束（部分唯一索引），并发改默认时第二个写如实回 409 而不是静默留下两个默认 |
| fix | llmgw | 范围契约要求有效边界：0 当最小值等于没填、当最大值会把请求夹成 0x0，整除填 0 或 1 运行时直接跳过 |
| fix | llmgw | 默认/认领冲突时把已摘掉的调用方认领还回去：此前一次被拒绝的保存照样把那些调用方从对手模型上摘走，操作者只看到失败 |
| fix | llmgw | 同用途唯一认领升成多键唯一索引：并发改认领时第二个写如实回 409，不再两边都成功而由排序决定谁生效 |
| fix | llmgw | 范围契约校验加可行性判断：整除向上取整超最大值、最小边长与像素上限打架这类合起来无解的组合不再放行 |
| fix | llmgw | 池搬迁把池级授权限制翻译成模型授权名单：此前只写空名单等于对所有调用方开放，原本限制在某个池的调用方搬完就能点名调别的池搬来的模型 |
| fix | llmgw | 只配「边长必须整除」而不配最小宽高的范围契约不再放行：运行时向下取整会把小于整除值的边长抹成 0，发出去是 1024x0 |
| fix | llmgw | 对外模型清单的报价与计价同口径：有按次价时不再并列报出 token 单价，避免对方按「两种都收」估费 |
| fix | llmgw | 改默认/认领失败时的补偿覆盖所有失败路径：此前只在并发冲突那一条回滚，目标被删或连接抖动时这个用途会一个默认都不剩 |
| fix | llmgw | 配了边长整除就必须两个最小值都给：只给一个轴时另一个轴仍会被向下取整抹成 0 |
| fix | llmgw | 搬迁按索引名区分默认冲突与认领冲突：此前认领撞车会带着同一份认领数组重插，必然二次抛出变成 500 |
| fix | prd-api | 应用侧模型清单也过名录门：名录外又没放行的模型此前会出现在选择器里，选中即 MODEL_NOT_IN_CATALOG |
| fix | llmgw | 池搬迁逐条报出会丢掉的成员价格覆盖：线路没有价格字段，成员上配过的价搬完只看模型文档，兑换所成员更直接判成未计价 |
| fix | llmgw | 池数超上限时支持按 modelType 分批搬：此前只有 apply 一个参数，池多于上限的租户永远收到 TOO_MANY 而再无路可走 |
| fix | llmgw | 搬迁复用已有模型时，授权名单任何不一致都点名报出（此前只报「已有名单为空」那一种特例，两边都非空且不等时越权与失权同时发生且无提示） |
| fix | llmgw | serving 就绪探针认「对外模型认领/用途默认」这条路：池退场后正确迁移的部署此前会被判 503 摘掉 |
| fix | llmgw | 生图契约同租户模式唯一升成库级约束，并发新建时第二个写如实回 409 |
| fix | prd-api | 生图范围归一化在像素缩放与整除取整之后重新套边界：此前会发出低于最小值、超过最大值甚至为 0 的尺寸 |
| fix | llmgw | serving 就绪探针判「有没有对外模型接得住这个调用方」时同时校验线路目标可用与所选那条模型的场景能力，不再只看候选存在 |
| fix | prd-api | 生图范围契约把最小边长与像素上限一起解，补回最小边长不会重新顶破像素上限，整除刻度全程只对齐一次 |
| fix | llmgw | 目录补登的规范标识与等价写法合成一份键落库，加多键唯一索引把键空间升成库级不变量，并发补登撞索引如实回 409 |
| fix | llmgw | serving 就绪探针按调用方自己的租户分组判可路由，并对线路指向的物理模型补上名录门，不再让别的租户或会被名录门拦死的配置把部署报成绿的 |
| refactor | prd-api | 名录门的判据（要不要拦 + 一条模型过不过得了）收进 GatewayCatalogGate 一处，解析、对外目录端点、就绪探针三份拷贝合并 |
| fix | prd-api | 生图请求没给尺寸时退回的默认值也走契约规整，不再绕过范围与像素上限直接发 1024x1024 |
| fix | prd-api | 多租户宿主逐个租户回写生图契约同步状态，外部租户不再看到「同步从未发生」而误判进程宕机 |
| fix | llmgw | 池搬迁时同一个兑换所下的不同别名各成一条线路，去重键与查库判据都带上游模型标识，不再静默丢掉除第一个以外的别名 |
| fix | llmgw | 还在接不点名请求的对外模型（用途默认或认领着调用方）不许直接删，服务端回 409 并给出转移路径，控制台在确认框之前先行拦一道 |
| fix | llmgw | 白名单模式一个尺寸都不配的生图契约拒绝写入，不再压过内置契约后把所有请求改写成写死的 1024x1024 |
| fix | llmgw | 对外模型清单的兑换所线路判到别名这一层（兑换所声明过 + 过名录门），判据与运行时收进同一份，不再列出选中即失败的模型 |
| fix | llmgw | 对外报价补上缓存写入那一档，与计价侧真的会收的钱同口径 |
| fix | llmgw | Quickstart 模型选择器按服务端算好的排队名次筛，熔断中与指向已停用目标的不再出现；三条修复入口从已重定向的 /pools 改指模型页与上游页 |
| docs | llmgw | 计划文档剔除实现清单（函数名、类型名、逐文件行数），只留决策层的体量与契约 |
| fix | llmgw | serving 就绪探针的兑换所线路也判到别名这一层，与运行时、对外清单共用同一份判据 |
| fix | llmgw | 比例模式一个尺寸档位都不配的生图契约拒绝写入，不再把请求静默改写成比例都不对的 1024x1024 |
| fix | llmgw | 生图契约的「谁最后改的」记的是操作人而不是租户 |
| fix | llmgw | 池搬迁把近期的降级状态也一起搬过去（此前只搬熔断），正在失败的高优先级成员不再在切换那一刻重新拿到主流量 |
| fix | llmgw | 池搬迁如实报出会丢掉的成员输出上限；线路没有这个字段，走线路解析时取的是物理模型上的那个 |
| fix | llmgw | 对外报价的两档缓存价报的是实际会收的数（没配缓存价时按输入全价回落），与计价侧同口径 |
| fix | llmgw | 生图契约的尺寸档位必须是正数，`00x00` 这类存不进来 |
| docs | llmgw | 教程里三章「第一步点左侧模型池」的写明本章待重拍并给出新位置 |
| fix | llmgw | serving 就绪判据删掉模型池那一半——运行时早已没有池分支，留着它会让「只绑了池」的调用方被报成可路由而每次请求都失败 |
| refactor | llmgw | 就绪探针的 router 与 scenario-capability 两个组件共用同一份租户路由视图，可用线路的四层判据（目标可用、名录门、兑换所别名、租户）只剩一份 |
| fix | llmgw | serving 就绪探针先按认领挑出那一条再看它能不能用，与运行时同序；认领的模型线路全挂时不再被健康的用途默认顶替成绿灯 |
| fix | prd-api | 兼容入口（raw）解析并记录缓存 token（OpenAI / Anthropic / Gemini 三家字段），此前一律记 null 导致计价与预算失真 |
| fix | llmgw | 删除对外模型改为条件删（删的那一刻再判一次没在接流量），并先删模型后删线路，被拒的删除不再带着破坏 |
| fix | llmgw | 池搬迁的授权名单按用途分开算，一个调用方在别的用途下没设限制不再让它进到这个用途的授权名单 |
| fix | llmgw | 模型卡与调用全貌账的花费聚合认存量日志（2026-09 前没有 CostStatus 字段那批），两处共用同一份判据 |
| fix | llmgw | 调用全貌的落点判定过「这个模型服不服务这个调用方」（授权名单 + 场景能力），不再指着一条运行时必拒的路说会落到这里 |
| fix | llmgw | 搬迁撞上公开名唯一索引时认下对方刚建的那一条并把线路挂上去，不再按默认冲突重插而必炸 |
| fix | prd-api | 线路身份唯一索引升到 v3 并带上 UpstreamModelId——同一个兑换所下的不同别名是不同线路，此前第二条插入会撞 E11000 让搬迁半途而废 |
| fix | llmgw | 搬迁撞上公开名唯一索引时先复核用途再认下对方，并把这个池的 id 记进赢家的搬迁来源 |
| fix | prd-api | 兑换所里被单独停掉的那条别名不再算可用——目录、就绪、运行时解析三处共用同一份判据 |
| fix | llmgw | 模型的「可计费」按计价侧真正的口径判（有按次价，或输入与输出单价都有），只配一半不再被标成算得出钱 |
| fix | prd-api | 生图契约同模式时租户自己那条排在平台级之前，生效的是哪条不再看 Mongo 返回顺序 |
| fix | prd-api | 启动不再丢弃正在生效的线路身份唯一索引，发现旧版时如实报出来并给出 DBA 要跑的那一步 |
| fix | llmgw | 对外报价要么配齐要么不报（有按次价，或输入与输出都有），与计价侧同口径 |
| fix | prd-api | 计费模式看配没配按次价，而不是看这一次收不收那笔固定费；按次而不收费的调用算出来是零，不是算不出 |
| docs | platform | MongoDB 索引指南新增网关自持库的一次性索引升级说明 |
| fix | llmgw | 对外清单的物理线路按它实际会打出去的那个模型名过名录门（线路可用 UpstreamModelId 覆盖），不再只判目标文档自己的名字 |
| fix | llmgw | 就绪探针的物理线路同样按实际上游名过名录门，不再只判目标文档；对外清单与探针两处判据合并成一条扫描式守卫 |
| fix | llmgw | 发布闸的「不点名落到谁」改成认领排他：认领挑中的那一条不可用就如实判红，不再回落到用途默认（与运行时同序） |
| fix | llmgw | 池搬迁不再写出「授权名单容不下、认领里却有它」的模型，名单外的认领不转并报出下一步 |
| fix | llmgw | 挑同名模型文档的谓词收进共享那一份，大小写与库里存的不一致时不再查空放行（对外清单、就绪探针、运行时同一套） |
| fix | llmgw | 系统级模型来源选「模型池」时判的是「解析得到它」而不是「池还在」，没搬成对外模型的池保存即拒并给出下一步 |
| fix | llmgw | 配置权威报告改判「有没有对外模型接得住」，与发布闸同一份判据；否则池退场后一份正确配置会被自己的报告挡住发布 |
| fix | llmgw | 重新导入时把名录算出来的用途补给能力为空的存量模型，让「补登名录后重新导入」这条恢复路真的走得通 |
| refactor | llmgw | 删掉按池绑定判调用方可用性的两个死判据与它们的取值函数 |
| fix | prd-api | 协议别名归一化收敛成唯一一份，计价的用量口径与适配器选择共用它；claude-compatible 不再被当成 OpenAI 口径把缓存读从输入里减一遍 |
| fix | llmgw | 线路判重带上实际上游模型，与唯一索引 v3 同一份身份；一个兑换所挂多条别名的合法拓扑不再被拒 |
| fix | llmgw | 兑换所线路保存时就确认别名存在且启用，判据是运行时的镜像（有逐例对照守卫），不再存下一条运行时必跳过的线路 |
| fix | llmgw | 生图契约同步状态对不服务本租户的进程显式报 not-applicable，不再把单租户的 prd-api 在外部租户下报成「从没回写过」 |
| fix | llmgw | 模型名录与生图契约两屏首次读失败时先摆错误并给重试，不再永远停在「正在读…」 |
| fix | llmgw | 生图契约同步没落定时按它自己报出来的刷新周期再读一次，兑现界面上那句「最长 N 秒后再看」 |
| security | llmgw | 池搬迁拒绝把「一个调用方都没被授权」翻译成空名单（空名单在运行时等于对所有调用方开放），该池不搬并报出下一步 |
| fix | llmgw | 场景能力就绪判据改用与 router 同一份排他挑选，不再扫遍同用途全部模型拿无关模型顶上 |
| fix | llmgw | 认领撞唯一索引时把位移的用途默认还回去；先分清撞的是哪条索引再补偿，不再让一次被拒的保存弄丢整个用途的默认 |
| fix | llmgw | 调用全貌的加权结论只数参与轮转的那一档，并说清健康档更低的线路是后备，不再出现「分到 2 条」却只列一条比例 |
| fix | llmgw | 生图契约轮询失败也重排下一次，并给非空态补重试入口，不再因一次抖动永久停在「装的还不是当前这一版」 |
| fix | llmgw | 发布闸与配置权威报告共用的那份「谁接得住」补上场景能力门，不再把只会文生图的模型判成接得住图生图调用方 |
| fix | llmgw | 同一份判据的兑换所线路判到别名这一层，不再因为兑换所文档启用就说一条别名已被摘掉的线路可用 |
| security | llmgw | 「谁都没被授权」的池在复用已有模型那条路上也不搬，不再把一批谁都够不到的上游挂给别人的授权名单 |
| fix | llmgw | 发布闸与配置权威共用的那份「谁接得住」补上名录门（物理线路按实际上游名、兑换所按别名），不再替一条运行时必拒的线路作保 |
| fix | llmgw | 对外模型更新把纯校验全部提到位移之前，被拒绝的保存不再把这个用途的默认拆掉还不还回去 |
| fix | llmgw | 存量日志的计价状态只认美金，非美金一律算口径待迁移，摘要不再与聚合对同一行给出两种说法 |
| fix | llmgw | 换上游那条替换链撞身份索引时把原线路还回去：动写之前先判撞车，晋升那步接住唯一索引异常并走同一段回滚 |
| fix | llmgw | 并发创建对外模型撞唯一索引翻成 409（认领被占 / 标识被占），不再漏成一句 500 |
| fix | llmgw | 挂线路时校验物理模型挂的 Provider 在且启用，不再存下一条运行时会整条丢掉的线路 |
| fix | llmgw | 删对外模型前先查在途视频任务是否还在用它名下的线路，避免已受理任务失去上游 |
| fix | prd-api | 线路身份唯一索引改为只报不建，启动路径不再创建索引 |
| fix | llmgw | 启用线路与新建线路共用同一道上游资格闸，不再只判 ASR 契约 |
| fix | llmgw | 并发挂线路撞唯一索引翻成 409，不再漏成 500 |
| fix | llmgw | 控制台启动改为只查索引不建索引，缺了写明会退化成什么样并指向 DBA 指南 |
| fix | llmgw | 删对外模型时级联删线路失败会把模型放回去，不留半截状态 |
| fix | prd-api | 生图契约同步状态写给每一个租户，新租户不再被报成「同步从未发生」 |
| docs | prd-api | DBA 索引指南补齐网关五条唯一索引的定义与缺席后果 |
| fix | llmgw | 晋升新线路失败时一律回滚，不再只接撞键那一种异常 |
| fix | llmgw | 级联删线路失败时把模型与它的线路都放回去，不再在删了一半时谎称没留下半截状态 |
| fix | llmgw | 池搬迁遇认领撞车只去掉被抢走的那几个调用方，其余照常带过来 |
| fix | llmgw | 池搬迁后一条线路都没建成的模型停用并摘掉默认与认领，不再把流量引向没有上游的模型 |
| fix | prd-api | 兼容入口也从 input_tokens_details 读缓存 token，Responses 形态不再按输入全价计费 |
| fix | prd-api | 线路健康记账失败不再抛出，成功的调用不会因此变 500、失败的调用仍能故障转移 |
| fix | llmgw | 删模型回滚撞键时按 id 复核原模型是否真的还在，不把撞到别人当成已恢复 |
| fix | llmgw | 池搬迁的零线路闸改数「现在承接得了流量的线路」，上游全不可用的模型不再保留默认与认领 |
| fix | llmgw | 搬迁停用的模型在上游修好后重跑搬迁会自动启用回来，手动改过开关的不受影响 |
| fix | llmgw | 对外模型清单的报价加 covers_all_routes，说清这份价盖没盖住全部可用线路 |
| fix | llmgw | 搬迁的可用线路计数同时判启用、非熔断与上游资格，照搬熔断态的线路不再算能接流量 |
| fix | llmgw | 重跑搬迁时已存在的线路也计入可用，上游修好后停用的模型才真能放回来 |
| fix | llmgw | 删模型回滚时父没放回去就不放子，不造看不见的孤儿 |
| fix | prd-api | 隔离记账失败不再抛出，上游的确定性配置错误不会被数据库抖动换成 500 |
| fix | llmgw | 控制面判上游启用与运行时逐字对齐，缺 Enabled 字段的目标不再被当成可用 |
| fix | llmgw | 生图契约的通配符只许有一个且只能在结尾，杜绝两条模式悄悄匹配同一批模型 |
| fix | llmgw | 生图契约编辑器补上启用开关，接口建出来的停用契约不再只能删掉重建 |
| fix | llmgw | 「不计入限额」分开说清是币种不对还是价只配了一半，不再把人指向错误的修法 |
| fix | llmgw | 上游被停用与熔断分成两种状态，不再对一个永远不会自愈的线路说「等冷却」 |
| fix | llmgw | 计价抽屉改口：线上计费读模型文档自己的价，旧模型池降为回滚备份且默认不勾 |
| fix | llmgw | 旧模型池那份回滚备份写不进去不再回删模型，降级信息如实回传 |
| fix | llmgw | 三处拒绝文案改为当场能走通的下一步，不再指向已下线的模型池页 |
| fix | llmgw | 排队名次对兑换所线路判到别名那一层，不再把运行时会拒的线路报成会落到它 |
| fix | llmgw | 调用全貌冒烟的加权断言收到最健康那一档，降级线路首发会被判红 |
| fix | prd-api | 网关五条唯一索引写进可执行 DBA 清单，按库判定跳过并先建新版再丢旧版 |
| fix | prd-api | 名录门的同名判据忽略大小写，存量文档换个大小写不再被判成「管不着」放行 |
| fix | llmgw | 生图契约同步状态落定后降频继续看，进程事后掉线不再永远显示「都已跟上」 |
| fix | prd-api | 多租户宿主自己那行状态的跳过数按租户算，不再把全局总数报给内部租户 |
| fix | llmgw | MAP 兜底退场闸的处置改成对外模型口径，不再让人去绑已经退场的模型池 |
| fix | llmgw | 池搬迁拒绝「动作能力成员 + 普通成员」的混合池：并起来的能力集会让动作能力独占整个模型的服务对象，原本的普通调用方全部被拒 |
| fix | llmgw | 控制台能力镜像补 IsOperationOnly / ImageLayeringPublicId，与运行时契约逐例对照 |
| fix | prd-api | 索引清单：空网关库用 PRD_GATEWAY_DB=1 点名即可一次建出五条唯一约束，跳过提示给出真的走得通的那条命令 |
| chore | llmgw | 删掉池退场后无人调用的两个成员可用性转发函数，清掉本 PR 引入的两条编译告警 |
| fix | llmgw | 线路判重按运行时实际打给上游的那个名字比：没写上游模型标识的线路会回落到目标名字，逐字比会把它与显式同名的那条判成两个身份、同一个上游拿两份权重 |
| fix | llmgw | 生图契约拒掉只差大小写的参数改名键，运行时那张表不分大小写，两条一起存会让整张表装不进去 |
| fix | prd-api | 生图契约逐条翻译，翻不过去的那一条跳过并点名，不再由一条坏数据把全部契约停在旧快照上 |
| fix | llmgw | 对外模型清单补上「这个调用方现在还能不能调」那道门，停用或归档之后不再列出一调就失败的模型 |
| fix | llmgw | 认领撞车的补偿本身撞唯一索引时不再把一句 409 变成 500，没还成的位移照常记进告警 |
| fix | llmgw | Quickstart 路由预检把选中的对外模型发出去：此前它恒发自动调度，默认那条健康就能替一条没验过的路放行 |
| fix | llmgw | 对外模型清单按用途逐条判调用方状态，一个用途停用不再连累另一个 |
| fix | prd-api | 三处批量预取的同名谓词收敛成一份不分大小写的共享入口，存量文档不再被判成「管不着」放行 |
| fix | llmgw | 认领位移改成原子加减，不再用读旧了的整份数组覆盖回去，并发下不会悄悄抹掉别人刚加的认领 |
| fix | prd-api | 生图契约同步的版本号与控制台数同一批行，一条翻不过去的契约不再让状态恒为「还没跟上」 |
| feat | llmgw | 同步状态单独报出翻不过去的那几条契约，控制台三处出口都说得出它与「按租户跳过」的区别 |
| security | llmgw | 公开模型名只在名录认得出来时才剥厂商前缀，名录外的私有上游不再被挂进同名公共模型 |
| fix | llmgw | 模型页 30 天调用量只数业务操作，异步模型的 status/download/cancel 不再被数成调用 |
| fix | llmgw | 搬迁重试撞键也按索引名分流，公开名被抢时认下赢家而不是拿同一个名字再插一次 |
| fix | llmgw | 总览页删掉那个写池绑定的「修复」按钮，换成指向对外模型页；端点回执明说它不改变路由 |
| fix | llmgw | 闸门与详情页里最后几条指向已退役模型池页的下一步全部改指对外模型 |
| security | llmgw | 对外清单与单模型详情拒绝「点名一个这把 key 调不动的调用方」，不再列出一份调不动的清单 |
| fix | llmgw | 补登名录后重新导入能盖掉按名字猜错的用途；能力来源如实落库，名录与上游声明不再被标成「猜的」 |
| fix | prd-api | 索引清单丢旧的线路身份索引之前，逐项核对 v3 真的建成且唯一，不再只看名字在不在 |
| fix | llmgw | 清单的越权判据复用鉴权那一份，通配授权的 key 不再被清单判成越权而调用路径放行 |
| fix | llmgw | 删对外模型的在途闸补上直连视频任务：它的线路 id 只落在归属表里，只查 run 表等于对这一类完全不设防 |
| fix | prd-api | 索引清单里补登那条建错了集合（少了 model_），真正那张表一条唯一约束都没有；守卫改成交叉核对集合名 |
| fix | llmgw | 调用全貌补上半开试探：已摘掉但过了冷却期的线路可能被运行时先拿去试探，面板不再指着队首谎报下一跳 |
| fix | llmgw | 调用全貌冒烟认半开候选为合法落点，不再把一次正常的半开试探报成推演与实打不一致 |
| fix | prd-agent | 视觉验收脚本不再写死某台机器的 checkout 路径，换个仓库位置也能跑起来；同形的技能 harness 一并改成相对引用 |
| test | prd-agent | 新增守卫：e2e / scripts / 技能脚本里不许出现写死的 checkout 绝对路径，并把被守目录登记进 CI 闸 |
| fix | llmgw | 认领比对改走 appCaller 身份规则（不分大小写）：登记成 Foo、请求带 foo 时不再静默掉到用途默认换一个模型 |
| fix | llmgw | 控制面四处认领查询与面板归属显示一并收齐同一份身份规则，不再比运行时松 |
| test | prd-api | 新增真 Mongo 行为用例（认领大小写三档 + 无人认领仍回落默认）与身份规则两侧镜像对照、认领查询接线守卫 |
| fix | llmgw | 批量导入的白名单发布改用共享同名谓词：存量模型换个大小写重试时不再查不到，用户照着「稍后重试导入」终于走得通 |
| test | prd-api | 同名谓词两侧镜像对照（含空集合恒不成立与忽略大小写的内容判据） |
| fix | llmgw | 白名单发布闸的同名查询与兜底挑选一并收齐共享判据：闸不再给一条运行时会拒的线路发证，也不再与运行时选中不同的模型 |
| test | prd-api | 守卫补计数配平兜底（过滤器传给局部函数那种写法此前罩不住）与「不许再手拼同名判据」 |
