| fix | cds | 五个角色的决策卡不再共用同一套段落，按角色给出各自的卡片标题、理解方向、开工前必问和专属段落 |
| feat | cds | Agent 上手助手角色卡展示角色关注点，完成页可直接预览本角色的决策卡 |
| refactor | cds | 决策卡抽出结构化模型，文本契约与界面预览同源，样式调整改不动段名与规则文案 |
| feat | cds | 决策卡预览改为四层分区（卡头 / 前言 / 编号段落清单 / 禁止项），角色专属段染主色、共享段保持中性 |
| feat | cds | 角色提成共享 SSOT（agent-role-store），上手助手与任务地图读同一个值，不再是组件私有的一次性选择 |
| feat | cds | 新增「只换角色卡」脚本：不下技能、不联网、不碰 .env 和凭据，只替换受管区块 |
| feat | cds | 任务地图按角色排序并标注常用任务，五个角色不再看到同一串顺序 |
| feat | cds | 项目 Agent 角色声明落到 CDS：新增 GET/PUT /api/projects/:id/agent-profile，项目选择列表直接显示角色 |
| feat | cds | 任务地图内置角色选择器，设角色不必先走完上手助手向导（打破「要排序得先设角色，要设角色得走完向导」的循环） |
| fix | cds | 角色默认值不再冒充用户声明：没选过角色时任务地图保持原顺序、不标「某某常用」 |
| fix | cds | 只选经验不再把默认角色记成用户声明，任务清单不会按他没选过的角色排序 |
| fix | cds | 任务分类条也按角色排序并标记常用分类，角色排序不再只在当前分类内部生效 |
| fix | cds | 角色在当前作用域一条首屏任务都没命中时，不再宣称「已按某某重排」 |
| fix | cds | 角色只在点生成时写入项目，完成屏换目标项目不再把上一个项目的角色盖过去 |
| fix | cds | 新建项目场景明说角色暂未记到 CDS 及补记方式，不再静默跳过 |
| fix | cds | Agent 角色两条 API 的中文 label 补上运行时 pattern，活动流不再显示成「查询项目 / 更新项目」 |
| feat | cds | 项目设置页改为方向 A：workspace 满铺、导航贴边成固定栏、内容区自身滚动，不再是浮在页面中间的两张白卡 |
| feat | cds | 基础信息分区表单锁可读宽度，右列显示「当前生效 / 改动影响」，宽屏富余变成信息而不是留白 |
| feat | cds | CDS 系统设置页搬到与项目设置页同一套满铺外壳，两个设置页宽度不再一个 1440 一个 1240 |
| fix | cds | 设置页窄屏不再叠两层横向内边距，320px 屏内容区从 240px 回到 288px |
| fix | cds | 上手助手角色页五张卡不再被裁切、不再压住左下「返回」按钮（改可滚动网格 + compact 卡去掉占位的 hover 提示行） |
| fix | cds | 上手助手完成页内容超高时标题与「返回修改」不再被挤出可视区（居中改成 my-auto 包裹层 + 外层可滚） |
| fix | cds | 展开决策卡不再把标题和「复制启动提示词」顶出可视区：完成页改成固定操作区 + 只让卡片自身滚动 |
| fix | cds | 展开时收起庆祝性图标与说明、面板长高一档，12 段清单不再被挤成几十像素 |
| fix | cds | 上手助手面板 max-h 改按弹窗内可用高度算（原 calc(100vh-190px) 不生效，760px 会捅出弹窗底 74px） |
| test | cds | 补同源守卫：预览的每段段名与规则必须逐字出现在写进 AGENTS.md 的契约里 |
| test | cds | 新增角色决策卡差异守卫：卡片两两不同、每个角色至少三个专属段落、声明段落数与实际一致 |
| test | cds | 补角色差异与接线守卫：角色排序真的改变顺序、焦点表无幽灵任务、角色声明能被 /api/projects 带出 |
| test | cds | 补角色 store 守卫：未声明与真的选了 pm 必须能区分，脏记录和存储不可用都退回未声明 |
