| fix | llmgw | 服务网关设置写自己的池绑定时一并清掉严格池清单：解析器读这条 appCaller 时 `AllowedModelPoolIds` 非空就走严格分支当场返回，根本不看 `ModelPoolId`。而这条 SystemManaged 的 appCaller 在「调用用途」页照样能编辑——任何一次在那边设过严格清单，设置页从此说什么都不作数：写着新池 / 交给网关挑 / 钉一个逻辑模型，实际仍走旧的严格池，逻辑模型那一档还被严格契约整个掐掉 |
| fix | llmgw | Quickstart 换档（安全连通 / 真实模型）也走同一道作废：档位决定这次请求的性质（带不带 dry-run 头、花不花钱），真实那条还在路上时切到安全档，页面写着「未访问上游」，而那次付费调用照跑照计费、回来还会把结果写进这块写着安全档的面板 |
| test | prd-api | 守卫钉住两条：设置页两个分支都要 Unset 严格池清单（并先断言解析器里严格分支确实排在 ModelPoolId 之前，前提变了守卫要重写）；换档两处都必须调作废、调用点升到 8。红绿闭环已跑 |
| test | llmgw | Quickstart 验收补一组：真实流跑到一半切安全档，产物当场回到空态、被作废那条回来也不再写，切回真实档同样不留上一档结论。红绿闭环已跑（把换档改回只清 testResult 即三条同时红，且确认过产物重建） |
