| feat | cds | 验收报告主页重做（方向 A 结论优先）：新增 `GET /api/reports/overview` 聚合——同一验收目标只计最新版、按根因合并未通过簇、口径冲突单列、发布闸、每日验收连续性、主干合并记录 × 报告元数据算出合并未验 |
| feat | cds | 验收报告主页首屏改为结论头条 + 发布闸 + 结论分布图；未通过与待决表；每日验收连续性与合并覆盖两张图；报告台账按九类前缀分页签、默认折叠被取代版本 |
| test | cds | 新增 acceptance-overview 聚合守卫（标题合同解析、版本折叠、产品失败与验收失败分开、口径冲突、时区落日、合并覆盖匹配） |
| fix | cds | 验收发现的回归：桌面端打开报告后无法返回结论首页（返回按钮带 lg:hidden，同路由点导航不复位选中态），去掉断点限制并补源码守卫 |
| polish | cds | 验收报告主页首屏不再被右列拉高，支撑卡改自适应列宽，支撑卡少时不留大片空白 |
| fix | cds | 验收主页台账展开被取代版本时旧版整批掉进「其他」，overview 补返回 supersededReports 让旧版按标题合同归类 |
| test | cds | 补被取代版本 kind 解析的回归断言（红绿闭环验证过） |
| feat | cds | 结论头条补「根因集中度」水平条形图（按验收对象合并、口径冲突拆红橙两段、点击下钻），顶部不再是纯文字且左列不再留大片空白 |
| polish | cds | 验收主页版面重排为「单脊柱」：主栏叙述 + 320px 固定侧栏仪表（跨行 sticky），结论区脱掉卡片外框成为唯一主角，支撑句由卡中卡改行内条目，重复的发布闸支撑句不再渲染，台账降为查找工具层 |
| fix | cds | 未通过与待决的簇标签 ×N 数错类：1 份未通过 + 2 份有条件被渲染成「未通过 ×3」，改为按 failCount / conditionalCount 分别计数并在行内标出夹带的有条件份数 |
| feat | cds | 验收首页从需求重推：不选项目时改为跨项目「验收流水线总览」（改动→部署→验收→合并四环 + 环间落差 + 一行一个项目），新增只读接口 `GET /api/reports/pipeline` |
| feat | cds | 新增 acceptance-pipeline 聚合：按 PR 号 / commit / 分支名把报告对齐到改动单元，报出四种漏（合并了没验 / 验了没过还合并 / 部署了没验 / 报告没记它验的是谁），未接 GitHub 的项目单独标出 |
| refactor | cds | 报告与改动的对齐判据抽成唯一一份 `matchesChange`，合并覆盖与流水线共用 |
| polish | cds | 「有条件通过」全站转正为「原则性通过」（UI / 归档门禁 / 报告模板 / SSOT 规范）；归档门禁新旧两种写法都接受，历史周报与 CHANGELOG 保留原话不回溯改写 |
| fix | cds | 拿主实例真实数据核对流水线聚合，发现「报告对不上分支」是喊狼：153 份对不上的报告里 152 份点名的分支根本已被 CDS 回收（现存仅 71 条），删掉这一档漏，改为背景数 staleReports；只保留「报告没记它验的是谁」（三个标识全空）这一条可证的缺口 |
| test | cds | 补三条守卫钉住「对不上不许一股脑喊成漏」，并新增前后端漏点顺序一致性守卫（防判据分裂漂移），两条都红绿闭环验证过 |
| fix | cds | 验收报告正文改存对象存储：元数据在 Mongo、正文在容器本地盘，容器重建后正文全丢而元数据还在，列表 23 份报告点开全是 404。正文改走对象存储（复用已有 R2 SigV4 客户端，无新依赖），本地盘降为读缓存并在未命中时回填；写入顺序改为「先正文后元数据」，上传失败整个归档失败，不再留下点不开的幽灵记录 |
| fix | cds | 未配置对象存储时不许假装持久：元数据记 `storage:'local'`，`/raw` 与 `/download` 的 404 改为说清三种成因（未配对象存储 / 归档于本次改动之前 / 对象确实不在），不再甩一句「内容文件已丢失」 |
| fix | cds | `fetchReportRaw` 抛错时把响应体传成 undefined，服务端给的原因被丢掉，用户看到「服务拒绝了请求，但没有返回可读错误原因」——改为先读错误体再抛 |
| feat | cds | 验收首页补结论层：第一屏改为规则生成的一句判断（挂真实数字、按严重度取第一条成立的、算不出就不出句）+ 支撑点 + 指到具体分支的下一步，漏斗降级为支撑图；内部词换人话（漏/在途/一次都没跑过的验收 → 该管的是这些/在改的分支/没做过的验收类型） |
| rule | platform | 新增强制规则 12「账在哪，货就在哪」+ `.claude/rules/durable-payload-storage.md`：元数据与实体必须同一持久等级，实体默认进对象存储，凭据不全拒绝静默回退本地，测试必须删本地目录模拟容器重建；附全仓扫描台账 |
| test | cds | 补对象存储守卫（9 条）与正文持久性守卫（7 条，用删除本地缓存模拟容器重建），红绿闭环验证过：回退旧行为后 7 条中 5 条变红 |
| feat | cds | 验收首页第一屏第三次重做（用户三轮否决后定稿）：顶部五步流程条先讲清「一条改动本来要走什么路、本屏只统计第三四环节」，再是 71 条分流图（已验收 / 未验收，一个方块一条改动），再一行一个项目，最后两条脚注。解决「正常人进来不知道这个页面在做什么」 |
| polish | cds | 首页文风改行政语气：标题由反问句改名词短语「代码改动的验收状态」，去掉「真的 / 只有 / 全场 / 最该 / 一条都没」等替读者下判断的词；结论层保留但用陈述句给 |
| fix | cds | 方块只编码验收状态（蓝 --info 已验收 / 灰 --surface-sunken 已部署未验收 / 虚线未部署），三档结论退到 chip 与状态句——「已验收」不等于「通过」（5 条已验收里 3 条未通过），同一颜色在一屏内不许有两个意思；--primary 只留给控件选中态 |
| fix | cds | 分流三段改用漏斗基数现推（accepted + deployed-accepted + changes-deployed），不用 deployed-not-accepted 漏点桶：该桶排除「合并了没验」，墓碑数据接入后会与总数对不上、图上凭空少方块，且今天恰好相等明天才静默错位 |
| test | cds | 漏点顺序守卫按新首页重写：前端不再逐类渲染漏点卡，改为守「前端硬编码的 LeakKind 字面量必须在后端联合类型里存在」（后端改名则示例分支静默消失），红绿闭环验证过 |
| refactor | cds | 验收报告首页 PipelinePanel 落 G 稿（厂房剖面空间隐喻）：入料溜槽 / 带面缺口 / 三道闸 / 货堆 / 三料仓 / 项目垛 / 场外点阵，颜色全走 CDS token 双主题，<1024px 转纵向流水与横条项目行 |
| fix | cds | LeakKind 守卫的判据太窄：只认 `kind === 'x'` 比较式，首页改用 `totalLeaks['x']` 下标式后既漏掉真实依赖又误报「没有依赖」。改为两种写法都认，且不再要求依赖非空（没有依赖是合法状态）。两种写法各做过红检 |
| feat | cds | 验收首页接回结论句（buildPipelineHeadline 此前无人引用，整块退化成看不懂的图形）：一句挂数字的判断 + 支撑点，紧凑态与放大态都渲染 |
| feat | cds | 首页默认紧凑：一句判断 + 一张厂房剪影并排，约占一屏三分之一；点「放大」才铺开完整三张图与下一步 |
| feat | cds | 厂房加背景外壳：锯齿天窗 + 桁架柱 + 地面，纯 hairline 衬托，不编码任何数据 |
| test | cds | 结论句补接线守卫：必须被页面真的调用（类型注解里的同名不算）、两态都渲染、默认紧凑、背景不许收数据参数 |
| fix | cds | 紧凑态砍字：只留一句判断，支撑点与下一步移到放大态；结论句去掉破折号对仗修辞 |
| fix | cds | 紧凑态改为厂房 + 堆场两张图并排，不再是「左边一堆字、右边一张图」 |
| test | cds | 补守卫：结论句不许出现破折号/问号/叹号；紧凑态只渲染一句；紧凑态两张图都要在（锚点修正，原锚点会命中前面 GateH 的同款三元而漏判） |
| fix | cds | 堆场宽度下限从 560 降到 300 并让整排垛居中：一个项目时不再是一根垛缩在左边、右边一路空地面线 |
| feat | cds | 验收首页厂房剖面加入场动效：货堆按行摞起、未部署那只真的往下掉、过闸的条落进料仓、项目垛逐根长起、场外点阵散开，滚轮常驻慢转；大数字从 0 涨到真值 |
| feat | cds | 动效整体关在 prefers-reduced-motion: no-preference 内，基础样式即终态，动画没跑/reduce 用户看到的都是完整静态图 |
| test | cds | 新增动效接线守卫（keyframes 两头对齐、animation 不许逸出媒体块、基础样式不许写 opacity:0、类名两头都挂上、计数初值必须是真值） |
| feat | cds | 验收首页厂房几何从写死坐标改为按数据累加：货箱尺寸由数量反算（71 条时箱子从 16px 变 32px、货堆占地大 2.5 倍）、门洞高度由货堆反推、画布宽度各段累加 |
| feat | cds | 入料溜槽加宽并装料，料沿槽循环下流；「未验收」大数字移到带面下方空地，底部数字排成一条基线 |
| feat | cds | 堆场层高按最高一垛反算、画布宽度按真实项目数算，场外点阵宽度按实际点数算；三张图显示宽度随内容量次线性收缩，数据少时整图连同高度一起收小 |
| test | cds | 新增尺寸自适应行为守卫 44 条（分流恒等、货箱不丢不越界、越多越大直到画布用满、数据少画布要小、各段首尾相接、显示宽度次线性） |
| fix | cds | 「未验收」数字改用与同排一致的字号（46px 大字会盖住上面那行标签），三张图在卡片内居中，溜槽料量跟着总量走 |
| feat | cds | 图缩小时字号反向补偿（--ts），文字显示尺寸与缩放无关；整块面板宽度跟着最宽的那张图收，避免大卡片装小图 |
| feat | cds | 验收首页厂房加悬浮提示：货箱显示它是哪条分支（对不上就退回通用提示，不张冠李戴）、缺口/料仓/三道闸/溜槽各说明这一环、项目垛列出改动·部署·验过·三档·最近动静·GitHub 接没接 |
| feat | cds | 提示走事件委托（元素只挂 data-tip），替换掉柱子上那个只重复图上已有项目名的原生 title |
| test | cds | 新增悬浮提示守卫 16 条（条数对不上必须不绑、每类实体都挂得上、委托与提示框两头都在、三道闸的提示不能是占位空串） |
| feat | cds | 验收首页紧凑态换成「紧凑条」：改动总量领衔，拆成没起预览 / 待验收 / 已验完三段，每段一条点阵（共用同一分母，可直接对看），右侧结论三档、项目分布、场外报告 |
| feat | cds | 点子尺寸随条数分档：5 条时 20px 大方块、71 条时 5px 细点，数据少时格子变大而不是画面变空 |
| refactor | cds | 删掉 pp-mini（把厂房整个缩小的旧紧凑态）——该隐喻要占半屏带标签才读得懂，缩小后只剩灰方块 |
| test | cds | 新增紧凑条守卫 15 条（分母共用、格子随条数反向变化、项目条宽高比例、零改动画空槽、颜色全走 token、窄屏不带定高） |
| fix | cds | 紧凑条点阵的点亮色写在基础底色之前被整条盖掉（同特异性后写优先），三段一个都不亮；挪到基础规则之后并补守卫 |
| feat | cds | 验收报告首页放大态换成与紧凑态同一套视觉语言（判断句 + 总览条 + 逐项目明细），删除厂房剖面 |
| feat | cds | 已验完那段的点阵按通过 / 原则性 / 未通过三档着色，主体不再是一片灰阶 |
| refactor | cds | PipelinePanel 收敛为编排层（1400 行降到 270 行），图形迁入 CompactStrip |
| fix | cds | CountText 改用 PlayCtx 取播放信号并渲染为 span，此前写死 true 且用了 SVG 的 text 标签 |
| test | cds | 尺寸 / 动效 / 提示三份厂房时代守卫按新结构重写，新增结论三档与两态同源的守卫
| feat | cds | 验收报告首页新增走向折线：结论三档 / 项目 / 改动对报告，紧凑与放大两态 |
| feat | cds | 后端新增 buildPipelineSeries 日分桶序列，与总览同源折叠报告版本，随 /api/reports/pipeline 一并返回 |
| fix | cds | niceMax 未挡 NaN，刻度为 NaN 时整张图会静默消失 |
| docs | cds | debt.cds.md 记两笔：部署无日序列导致流转链缺中间一环、夜间语义三色越出明度带 |
| test | cds | 新增 41 条守卫（后端序列 17 / 前端折线 24），十一条篡改逐一验过会红 |
| fix | cds | 验收首页面板恢复满宽：此前跟着最宽那张图一起收窄，数据薄时只占可用宽的三分之二，右边整条空着像没渲染完；改为卡片满宽、图各自收并居中 |
| fix | cds | 堆场 viewBox 上沿按字号补偿系数留白：补偿把数字放大后原余量兜不住，一个项目时垛顶的数字上半截会被干净裁掉 |
| test | cds | 补守卫：pp-root 不许再设宽度上限；六组项目数/最高条数下垛顶数字必须完整落在画布内 |
| fix | cds | 厂房顶部的总量数字、标签、锯齿屋顶按字号补偿系数自下而上排布，不再互相压；三张图改为左对齐，与标题和下方台账左缘一致 |
| feat | cds | 预览实例演示数据补入主实例形状快照（9 项目 / 70 分支 / 319 报告），首屏曲线不再是三条贴零直线 |
| feat | cds | createAcceptanceReport 支持种子指定 createdAt，演示数据得以有真实时间分布 |
| test | cds | 新增 12 条快照种子守卫，八条篡改逐一验过会红 |
| fix | cds | 报告台账「验收对象」列的胶囊写死了高度，长分支名换行后第二行掉出背景 |
| refactor | cds | 分支 / commit / PR 三处胶囊抽成 ChangeKeyChip，不再各抄一份 class |
| test | cds | 新增 7 条守卫，五条篡改逐一验过会红 |
| fix | cds | 报告对象存储凭据配一半时改为抛错，不再静默退回本地盘 |
| fix | cds | 报告对象键拼上配置的前缀，此前前缀是死配置、报告落在桶根与备份混放 |
| fix | cds | 流水线聚合剔除上一代同名分支的墓碑，避免把在跑的分支报成「没验就合并」 |
| fix | cds | 三段分流收敛为单一口径，脏数据下三段之和不再超过总数 |
| fix | cds | 走向图按后端给的条数画项目线，不再自己截成 3 条导致图例对不上账 |
| fix | cds | 报告增删后一并重算流水线，且走静默刷新不把面板打回 loading |
| test | cds | 新增 36 条守卫，八条篡改逐一验过会红；修正一条把错误行为反向锁死的旧用例 |
| fix | cds | 正文登记在对象存储却取不回来时给出可执行原因，不再与「历史数据缺失」混成一句 |
| fix | cds | 预览实例形状快照每次启动重锚时间，跨部署保留的实例不会在 90 天后变空图 |
| docs | cds | debt.cds.md 记一笔：报告截图资源仍只在容器本地盘 |
| fix | cds | 「部署了没验收」只数在途改动，放弃的分支（PR 关掉 / 分支删掉）不再被整批算进落差 |
| fix | cds | 报告对齐判据在两边 PR 号都记了且不同时直接否决，不再退回会被复用的分支名 |
| fix | cds | 首页时间窗按钮真的改流水线请求，此前只换选中底色、屏幕上一个数不动 |
| fix | cds | 走向图紧凑态柱子改画 7 日均，与它自己标注的刻度口径一致，几个高矮不同的日子不再被夹成一样高 |
| test | cds | 新增 14 条守卫，四条篡改逐一验过会红；删掉一条恒真因而永远不会红的旧断言 |
| fix | cds | 放弃的分支（PR 关掉不合 / 分支删了）不再进漏斗，此前它撑着「在改的分支」与「待验收」 |
| fix | cds | 报告正文缓存改原子写，写失败时作废旧缓存，不再「改成功了但页面永远显示旧版」 |
| fix | cds | 台账一份报告都没有时仍渲染流水线，此前整块被空状态吞掉，而那时那句警告最该被看见 |
| docs | cds | 走向图页脚说明「新开改动」柱子会随分支回收变矮；debt.cds.md 记改动开启事件表与部署证据两笔 |
| fix | cds | 报告更新走对象存储失败时回滚内存里的标题与格式，此前接口回 500 而同进程后续读到的已是被拒绝的那一版 |
| fix | cds | 走向图的 7 日均补预热段：后端多给窗口前 6 天、前端滚完再裁，开头六天不再只有 1~6 个样本却标着 7 日均 |
| chore | cds | 本 PR 的 14 份改动记录碎片合并为一份（AGENTS.md 规则 #4：同一 PR 全部变更放一个文件） |
| test | cds | 新增 11 条守卫，三条篡改逐一验过会红 |
| fix | cds | 演示数据补播时回填丢失的正文，容器重建后那批报告不再点开全是 404 |
| fix | cds | 无主报告（没记项目的）计入流水线总览的「报告没记它验的是谁」与背景数，不再与走向序列对不上账 |
| fix | cds | 台账空时只有流水线「成功地报了零」才让位空状态，加载中与加载失败不再被空卡片吞掉重试按钮 |
| docs | cds | debt.cds.md 记一笔：无 PR 号无 commit 的历史报告仍只能靠分支名认领，按 §5.5 停止扩张该判据 |
| test | cds | 新增 10 条守卫，三条篡改逐一验过会红；容器重建那条按规则真的删掉了本地目录 |
| fix | cds | 结论头条把「没有不安全的合并」与「有几个项目查不到」合成一句，支撑点截断不再只留前半句变成假保证 |
| fix | cds | 下钻到项目走与筛选菜单同一条路，会复位文件夹筛选，不再点进去看到空台账 |
| docs | cds | debt.cds.md 记一笔：报告版本折叠只按标题元组，按 §5.5 熔断不在本 PR 扩张该判据 |
| test | cds | 新增 8 条守卫，两条篡改逐一验过会红；修正一条构造不到截断线因而测不到目标的断言 |
| fix | cds | 改格式时不再当场删旧对象：save 是写后即返回，删在元数据落定之前会让报告重启即读不出 |
| fix | cds | 第一屏那句判断改用 splitFunnel 的三段，不再与同屏分流图各算各的、说出「都验过了」配未验收方块 |
| fix | cds | 指着已删项目的报告与无主报告同样落账，判据从「projectId 是否为空」放宽为「那个项目还在不在」 |
| refactor | cds | splitFunnel 从 CompactStrip 挪进 lib，图 / 明细行 / 判断句共用同一个函数，lib 不再被 pages 倒挂 |
| docs | cds | debt.cds.md 记一笔：报告旧版本对象改为留存，欠一次可重跑的孤儿回收 |
| test | cds | 新增 10 条守卫，三条篡改逐一验过会红 |
| fix | cds | 预览实例清洗补齐 R2 成组凭据：此前只删两把密钥、留下 ENDPOINT 与 BUCKET，被清洗成「四缺二」触发配置事故判据，子实例开不了机（实测 /healthz 503） |
| fix | cds | 删报告改为先落元数据再删远端对象，避免对象已删而元数据写盘失败导致报告永远打不开 |
| fix | cds | 换时间窗失败时不再静默保留旧数据：保留只对同一档的刷新成立，换档失败照实报错 |
| docs | cds | debt.cds.md 记一笔：storage='local' 标记未端到用户眼前 |
| test | cds | 新增 10 条守卫，四条篡改逐一验过会红；其中一条端到端复现「子实例开不了机」 |
| fix | cds | 报告台账列宽改为表头一次定死 + table-fixed，宽屏下不再把全部富余灌进唯一没写宽度的标题列、表中间空出一大片 |
| fix | cds | 流水线请求加代次号：连点档位时慢响应不再盖掉新结果，按钮与数字不会停在两个档上 |
| docs | cds | debt.cds.md 记一笔：结论三档计数与漏斗「已验完」段对不上，与部署信号偏弱同根 |
| test | cds | 新增 10 条守卫，三条篡改逐一验过会红；修正一条被自己注释命中因而永远不会红的断言 |
| fix | cds | 更新报告时空正文当场拒绝：路由回 400、服务层拒绝退回本地，不再清掉 objectKey 留下一条重建即打不开的记录 |
| fix | cds | 项目活跃时间算上「分支已回收」的报告，昨天刚验过的项目不再显示成「最近动静：无」 |
| fix | cds | 结论聚合请求补上与流水线同样的代次保护，快速切项目时慢响应不再盖掉新结果 |
| docs | cds | debt.cds.md 记一笔：对象存储取回失败的原文直接回给了用户，欠一张稳定错误码表 |
| test | cds | 新增 7 条守卫，三条篡改逐一验过会红 |
| fix | cds | 读缓存回填改原子写，回填失败不再留下半份文件被后续请求一直当成正文端出去 |
| fix | cds | 项目视图不再因台账为空而整页让位，合并覆盖等证据照常展示（上一轮修空状态时引入的回归） |
| docs | cds | debt.cds.md 记两笔：删报告时元数据未真正落盘就删对象、版本折叠先于时间窗切分削掉上一窗分母 |
| test | cds | 新增 3 条守卫，两条篡改逐一验过会红 |
| fix | cds | 首屏不再把没填结论的报告算成通过（零通过时曾显示「可以正常使用 · N 份全部通过」） |
| fix | cds | 台账选中的报告类型在新作用域里没有时自动退回全部，不再留下没人能解释的空台账 |
| docs | cds | debt.cds.md 记一笔：报告正文换版仍写同一个对象键，欠「先落新键再切元数据」 |
| test | cds | 新增 9 条守卫，两处篡改逐一验过会红 |
| fix | cds | 项目悬浮提示不再一边写「验过 5」一边写「一条都没验过」，区分没验与验了没填结论 |
| fix | cds | 紧凑态项目垛改走同一份 splitFunnel，不再与总览条、明细行各算各的 |
| fix | cds | 第四条项目走向线有了独立线型与图例样式，不再与第三条完全同形 |
| fix | cds | 头条不再把已合并、已撤下的改动说成「在改的分支」，措辞对齐总览条的「改动」 |
| test | cds | 新增 8 条守卫，四处篡改逐一验过会红；另把一条反锁字面实现的旧断言改成行为断言 |
| docs | cds | debt.cds.md 记一笔：走向图末格是半天，这件事只在放大态说了 |
| fix | cds | 项目悬浮提示的改动、部署过、验过三个数改走同一份夹取值，与图上画的数一致 |
| test | cds | 新增 2 条守卫，篡改验过会红 |
| docs | cds | debt.cds.md 记一笔：同一份报告的并发改写没有串行化，回滚会覆盖后到那一版 |
| fix | cds | 根因下钻一并放开文件夹与系统视图，不再出现「链接写着 N 份、点进去是空台账」 |
| test | cds | 新增 2 条守卫，篡改验过会红 |
| docs | cds | debt.cds.md 记两笔：发布闸不认当前候选版本、读正文回填与改正文写入互相覆盖 |
