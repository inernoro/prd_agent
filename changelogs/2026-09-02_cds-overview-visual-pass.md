| fix | cds | 内存从时间序列面积图改成横向构成条 + 全名清单：内存几乎不随时间变，30 分钟面积图就是几条水平直线白占半屏，而全部信息图例里早已说完。dataviz 形式表：part-to-whole 的默认形式是 stacked bar，服务名又多又长走横向 |
| fix | cds | 堆叠段之间留 2px 表面色的缝（不给每段描边），面积不透明度 0.9 降到 0.8；验证器提示 series-4↔series-5 的 tritan ΔE 7.1 落在底线带，本就必须配二级编码 |
| fix | cds | 「其他 N 个」不再用描边色 hairline-strong —— 它明度高、面积一大就成了图里最抢眼的一块死灰；改为去饱和的 muted-foreground 低透明度，明确弱于任何具名服务 |
| fix | cds | 网络图改用单一色相两档（收深发浅），新增 --series-net 双主题 token。此前用 --info 与 --primary，违反本面板自己声明的「语义四色状态专用」不变量；新色经 dataviz 验证器双主题六项全 PASS |
| fix | cds | Y 轴刻度吸附到 1/2/2.5/5/10 整数步长，小数位由步长定一次。此前上限乘 1.12 余量再取 0.66/0.33，打出 15/10/5.0/0.0 这种混排 |
| fix | cds | 图例从等宽 N 列改为按内容换行，长服务名不再一律截断成「cloudbridge-...」；两张卡改 items-start，不再为对齐在矮的那张里留空洞 |
| test | cds | 新增 5 条守卫（内存用构成条 / 图形取色不用保留色 / --series-net 双主题定义 / 堆叠靠缝不靠描边 / 刻度落整数步长），做过红绿闭环 |
