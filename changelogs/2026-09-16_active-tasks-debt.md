| feat | prd-api | 新增活动任务清单：人维度的此刻/接下来/做成了什么，含结案说明、委派、堆积量与匿名脱敏三档 |
| feat | prd-admin | 新增我的任务、大家在做什么、做成了什么三个页面，以及 /board/active-tasks 匿名只读看板 |
| feat | prd-api | 任务台接入 MCP：map_tasks_mine / add / team / assign 四个工具，智能体可读可填不可结案 |
| feat | prd-api | 新增 active-tasks.use / active-tasks.manage 权限点与两个同名 scope |
| feat | prd-api | 任务可选带「什么时候要」，含日期人话标签与过期标记；MCP 两个写工具同步支持 dueAt |
| feat | prd-admin | 时间输入照提醒事项做四个快捷胶囊，并从标题里认出「明天」「周五之前」自动填时间 |
| refactor | prd-api | 删掉上一版遗留的预估耗时与超期判定（前端已无入口，且超期判定属于被否掉的 KPI） |
| refactor | prd-admin | 任务台圆圈语义统一为「做完了」，切换当前任务移到行尾「开始」 |
| feat | prd-admin | 任务台点圆圈改为乐观完成 + 底部补写「做成了什么样」+ 撤销 |
| feat | prd-admin | 任务台「加一件」改为行内新增，回车提交后接着敲下一条 |
| feat | prd-admin | 任务台桌面两栏（左侧 source list）/ 手机单栏 + 分段控件，救活历史页孤儿路由 |
| feat | prd-admin | 任务台浮层两端分形态：桌面贴顶按钮右对齐 / 手机底部升起，补 createPortal + ESC + 焦点陷阱 |
| feat | prd-admin | 任务台首次进入一次性新人指引（一屏三行，不做逐步高亮导览） |
| fix | prd-admin | DuePicker 过 18:00 后点「今天」会生成过去时间导致立刻逾期 |
| fix | prd-admin | DuePicker「自定」选中后展开状态收不起来 |
| refactor | prd-admin | DuePicker 自定日期改自绘月历网格，替掉 input type=date |
| fix | prd-admin | 任务台三处 whenLabel 拷贝合一并改用 serverNow |
| fix | prd-admin | 任务台 hover 规则收进 @media (hover:hover)，补 :active 与 reduce-motion |
| fix | prd-admin | 任务台字号全面改 rem，标题改换行不截断 |
| fix | prd-admin | 任务台过期时间补感叹号角标，不再只靠颜色表达状态 |
| fix | prd-admin | 任务台删除按钮不再隐形消失，投入过时间的显示为「放下」 |
| fix | prd-admin | 团队页轮询在标签页不可见时停跑 |
| feat | prd-api | 任务台新增撤销结案 POST {id}/reopen，PUT 支持补写 closingNote |
| polish | prd-admin | 任务台界面文案去掉「老板 / 让他」语气与解释性长句 |
| docs | doc | 任务台设计文档补两端分化与新人上手两节，债务台账补 4 条（拖拽排序 / 不可编辑 / 删除无撤销 / 我的任务不自刷新） |
| fix | prd-admin | 任务台列表分隔线在带头像的行上左缘偏移到文字左缘（真机截图抓出） |
| polish | prd-admin | 匿名看板空态文案与另外三页对齐，人员行补 role=listitem |
| docs | doc | 新增《活动任务清单 · 指南》：一分钟上手、一天怎么用、建议与派活的区别、智能体四个工具、常见疑问 |
| docs | doc | 设计文档补联动总览图、AI 两步的外部依赖图、任务状态机、建议时序图（四张图均经真浏览器渲染验证） |
| feat | prd-admin | 正在做的那一行补「放下」入口，不必先切到别的任务再回头放下 |
| fix | prd-admin | 放下/删除按状态判而不按秒数判，避免刚开始计时的任务走到「真删」被后端 400 |
| fix | prd-api | AI 导入/吸取：原文没有时间词时硬性丢弃模型给的 dueAt，并不再给日期对照表当诱饵 |
| fix | prd-api | AI 导入/吸取：skipped 截断到 80 字，模型抄整段原文时不再占掉半个浮层 |
| fix | prd-admin | 候选任务行标题被右侧日期挤掉尾巴（input 在 flex 里缺 min-width:0） |
| docs | doc | 台账记两条实测发现：未注册百宝箱（欠卡片插画）、截止日靠规则兜底不靠模型自觉 |
| fix | prd-admin | 任务台切换视图时左栏左右平移（三个视图内容列宽度不同 + 滚动条有无），改定宽双轨 + scrollbar-gutter |
| feat | prd-admin | 任务台备用队列支持拖拽排序，不再只能「提前」置顶 |
| feat | prd-admin | 任务台点行进去改标题、时间、备注（note 字段终于接上渲染） |
| feat | prd-admin | 任务台删除/放下可撤销，删掉的按原位置建回来 |
| feat | prd-admin | 我的任务页加可见性感知轮询，别人派活提建议会自己跟上 |
| feat | prd-admin | 任务台「粘一段话，AI 帮你拆」：流式吐候选任务，勾选后才入库 |
| feat | prd-admin | 任务台新增「建议」：任何人可提，提了不进对方队列，收件人自己吸取 |
| feat | prd-admin | 吸取建议可引用知识库（服务端记住上次选的）与补一句额外要求 |
| feat | prd-admin | 建议可拿去涌现派生，回写涌现树 Id 便于回溯 |
| feat | prd-api | 任务台新增拖拽排序 POST {id}/reorder，创建支持 orderKey 还原位置 |
| feat | prd-api | 任务台新增 AI 一键导入 SSE 端点，含日期对照表与逾期日期兜底 |
| feat | prd-api | 新增建议实体与收件箱/吸取/放下/涌现回写端点，吸取走 SSE |
| feat | prd-api | 开放接口与 MCP 新增 map_tasks_suggest（use 档即可提建议） |
| polish | prd-api | MCP 派活工具描述去掉「让他做什么」的支配语气 |
| docs | doc | 设计文档补「建议和派活是两码事」与「一段话进来 AI 拆成一条条」两节；债务台账关掉补完的四条、新增四条 |
| polish | prd-admin | 任务台文案不再出现「老板」，结案与卡住两条提示按苹果文案习惯砍短 |
| polish | prd-api | 任务台后端注释与报错文案同步改用中性称呼 |
| fix | prd-admin | AI 候选任务标题改自适应换行（input 天生单行，上一版加 min-width:0 反而更容易被裁） |
| polish | prd-admin | 桌面浮层宽度 420 → 460，二十几字的任务标题不再被迫换行 |
| fix | prd-admin | 手机端行上撤掉常驻操作（挤得标题五条腰斩四条），收进「点行进入」的详情浮层 |
| fix | prd-admin | 手机端列表底部补 TabBar 高度，最后一行不再被底部导航压住 |
| feat | prd-admin | 任务详情浮层新增操作排：现在就做 / 排到最前 / 放下或删除 |
| fix | prd-admin | 手机端任务行补 scroll-margin-bottom，浏览器自己发起的滚动不再把末行停在底部导航底下 |
| fix | prd-admin | AI 候选行的来源标签与理由不再连读成一句话 |
| feat | prd-api | 债务接进任务台：稳定标识 + 幂等同步端点 + 认领/转成任务/放回/了结 |
| feat | prd-api | 开放接口与 MCP 新增 map_debt_list / map_debt_sync 两个工具 |
| feat | prd-admin | 任务台下半屏新增「欠着的」：折叠展示、展开看现状与补的条件、一键转成我的活 |
| feat | prd-agent | 新增 scripts/sync-debt-ledger.py，把 doc/debt.*.md 的主表解析成同步载荷 |
| test | prd-agent | 补债务台账解析器守卫（标识唯一性/格式同源/一份台账一张主表）与后端纯函数判据 |
| docs | prd-agent | 设计文档补「下半屏：我们欠着什么」，指南补用法，台账第 15 条收口并开 16/17 两条边界 |
| fix | prd-admin | 债务区挪到「做完的」之前——能动手的东西不该排在存档后面 |
| fix | prd-api | 放回认领与认领走同一个状态判定源，不再出现「还没人管」配「已转成 1 条活」 |
| fix | prd-agent | 同步脚本带上 User-Agent，绕开 CDN 对默认 Python-urllib 的 403 |
| fix | prd-api | 债务看板的计数与结论句按整块算，不再跟着「只看我的」走（原本会说出「其余都有人管了」而实际上百条没人管） |
| fix | prd-api | 转出去的任务被删掉后，债务不再声称「已转成 N 条活」：读的时候对活人名单、摘掉悬空 id 并把状态落回真值 |
| fix | prd-api | 开放接口的债务清单改走界面那一侧的同一个判定源，不再自己抄一遍计数 |
| fix | prd-admin | 「只看我的」在零认领时不再把整块债务区连同切回「看全部」的开关一起隐藏，改为给一行空态 |
| test | prd-api | 补三条守卫：筛选不许改分母、悬空引用必须摘干净、「没人认领」这个判断全仓只许有一处定义 |
| fix | prd-api | 勾掉一条备用任务不再把「正在做」那件换走（原来 Finish 无条件提队首，会顺带清掉在做那条的卡住状态） |
| fix | prd-api | 匿名看板脱敏后不再保留标题开头一两个字——匿名面板默认开着，标题开头常是客户名与缺陷编号 |
| fix | prd-api | 「今天是哪天」收敛成一个团队日历判定源，标签侧不再跟着容器时区走（UTC 容器上曾把「今天」标成「明天」） |
| fix | prd-api | 任务台使用权发到 operator / viewer / agent_tester 三个内置角色，非管理员装完即可用 |
| fix | prd-api | 转成任务与认领走同一道归属校验，不再能一键把别人认领的债务划走 |
| fix | prd-api | 建议可引用的知识库补上团队共享库，挑选清单与取正文共用同一个可读判定 |
| fix | prd-admin | 归别人的债务不再显示「转成我的活」 |
| fix | prd-admin | 拆解与吸取两个浮层在流式未完成时禁用确认，避免只建一半却把全部来源标记为已吸取 |
| test | prd-agent | 补五条守卫：队列顶替时机、脱敏零残留、内置角色覆盖、认领与转换同一道门、浮层流式禁用（含红绿闭环验证） |
| fix | prd-api | 撤销结案照原来那一档还原：结案时记下它是不是「正在做」，撤销一条备用不再把手上那件换走 |
| fix | prd-admin | 浮层的键盘监听改为稳定引用，打开时只对焦一次——派活弹窗原本敲第一个字焦点就被弹回选择框，整张表填不进去 |
| fix | prd-admin | 吸取时有一条没建上就不再把全部来源标记为已吸取：没成的留在表上等重试，如实告知几件成几件没成 |
| fix | prd-api | 卡住要卡够 BlockedEscalateMinutes 才升到管理侧，那个 5-1440 的旋钮此前转了等于没转 |
| fix | prd-api | 切换「正在做」加 CAS 与收尾清扫，并发窗口后末态恒为一条（真原子性的残留边界记台账第 19 条） |
| ci | prd-agent | 债务台账本身登记进 CI path filter——只改一份台账的 PR 此前完全跳过那道闸 |
| test | prd-agent | 债务守卫补「表格行不许折行」：折行会让后面的条目静默消失（实测 108 掉到 101 而判据全绿），含红绿闭环验证 |
| fix | prd-api | 放下也落「结案前在哪一档」的戳，撤销一条放下的活不再被还原成备用 |
| fix | prd-api | 债务转任务改用追加而非整表覆盖，并发下不再丢掉对方那条任务 id；归属改写带条件 |
| fix | prd-admin | 导入时有一条没加上就不关窗，没成的留在表上等重试（与吸取同一口径） |
| fix | prd-admin | 撤销结案前撤掉还没落地的「做成了什么样」自动保存，避免它在 reopen 之后又写回去 |
| test | prd-agent | 补两条守卫：放下也要落戳、两个浮层部分失败都不许收摊（含红绿闭环验证） |
| fix | prd-api | 撤掉「切换正在做时收尾清扫」——它会让两次并发切换互相降级到零条正在做，比原本的两条更糟 |
| fix | prd-api | 卡住升级量的是这一轮卡了多久，不是累计（之前卡过 110 分钟、这次刚卡 10 分钟会立刻升级） |
| fix | prd-api | AI 给的截止日带上团队时区偏移，并把两份 NormalizeDue 收敛成一个判定源（不带偏移会让 9/19 显示成 9/20） |
| fix | prd-api | 认领改为条件更新，零匹配即冲突——并发认领同一条不再后写顶掉先写 |
| fix | prd-admin | 吸取重试跨趟累积已建任务 id，并检查标记已吸取的返回值（失败不再报成功关窗，避免重复吸取建出重复任务） |
| test | prd-agent | 守卫改为「不许再加收尾清扫」，另补升级口径与截止日时区两条 |
| fix | prd-api | 匿名看板默认改为关闭：它对着不需要登录的任何人，端出的是同事真名与此刻在做什么，安全默认只能是关 |
| feat | prd-admin | 「大家在做什么」补上看板设置面板：匿名开关、三档可见性、卡住升级分钟、堆太多阈值——这四项此前只有后端端点，前端封装写了却无人调用，管理员既关不掉也改不了 |
| fix | prd-admin | 日期一律按团队日历（东八区）算，不再跟浏览器时区走：人在美西点「今天」，落到后端原本会成为团队的明天 |
| fix | prd-admin | 建任务那几秒不许关窗：abort() 掐不断已经在跑的创建循环，窗关了活照建、建议照了结，用户以为取消了其实没有 |
| fix | prd-admin | 「回去重选」重新生成时把「哪几行已经建过」一起清掉，否则新行顶着上一代行号被跳过，拿旧任务 id 去标记这批建议 |
| fix | prd-api | 「要你看一下」算上堆超阈值的人：原本结论句写着「没有要你管的」，底下明晃晃标着两个堆红了的人 |
| fix | prd-admin | 补上 atb-sheet__hint 的样式：这个类名此前六处在用而规则一直不存在，那几行说明字跟正文一样大一样黑 |
| test | prd-agent | 补六条守卫：匿名默认关、结论句与标红同一批人、两个浮层写入期不许关闭、重试键要重置、看板设置必须有人调用、日期按团队日历（含红绿闭环验证） |
| docs | prd-agent | 台账补第 22 条：改默认值只保护「没存过设置」的部署，已经开过匿名面板的升上来之后仍是开着的 |
| security | prd-api | 同步整块债务台账改为要管理档：它按调用方给的 Key 覆写所有人看到的内容，而使用档发到了 operator/viewer，留在使用档等于任何登录用户都能重写全公司的债务台账 |
| fix | prd-api | 了结一条债务也要过归属门，不能替别人宣布做完了（与认领、转换同一道门） |
| security | prd-api | 模型报错只进服务端日志，不再把网关原文（HTTP 状态与响应体、供应商与模型名、异常文本）吐进浏览器 toast |
| fix | prd-agent | 「卡够时长了吗」由后端下发，前端不再拿阈值自己算一遍——否则头条说「没有要你管的」而底下那行标着红 |
| fix | prd-admin | 匿名面板轮询成功要把「没有开放」撤回来：一次网络抖动原本会让这一屏永久停在那句话上 |
| fix | prd-admin | 吸取的写入保护盖到标记已吸取结束，不在中途放开（上一轮那条只盖住了建任务那几秒） |
| fix | prd-admin | 说到的那天就是那天：周一早上写「周一交周报」指今天，不再被推到下周一；「这周末」同理 |
| test | prd-agent | 补六条守卫：同步要管理档而认领不许被提档、三个端点同一道归属门、网关原文不许直吐、卡够时长由后端下发、写入保护范围、匿名面板恢复（后两条跑过红绿闭环） |
| fix | prd-api | 已经结案的不许被激活成「正在做」：两条几乎同时勾掉时，A 的结案会把刚 done 的 B 无条件设回 active，带着结案时间继续计时 |
| fix | prd-admin | 撤销删除改为重建成功才收起提示：真删那条的标题、时间和位置只剩这一份副本，先清掉再重建，请求一失败就找不回来了 |
| fix | prd-admin | 涌现树没挂上建议时不再静默跳走，否则再吸一次会长出第二棵孤树 |
| fix | prd-admin | 历史页档位标签改成「近 7/30/90 天」：发出去的本就是滚动天数，叫「本月」会带出上个月的记录 |
| test | prd-agent | 补一条守卫：激活必须限定「还没结案」，且撤销必须先放回 standby 再激活（这两处是一对，改一个忘另一个会悄悄废掉撤销按钮）|
| fix | prd-api | 顺手清理悬空引用改成原子 PullAll + 快照条件：它跑在读路径上，原来用 Set 整表覆盖，一次列表请求就能抹掉别人刚转出去的那条任务 id |
| fix | prd-api | 历史按结案时间落窗口：改一句半年前那条的结案说明，原本会让它出现在「近 7 天」里并被算进合计 |
| fix | prd-admin | 吸取浮层的行号跨代唯一，不再每次重拆从 0 数起——键会重复才是那几个洞的根，清不清都不对 |
| fix | prd-admin | 新建任务的回车这条路也挡住重复提交：旁边按钮有 disabled，回车没有，按住回车会建出几条一样的 |
| docs | prd-agent | 台账第 23 条：SSE 心跳仍挂在模型吐字上，独立节拍要配锁，不在这个 PR 里做 |
| fix | prd-api | 结案那段写序列改用 CancellationToken.None：客户端断开会把它切一半，结算落了库而「从哪一档结案的」戳和队首顶替被取消，重试又被 alreadyDone 挡在门外 |
| fix | prd-api | 真删带齐条件（属主 + 还在待做 + 零投入），零匹配如实回绝：同一行上「开始」和「删除」挨着点，只按 Id 删会把已开始计时的任务永久删掉 |
| feat | prd-admin | 债务那一块补「了了」入口：此前 closeDebt 封装写了却全前端无人调用，列表只隐藏已 closed 的，而标成 closed 的入口只有 REST 端点，下半屏只进不出 |
| test | prd-agent | 补三条守卫：结案写序列不许跟请求 ct、真删要带条件、债务四个动作都接进界面 |
| docs | prd-agent | 台账新增第 24-33 条：第九、十轮的 P2 全部记账排后续（终态转换、派活回车、空队列标红、月日校验、来源快照、涌现出箱、跨零点标签、导入提示词、台账转义、吸取骨架）|
| fix | prd-api | 撤销结案那段写序列也改用 CancellationToken.None：上一处只修了结案，撤销这条原样留着——断在中间会把结案说明清掉却没把人放回手上那件事 |
| test | prd-agent | 「多步写序列不许被切一半」改成 Theory 覆盖结案与撤销两个端点，加新端点补一行 InlineData 即可 |
| docs | prd-agent | 台账新增第 34-36 条：放下不顶队首、取正文的知识库判定只用了一半、了结的归属判断挡不住并发 |
| docs | prd-agent | 台账新增第 37-39 条：看别人历史没认超管、导入重试不剔已建成的、匿名面板分不清拉取失败与被关掉 |
