| refactor | cds | 验收报告首页 PipelinePanel 落 G 稿（厂房剖面空间隐喻）：入料溜槽 / 带面缺口 / 三道闸 / 货堆 / 三料仓 / 项目垛 / 场外点阵，颜色全走 CDS token 双主题，<1024px 转纵向流水与横条项目行 |
| fix | cds | LeakKind 守卫的判据太窄：只认 `kind === 'x'` 比较式，首页改用 `totalLeaks['x']` 下标式后既漏掉真实依赖又误报「没有依赖」。改为两种写法都认，且不再要求依赖非空（没有依赖是合法状态）。两种写法各做过红检 |
