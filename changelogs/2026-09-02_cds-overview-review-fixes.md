| fix | cds | 同名重建不再跨生命周期做速率差分：docker stats format 末尾加 {{.ID}}，容器短 ID 变了就切断差分。只靠「累计值变小」判重建是漏的——部署复用容器名，新容器若在首次采样前已比旧容器最后读数跑得更多，差值仍为正，会记出巨大的假速率尖峰（实测 99800 B/s） |
| fix | cds | docker stats 解析宽容到 7 段：ID 是新加的末段，缺了退回旧行为。硬要求 8 段的话任何一次拿不到 ID 都会让整批指标静默消失，比漏判一次重建严重得多 |
| fix | cds | 补上 GET /branches/:id/metrics/series 的 Activity Monitor 中文 label。已有的 /metrics pattern 用 $ 收尾接不住子路径，缺了只显示裸 URL，启动时 auditApiLabels 也会告警（cds/CLAUDE.md §0.1） |
| chore | cds | 删除无人调用的 forgetContainer 导出（形状 2 死接线）：容器身份改由 ID 判定，单点且能覆盖 CDS 之外的销毁，比在七八处销毁点逐个挂钩子稳；内存已由 MAX_CONTAINERS 兜底 |
| test | cds | 新增 5 条（ID 变了不做差 / ID 没变照常差分 / 没给 ID 时降级 / 老格式仍可解析 / metrics 与 metrics/series 各有 label），均做过红绿闭环 |
| fix | cds | 合计与构成条宽度改走 nowValue（实时优先），与旁边每服务的数字同一口径。此前图例走实时、合计走桶末值，同屏上大数比它旁边的数字慢 30-70 秒 |
| fix | cds | 历史端点失败不再被 catch 静默吞掉：新增 seriesError，持续失败时明说「曲线来不了，不是还没攒够」并端出实时读数。此前骨架屏会永远承诺一条不会出现的曲线 |
| fix | cds | 实时采样失败不再藏掉历史图：docker stats 挂了只在图上方加一条提示条，历史照画。两个数据源两个错误面，互不牵连 |
| test | cds | 新增 3 条守卫；并把两条被本次重构打红的既有守卫改成按意图断言——一条把闸门挂在错误文案上（文案一改就找不着），一条锁死了 `s.values.at(-1)` 字面量（形状 4a） |
| fix | cds | 冷启动的桶数封顶改按「样本覆盖了多长时间」，不按样本个数。桶宽 = 窗口 / 桶数，而窗口恒是完整 30 分钟：6 个 45s 样本（共覆盖 225s）拿到 8 个桶、每桶 225s，6 帧全落进一个桶，filled 停在 1，图一直藏着而骨架屏还在承诺「约还需 45 秒」。而「覆盖多长时间」正是采集节奏本身，故把节奏门槛从 3 个样本降到 2 个（一个间隔就够），两道闸合成一道 |
| fix | cds | 总览的服务成员集合也改取 /metrics 的这一帧，不再只把 status 换新鲜。上一轮只刷新了「每个成员的那个字段」，成员集合仍是抽屉打开时的快照：部署期间新增的 profile 在总览上根本不存在（健康环、服务数、堆叠层都没有它），删掉的 profile 赖着不走 |
| test | cds | 新增 4 条守卫（六帧必须落进 ≥2 个桶 / 两帧时桶宽仍不细于节奏 / 服务清单整份来自同一新鲜来源 / metrics 未就绪时有兜底），并把一条锁死旧行为的「点数按样本数封顶」断言改写成按不变量断言（形状 4a），均做过红绿闭环 |
| fix | cds | 缺口不再被画成 0：几何按「有样本的桶」切成一段段，没采到的桶留空（Netdata 的画法：没有的值一律不画）。此前一个漏采的桶被映射成 0 画出来，图上就是一道「掉到 0 又冲上去」的深谷，而那一刻没有任何服务空闲过 |
| fix | cds | 桶边界吸附到绝对时间网格，不再跟着 now 漂。此前前端每 5 秒问一次「最近 30 分钟」，边界整体平移 5 秒，同一个采样点这次落第 12 桶、下次落第 11 桶——整张图每 5 秒重新洗牌，看起来「一直在变但动得没道理」。改为右端吸附到桶宽整数倍，时间往前走只在右边追加新桶 |
| fix | cds | 单桶孤岛画成一个桶宽的方块，而不是宽度为 0 的隐形路径 |
| test | cds | 新增 7 条守卫：几何层（空桶切两段 / 全有数据不切碎 / 堆叠各层断在同一处 / 单点有可见宽度 / present 掩码落进 seedMetricSeries）、组件层渲染冒烟（只看 CPU 卡那一段，防吞吐图替它答题）、服务端网格稳定性（5 秒后再查桶边界不动）。均做过红绿闭环 |
| fix | cds | 采集节奏判据从「降序取第 3 大」改为「最大的那个间隔，若找不到同量级的同伴（1.5 倍内）才退到第二大」。按位置丢弃会连同一整档节奏一起丢：抽屉在后台采样器攒够 4 帧之前打开时，窗口里只有 1-2 个 45s 间隔，正好被当成两个离群点丢掉，节奏又变回 5s，稀疏那一段三分之二是空桶。「出现次数少」不等于「是异常」——一档节奏可以只出现两次，而一次漏采只会出现一次 |
| fix | cds | 相隔不到 1 秒的第二次写入直接丢弃。两个写入方（45s 常驻采样器、抽屉打开时的 5s 端点）各自独立跑 docker stats，可能相隔几毫秒先后落点；速率是累计差 / 间隔，除以 0.003 秒能把 300 字节放大成 51111 B/s 的假尖峰（实测值）。原护栏只挡「时间戳没往前走」，挡不住「往前走了 1 毫秒」 |
| test | cds | 新增 3 条守卫（稀疏档只出现两次仍算一档节奏 / 毫秒级撞车不产生假尖峰 / 相隔正好 1 秒仍收下，去重不误伤真实节奏），均做过红绿闭环 |
| docs | cds | 台账新增 D6：/metrics 的 status 抄自持久化状态而非当下运行事实，容器在 CDS 之外消失时总览仍按在跑计算 |
| fix | cds | 网格吸附不再突破 points 上限：桶宽按 span/(points-1) 取，把「右端吸附最多右移一个桶宽」这一个桶的余量先算进桶宽里。此前未对齐的窗口（before 默认是 Date.now，几乎不可能正好落在桶边界上）points=1 会返回 2 个点、points=120 返回 121 个 |
| fix | cds | 骨架屏的「还要等多久」改按服务端实际用的桶宽算，不再写死采样器的 45s。抽屉开着时 5s 端点也在写，桶宽会自己收窄到十几秒，拿 45s 去算会把 30 秒说成 90 秒；窗口里只剩稀疏样本时桶宽又比 45s 粗，45s 成了低估。桶宽还没拿到就不给数字，只说「攒够两帧就出现」 |
| test | cds | 新增 3 条守卫（未对齐窗口点数不超请求值且仍盖住窗口 / 桶宽从 series 响应接到骨架屏 / 写死常量已删），均做过红绿闭环；并把切 JSX 调用的守卫从「拍一个字符数」改成切到元素收尾——属性一多就切不到最后几个，守卫会在代码正确时变红 |
| fix | cds | 收样本按调用方问的区间，不按吸附后的网格。网格两端各溢出不到一个桶宽是为了让桶边界稳定，但它不该改变「这次查询覆盖哪段时间」——显式问一段历史时，before 之后发生的采样被算进最后一个桶，返回的就是问的那一刻还没发生的数据 |
| test | cds | 新增 1 条守卫（区间外样本不算数），数值是量出来的：第一版把 before 写成整秒、样本放在网格之外，旧写法也会排除它，用例永远绿什么都没测（形状 4b） |
