| docs | doc | 新增 MCP 智能体接入台方案草稿（左下角接入入口 + 五块能力工具化 + 授权与配额收口） |
| feat | prd-api | MCP 接入台第一期后端：新增能力目录（视觉创作/文学创作/知识库/网页托管/海鲜市场）作为 scope 与工具的唯一事实源 |
| security | prd-api | 签发 AgentApiKey 时校验用户自身权限位与请求 scope 的交集，鉴权时对新 scope 二次核对，权限被回收后旧密钥即刻失效 |
| feat | prd-api | 新增视觉创作、文学创作、网页托管三个开放接口层，知识库开放层补写入端点（建库/写文档/改正文），均走 sk-ak + scope + boundUserId |
| feat | prd-api | MCP 内置工具从 5 个扩到 18 个，新增生图、创作、知识库写入、网页托管、市场取用类工具 |
| refactor | prd-api | scope「写蕴含读」判据收敛到能力目录一处，不再按资源名硬编码 document-store |
| feat | prd-api | 接入台调用记录：每次工具调用（含被挡下的）落 mcp_call_logs，自动认产物（站点/文档/工作区/生图任务）供一键打开 |
| feat | prd-api | 按密钥的用量闸门：每日生图 50 张、每日写入 200 次、每分钟 60 次调用，均可按密钥调整 |
| feat | prd-api | 新增接入台面板接口 /api/mcp-console（能力清单、客户端、今日额度、调用记录、密钥可见工具自检） |
| ops | scripts | mongodb-indexes.js 补 mcp_call_logs 两条索引（面板翻页与日额度统计） |
| feat | prd-admin | 新增智能体接入台页面 /mcp-console：能力清单（每块能力挂着哪些工具）、连着的客户端、今日额度、调用记录与产物直达 |
| feat | prd-admin | 新增三步接入向导：勾能力（默认只读、写入需再点一次）→ 出钥匙 → 三份客户端配置一键复制 + 授权自检 |
| feat | prd-admin | 侧栏左下角账号菜单新增「智能体接入台」入口，导航注册表登记到「实用工具」分组 |
| docs | doc | debt.platform 记一条：接入台暂未进百宝箱（缺卡片插画素材），补图后三处登记即可 |
| fix | prd-api | 修复签发密钥时把海鲜市场 scope 也拿去做权限交集校验的问题：权限目录里没有 marketplace.skills.* 权限位，那条校验会让所有人（含 root）都签不出市场密钥 |
| test | prd-api | 权限映射守卫改为只覆盖真正参与校验的四个 scope，并补两条断言钉住「为什么排除市场与知识库」 |
| fix | prd-api | 权限判定认 super 通配：持超级权限的管理员此前签不出接入台密钥、面板还把能力显示成灰的 |
| fix | prd-api | root 破窗账户签出的密钥不再被鉴权剥光 scope（root 不在库里，按 owner id 判身份） |
| fix | prd-api | 日配额改为原子占坑 + 失败退还，并发请求不再一起冲破上限；新增 mcp_usage_counters 集合 |
| fix | prd-api | 接入台自检与「已授权」按主人当前权限重新核对，不再报出智能体其实看不见的工具 |
| fix | prd-api | 文学创作建工作区兑现 clientRequestId 幂等（确定性 id），重试不再攒出重复工作区 |
| feat | prd-api | 密钥配额上限可改（每日生图/写入、每分钟调用），配额触顶提示里的入口现在真的存在 |
| feat | prd-admin | 接入台今日额度卡新增「调整上限」编辑弹窗，已用数与闸门读同一份计数器 |
| fix | prd-api | 写蕴含读改为按能力声明（跟随真实闸门），海鲜市场写 scope 不再被当成能读，避免列出一批调用即 403 的工具 |
| fix | prd-api | 新增的两个集合登记进数据同步排除表，补上漏掉的分类 |
| fix | prd-api | 接入台今日计数改 Mongo 聚合，不再因取最近 1000 条而系统性少报 |
| fix | prd-api | MCP 端点地址按转发头解析，反代终止 TLS 时不再给出 http:// 的连接地址 |
| fix | prd-api | 调用记录与配额退还改用服务端令牌，客户端断开后不再丢记账 |
| fix | prd-api | 幂等命中的重试不再重复扣配额（占坑退还 + 记录标记） |
| fix | prd-admin | 接入向导说清「只有写入档」的能力：勾上即授予写入，不再用只读名义给出改动权限 |
| fix | prd-api | 智能体写文学正文时复位配图派生状态（版本 +1、清标记与带标记正文），旧图不再挂在已经变了的正文上 |
| fix | prd-api | 知识库建文档时正文没落盘就撤回条目：不再留下一篇空文档，同幂等键重试也不会拿到「成功」 |
| fix | prd-api | 网页托管建站的幂等键压成确定性站点 id，超时重试与原请求撞在一起时不再各建一个站 |
| fix | prd-api | 智能体建分享链改走复用路径，重试不再攒出重复链接、重复扣写入额度 |
| fix | prd-api | 文学正文 append 带乐观并发条件，并发追加不再互相覆盖丢段落 |
| fix | prd-api | 调用记录认出挂在 images 数组里的产物地址（查生图结果那条记录因此点得开；入队那条当时还没有图片地址，仍然没有「打开」，见验收报告 P2） |
| fix | prd-api | 密钥配额校验挪到写库之前，一次 PATCH 不会「报错了但名字已经改了」 |
| fix | prd-api | 接入台按读/写分别上报可用性，只有只读权限的人不会被点亮写入勾选 |
| fix | prd-admin | 接入向导：只读权限的人写入勾选框置灰并说明；签发按钮在自检结束前保持锁定，避免连点签出两把钥匙 |
| fix | prd-admin | 接入台两列各自滚动，能力卡变多时不再把整页顶出去 |
| test | prd-api | 新增调用记录产物识别守卫（含 images 数组、读类工具不算产物、幂等命中标记） |
| fix | prd-api | 配额退还按占坑那天算，跨 UTC 午夜的失败调用不再把新一天的计数器减成负数、放松当天闸门 |
| fix | prd-api | 「密钥还能不能用」收敛成 AgentApiKey.IsUsableAt 一处判据（鉴权/面板/状态标签同源），过了宽限期的钥匙不再在接入台显示成已连接 |
| fix | prd-api | 能力自检补上动态开放接口，只带 agent.* scope 的钥匙不再被报成「0 个工具」；可见性判据复用网关那一处 |
| fix | prd-api | 带结构模板的知识库（如验收报告库）暂不开放给智能体写入，避免绕过模板校验塞进不合规记录 |
| test | prd-api | 新增密钥可用性判据守卫（含「过了宽限期但 IsActive 仍为 true」这条事故形状） |
| fix | prd-api | 产物地址的数组下探收窄到生图任务：列表类工具（如列站点）不再把第一条既有记录当成这次的产物 |
| fix | prd-api | 知识库建库/建文档的幂等改走确定性 id（撞主键即命中），叠在一起的两次重试不再各建一份 |
| feat | prd-api | map_kb_create_store 新增 clientRequestId 幂等键，四个写入类工具的重试语义统一 |
| fix | prd-api | 动态开放接口的写入也计日额度：闸门改读记录里的 ImageCount/IsWrite，不再因「没有工具定义」整个跳过日额度 |
| fix | prd-api | 知识库建文档的幂等命中区分「已完整」与「正文还在落盘」，撞在写入窗口里的重试回 409 而不是交出一篇空文档 |
| fix | prd-admin | 接入台刷新按钮同时刷新调用记录列表，停在记录页点刷新不再纹丝不动 |
| fix | prd-api | 外部地址解析改为协议与主机分开取：本仓库 nginx 从不设 X-Forwarded-Host，上一版的判据在真实部署里永远走不到，连接地址仍是 http:// |
| fix | prd-api | 密钥的元数据与配额合成同一次写入，PATCH 不再出现「报错了但名字已经改了」的半截状态 |
| fix | prd-api | 今日用量统计包含当天被撤销的密钥，today.calls 与 today.images/writes 不再自己跟自己对不上 |
| test | prd-api | 新增外部地址解析守卫，含「只有 X-Forwarded-Proto、没有 X-Forwarded-Host」这条真实 nginx 形态 |
| fix | prd-admin | Codex 配置改用 http_headers（官方键名），此前写成嵌套 headers 表会被静默丢弃、请求以匿名身份打到 MCP 端点 |
| fix | prd-api | 海鲜市场能力卡去掉写入档：上传走 multipart、MCP 传不了二进制，那个 scope 挂零个工具；scope 本身在签发白名单里保留（市场上传 REST 接口仍在用） |
| fix | prd-api | 工具的写入语义可显式声明（WritesData），取用技能虽是 POST 但不再占用每日写入额度 |
| fix | prd-api | 知识库建文档把计数与正文纳入同一段补偿，计数失败不再留下永远 409 的死记录 |
| test | prd-api | 守卫从「每块能力有工具」收紧到「每个声明的 scope 都有工具」，空头 scope 不再能摆进向导 |
| fix | prd-api | 配额计数器带上部署作用域：在分支预览上跑几张图不再吃掉同一把密钥在生产的当日额度（共库共密钥，跨项目隔离通道 4/8） |
| fix | prd-api | 调用记录与额度改用稳定分支作用域（不含 commit revision），同一分支重新部署后记录不再整批消失、今日计数不再归零 |
| test | prd-api | 新增计数器作用域守卫（纯函数版，不改进程 env），并钉住「取 CurrentDurable 不取 Current」 |
| fix | prd-api | 调用记录详情也按部署作用域过滤，与列表/概览同一把尺子，拿别的分支的记录 id 打过来不再读得到 |
| fix | prd-api | 撤回没建成的条目时一并清版本快照与双链账本，不再留下指向已删条目的派生行 |
| fix | prd-api | 今日用量覆盖「今天有过调用但密钥已被撤销或硬删」的那几把，calls 与 images/writes 不再对不上 |
| fix | prd-api | 速率闸提到工具解析之前：刷不存在的工具名、刷无权限的调用不再绕过每分钟上限、把审计集合刷爆 |
| fix | prd-api | 产物识别改用工具的写入语义，不再看工具名里有没有 _get_ 片段；纯读工具（如读文档正文）不再被记成产出了东西 |
| fix | prd-admin | 签发在途时不允许关闭接入向导，避免密钥已建好而明文被 reset 掉、用户手里多一把看不到的钥匙 |
| docs | doc | 计划文档的工具清单换成按能力讲的行为表：原表还写着已删掉的上传工具与 19 个工具，且实现级清单本就不该进 doc/ |
| fix | prd-api | 智能体生图绑定「智能体生图」工作区：此前不绑工作区导致结果只留 base64、接口回 url:null，图既拿不到也不落进视觉创作空间 |
| fix | prd-api | 文学正文的 mode 只认 replace/append，写错一个字母不再默默走整篇覆盖 |
| fix | prd-api | 知识库建文档校验 parentId 必须是同库文件夹，不再插出挂在错地方、界面看不见也删不掉的条目 |
| perf | prd-api | 接入台今日已用改一次批量查询，密钥多时首屏不再逐把往返 |
| fix | prd-api | 分享链回绝对地址：相对路径会被智能体的客户端按自己的域名解析，用户点开 404 |
| fix | prd-api | 文学写入只认文学场景的工作区，不再能把正文写进用户名下别的场景（如智能体生图）的工作区 |
| fix | prd-api | 被限流的调用一分钟内只落一条审计，限流终于能保护它本来要保护的那张表 |
| fix | prd-api | 幂等命中时不再连「这工具会不会产出东西」一起清零，重试记录不再丢掉既有产物地址 |
| fix | prd-api | 撤回条目改为先清派生、最后删条目，等在旁边的重试不会被这段清理误删自己的派生记录 |
| fix | prd-api | 「扣不扣额度」与「出不出产物」拆成两个判据：取用技能不占写入额度但确实取到了东西，记录不再抹掉它返回的下载地址 |
| test | prd-api | 补守卫钉住这两个判据必须分开，并要求所有 GET 工具两者皆假 |
| fix | prd-api | 限流去重的胜负改用 TryAdd 原子判定：ConcurrentDictionary 更新委托在竞争下会被调多次，靠它设标志会让并发的每一条都自认第一条 |
| fix | prd-api | 派生清理失败时保留条目占位、不释放确定性 id，避免重试建的新条目继承残留的版本与双链 |
| fix | prd-api | 直连开放接口（绕开 /api/mcp）现在也过同一套用量闸门：接入台上的每日配额对两条路都成立，不再是「走网关才扣」 |
| fix | prd-api | 网关回环续跳带进程内令牌自证，全局限流不再把所有密钥挤进同一个 127.0.0.1 桶；sk-ak 改按密钥 id 分桶 |
| fix | prd-api | 回环失败不再把异常原文（回环主机名/端口/传输层细节）回给外部客户端，只回一句稳定说明，原因留服务端日志 |
| refactor | prd-api | 生图张数的收敛区间收成一处，网关与控制器读同一个 ResolveImageCount |
| test | prd-api | 补守卫：每个内置工具都反查得回自己、动词参与判定、回环令牌只认本进程那一份、两处生图张数必须一致 |
| fix | prd-api | 直连闸门补上动态工具那一半：登记表（AgentOpenEndpoint）接口直连也扣写入额度、也进调用记录，不再只堵内置工具 |
| security | prd-api | 签发带 document-store 读/写 scope 的密钥时先查密钥主人自己有没有对应权限位：这个 scope 等价于文档空间读写权限，自签等于凭空长权限。鉴权口径不变，存量密钥照常能用 |
| fix | prd-api | 速率窗口与限流拒绝标记按两分钟保留期定期清扫，不再随进程见过的密钥数无界增长 |
| test | prd-api | 补守卫：签发口径要查权限而鉴权口径不查、签发要查的 scope 都对得上真实权限位、保留期不许短到把当前分钟扫掉 |
| fix | prd-api | 建站改为先插站点记录再上传对象：站点 id 带幂等键时是确定性的，先传后插会让并发的输家覆盖赢家已发布的页面 |
| fix | prd-api | 回滚知识库条目时，只有条目真的删掉了才把库的文档计数减回去；条目被保留占位时再减会让摘要永久少算一条 |
| test | prd-api | 补守卫：建站两步的先后顺序、以及「条目没删就不减计数」的真值表 |
| feat | prd-admin | 智能体接入台注册进百宝箱（带 wip 标记），并补上双主题卡片插画与任务文案 |
| fix | prd-api | 分享链复用时如实回报 deduplicated，网关据此退还已占的日写入额度，重试不再白扣 |
| fix | prd-api | 建站占坑期间的记录对幂等查询与列表都不可见；上一次没传完的坑由下一次接手补传，不再永久占死幂等键 |
| fix | prd-api | 建站占坑改成抢租约：只有租约过期（上一次的进程挂在窗口里）才接手，撤回与收尾都按租约持有者过滤；别人正在传时回 409 让重试，不再两边各传一次、各自收尾 |
| fix | prd-api | 幂等命中不再把动作分类抹成「只读·0 张图」，改由 deduplicated 表达「没有新副作用」；日额度的账本本来就不从这两个字段加 |
| feat | prd-admin | 调用记录里把「幂等命中 · 未计额度」显式标出来，动作分类照实显示 |
| test | prd-api | 补守卫：租约窗口要明显长于一次上传、撤回与收尾都必须按租约持有者过滤 |
| security | prd-api | 外部请求填的 X-Forwarded-Host 不再被采信（nginx 不覆盖它），主机只认 Request.Host；网关回环续跳凭进程内令牌自证时才例外 |
| fix | prd-api | 分享链复用只在「一个字段都没动」时才报幂等命中：刷新有效期 + 追一条续期审计是真写入，照扣额度 |
| refactor | prd-api | 网页发布的「确定性 id + 占坑 + 租约 + 接手补传」整套撤除，退回随机 id + 先传后插；先后两次重试仍去重，叠在一起的两次各建一个站（取舍见 debt 边界 12） |
| test | prd-api | 转发主机的可信度判据补守卫：外部填的不采信、别的进程的令牌不算自证、Origin 不参与 |
| security | prd-api | 回环续跳不再转发调用方填的 X-Client-Base-Url / X-Forwarded-Host，一律由服务端按 nginx 清洗过的来源自己算 |
| fix | prd-admin | 接入台加载失败留持续错误态 + 重试，不再让「读不到数据」长得像「账号是空的」 |
| fix | prd-api | 只回 id 的写入工具（建库/建条目/建文学工作区）按类型反推站内深链，记录里那个「打开」不再缺席 |
| fix | prd-admin | 没有可点地址时把产物类型与 id 显示出来 |
| test | prd-api | 补守卫：三种深链形态各自反推正确，缺 storeId 与未跑完的生图 run 一律不编地址 |
| fix | prd-admin | 刷新不再把整页切回 loader：发钥匙成功后向导回调父级刷新会连带卸载弹窗，那把只出现一次的明文就此看不到，而钥匙已经生效。刷新态改挂在按钮上，已有数据时刷新失败只挂条幅 |
| fix | prd-api | 知识库条目的幂等 id 带上所属库：同一个 clientRequestId 往两个库各写一篇时不再撞主键报 500 |
| fix | prd-api | 日额度改条件自增（加完不超才加得上）：别人的退还还没落库时不再把额度够的请求误判成超限，同时省掉「加了再退」那套补偿 |
| test | prd-api | 补守卫：同幂等键在两个库推出不同 id、条件自增的上界不许差一格 |
| docs | doc | 计划文档去掉接口签名与源码路径（按 AGENTS.md §10），改为按能力与数据流讲，路径集中到文末「实现来源」 |
| security | prd-api | 调用记录的入参摘要按参数名隐去凭据（password/token/apiKey 等）：动态工具的入参形状由登记方决定，截断到 120 字对短口令没有保护，而这份摘要会显示在接入台上 |
| fix | prd-api | 直连调用抛异常时按 500 记账，不再出现「失败 · HTTP 200」这种自相矛盾的审计行 |
| ops | scripts | mcp_usage_counters 加 90 天 TTL（只读当天、历史行全是死数据）；mcp_call_logs 的保留期留一行注释交 DBA 按产品策略定 |
| test | prd-api | 补守卫：凭据参数必须隐去而 keyword/author 这类不许被隐掉、抛异常记 500 而结果自带状态码时照它记 |
| fix | prd-api | 海鲜市场的下载地址（fork 的 downloadUrl 与列表 zipUrl）改按外部来源算：经 MCP 网关回环调用时原来会回 http://127.0.0.1:5000/…，远端客户端一个字节也下不下来 |
| fix | prd-api | 标了 AllowAnonymous 的市场读端点上补跑一次密钥认证（只对带 sk-ak- 的请求）：搜索/详情/取用三个工具直连时不再绕过每分钟窗口与调用记录 |
| fix | prd-admin | 签发成功后立刻交出明文并切到「接上」那一步，授权自检退到后台并有 15 秒上限：自检挂住时不再让用户既看不到已生效的钥匙、又关不掉弹窗；自检超时可就地重试 |
| test | prd-api | 补守卫：带 sk-ak- 的凭据要认出来、JWT 与旧版 key 形态不许触发补认证 |
| test | prd-admin | 补守卫：明文与步骤切换必须排在自检之前、自检必须有等待上限、父页面刷新不许把整页换成 loader（两次事故形态各钉一条，已做红绿闭环） |
| fix | prd-api | 日额度占坑不再跟客户端的取消令牌走：客户端在这一笔在途时断开，Mongo 可能已经自增而结论回不来，用户白扣一天额度（server-authority） |
| fix | prd-api | 匿名端点上补认证拿到的主体也用于写审计行：此前记录的主人是空的，额度扣了但接入台按主人过滤看不到那几条 |
| fix | prd-api | 密钥识别补上 X-AI-Access-Key（与认证处理器同一套取头顺序），换个头写法不再绕过闸门与记录 |
| security | prd-api | 入参摘要递归隐去嵌套在对象/数组里的凭据（config.apiKey 这种），并设深度上限 |
| fix | prd-api | 入参摘要不再转义非 ASCII：中文入参此前在调用记录里显示成 `\u5468\u62A5` 那样的转义串，而这块面板就是给人看懂智能体刚才要干什么的 |
| test | prd-api | 补守卫：嵌套与数组里的凭据都要隐去、超深层级整块不展开、普通入参不受影响；认证处理器认哪些头这里就得认哪些；审计行不许再从 HttpContext.User 取主人；占坑不许跟请求取消令牌走 |
| docs | doc | debt.platform 记两条：建条目与删库并发留下孤儿条目（需删除状态位或事务，且人工路径同病）、入参摘要只按键名判凭据（值形态启发式另开一轮） |
| test | prd-api | 两条源码守卫先去掉注释行再判：第一版被自己那句解释文字（注释里写着「不是 http.User」）判红，会因注释变红的守卫只会让人去改注释 |
| fix | prd-api | 智能体覆盖知识库正文接上乐观并发：两个智能体同时覆盖同一篇时不再各写各的（冲突回 409，让它先重读） |
| fix | prd-api | 覆盖正文的派生失败（重锚评论/重算双链/版本快照）不再变成 500：正文早就提交了，报失败会让智能体重试一件已经生效的事；改为如实回 derivedState |
| fix | prd-api | 回环那一跳不再重复计一次密钥用量：一次工具调用此前让「累计请求数」涨 2，一批 N 个工具涨 N+1 |
| fix | prd-api | 网页托管的 4MB 上限按 UTF-8 字节判：按字符数判等于给中文页面开了三倍的口子，而工具描述与对象存储都按字节算 |
| test | prd-api | 源码守卫的三个共用件收敛成一处（读文件/去注释/切片），并补第 30 轮那四处接线的守卫 |
| fix | prd-api | 建知识库条目那一笔插入不再跟请求取消令牌走：客户端在它在途时断开会留下一条空条目，此后同一个 clientRequestId 的重试永远拿 409 |
| fix | prd-api | 调用方明确给的摘要不再被正文前 200 字顶掉：同时给 summary 和 content 时接口回成功、库里存的却是另一段文字 |
| revert | prd-api | 删掉限流中间件里按密钥 id 分桶那条分支：ApiKey 是非默认认证方案，要等授权环节才被选中，而它排在限流之后——那条分支一次也没生效过。每把密钥各算各的由接入台的每分钟窗口兑现 |
| docs | doc | debt.platform 边界 15：全局防滥用限流认不出智能体密钥，出路是把 ApiKey 并进默认认证方案（会改全站端点看到的身份，另开一轮） |
| test | prd-api | 补守卫：插入不跟取消走、摘要要写回、限流层不许再按密钥分桶，并钉住「限流排在授权之前」这个前提 |
| fix | prd-api | 接入台连接地址不再是回环：CDS 转发器故意把 Host 改写成上游 127.0.0.1:port，真域名只在可伪造的 X-Forwarded-Host 里，于是用户复制进客户端的连接地址是 https://127.0.0.1:48798/api/mcp。改为优先取平台/运维显式声明的公网入口（ServerUrl 配置 / CDS_PREVIEW_URL 注入），请求方够不着，既治回环也不重开伪造口子 |
| test | prd-api | 补守卫：回环 Host + 外部伪造转发头时必须取声明入口、ServerUrl 优先于平台注入、声明值写歪了当没配 |
| fix | prd-api | 开放层七处写库全部改成服务端自己的令牌（建工作区/入队生图/写正文/建库/加计数）：客户端断开时 Mongo 可能已经落库而驱动抛取消，此前会留下孤儿工作区、重复计费的生图、永久多算一条的库计数 |
| fix | prd-api | 网页托管的上限改按**变换之后**的字节判：服务端会重写绝对路径并注入近 10KB 翻页垫片，只校验入参等于承诺 4MB 却存进去 4MB 多；超限回 400 并说清为什么比提交的大 |
| fix | prd-admin | 调用记录读失败留持续错误态 + 重试：此前只弹一下 toast，首次加载会显示成「还没有调用记录」，换筛选失败则把上一次的行留在新条件下——审计面板开始说谎比没有更糟 |
| fix | prd-admin | 授权自检加代次：弹窗关掉/重开/再发一把钥匙时，上一把钥匙的回应不会再落到界面上（否则会显示成新钥匙能调的工具） |
| fix | prd-admin | 手机端侧滑抽屉补「智能体接入台」入口：桌面端那条在侧栏账号菜单第一项，而侧栏在小屏是 display:none 的，手机端此前只剩「汉堡 → 百宝箱 → 在几十张卡片里找」这条三层路径 |
| fix | prd-api | 网页托管开放层回绝对地址：ASSETS_PROVIDER=local（dev compose 的默认值）时 SiteUrl 是 /local-assets/… 相对路径，远端智能体拿到后按自己的域名解析，点开 404；发布、幂等命中与列表三处统一补齐来源域名 |
| fix | prd-api | 建站改用服务端令牌落库：对象已用 CancellationToken.None 传上去，插库那一步若跟着客户端断开被取消会留下没人指向的孤儿对象，且因为没有 SourceRef 行，同一个 clientRequestId 的重试会再传一个 |
| fix | prd-admin | 调用记录的读请求加代次令牌：筛选条件连改两次时慢的那个旧请求回来会覆盖新结果，当前筛选下显示别的客户端或别的结果状态的记录 |
| fix | prd-api | 幂等键不再截断到 120 字：前 120 字相同、后面不同的两个合法键会压成同一个，第二次写入被报成幂等命中、悄悄不做（调用方拿到 success 却什么都没写） |
| refactor | prd-api | 幂等键归一化收敛到 McpIdempotency 一处：原来视觉 / 知识库 / 网页托管各抄一份，同一个截断缺陷同时活在三处，自动 review 只在其中一处发现它 |
| fix | prd-api | McpUsageCounter.Id 按实体契约给随机 id 默认值：空字符串会让任何走普通插入的路径共用同一个 _id |
| test | prd-api | 补幂等键守卫：长键不许坍缩（含下游确定性 id）、两把密钥互不干扰、三个开放层控制器必须走同一判定源且不许再自己取密钥 id |
| fix | prd-api | 文学创作的幂等键也收敛进 McpIdempotency：上一轮只收了三个控制器，第四个漏在外面，同一处截断在那儿又活了一轮 |
| fix | prd-api | 开放层返回给智能体的存储地址统一绝对化（视觉创作的图、知识库附件）：本地资产驱动下回的是 /local-assets/… 相对路径，远端客户端点开 404 |
| fix | prd-api | 分享链写库改服务端令牌：写已提交而驱动报取消时，用量过滤器会退还额度，重试于是又建/又续一条 |
| refactor | prd-api | 绝对地址助手从 WebPages 的私有方法升到 RequestOriginExtensions.ResolveAbsoluteUrl，兄弟控制器不再各漏各的 |
| fix | prd-api | 删掉没人调用的 GET /api/mcp-console/calls/{id} 与它那条前端 service：列表本来就回完整记录，展开行用的就是列表数据 |
| fix | doc | 计划文档删掉逐文件实现清单（AGENTS.md §10：实现代码/接口签名/目录树/逐文件改法不进 doc/） |
| docs | doc | 看板 P4 行回填：验收已完成并归档（此前记的「口令不在任何人手里」已证伪——它一直在容器 env 里），补上 CDS 报告深链与剩余三项 |
| fix | prd-api | 三处「注释/文档说的」与代码不符改正：幂等命中不抹动作分类、日额度不查 mcp_call_logs、生图入队那条记录仍然没有「打开」 |
| test | prd-api | 幂等守卫从写死三个文件名改成枚举全部 *OpenApiController.cs——写死清单正是文学创作那处漏检的成因；另补绝对地址助手的行为测试与分享链写库的令牌守卫 |
| fix | skill | 验收 harness 的 theme 字段改为实测（量真实底色亮度），不再记调用方传进来的意图——那次 20 张暗色图被整批记成 light 就是这么来的 |
| chore | prd-api | 重跑 bundle-official-skills 同步 official-skills.generated.json：验收技能的 harness 改过，而它的全文内嵌在这份生成物里，不同步 CI 的「生成物新鲜度」会红 |
| fix | prd-api | 视觉创作与知识库开放层补 using PrdAgent.Api.Extensions：上一轮把绝对地址助手推广到四个控制器时漏了这两处的 using，编译不过 |
| fix | prd-api | 视觉创作生图的尺寸参数改用 req?.Size：同一个方法里上面已按可空处理 req，这里不该再直接解引用（本轮新引入的 CS8602） |
| fix | prd-api | 绝对地址助手补 scheme 判断：裸用 Uri.TryCreate(UriKind.Absolute) 时 Linux 会把 /local-assets/… 当成合法的 file:// 绝对地址原样返回，于是它对唯一要治的那种输入在 CI 与生产上完全不生效 |
| fix | prd-api | 网页托管的幂等键压成定长指纹再进库：去掉截断后它成了唯一把调用方原文存进 Mongo 并当查询键的一路，30MB body 能造出同样大的文档 |
| fix | prd-api | 直连闸门排到自动模型校验之前（Order = -2001）：body 传坏的 sk-ak 请求原来会被 400 短路，既不过每分钟窗口也不进调用记录 |
| test | prd-api | 补三条守卫：绝对地址助手对 Linux file:// 那种输入的行为、SourceRef 必须是定长指纹、闸门顺序必须小于 -2000 |
| fix | prd-api | 视觉创作与知识库的幂等键也压成定长指纹再落库：前者原样进带唯一索引的 ImageGenRun.IdempotencyKey、后者原样进条目 Metadata["mcpRequestId"]（写完从没被读过），clientRequestId 无界，一个超长键就能撑大甚至顶破文档 |
| refactor | prd-api | 四个开放层的 SHA-256 收敛到 McpIdempotency.Fingerprint 一处：原本网页托管内联、知识库 DeterministicId、文学 Sha256Hex 各写一份，视觉创作一份都没有——判据分裂之后漏掉的那个兄弟 |
| fix | skill | 验收 harness 的主题探测改为只认不透明底色：body 常是 rgba(0,0,0,0)，只看 rgb 三个 0 会把浅色页记成 dark，双主题证据比不量还错 |
| test | prd-api | 幂等守卫加两条：指纹定长且长键互不坍缩（行为）、ScopedByKey 的结果必须当场被 Fingerprint 包住（结构，视觉那一路原来正好违反） |
| test | scripts | 新增 visual-harness-theme-probe.test.mjs：把 harness 里的判定函数取出来真跑（透明/半透明/不透明各一档），不是断言几行字面存在 |
| ci | ci | harness.mjs 登记进 release-script-test 的 path filter：它是 visual-harness-*.test.mjs 的被测文件却一直没登记，只改它的 PR 完全跳过这道闸（形状 7） |
| test | prd-api | 上一轮那条「SourceRef 必须是定长指纹」的守卫断言的是 SHA256.HashData 的字面存在，于是把内联 SHA 收敛进共用指纹时反过来把修复判红了（形状 4a，谁修 bug 谁的 CI 红）；改成只钉不变量「不许拼原文」，必须走共用指纹与行为面交给枚举全部开放层控制器的那条守卫 |
| fix | prd-api | 直连闸门改按框架选中的路由模板反查工具：按原始路径匹配时占位段会吞掉同级静态路由（GET /api/open/marketplace/skills/tags 被 /skills/{id} 认成 marketplace_get_skill），那条非工具接口会白扣一次分钟窗口并在调用记录里留一条假审计，反复请求还能触发 429 |
| fix | prd-api | 网页托管的 title/description/folder/tags 补上限（按 UTF-8 字节），且判在调用服务之前：服务是先传对象后插库，元数据过大会让对象传上去、插库时顶破文档上限失败，留下没人指向的孤儿对象 |
| docs | doc | 删掉 debt.platform.md 里那条指向插画生成脚本的逐文件实现指针（AGENTS.md §10：实现细节不进 doc） |
| docs | doc | debt.platform.md 记边界 16：网页托管列表只有 limit 没有分页，超过 100 个站的账号翻不到更旧的 |
| test | prd-api | 补两条守卫：静态兄弟路由不许被占位段吃掉（含占位符改名/带约束仍算同一条）、网页托管元数据必须在传对象之前判上限（含中文按字节算那一档） |
| feat | prd-api | 文学创作补一条读正文的开放接口与 map_literary_get_workspace 工具（内置工具 18 → 19）：此前开放层里没有任何一条路能读到正文，而 append 冲突时的 409 文案写着「请重新读一遍正文再追加」——那句指引在当时根本做不到，改稿与续写这两件工具描述里承诺过的事同样无从下手 |
| fix | prd-api | 知识库回滚清理区分「这次真删了 / 早已不在 / 刻意保留」三态：正文落盘期间用户在界面上删掉这条时，那条路径已经扣过 DocumentCount，回滚再按「是我删的」扣一次会把计数扣成负数 |
| fix | prd-api | 调用记录的入参摘要限制遍历宽度（256 个节点）：深度早就卡住了，宽度没有，一个几万元素的平铺数组会被整个克隆与序列化后只留 120 字符；而摘要构造在限流之前，等于给调用方一个用自己的 body 放大服务端开销的杠杆 |
| fix | prd-api | 知识库建文档时递增库计数必须看 MatchedCount：库在写入过程中被主人删掉时原来一行没命中也照样往下走，给调用方回「建好了」，而那篇文档挂在一个已不存在的库上、正文非空还会写出孤儿正文与版本；现在按 STORE_DELETED 回 409 并撤回条目 |
| refactor | prd-api | 开放层收调用方文本的上限收敛成 McpInputBounds 一处判定源，并补齐此前漏掉的五处：分享链 title/description、知识库建库 name/description/tags、建文档 title/summary/tags/parentId、生图 size（改成有限形状正则）|
| test | prd-api | 新增 McpInputBoundsTests：反射列出开放层全部请求 DTO 的每一个字符串字段，逐个要求在账本里有交代（字节上限/自有内容上限/有限形状/会被哈希/必须存在/不落库），新增字段不做决定就变红；账本里的死条目同样判红 |
| fix | prd-api | 知识库覆盖正文的乐观并发原来是假的：传给写入服务的 expectedUpdatedAt 是刚重新读出来的那个，条件永远成立，只挡得住相邻两行代码之间的缝隙，挡不住「智能体读到 → 用户改了 → 智能体覆盖」这种真会丢改动的时序；现在版本令牌由调用方给（读端点回 updatedAt，工具契约加 expectedUpdatedAt），认不出来回 400、对不上回 409 |
| fix | prd-api | 回环失败且响应体认不出结构时，不再把原文前 300 字当失败原因存进用户可见的调用记录（那种响应往往是代理页或框架错误页，带内部主机名与调用栈）；原文改落服务端日志，面板上给一句稳定可执行的说明 |
| fix | prd-admin | 接入台向导的授权自检：超时之外的失败（401/500/网络断）原来什么都不设，转圈直接消失、既无结果也无重试，而钥匙明文只出现一次；现在留错误态并复用同一个重试入口 |
| fix | prd-admin | 复制按钮改成等剪贴板真的写成功再报「已复制」：剪贴板 API 不可用或被拒时原来照样显示成功，而复制的是只显示一次的密钥明文，用户会据此安心关掉弹窗 |
| test | prd-api | 补版本令牌守卫：没给/原样传回/中间被改过/认不出来四档各断言一次 |
| fix | prd-api | 版本令牌解析改用单独的 RoundtripKind：上一版把它与 AdjustToUniversal 一起传给 TryParse，而这两个是互斥组合，会直接抛 ArgumentException 把整条判据炸掉（不是返回 false）；另补「没带时区信息按 UTC 认」，否则库里存的 UTC 被按本机时区解释，正确的令牌会被判成冲突 |
| fix | prd-api | 文学创作 mode=replace 补版本令牌：此前只按 workspaceId 过滤、无条件覆盖，智能体读到之后用户在界面上改了的那次编辑会被旧稿盖掉（append 那一路本来就带条件写入，缺的一直是 replace 这一半）|
| refactor | prd-api | 版本令牌判据收敛成 Mcp/McpRevision 一处（Check + Token），知识库与文学创作共用；判据留在某个控制器里就注定被抄第二份 |
| fix | prd-admin | 接入台首屏的「复制地址」同样改成等剪贴板真的写成功再报：判据抽成 mcp-console/clipboard.ts，与向导里那个复制按钮共用一处 |
| docs | doc | debt 边界 3 去掉指向生成脚本的实现指引，改写成决策/风险/验收层面（AGENTS.md §10）|
| test | prd-api | 守卫扩到「覆盖既有正文的入参必须能接版本令牌」：内容类字段要声明是新建还是覆盖，覆盖类的 DTO 必须有 ExpectedUpdatedAt；另加「读端点回的令牌必须能被写端点原样认回来」 |
| fix | prd-api | 文学 replace 的写入条件补齐：上一轮只在进函数时比了一次版本令牌，真正那条 UpdateOne 仍只按 id 过滤，校验与写入之间那段窗口里用户改一次照样被盖掉——检查和写入必须是同一个条件，否则那次检查只是个说法 |
| fix | prd-api | 建条目时的正文写入带上刚插入那条的 UpdatedAt 并看返回值：用户可以在正文落盘刚起头时把这条占位条目删掉，而写入服务只有在给了 expectedUpdatedAt 时才把「一行没命中」当冲突，不给就照常往下写，留下指向已删条目的孤儿正文与版本、接口还回成功 |
| fix | skill | 验收 harness 的主题标记降级为线索：切主题时 data-theme/class 先翻而对应 CSS 没生效是完全可能的，那正是双主题验收要抓的失败；改成先量真实底色，量得出来以量到的为准，量不出来才退回标记 |
| fix | prd-admin | 接入台首屏 overview 加载补代次令牌：顶部刷新在飞时发新钥匙会再触发一次加载，先发的那次后落地就把刚发的客户端与配额从界面抹掉（调用记录那一列上一轮已这么修过，这是漏掉的兄弟）|
| fix | prd-api | 整篇覆盖的两条端点（文学写正文、知识库改正文）区分「省略 content」与「显式给空串」：前者原来被当成空串，直连打一个 {} 过来（mode 默认 replace）就把整篇正文清空、配图流程复位，接口还回成功；MCP schema 里 content 标了 required，但网关不拿 schema 校验参数，schema 是描述不是闸门 |
| fix | prd-api | 入参摘要的字符串叶子补长度上限：宽度预算按节点数算，而一个几 MB 的字符串只占一个节点，照抄再序列化最后仍只留 120 字 |
| test | prd-api | 补两条守卫：巨大标量不许被整个克隆；被声明为「覆盖」的内容字段，其控制器必须走 RequireContent（守卫挂在字段声明上而不是全仓扫字符串，免得把新建类端点误伤成红）|
| fix | prd-api | 建条目时把 Conflicted 一律当成「条目被删了」是错的：任何 UpdatedAt 变化都会让条件写入落空，用户在界面上编辑这条刚出现的占位条目同样命中，而上一版的回滚会把用户刚写的内容连同版本、双链一起删掉——修一个竞态修出了一个更坏的；现在先回读判「删了还是改了」，被改就只摘掉 pending 标记并回 409，绝不删 |
| fix | prd-api | 入参摘要的键名也先截再归一：上一轮截了值没截键，一个几 MB 的键会被 Replace 复制两遍、ToLowerInvariant 再一遍，最后还要拼进摘要文本 |
| test | prd-api | 补键名上限守卫（短键不受影响、巨大键进归一化前必须已截）|
| security | prd-api | 智能体开放层与 MCP 网关的请求体不再落进 api_request_logs：那张与生产共用的诊断表原来会原样收下文学正文、知识库文档与整页 HTML（清理器只按键名摘 prompt/message，超长还会退回原文），日志页里那条可直接复制重放的 curl 也一并带着；诊断不受影响，这两条路自己的调用记录有按键名脱敏、按深度/宽度/长度三维截断的入参摘要 |
| fix | prd-api | 建条目的收尾一步补条件写入：正文提交之后还要重锚评论、重算双链、拍版本快照，用户完全可能在这中间保存一次这条已可见的条目，只按 id 更新会让智能体那份 summary 盖掉他刚改的；现在撞上就只摘掉 pending 标记、保留用户的摘要，并在响应里如实回一个 summaryApplied |
| fix | prd-api | 文学追加的条件写入补上 UpdatedAt：这次写入重置的不止正文，还按读到的快照重写 ArticleWorkflow 与历史，而界面或配图 worker 可以只动 workflow 不动正文——那时旧条件照样成立，新的「标记 → 资产」映射被旧快照抹掉 |
| docs | 平台 | 六条已知边界的补法列改写成取舍与验收标准，不再给类名/成员名与逐步改法（文档只写人类要掌控的层次，实现细节直接读源码）|
| fix | prd-api | 接口日志的「不许落库」清单改按路径段匹配：裸前缀会把名字以它开头的邻居一起挡掉（/api/mcp 顺手收走 /api/mcp-console），挡得过宽不报错，排障能力就这么无声地少一块 |
| test | prd-api | 补一条枚举式守卫：从开放层控制器自己的 [Route] 反推出全部路由，逐条问中间件那个真正生效的判据挡不挡，新增控制器自动进闸；同时反向断言别挡过宽。两处守卫共用同一个探针，不再各抄一份反射代码 |
| fix | prd-api | 文学写正文：写没写进去一律要判。不带版本令牌的覆盖那条路过滤器只有 id，工作区在读它与写它之间被删掉时匹配 0 条，上一版直接回 200——库里什么都没发生，调用方却被告知写好了；现在回读一次分清「没了」（404）与「被改了」（409） |
| fix | prd-api | 三条撤回路径改走同一处说法：上一版只有「没走完」那条看了清理结果，另外两条（条目被删、库被删）一律说「已经撤回」——而清理失败时条目是被刻意留着占住确定性 id 的，还带着未落盘标记，同键重试会一直撞 409，那两句话都不算数 |
| test | prd-api | 补三条守卫：撤回说法按清理结果分岔（干净/残留各断一次）、逐个撤回调用点都得用上清理结果（新增第四条自动进闸）、文学写入结果不许再被标志位挡在门外 |
| security | prd-api | 键名上限自己成了旁路：判据拿截断后的前缀去比，把 password 藏在第 129 个字符之后就正好绕过隐去，值原样落库并显示在面板上；改为超长键一律按敏感处理（正常参数名没有超过 128 字的，漏隐一个凭据不可逆） |
| security | prd-api | 工具名进审计之前先截：认不出来的工具名照样写一行审计，而它同时进 ToolName 与拒绝语——拿合法密钥刷几 MB 的名字就能按每分钟速率把审计集合撑爆，大到超过 Mongo 单文档上限时那行还插不进去，而写审计包了 try，连失败都没有声音 |
| test | prd-api | 补三条守卫：凭据名藏在截断点之后照样要隐、工具名进审计前必须截、且必须截在认工具之前（认出来之后再截，认不出来的那条路正好绕过） |
| docs | 平台 | 接入台计划里的成本一条改写成成本口径与验收标准（读不计、写按次、生图按张、UTC 日重置），不再给字段名与接线做法；另清掉两处组件名与实现字段名 |
| fix | prd-api | 撤回不许删已经提交的正文：知识库写正文是「先提交正文，再重锚评论/重算双链/拍版本快照」，派生那几步抛出来时正文其实已经落库，而上一版一律走撤回，会把一篇已经可见、用户可能还编辑过的文档连同版本与双链一起删掉；现在先确认它还是那条原封不动的占位（UpdatedAt 未变）才删，删除本身也带同一个条件 |
| security | prd-api | 签密钥与鉴权的 scope 判据补上 access 前置闸：管理员把 access 收走、角色里那条功能权限还留着的人，界面上已经进不去，却照样签得出密钥、也照样能拿旧密钥打要花钱的开放接口——同一个判据在两条路上不一致 |
| test | prd-api | 补 access 前置闸的守卫（写蕴含读也绕不过；super 仍是全站兜底），并把四条既有用例的权限集补成真实形态 |
| docs | 平台 | 新增已知边界 17：生图额度按「入队成功」结算而不按「真的出图」，反复失败能耗光当天额度且记录仍显示成功；取舍与验收标准已写明，按 §5.5 归后续 PR |
| fix | prd-api | 摘掉「正文未落盘」标记的四处出口全部带上刚读到的版本：这个 id 是幂等键推出来的确定性 id，回读之后、更新之前完全可能被删掉又被同键重试插进一条新占位，只按 id 摘等于摘掉新那次的标记，于是再一次重试拿到「已去重、成功」而那条正文还没写（Review 只点了一处，同族另两处一并扫掉）|
| security | prd-api | 密钥名进审计前先截，源头也收：它每一次调用都被整个抄进调用记录，一把名字几 MB 的密钥等于给自己配了个放大器，而超过 Mongo 单文档上限的行插不进去、写审计又包了 try，审计会静静缺一段。审计文本上限收敛成一个 ForAudit，不再各抄一份单行函数 |
| test | prd-api | 补两条枚举式守卫：摘标记的每一处出口都要带版本条件（新增第五处自动进闸）、审计行里调用方给的文本都得先截（逐个字段点名，新增字段会红）|
| security | prd-api | 直连那条路的审计行也截密钥名与工具名：新上限只管住以后建的密钥，此前起了个几 MB 名字的老密钥每打一次直连就放大一次；上一轮只在网关截了，而这行审计有两个建法——判据只长在一条路上，守卫也只盯着那一条 |
| test | prd-api | 审计字段守卫改为扫全仓找建审计行的地方，逐处逐字段过（第三个建法出现时自动进闸），不再只看网关那一个文件 |
| docs | 平台 | 已知边界 4/5/6/7/8/9/12 的补法列一并改写成取舍与验收标准（前一轮只改了 10/11/13/14/15/16，同一张表又被点了一次——扫同类扫漏的老形状）|
| fix | prd-api | 生图状态的「跑完了没有」先认终态：用户中途取消时，还没开始的那几张既不算 done 也不算 failed，计数器永远凑不齐，于是这个标志永远是 false——跟着它轮询的智能体会一直问到被限流，而这次生图其实早就结束了 |
| test | prd-api | 补守卫：取消/失败/完成一律算结束，还在跑不算，计数器凑齐也算（worker 落终态与写计数之间那一小段不让调用方多问一轮）|
| fix | prd-api | 正文其实写进去了就不能报失败：网关按 HTTP 状态码判成没成，报 500 会退掉占的额度、把这笔记成错误——文档明明躺在用户知识库里，账面上却是「没发生过、也没花钱」；改成按成功回并把没做完的部分写进 warning |
| fix | prd-api | 撤回时回读发现条目已经不在就立刻收手：那个确定性 id 已经空出来，同键重试可能已经插了一条新的，再按 id 清一遍派生，清掉的是新那次的版本与双链，而那次还会照常报成功 |
| test | prd-api | 补两条守卫：回读为 null 必须在清派生之前返回、「正文已提交」必须排在 500 之前并按成功回 |
| fix | prd-api | 「条目被动过」不等于「我的正文写进去了」：用户在界面上改一次那条可见的占位，UpdatedAt 照样变，而上一版把这种情况也判成「写成了」并回 200——调用方要写的内容被悄悄丢掉，摘掉标记之后同键重试还会拿到「已去重、成功」，从头到尾没人会发现；现在直接比内容本身（正文是内容寻址存的），两种结局分开说，别人改的回 409 |
| fix | prd-api | 去重也改问同一个问题：标记被摘掉只说明上一次收尾过了，不说明这条装的是这次要写的正文；两处各判各的就会出现「撤回那边如实说没写进去、去重这边照样报成功」 |
| test | prd-api | 补两条守卫：别人改的与我写成的不许给同一种说法（含计数不退）、撤回与去重必须调用同一个判定源 |
| fix | prd-api | 建条目收尾时回读不到条目不再静默报成功：正文提交之后、收尾之前条目（或整个库）被删掉时，上一版只是「没有标记可摘」就往下走，最后照样回成功——调用方被告知建好了、额度也照扣，而那条根本不在了；现在交给既有的「条目没了」出口收尾 |
| security | prd-api | 产物字段（id / url / title）进审计行之前先截：它们来自下游响应体，登记的动态接口想回多长就回多长，够大的一条会顶破 Mongo 单文档上限，而写审计包了 try——一次已经扣过额度的成功调用就此从审计里消失。去重与正常两条分支原来各抄一遍四行赋值，一并收敛成一处 |
| fix | prd-admin | 接入台调用记录不再显示原始 HTTP 状态码：这是普通用户（access 权限）的页面不是管理员诊断面，结果由状态徽章说、失败原因由 errorMessage 用人话说；状态码留在服务端记录里，接口也不再下发 |
| test | prd-api | 审计字段守卫扩到产物三列（逐个赋值点枚举，Kind 例外），新增第四个字段自动进闸 |
| fix | prd-admin | 接入台页面手机端不再自己加左右 padding：外层 gutter 由 AppShell 统一给，再叠一层 16px 会让 375 宽下外边距变成 24px，卡片自己的 padding 还要再吃一层——正是密度规则点名禁止的三层叠加 |
| fix | prd-admin | 选中的客户端被吊销/删除后把筛选条件收回来：受控 select 找不到对应 option 时浏览器显示第一项「全部客户端」，请求却还带着那个 keyId——看到的筛选条件和实际在筛的不是一回事，而这块面板是审计用的 |
| fix | prd-api | 分享链有效期显式给范围外的值改为 400，不再默默替换成 7 天：调用方拿到的链接寿命和它要的不一样，多半要到提前失效那天才发现（与 content 那条同一个道理：「没说」和「说了但说错了」是两件事）|
| fix | prd-api | 占位条目的时间戳改用毫秒精度：BSON 日期只有毫秒而 DateTime.UtcNow 是 100 纳秒刻度，写进去再读出来就不等于手里那个值——整套撤回的乐观并发条件因此**每次**都不成立，一条谁也没碰过的占位会被判成「被人改过」，留下收不了尾的空条目；正文为空时还会把失败报成成功 |
| fix | prd-api | 分享链复用改比「当初要的是多少天」，不比算出来的到期时刻：后者每次重试都不同，于是幂等重试永远被判成真实改动——有效期每次被刷、续期审计每次追一条、而「这次没产生副作用」这个信号（网关据它退额度）彻底失效 |
| test | prd-api | 补三条守卫：占位时间戳必须毫秒精度且不许直接用 UtcNow、分享链复用不许再比绝对时刻 |
| fix | prd-api | 建条目时盖一个「这一次插入的代次」，此后每一处要动那条条目的地方先核对它：确定性 id 会被重用（原来那条删掉后同键重试可以用同一个 id 插新的），于是「id 存不存在」「时间戳变没变」都答不了真正的问题——摘标记摘掉的会是新那次的、删掉的会是别人的条目，而这一次还照样回成功。删除那一下把代次写进过滤器本身 |
| fix | prd-api | 有效期意图字段在所有改到期时刻的路径上同步（手动续期置未知、面板重设记确切天数、改永久记 0）：只在复用那条路写、别处不维护，它就变成一份过期的说法——建成 7 天后被改成 30 天的链接仍记着 7，此后「我要 7 天」被判成没变、返回去重成功而链接还是 30 天 |
| fix | prd-api | 生图张数显式给范围外的值改为 400，不再默默改成 1 或 4：调用方拿到的图数和它要的不一样，而且是要花钱的那种不一样（clamp 保留给闸门算占坑，两处仍读同一个数）|
| test | prd-api | 补三条守卫：动条目前必须核代次（含删除过滤器）、改到期时刻的每一处都要同步意图字段（枚举四处）、显式越界张数必须报错而省略仍走默认 |
| fix | prd-api | 修红：撤回那条守卫锚在整串签名（连右括号）与某句实现的字面上，给撤回补一个参数（更强的判据）反而把它弄红了；改成锚在方法定义、判「它知不知道这三件事」，删除条件也改判「版本 + 代次都在过滤器里」而不是某种写法 |
| chore | prd-api | 代次判据加 NotNullWhen 标注：核过之后编译器就知道非空，省掉调用点的空值告警与 ! |
| fix | prd-api | 代次也进正文那条原子写的过滤器：此前只在它前后的回读里核，而时间戳只回答「有没有被动过」——同一毫秒内条目被删掉、同键重试又插了一条新的，两条在 Id + UpdatedAt 上一模一样，正文会落进别人那一代里还接着参与它的收尾 |
| fix | prd-api | 分享链复用的有效期判据换成「这是重试还是又一次新请求」：直接判等永远说变了（重试也差几毫秒），只比要的天数又会让六天前建的 7 天链接遇上今天的「我要 7 天」被判成没变、实际明天就过期；没有请求键时能分开这两件事的只有时间距离 |
| refactor | prd-api | 撤掉上一轮加的有效期意图字段：新判据不需要它，而它是个要在四处维护的第二真相源——少一个字段就少一处会漂的说法（真正的解法是给这个工具一个 clientRequestId，见边界 7）|
| test | prd-api | 守卫跟着换成行为断言：秒级重试不算变 / 隔天再来才算 / 换天数算 / 永久与有期限互换算；另补一条「正文原子写必须带代次」|
| fix | prd-api | MCP 网关不再把登记表里的 DELETE 接口当成可调用工具（向导承诺过删除类动作不开放，动态这条路原来是敞开的），列举与调用共用同一判据 |
| fix | prd-api | 下游结构化错误先归类再展示：带堆栈/凭据/内部地址的 error.message 不再原样进接入台，只换成固定说法 |
| fix | prd-api | 认不出结构的响应体不再整段进日志，改为有界开头 + 指纹，且开头先擦一遍凭据形状 |
| fix | prd-api | 知识库建条目的四处「摘正文未落盘标记」出口统一走带代次的过滤器（原来只按 id + 毫秒时间戳，同一毫秒内被同键重试重建的那条会被误当成自己的） |
| fix | cds | 测试收尾漏 await 落盘，rmSync 撞上还在飞的写盘偶发 ENOTEMPTY（96 处调用里 10 处漏了），补 await 并加一条守卫钉住 |
| fix | prd-api | JSON-RPC 批量条数在派发之前收到 50 条（一个包塞几千条 tools/call 时，外层限流只看到一个请求，每条却各查一次库） |
| fix | prd-api | 接入自检先判钥匙还能不能用再算工具清单，停用/吊销/过宽限期的钥匙不再被报成「授权自检通过」 |
| fix | prd-admin | 接入向导按 isActive 分岔：钥匙不可用时给红色说明与下一步，宽限期给黄色提醒，不再一律报通过 |
| feat | prd-admin | 接入台的客户端行加「断开」：钥匙泄露或客户端不用了能当场作废，不必去另一个密钥管理页找入口 |
| fix | prd-api | 知识库收尾兜底那次摘标记要看结果：匹配为 0 时标记还挂着却回成功，同键重试从此永远撞 409 |
| fix | prd-api | 直连开放接口的审计行改记发起时刻（原来走模型默认 = 完成时刻），跨 UTC 午夜时额度与记录不再分到两天 |
