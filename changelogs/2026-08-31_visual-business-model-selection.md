| fix | prd-api | 统一过滤 GPT Image 不支持的 response_format，覆盖文生图、图生图和网关请求重建 |
| fix | prd-api | 视觉创作返回可选业务模型与默认项，显式选择在既有授权范围内解析，不再以默认池作为模型列表 |
| fix | prd-admin | 视觉创作选择器只展示业务模型，删除写死的推荐型号，结果优先显示实际模型 |
| test | prd-api | 新增生图参数契约、业务模型目录和授权边界回归测试 |
| fix | prd-admin | 生图进度条随画布可见区域定位，避免高倍率或平移后移出屏幕，并增加多尺寸回归 |
