| fix | prd-api | 统一过滤 GPT Image 不支持的 response_format，覆盖文生图、图生图和网关请求重建 |
| fix | prd-api | 视觉创作返回可选业务模型与默认项，显式选择在既有授权范围内解析，不再以默认池作为模型列表 |
| fix | prd-admin | 视觉创作选择器只展示业务模型，删除写死的推荐型号，结果优先显示实际模型 |
| test | prd-api | 新增生图参数契约、业务模型目录和授权边界回归测试 |
| fix | prd-admin | 生图进度条随画布可见区域定位，避免高倍率或平移后移出屏幕，并增加多尺寸回归 |
| fix | prd-api | 补齐默认业务模型 GPT Image 1 的方形、横版与竖版尺寸能力，避免尺寸菜单为空 |
| feat | prd-admin | 新增视觉模型业务配置，显式管理开放列表、默认项、排序和用途说明，不自动采用网关排序 |
| refactor | llmgw | 通过带调用方授权的 HTTP 目录发布图片模型能力，集中处理标准生图请求、供应商参数与结果规范化 |
| fix | prd-api | 保持用户选择的业务模型身份，禁用静默回退；保留参考图资产关联，拒绝空图片成功响应 |
| fix | prd-api | 视频任务续租按匹配记录判断所有权，避免同毫秒幂等更新被误判为租约丢失 |
| test | prd-api | 增加模型策略、目录授权和图片协议边界测试，并修正 worktree 下测试根目录识别 |
| test | prd-admin | 将显式默认模型与真实产物校验纳入既有 48 小时稳定冒烟 |
| fix | prd-admin | 修正手机模型设置说明裁切与默认标签挤压，空模型状态指向业务配置入口 |
| fix | prd-admin | 画布节流结束后补齐最后一次缩放状态，保证进度层定位与比例数字一致 |
| fix | prd-api | 从网关结果透传实际执行模型到实时事件与持久化结果，保留任务原有业务模型身份 |
| fix | prd-admin | 手机生成流复用业务默认与模型能力目录，移除首项回退和写死尺寸，校验参考图能力后再发送 |
| fix | prd-admin | 固定缩放控件高于画布标签层，避免竖图连续缩小时按钮被图片标题挡住 |
