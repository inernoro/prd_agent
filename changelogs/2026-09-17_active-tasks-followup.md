| security | prd-api | 开放接口与 MCP 的债务同步都收到管理档：上一个 PR 只给界面那条加了门，这条是同一能力的第二个入口，仍是 use 档就能覆写整块共享台账 |
| fix | prd-api | 放下、债务转任务、新建任务、派活四处的多步写序列改用 CancellationToken.None：客户端一断就会留下不可重试的中间态 |
| test | prd-agent | 「多步写序列不许被切一半」改成全扫控制器（不再逐个列举端点），当场又扫出两处 review 没报的同族泄漏；另补一条「改整块台账的每个入口都要管理档」（跑过红绿闭环）|
| docs | prd-agent | 台账新增第 40-44 条：历史排序、认领不看终态、标记卡住不看终态、结案备注不 flush、知识库判定不含公开库 |
