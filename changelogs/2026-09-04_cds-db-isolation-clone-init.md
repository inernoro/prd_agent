| feat | cds | 数据库隔离收敛 4：分支独立库新增「从共享库时间点克隆」初始化方式（项目设置 → 数据库隔离 每服务下拉，保存走同一个 PUT 的 inits），首次部署前自动从共享库克隆到折算后的独立库，目标库已存在则跳过不覆盖 |
| refactor | cds | 关系型整库克隆抽成「来源库、目标库、实例」三元组管线（db-clone-pipeline），复制集一键隔离与分支独立库时间点克隆共用同一份 dump→导入脚本与辅助容器限额 |
| feat | cds | 克隆后逐表行数校验：源库与目标库各数一遍，不一致的表连同两边行数记进数据台账并在台账行展示；克隆是时间点快照，校验不一致不自动判死 |
| feat | cds | 数据台账：分支独立库条目展示克隆来源、时间点与校验结果；选了时间点克隆但还没建库的条目给「现在克隆」按钮（POST /api/branches/:id/db-init/:profileId，与部署前钩子同一条路径） |
| feat | cds | 分支覆盖（profileOverrides）支持按分支覆盖 dbInit；mongo 的分支独立库明确不支持时间点克隆并说明原因（共享实例大批量写入会崩） |
| test | cds | 新增克隆管线 / 逐表校验 / 时间点克隆初始化 17 例、项目页签 inits 与分支 db-init 路由 3 例、前端初始化下拉与台账克隆信息 5 例，含三处接线守卫 |
| docs | cds | 数据库隔离设计文档 §十五 收敛 3、4 标已落地；多分支库指南补初始化方式与已知边界；看板翻转 |
