| feat | cds | 数据库隔离收敛 5：派生库回写——分支独立库与隔离库共用同一套门禁、冲突清单与整库替换脚本；回写前自动备份目标库并演练验证（演练不过整个回写中止、目标库一个字节不动），复述目标库名才执行，回写后逐表校验 |
| feat | cds | 回写预览：两边逐表行数并排 + 冲突清单（有克隆时基线则列「主库在克隆之后改过的表」，没有基线按当前差异列并标明），回写会覆盖的表一目了然 |
| feat | cds | 回写可回退：回写前快照进台账（可回退到 时间），「回退」用快照整库还原目标库，回退前再拍一次快照；台账条目展示回写时间、覆盖了几张主库改过的表、回退状态 |
| refactor | cds | 克隆管线新增整库替换与还原脚本（relationalReplaceArgv / relationalRestoreScript），真实 ops 的替换、还原、逐表行数全部走管线，不自己拼命令 |
| test | cds | 回写冲突清单 / 门禁 / 脚本 11 例，路由预览 / 执行 / 回退 4 例，前端回写行与确认面板 2 例，含接线守卫 |
| docs | cds | 数据库隔离设计文档 §十五 收敛 5 标已落地并注明整库回写与增量回写的边界；多分支库指南补回写与回退；债务台账记增量回写待波 5 |
