| fix | cds | 指标分辨率不再细于采集节奏：桶宽至少是观测节奏（各容器相邻样本间隔 p90 取最大）的 1.5 倍。此前 30 分钟窗口固定切 120 桶（每桶 15s）而采样器 45s 一帧，120 个桶里 79 个是空的，空桶画成 0 → 整图变成均匀锯齿林，画的是采集节奏不是 CPU |
| fix | cds | 总览图表闸门只看有没有历史，不再等实时快照。此前要等 /metrics 跑完 docker stats（10 容器、超时 5s）才肯画，而纯内存的 /metrics/series 早已返回整段历史 —— 打开抽屉长时间空白 |
| test | cds | 新增 4 条守卫（分辨率不细于节奏 / 混合节奏跟稀疏档走 / points=1 不受下限影响 / 闸门不依赖实时快照），均做过红绿闭环；并给源码扫描守卫加去注释视图（注释里引用错误写法会让守卫自己判红，形状 6） |
| fix | cds | 点数再按样本数封顶（1.2 倍余量）：节奏要三个样本才算得出来，CDS 每次重启后头两分钟只有一两个样本，光靠节奏那道闸会退回按请求值切 120 个桶 —— 冷启动那几分钟又是锯齿；何况 2 个样本画 120 个点本就是无中生有 |
| test | cds | 把三处「精确点数 / 写死下标」的断言改成按不变量断言（轴两端贴着窗口、断档按时间挑）：点数由请求值/节奏/样本数三者取小算出，写死等于把实现锁进断言（形状 4a） |
