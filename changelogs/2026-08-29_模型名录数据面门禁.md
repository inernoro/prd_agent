| feat | prd-api | 模型白名单补上第二道门（数据面）：模型解析器的两个入口统一罩一层名录门，名录外且无放行标记的模型直接结构化失败，重试候选一并过门——此前白名单只拦在控制台导入，绕过控制台直接写库的模型照样能被请求打出去 |
| feat | prd-api | 新增结构化失败原因 `MODEL_NOT_IN_CATALOG`，与「池空 / 全熔断」分开：那两个是「没有可用成员」，这个是「这个成员不该被用」，管理员的处置动作完全不同 |
| feat | prd-api | 运行时侧新增内置名录镜像（与控制台侧同 38 条，同一套归一化），因为控制台按既定隔离架构不引用主站项目；两份由守卫逐条比对，任一侧漂移 CI 立刻红 |
| feat | prd-api | 名录门可降档：`LlmGateway:ModelCatalogGate=observe` 只记日志不拦截，用来先看清存量里有多少会被拦；只认这一个降档值，拼错或留空一律回到拦截档 |
| feat | llmgw | 模型文档新增放行标记（放行人 + 时间），三条写入路径都盖戳：批量导入按管理员勾选、手工新增与认领 MAP 模型视为显式放行——否则管理员亲手加的模型会进池但调不通 |
| feat | llmgw | 名录门上线时一次性把存量模型补成已放行，放行人如实记成「存量迁移，未经人工审阅」，幂等；不补这一手，升级那一刻所有名录外的存量模型会集体开始被拒 |
| feat | llmgw | Quickstart 失败归因与控制台系统调用的诊断句补上名录外这一类，给出「去 Provider 页重新导入并放行 / 换一个名录内的模型」的下一步 |
| feat | prd-admin | 前端错误文案补上名录外这一类，归入「重试无解、找管理员」那一组，不再落到「服务暂时不可用」的兜底句 |
| test | prd-api | 接线守卫：从接口反推每个解析入口，逐个断言必须经过名录门（删掉任一处即红），并断言门内点名了专属错误码、重试候选一并过门 |
| test | prd-api | 名录镜像守卫：两侧名录逐条一致、归一化与命中判定在两侧结果相同 |
| fix | prd-api | 网关会话时长用例改读生产常量：本分支早先把默认会话从 7 天改成一个月，这两条用例仍逐字要求 7 天，反过来锁住了被推翻的旧行为，也是这次跑全量 CI 才暴露出来 |
| test | prd-api | 名录门行为用例（真 Mongo、真解析器）：名录内放行、名录外无标记拦下并点名错误码、同一条补上标记后放行——线上造不出「没标记」的行（每条写入路径都盖戳），只有这里能把那个形态真做出来 |
| fix | llmgw | 产物屏补上设计稿里一直没落地的两条结构改动：一次性密钥常驻到页签之上（此前在「接入信息」页签内，切到 cURL 页就消失，而它是全屏唯一取不回来的东西）；页签从三张带副标题的大卡降级成分段控件（导航不该和内容同重） |
| test | llmgw | Quickstart 守卫补两条钉住上面这条结构约束：密钥主区必须排在页签容器之上、切到 cURL 页后密钥仍在；红绿闭环已跑（把密钥塞回页签即红） |
| fix | llmgw | 服务网关设置的逻辑模型下拉漏了租户过滤：模型库是跨租户同一个集合，任何管理员打开这一页都能看到并选中别的租户的模型；读端补上 TenantId 谓词，写端也自己判一次（认 PublicId，本租户内不存在就 404），不然直接构造请求照样能钉进来 |
| fix | llmgw | 服务网关设置存的是显示名而不是 PublicId：解析器只按 PublicId 匹配，显示名与它不同的模型会匹配不上、静默落到别的默认模型。接口分开回 publicId 与 name，页面提交 publicId、只拿 name 做标签 |
| fix | llmgw | 「测试连接」测的是库里已保存的那一份配置：改完没保存就点，会拿旧配置的成功来证明新选择。改动未保存时禁用该按钮并说明先保存再测 |
| fix | llmgw | 多团队 Developer 新建密钥留空团队时会留下孤儿登记：调用用途已按身份的团队登记出去，密钥这一步服务端不替他推断（只有刚好一个团队才推）且不允许租户级，被 TEAM_SCOPE_REQUIRED 挡回。现在替 Developer 把两个请求落在同一个团队；Owner/Admin 留空仍是租户级密钥，语义不变 |
| test | llmgw | 三条守卫钉住上面这些：提交的是 publicId 不是显示名、未保存时测试连接禁用、多团队 Developer 的密钥与用途同团队（均跑过红绿闭环） |
| merge | llmgw | 合入主干并解掉台账、控制台、样式三处冲突；样式那处两段各自追加时会把主干那段吞进未闭合的媒体查询，已补回闭合花括号 |
| fix | llmgw | 存量放行迁移改成靠库里的迁移标记只跑一次：原来判据是「此刻没有这个字段」，只在第一次启动时等于「名录门上线前就已入库」——此后绕过控制台直接写库塞进来的模型同样缺标记，下次重启就被自动放行，门每重启一次自己开一道缝 |
| fix | llmgw | 系统级密钥并发签发会让设置指向一把已被对方停用的密钥：改成回读设置取胜者、停用除胜者以外的；自己落败时直接用设置里那把（密文可解就解出来用），保住「设置指向的那把一定是启用的」 |
| test | prd-api | 守卫钉住存量放行迁移必须在认领分支之内、盖戳只此一处：把它挪回启动路径无条件执行即红 |
| fix | llmgw | 系统级密钥退役再收紧：只认胜者还不够（两个并发请求各自回读到自己那把，接着互相停用，最后两把都停用），补上「只停用 5 分钟前就存在的 key」——谁也够不着对方刚签出来的那把，不需要锁 |
| fix | llmgw | 系统团队不再顺手认领同名的业务团队：撞唯一索引时只有同 id 才算并发，名字被业务团队占用就另起带后缀的名字（id 仍是确定值），否则系统的消耗与账单会挂到别人团队上、那个团队停用还会让系统密钥全部过不了生命周期门；备用名也占满时两个调用方各自给出说得出原因的失败，不抛裸 500 |
| test | prd-api | 守卫钉住退役过滤必须同时避开胜者与新签的那把，删任一条件即红 |
| docs | llmgw | 名录归一化会丢掉任意厂商前缀（`private/gpt-4o` 判成名录内的 `gpt-4o`）记入债务台账：收紧要改成登记完整别名，会把大量存量模型判成名录外，而存量放行迁移已是一次性的，必须单独一轮先在 observe 档统计再切 |
| fix | llmgw | Quickstart 一句话推导补上流式 deadline：`HttpClient.Timeout` 只管到响应头，用 ResponseHeadersRead 流式读时 serving 卡在 body 上就没人兜底、读行又只在浏览器断开时才醒，这一屏会无限停在「模型正在推导」——承诺的 40 秒静默失效。整条链路改用同一个 deadline，到点给出「已退回本地关键词判定」而不是干等 |
| fix | llmgw | 系统密钥退役判定抽成唯一函数，签发路径与复用路径都调：复用路径原来直接返回，并发落败那把永远等不到下一次签发，「只多留 5 分钟」成了空话 |
| test | prd-api | 守卫改钉共享判定本体（两个条件缺一即红）并断言两条路径各调一次退役 |
| docs | llmgw | 名录门滚动升级窗口的已知边界记入台账：迁移一次性之后，升级期间旧副本导入的名录外模型不会再被自动补戳，按 enforce 会被拒；正解是升级期间降到 observe，而不是放宽迁移条件 |
| fix | llmgw | 作废系统密钥时先确认设置里现在这把就是刚才失败的那把：并发下两个请求可能都拿着 A 去调，第一个失败后已重签成 B，第二个再进来就会把好端端的 B 撤掉，第一个的重试反而拿到一把刚被撤销的 key，凭据自愈变成互相拆台 |
| fix | prd-api | 名录门显式认出兑换所来的模型：它们不在网关模型库里，此前静默落进「管不着」——结果同样是放行，但那是漏判不是判断，读代码的人会以为兑换所本来就不在门的射程内。现在按「管理员建兑换所时当场列出别名」这层人工声明放行，并写明还欠一个 per-model 放行标记 |
| fix | llmgw | 「测试连接」等待期不再是一句不动的「正在测试」：按钮报出已等秒数并转圈，提示文案按秒数说清此刻在做什么（确认密钥 / 等模型回第一个字 / 已超过常态 / 正在自动重签重试），单轮上限 40 秒也说给用户听 |
| test | prd-api | 守卫钉住作废密钥必须逐字比对刚才失败的那把、两个调用点都要把它传进来，以及名录门必须显式认出兑换所（删调用即红，只留一个没人用的辅助方法不算） |
| test | llmgw | 网关设置验收补等待期断言：桩延迟 3.2 秒，断言按钮上的秒数真的在往前走、提示文案说清在做什么 |
| docs | llmgw | 兑换所模型缺 per-model 放行标记记入债务台账：绕过控制台直接往兑换所加别名，两道门都拦不住；要覆盖得先给兑换所写入路径盖戳 |
| fix | llmgw | Quickstart 的 Gemini 入口不再拿 body 里的 model 冒充「钉住了模型」：serving 只从路由段读模型，页面却固定打 `models/auto:generateContent`，于是选了具体模型时真跑的是池调度，屏幕上却写着「实际执行 X」。现在钉得住就写进路由段（cURL 样例同源），钉不住（名字带斜杠、路由段装不下）就保持 auto 并明说「本次按池调度执行」 |
| fix | llmgw | 一句话推导那条手写 fetch 补上滑动续期：响应头里的新 token 此前被丢掉，用户若只用这一页，会一边在用一边被本地那个还按旧到期时间走的计时器踢下线 |
| test | llmgw | Quickstart 验收补三条：Gemini 把选中模型钉进路由段、钉不住时保持 auto 且页面明说退回池调度、推导响应的续期 token 被后续请求用上（均跑过红绿闭环） |
| docs | llmgw | Gemini 入口模型只认路由段这条记入债务台账：真正的修法在 serving（路由段解码或回退读 body），按 §5.5 文件范围熔断不在本轮展开 |
| fix | prd-api | 名录门的 Provider 收窄改成写进 Mongo 谓词、在 Limit 之前生效：原来先按名字取 20 条再在内存里按 Provider 过滤，同名文档超过 20 条时那一页里可能根本没有当前 Provider 的记录，过滤后为空就判成「管不着」——没盖放行标记的模型反而被放过去 |
| fix | llmgw | 作废系统密钥的清设置那一步也带上密文条件：比对完到清设置之间别人可能已经重签，只按租户 id 清会把新签的一起抹掉，比对白做 |
| fix | llmgw | Quickstart 真实调用认出流里夹着的失败帧：上游在响应头发出之后才失败时 HTTP 仍是 200，失败只能夹在 SSE 里回来（finishReason error + error 对象），此前只挑 delta，会把一次真花了钱的失败报成「已返回」 |
| test | prd-api | 名录门行为用例补 24 条同名诱饵（挂在别的 Provider 下、先于真那条写入）：Provider 收窄若退回内存过滤，原有的「名录外必须被拦」当场变红 |
| test | prd-api | 守卫补一条：清设置的谓词必须带 ServiceKeyEncrypted |
| test | llmgw | Quickstart 验收补流中途失败四条断言（判成失败、带出上游原因、说明仍计费、保留已收到的半截输出），红绿闭环已跑 |
| fix | llmgw | 服务网关设置选「钉一个模型池」时真的钉到那个池：系统调用只发了池 id、没声明策略，而 serving 按 body 的 model 推策略——这两条请求的 model 恒是 auto（非空）于是被推成 pinned，池 id 只在策略为 pool 时才顶替 ExpectedModel 进解析，结果选中的池只进了日志上下文，真跑的仍是 appCaller 绑定或默认池，设置页却说着这个池 |
| fix | llmgw | 存设置时的校验谓词与读端点、运行时对齐（本租户 + chat + 未停用）：页面加载之后被停用或改了类型的模型此前仍能存进去，下一次系统调用解析不到它就静默落回默认池，而「测试连接」还会报成功 |
| test | prd-api | 守卫钉住这两条：每一处发池 id 的系统调用都要同时发 X-Gateway-Model-Policy（数量必须相等），写端点校验必须带 ModelType chat 与 Enabled |
| fix | llmgw | 系统密钥退役认 SystemManaged 标记而不是名字：接入密钥页不拦着谁把自己的 key 也叫 llmgw-system-internal，只按名字扫的话，用户那把同名 key 会被每一次系统调用连坐撤销——自愈本来是修自己的凭据，不该动别人的 |
| test | prd-api | 守卫补一条：退役过滤必须带 SystemManaged 标记 |
| docs | llmgw | 两条 review 发现记入债务台账而非本轮修复（PR 已过 §5.5 熔断线，停不停待仓库负责人定）：真实调用的流在没收到 [DONE] 就结束时仍报成功；视觉类无附件分支丢掉用户输入的提示词 |
| docs | llmgw | 第 11 轮 review 的两条记入债务台账（同样因 §5.5 熔断线未在本轮修）：系统 appCaller 自愈会接管同码的业务文档并把它搬到系统团队（合并前应先修，是唯一一条会动用户既有数据的）；Quickstart 选中池内成员时发的是 pool 策略却没带池身份，严格池契约下会被当成池标识而路由失败 |
| docs | llmgw | 第 12 轮 review 的三条记入债务台账（同因 §5.5 熔断线未修）：推导端点的服务端读流不认失败帧、推导调用没有 completion 上限、设置页模型下拉取前 200 条可能漏掉已保存的那个 |
| fix | llmgw | 系统 appCaller 的归属改认 SystemManaged 标记：只按码认的话，租户自助登记的同码 appCaller 会被搬进系统团队，其团队级密钥随后在 serving 的归属比对上被判 403——而触发它的只是有人打开了设置页。存量按当时写死的 SourceSystem 认亲并补标记；遇到同码的业务文档不再接管，返回说得出原因的失败 |
| fix | llmgw | 产物屏的「真实调用」主按钮补上真实路由闸（闸同时放进 runRealTest）：路由还在校验、校验失败或识别出 stub/mock 时按下去，跑的是假上游、页面却报「真实调用已返回、本次计费」 |
| fix | llmgw | 真实调用的流在没收到 [DONE] 就结束时判「未完成」而不是成功：serving 在响应头已发出后遇到取消会直接收尾，既不发失败帧也不发完成帧，只认失败帧会把截断当成收完 |
| fix | llmgw | 视觉类测试没上传图片时不再丢掉用户写的提示词：三个内嵌图助手改为接收 prompt，输入框里写什么就发什么 |
| fix | llmgw | 选中池内成员改按「在已绑池内钉这个成员」的契约发（pinned 策略 + pinned_model_id 与 model 同值）：原来声明 pool 却不给池身份，解析器把模型名当池标识去找，严格池契约下直接 ROUTE_CONFIG_INCOMPATIBLE |
| fix | llmgw | 一句话推导的服务端读流认失败帧（两种命名都认），命中即中止并降级；此前模型在失败前吐出可解析 JSON 时，端点会对一次失败且计费的调用回 ok:true |
| fix | llmgw | 一句话推导补成本闸：max_tokens=256（产物只是四个短字段）+ 累积体积上限，兜住无视上限的上游；系统 appCaller 没有预算闸、密钥 120 次/分 |
| fix | llmgw | 服务网关设置的模型下拉加固定排序，并把已保存的那条显式并进结果：启用模型超过 200 个时它可能不在页内，页面看起来像没选、系统调用却仍在用它 |
| fix | llmgw | MAP SSO 并发激活成员资格不再各自自增版本：把「还没就位」写进谓词让库裁决谁做出改变，落败方回去读胜者——原来两个请求各 +1，先返回的那把 token 立刻失效，正是这次要消除的「刚登进去就被登出」 |
| fix | prd-api | serving 的 Gemini 入口在路由段为 auto/空时回退读 body 的 model，并把 model 从 dropped parameters 里摘掉：带斜杠的模型名放不进单个路由段，客户端只能放 body |
| test | prd-api | 守卫补三条：系统 appCaller 认标记不认码、推导端点认失败帧且有 completion 上限、视觉类无附件分支必须传 prompt |
| test | llmgw | Quickstart 验收补六条：选成员发 pinned 契约、auto 不带 pin、Gemini 斜杠名改由 body 携带、流被截断判失败、识别出开发桩时真实调用被拦、上游恢复后又可点（均跑过红绿闭环） |
| fix | llmgw | 系统 appCaller 撞唯一索引后回读胜者并复判归属：用户在这一刻自助登记同一个码时赢的是业务文档，假定「赢的是我们」会让后面签出来的系统密钥一路 403，而凭据自愈修不好归属 |
| fix | llmgw | 上游失败文案进 SSE 前设长度上限（内容保留，受众是本租户网关管理员，与既有失败详情同一口径） |
| test | prd-api | 守卫补：撞索引后必须复判归属，且两条归属拒绝路径都要给说得出原因的失败（少一条即红） |
| test | prd-api | 修守卫自己的取值范围：归属守卫原来只切到 else 之前，撞索引复判那一支整段落在范围外，断言必然落空——改为盖住 if/else 两支
| fix | llmgw | 一句话推导的服务端读流认「有没有收完」：收到结束标记或我方到顶收工才算收完，EOF 提前到达判未完成并降级——serving 的取消路径既不发失败帧也不发结束标记，此前一次被截断的计费调用会被当成成功的推导结果交出去 |
| fix | llmgw | 接入密钥的调用身份不再把拼不出拉丁字母的用途塌成同一条码：中文用途会要一个短英文标识（拼不出时不许系统替用户挑常量），否则不同集成会共用一条路由身份与一份预算 |
| docs | llmgw | 教程双链图谱登记「服务网关设置」页，第 30 章补一条对应的排查项：控制台自己的功能报凭据错误时去哪一页、看什么、不要动业务 key（教程漂移闸此前报的三条缺失即此）|
| fix | llmgw | 模型名录归一化不再剥任意厂商前缀：查名录先按完整标识查，只对名录自己登记过的厂商段剥一层重查——此前 `private-provider/gpt-4o` 会被认成登记过的 `gpt-4o` 并继承它的能力登记，导入确认与数据面名录门一起失守 |
| fix | llmgw | 名录归一化口径收紧自带一次性窗口（新迁移 id）：旧口径判成名录内、因而导入时没盖过放行标记的存量模型，会在新口径下变成名录外，而上一条存量迁移早已跑完——没有这个窗口它们在 enforce 档下会当场开始被拒 |
| fix | llmgw | 兑换所改成逐条放行：写入路径对名录外的别名逐条盖标记（谁放的、什么时候），数据面核对兑换所确实声明过这条别名且它带着标记——此前认的是「这个 PlatformId 是一条兑换所记录」，等于认容器，往兑换所里加一个没人看过的别名照样过门 |
| fix | llmgw | 兑换所逐条放行配套一次性窗口 exchange-model-allowance-v1：存量兑换所建于盖戳之前，不补这一手判据一收紧它们会集体开始被拒 |
