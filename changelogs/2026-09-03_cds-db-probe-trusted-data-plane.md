| feat | cds | 新增分支级数据库实测 `GET /api/branches/:id/db-probe`：docker inspect 应用容器真实 env 取「容器持有」的库名，再用应用自己的凭据进基础设施容器连一次库取「连上的库」、版本与表数，只读不写、报告不落密码 |
| feat | cds | 「配置说的 / 容器持有 / 连上的库」三列并排落到三个入口：分支抽屉配置检查器、分支设置的数据库档位旁、项目设置数据库隔离页签的「各分支实测」；机器判定一致 / 不一致 / 未运行 / 实测失败 / 无数据库，不一致时写明「容器未按当前配置重新部署」或「连接串未跟随」 |
| feat | cds | 实测值带探测时间，超过 10 分钟标「可能已过期」并灰掉；等待期显示已等待秒数 |
| test | cds | 补库探测判定纯函数与注入 exec 的服务测试 15 例、路由测试 5 例、三列渲染与三入口接线守卫 10 例；项目数据库隔离视图补分支清单断言 |
| fix | cds | 库探测定位实例时不再按基础设施台账状态过滤（台账 error 可能只是上次重建失败），容器是否在跑由 docker 实测说了算；项目页签各分支块标题改为分支名原样 + 中文状态 |
| fix | cds | 实测三列表改固定列宽并允许换行，判定列原因在分支抽屉里不再被裁掉 |
| feat | cds | 数据库隔离收敛 1：库名变量分类器成为唯一来源，项目设置页签识别 .NET 框架变量与 DB_NAME 并标「已识别，按项目约定不加后缀」，项目级环境变量一并纳入定位，与复制集定位、库探测口径一致 |
| fix | cds | 数据库隔离页签服务行在手机上改为纵向堆叠，档位开关不再压住镜像名与变量提示 |
