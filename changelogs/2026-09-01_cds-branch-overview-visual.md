| feat | cds | 分支详情「总览」视觉化重排：八个等重方块 + 每服务一张大卡，换成分段健康环、CPU/内存堆叠面积图（含彩色图例）、网络双向面积图、部署耗时柱状图 |
| feat | cds | 新增系列色 token --series-1..5（双主题同值，过 dataviz 六项校验），语义四色 ok/warn/bad/info 保持状态专用不参与系列配色 |
| fix | cds | 指标环形缓冲改存内存绝对字节：没配 mem_limit 时 Docker 报的限额是宿主机总量，百分比四舍五入恒为 0，读不出信息 |
| fix | cds | 入口卡去重：原先抽屉头部一份、总览里一个「入口 N 个」死计数各说各话，现在只在总览面板出现一次 |
| test | cds | 新增 branch-overview-panel 守卫：接线、系列色跟实体不跟名次、token 双主题定义、内存绝对值 |
