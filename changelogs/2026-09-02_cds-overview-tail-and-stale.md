| fix | cds | 「其他 N 个」的当前值只算还在跑的服务：这个聚合项带不了 stopped 标记，后面那道过滤够不着它，于是已停容器停机前的旧读数会一直算进「其他」和顶部合计，谎称它还在吃资源。历史那一条仍含全部尾部服务——它画的是过去 |
| fix | cds | series 拉取成功后无条件整体替换 metricSeries。「非空才替换」是「铺底不覆盖已追加点」那套逻辑的遗留，留着它会让上一次的曲线在服务全删/历史过期后继续挂在屏幕上冒充当前数据 |
| test | cds | 新增 3 条守卫（尾部当前值跳过停机 / 历史几何不按当前状态裁剪 / 无条件替换），做过红绿闭环 |
