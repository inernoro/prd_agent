| feat | cds | 预览实例支持子实例专用 AI 静态钥匙 CDS_PREVIEW_AI_ACCESS_KEY（启动清洗后重映射为 CDS_AI_ACCESS_KEY），父实例的 AI_ACCESS_KEY 照旧清除 |
| fix | cds | 任务调度新建/编辑浮层改限高（原写死 820px 留大片空白），并删掉右栏时代残留的内层滚动容器 |
| refactor | cds | 任务调度改主从布局：左栏任务索引满高常驻，右栏按选中与否分「值班概览 / 单任务」两态，时间轴在选中态收成细带；删掉 2xl 才存在的第三栏分支 |
| polish | cds | 左侧系统导航栏瘦身：栏宽 72→60px，项高 52→44px，图标 20→18px，去掉悬空的首段分割线；十项累计从 520px 收到 440px |
| polish | cds | 左栏定档 72px：项 56x52、图标 20/描边1.75、标签 11px、行距 54（两位设计评审独立比对四稿后一致选定）；新增左栏选中态对比度守卫 |
| fix | cds | 任务调度结论条不再与统计段矛盾（今日挂过又恢复时说「均已恢复」）；待触发序列锚在已存 nextRunAt 上，间隔任务的时间轴右半边不再整体后移 |
| fix | cds | 动作抛异常时逐步结果不再丢失；结论条状态改有限枚举（停用不算在跑、被跳过不算恢复）；布局冒烟在空实例上按空状态走并打印跳过原因 |
