| fix | cds | 报告对象存储凭据配一半时改为抛错，不再静默退回本地盘 |
| fix | cds | 报告对象键拼上配置的前缀，此前前缀是死配置、报告落在桶根与备份混放 |
| fix | cds | 流水线聚合剔除上一代同名分支的墓碑，避免把在跑的分支报成「没验就合并」 |
| fix | cds | 三段分流收敛为单一口径，脏数据下三段之和不再超过总数 |
| fix | cds | 走向图按后端给的条数画项目线，不再自己截成 3 条导致图例对不上账 |
| fix | cds | 报告增删后一并重算流水线，且走静默刷新不把面板打回 loading |
| test | cds | 新增 36 条守卫，八条篡改逐一验过会红；修正一条把错误行为反向锁死的旧用例 |
