| refactor | cds | 任务调度页重构为值班调度台：结论条 + 今日调度轴常驻，右栏按是否选中任务分「值班概览 / 单任务」两态，左栏任务索引满高常驻 |
| feat | cds | 运行记录新增逐动作结果：每个动作各留状态、耗时与返回码，动作抛异常（网络失败 / 超时）时逐步结果照样留下 |
| feat | cds | 任务列表接口可投影后续触发时刻，序列锚在已存的下次触发时间上，前端不再复制一套到期判定 |
| feat | cds | 运行流常驻并带全部/失败/手动过滤与作用域开关，选中低频任务时可一键看回全部任务 |
| fix | cds | 运行记录保留策略重写：每任务各留 120 条；全局上限按任务均分而非按全局新旧一刀切；任务数本身超过上限时整任务淘汰，且先保还存在的任务、已删除任务的历史先出局 |
| fix | cds | 选中任务时按任务取它的完整运行史，低频任务的健康条、P50、细带、运行流不再被高频任务挤空 |
| fix | cds | 结论条状态改为有限枚举，每档一个显式判据：停用的不算「在跑」、「已恢复」要有成功重跑的证据、已删除任务的旧失败不参与判定、有连续失败时句子不再断在「今日。」 |
| fix | cds | 健康度 P50 只统计真正执行过的运行，跳过写入的零耗时不再压低中位数 |
| fix | cds | 已过点还没跑的任务归「需要注意」，不再安静地待在「即将触发」里 |
| fix | cds | 今日调度轴按真实本地日长度换算，刻度与事件走同一套基准，夏令时切换那两天不再整体偏移、不再误收次日或丢掉当天末尾事件 |
| fix | cds | 编辑弹窗内的失败与成功都就地显示，此前都渲染在遮罩之后、用户只看到忙碌态停下；弹窗内不再有重复的关闭控件 |
| fix | cds | 窄屏兜底：调度轴包横滚容器、单任务详情头竖向堆叠、固定宽统计段等宽度够了才露，结论条不再被挤没 |
| refactor | cds | 左侧栏改为「图标 + 两字短标签」，当前页用竖条 + 抬升底 + 主色图标 + 满墨标签，主色面积由约 3400px² 降到约 60px²；「接入 Agent」不再是第二块橙，强调改为图标右上一颗点 |
| refactor | cds | 左栏工具组跟着导航贴顶，栏底只留主题与账号；图标统一为几何符号（向上箭头 / 表盘 / 文档 / 推子 / 半明圆 / 警示圆） |
| refactor | cds | 清掉 18 个文件里 113 处按钮内的死图标尺寸：调用点写的值一直被统一样式吃掉 |
| test | cds | 新增调度台判据与守卫：结论条枚举表、时间轴与健康度算术、本地日边界、逐动作结果、保留策略三档、页面接线（错误出口 / 打开编辑器清错误 / 按任务取运行史）、按钮图标零残留、左栏选中态双主题对比度 |
| rule | platform | 隔离台账新增通道 9（CDS 项目级 env 跨分支穿透）与 2026-09-01 事故行；该行明确标注被撤除的子实例静态钥匙方案不要照做，并写清正解与判据 |
| docs | cds | debt.cds.md 补三条并按文档规则改写为现状/影响/取舍/判据：日计数分母被保留策略截断、「接下来 6H」按条数投影少报、Agent 没有免登录自测子实例预览的通道 |
| fix | cds | 子实例静态钥匙守卫的扫描范围扩到残留真出现过的那几处（源码 / 两份 compose / 隔离台账 / debt / changelog 碎片）——此前只扫一个文件，防不住它自己记录的那三次复发；同时更正「换成项目级 Agent Key 就行」的说法：那把钥匙比对的是收方实例自己库里的哈希，子实例独立存储、认不了父实例签的 |
