| fix | cds | 合并主干后修复主域默认站选取：根路径声明与按名兜底都在分支声明过的全部服务里选，主入口不可路由时不发默认路由，避免主域滑到兄弟网关 |
| security | cds | OpenDesign 会话必须由已验证的 MAP 连接来源发起，workspaceTransfer 的下载与回写地址逐一钉在该来源上，堵住项目级 Key／全局 Key／仪表盘 cookie 三条路径下的 SSRF |
| fix | prd-api | 原生 Responses 非流式响应改为先读完校验再交付，上游 200 却缺终态或内容类型不符时如实返回 502，不再让调用方读到一次干净的 200 加残缺输出 |
| fix | llmgw | 流式原生响应在终态缺失时可观测地终止（补 error 事件并中断连接），不再干净收尾冒充完整成功流 |
| fix | prd-api | 完整版本预览的可嵌入来源跟随已声明的 Cors:AllowedOrigins，跨源部署下 iframe 不再被 frame-ancestors 'self' 挡成空白；跨站嵌入时预览票据 cookie 降到 SameSite=None，同源部署维持 Strict |
| fix | prd-admin | 知识条目选择弹窗不再在容器上叠整体内边距（会和分区内边距叠起来），并把钉死共享弹窗实现的那条断言收回本组件自己的契约 |
| fix | prd-api | OpenDesign 运行时数据面放行清单补上 llm/v1/responses（Codex 运行时用的正是 responses 协议），并加反射守卫防止新增端点再漏登记 |
| fix | cds | 连接凭据放行 MAP 的单条会话回读（instance:read），停止返回不可判定错误时才分得清「已经没了」与「真失败」；会话列表仍不开放 |
| fix | prd-api | 设计运行时模型代理不再吞掉上游刻意造出的中断：已发头就同样中断下游，未发头返回 502 说明被上游中断，调用方断开仍保持安静 |
| test | prd-api | 原生 Responses 终态失败用例不再断言「已转发字节仍在」——abort 与缓冲刷出是竞态，那条断言会随机红；判据收敛为「要么中断、要么有 error 事件，且不能干净收完残缺内容」 |
| fix | prd-api | 网页生成与改写把网关 Start 分片解析出的实际模型透出成 model 事件并落到 run 上，面板顶部按「模型 · 平台」展示，满足 ai-model-visibility |
| fix | prd-admin | 生成弹窗与改写面板订阅 model 事件并在顶部渲染实际模型与平台，值全部来自后端不推断 |
| ops | cds | 索引目录退出部署链路：撤掉 mongodb-indexes 一次性容器与 api 对它的启动依赖，索引仍由 DBA 手动跑，并补守卫防再加回来 |
| fix | prd-api | 实际模型落库补上：worker 逐字段写库，原先只改内存对象，导致 run DTO 字段在而值恒为 null，刷新后面板显示不出模型 |
| fix | prd-api | 公开生成流的事件白名单补上 model，否则 worker 发出的模型事件在 SSE 出口被静默丢弃，前端永远收不到；并加守卫按 worker 实际 append 的事件名逐一比对白名单 |
| security | cds | 伙伴侧 workspaceTransfer 的下载与回写一律不跟随重定向：会话创建时钉死的 origin 在 3xx 之后不再成立，改走唯一入口 fetchPartnerTransfer 并把 3xx 判成失败，另加接线守卫防止新调用点绕过 |
| fix | prd-api | 刷新恢复读回的 run DTO 补上 ResolvedModel／ResolvedPlatform，否则模型事件只在流的开头出现一次，刷新之后徽章再也回不来；并加守卫对齐两个 Controller 的 run 投影 |
| fix | prd-admin | 生成弹窗与改写面板的恢复路径读回模型徽章，且弹窗重开时清空上一轮模型（常驻挂载，不清会把上一轮的模型当成本轮的显示出来）；平台兜底称谓收敛成共用常量 |
| fix | prd-api | 删除站点遇到发布租约而推迟清理时不再往团队活动流记「删除了站点」——202 也是 2xx，会被记成已完成的删除，而站点还在；改用过滤器既有的抑制钩子，等真删掉再留痕 |
| fix | prd-admin | 生成弹窗的恢复轮询把 NOT_FOUND 当终态收尾（停 generating、清 sessionStorage 旧 run），不再把永久失败当断线每 1.5 秒无限重试、让用户连下一次生成都发不起来；改写面板原本就是这么处理的 |
| fix | prd-admin | AI 调整大纲时把当前知识来源一并带进新的大纲 run，原先传空数组会让确认生成撞上后端的来源一致性校验（409 outline_knowledge_mismatch），知识驱动的 PPT 一经调整就再也生成不出来 |
| fix | prd-api | 实际模型落库改走与 phase 相同的租约闸：丢了租约的 worker 不再覆盖接管者写入的模型、也不再推一条骗人的 model 事件，未命中即按租约丢失停手 |
| fix | prd-api | Redis 投影不可用时的 Mongo 兜底补发 model 事件（生成流与改写流各一处），值就在库里，不补的话「读不到模型」跟「这次没有模型」长得一模一样 |
| fix | prd-admin | 删除被发布租约推迟时不再把卡片抹掉：两个单站点删除处理器改为先看 deleted 再决定，如实留住卡片并说明在等什么 |
| test | prd-api | 模型落库守卫从钉住实现字面量改为断言行为：原先断言 `Set(item => item.ResolvedModel` 这串写法，补租约闸把写入抽成 PersistResolvedModelAsync 后 lambda 形参改名，守卫就红了而代码其实更对——反向锁死住实现的断言（形状 4a）换成「模型分支必须调落库方法、落库方法必须写这两个字段」 |
| fix | prd-api | 设计模型代理超时且已发过响应头时中断下游连接：不中断的话 Kestrel 会把它收成一次干净的 200 EOF，超时被抹平，调用方读到「完整的成功」；上一轮只给上游中断那一支加了 Abort，超时这一支漏了 |
| fix | prd-api | 改写流的 Mongo 兜底补发 model 事件时去掉 !redisProjectionAvailable 条件：该标志只在本 Controller 读 Redis 失败时才翻，而 worker 的写入侧独立失效，写侧挂了读侧好着就会把模型事件永久抑制 |
| fix | cds | 基础设施维护 job 记进程代次，读取点先把上一个进程遗留的 active 收敛成 failed：执行体只活在进程内存里，CDS 在 begin 与 finish 之间重启会让这条记录永远挡住凭据轮换且无从清理 |
| docs | cds | compose 就地写明 llmgw-serve 的就绪声明实为存活检查（readyz 带密钥门、CDS 探针匿名且把 401 当就绪），三个候选方案与取舍记入 debt 台账待作者拍板 |
| security | cds | 密封旧版迁移凭据前先脱敏日志：publicDataMigration 靠 source/target 上的明文密码比对着抹掉 log/progressMessage/errorMessage 里的密码，密封把明文拿走后就再也抹不掉了，存量 migration.log 里旧管线写进去的 --password <secret> 会从接口原样吐出且永久留存 |
| security | cds | 凭据升级的落盘顺序改成备份在前、主文件在后：该流程只在「主文件报告发生变化」时触发，先写主文件则崩在中途会让备份里的明文永远不再被重扫；倒过来写则崩溃后下次启动仍能检测并续跑 |
| perf | prd-api | 版本历史列表排除整页 HTML 与文件字节数组：一条记录可带多兆的整页与整包文件，列表一次取 100 条而对外只映射元数据，不排除就等于打开版本面板即分配几百兆；「只带元数据」同时写进接口契约，不在实现里偷偷 Project |
| fix | cds | 凭据轮换的 verify 与 enumerate 对齐判据：认哪个应用连接键改用 profile 自己声明的键（写死 .NET 键名会让靠 ${CDS_*_URL}/MONGO_URI/DATABASE_URL/CACHE_URL 接入的消费者查无此键、整次轮换回滚），就绪探测改走 profile 声明的路径而非按 api/llmgw 命名设白名单；已知形态仍做深度校验，认不得的退到「声明的探针返回 200」 |
| fix | prd-admin | 团队空间里「引用知识生成」的网页也归属该团队与分组：归属逻辑抽成共用函数，上传与生成两条创建路径共用；深链直接开生成弹窗时也快照当前空间，不再沿用上一次的旧值 |
| fix | prd-admin | 主区外边距四边同宽：外壳桌面分支 px-4 py-3（左右 16、上下 12）改为 p-4，消除方向差 |
| fix | prd-admin | 网页托管桌面端不再渲染那个恒为空的 toolbar 包裹层：它高度为 0 却仍是 flex item，和下一个子元素之间白吃根上的 gap-4，把整页内容多推下去 16px（顶部实测 29px、左侧 17px 的来源之一） |
| fix | prd-admin | 生成弹窗的 sessionStorage 访问全部收敛进带 try 的封装：那次写入夹在「服务端任务已创建」与「进入流式 try」之间，隐私窗口/配额用尽时会抛异常并就地中断——服务端继续生成，弹窗永远停在「正在校验所选知识」 |
| fix | prd-admin | PPT 页不再把知识正文写进 session（正文可达数兆且同一份存两遍）：超配额时 saveSession 静默吞掉、快照停在上一版，刷新后连 runId 都恢复不出来；恢复只需身份，正文不落盘 |
| test | prd-admin | 终态分支守卫改断言行为而非 sessionStorage.removeItem 的字面拼写——收敛进封装后它会红而代码更对（形状 4a，今日第二次同形） |
| security | cds | 旧版迁移日志里「紧跟密码旗标的那一个值」不看长度一律掩掉：创建接口对口令长度没有下限，一两位的口令会跳过 length>=3 的整串脱敏，而密封紧接着拿走明文，那串密码从此永久留在 GET /data-migrations/:id/log 里 |
| refactor | cds | 旧版迁移遗留文本（log / progressMessage / errorMessage）的脱敏收敛成唯一口径 redactLegacyMigrationText，升级与对外投影两处共用，不再各写一套 |
| fix | prd-admin | 生成任务恢复不到（NOT_FOUND）时不再谎称「原来的知识与要求仍保留，可以直接重新生成」：打开弹窗已清空要求、无 initialSource 时连知识也清空，生成按钮此刻是禁用的；文案改为按真实状态分两种说法 |
| fix | prd-api | 保存分享时复制循环的三个早退出口统一先清理已上传对象：出口都排在 InsertManyAsync 之前，那批对象没有任何 HostedSite 认领也没有清理账本，而去重那关看的是 HostedSite，插入没发生就不算数，用户每重试一次都会再留下一批孤儿对象 |
| fix | prd-api | 改写流的 Mongo 兜底补发 phase 也去掉 !redisProjectionAvailable：写侧独立失效时该标志恒为 true，阶段与进度会被一路抑制到终态，用户盯着几分钟不动的进度而库里一直在推进；补一条守卫钉住两条流的四处补发都不看读侧健康 |
| fix | prd-admin | 分享页修改坞的 Escape 只关最上面那一层：嵌套的知识选择弹窗在捕获阶段已处理并 preventDefault，坞不再把同一次按键当成关自己，避免连带丢掉还没保存的修改要求 |
| fix | prd-api | 保存分享的复制阶段外面再罩一层 catch → 收尾 → 重抛：finally 里释放借用围栏那步写 Mongo 失败时，异常会直接穿过整个方法、一个 return 都不经过，已上传的对象照样无主无账本 |
| fix | prd-admin | Escape 只关最上面那一层：预览浮层与分享页修改坞共用同一个 shouldCloseOnEscape，嵌套弹窗已处理掉的按键不再连带关闭外层、丢掉未保存的修改要求 |
| fix | prd-admin | 改写面板的模型徽章在进入 generating 的同一拍清空：原先排在创建任务之后，创建期间顶上挂着上一轮的模型，创建失败时更会把它留在一次根本没发生的调用上 |
| fix | prd-admin | 知识条目落盘只扔正文、留篇幅：页数估算靠的正是篇幅，刷新后一篇长文档原本会被估成最低 4 页，而服务端严格按客户端给的页数执行 |
| fix | cds | env 文件目录 fsync 的路径为空时退回当前目录：CDS_ENV_FILE 写成相对文件名时 dirname 是空串，os.open 当场抛，而它排在 mv 之后——文件已换好却报失败，strict 模式下留半套配置。两处写法一并修 |
| fix | cds | 凭据轮换读部署结果改为解析 SSE 终态帧并要求 complete 的 ok 为真：原判据是「出现 event: complete 或任意 "ok":true」，而失败部署同样发 complete（只是 ok 为 false），于是失败被读成成功，下游继续走向撤销旧凭据 |
| fix | cds | OpenDesign 产物导出改用本次会话真正在跑的镜像：能力探测、准备、主会话都走 this.image，只有导出退回编译进来的默认镜像，离线节点上会整轮跑完只在导出失败，且失败指向一个与本次运行无关的镜像 |
| fix | prd-admin | 生成弹窗的模型徽章也在进入 generating 的同一拍清空：与改写面板同一处判据的另一半，不关弹窗再发起一次生成时，创建期间顶上挂着上一轮的模型，创建失败时更会留在一次根本没发生的调用上 |
| fix | cds | 维护 job 落盘失败时先摘掉内存里那条预约再抛：它盖的是当前代次而收割器只收上一代，调用方又拿不到 handle 去 finish，一次写盘抖动就把该服务的凭据轮换钉死到进程重启 |
| feat | prd-api | 分享响应逐站点带上 viewerCanEdit：由后端用编辑端点那同一道角色门算出（抽成 CanEditSiteAsync，编辑入口自己也改走它），匿名恒为 false |
| fix | prd-admin | 分享页编辑坞的门改用后端给的 viewerCanEdit，不再拿 createdBy（「谁建了这条分享链接」）当编辑权判据——团队编辑者可建分享，旧判据会让真正的站点主人进不去、只建过链接的人反而看得见 |
| feat | prd-api | 生成任务的目标团队随创建请求冻结到 run 上，并在用户还在场那一刻用 CanPublishIntoTeamAsync 校验（不通过当场 403）；建站时由服务端应用，失败原因记在 run 上并随终态事件下发 |
| fix | prd-admin | 生成弹窗把目标空间随请求送出（打开那一刻冻结），完成回调不再自己归属团队——用户在终态前关掉页面时那条回调根本不会执行，网页会静默留在个人空间 |
| fix | llmgw | 控制台就绪探针超时时真的取消下游 Mongo 探测：原先只让调用方走人、探测留在后台跑，Mongo 掉线期间每 5 秒堆一条在途操作；外层仍留一层 5 秒上限，探测实现不认令牌时也成立 |
| fix | prd-api | 公开创作者榜里任一头像查存在性失败不再打成 500：这条校验只为「别把已删对象露出去」，查不清就按头像不可用降级、前端退回首字母占位，降级留痕，调用方取消仍照常上抛 |
| docs | doc | debt.platform.open-design 记第十五轮复审三条的处置：百宝箱 wip 那条不改（网页托管在主干早已是无 wip 的正式导航项，只是补了移动端入口），OpenDesign 模型分片维持 B 类，另两条按 A 类修掉 |
| fix | prd-api | 团队活动流留痕改成提交之后的尽力而为：站点文档已写进库，留痕再抛出去会把一次已成功的归属报告成失败——controller 那条路给 500，生成任务那条路记下 DestinationApplyError，终态事件于是告诉用户「网页留在个人空间」，而它已经在团队里了；吞掉但留日志 |
| security | prd-api | 归属失败给用户的那句话收敛成有限枚举，不再用异常的 Message：它经 run 落库、经终态事件下发、最后原样进浏览器提示框，而 Mongo／网络／驱动的消息里常带库名、主机与协议状态；诊断细节只进服务端日志 |
| refactor | prd-api | 测试里的一次性 Mongo 库收敛成共用 fixture：三份私有拷贝逐字相同、只有库名前缀不同（形状 3） |
| fix | prd-api | MAP 的 /health/ready 超时改成真的取消下游探测：Mongo / Redis 那两条此前只让调用方走人、探测留在后台跑，依赖掉线期间每次就绪请求都堆一条在途操作 |
| fix | prd-api | /health/ready 的对象存储探测补上与其余依赖相同的超时：此前它没有上限，存储卡住时 Task.WhenAll 一直等、整个就绪检查挂着，部署就绪检查会超时而调用方在它后面排队 |
| fix | prd-api | 原生 Responses 缓冲路径改为按手里那份完整 body 判定：观测器逐字累积的 4 MiB 字符上限与缓冲接受的 32 MiB 不一致，中间那一档合法响应会被判成未完成，一次成功的调用被退成 502 OUTCOME_UNKNOWN |
| fix | prd-api | 归属失败写回 run 加租约闸并对齐提交后阶段的判据（Running 或 Committing）：只按 run id 写的话，丢了租约的 worker 会把接管者已跑成功的那一轮改写成「归属失败」；被取消不再当成归属失败上报 |
| fix | prd-api | 回退重放放行过期的 Publishing 尝试：进程在标成 Publishing 之后、切换站点指针之前停掉时，指针永远对不上，而发布方法自己有按 TTL 接管的路径——此前被这道筛子挡掉，40 轮全空转，这条回退记录对回退接口永久失效；超时改为抛出最后一次的真实原因而非笼统的「正在处理」 |
| ops | prd-api | 网页托管删除清理的两条索引补进 DBA 可执行清单与索引文档：本仓库禁止启动时自动建索引，只写在 CreateIndexes() 里等于线上根本没有，而清理任务每分钟一轮整表扫描加排序 |
| test | prd-api | 新增索引目录覆盖守卫：代码里定义的每条索引都必须在 DBA 清单里有同名条目（按清单声明的名字集合比对，不做子串包含），落地时差集恰好为零 |
| fix | cds | 连接凭据放行 MAP 按 clientRequestId 回查会话（instance:read）：OpenDesign 创建必然返回 202（容器后台起），MAP 只能靠这条回查才知道预约建成了哪一份运行时；不开就是每次轮询都在进路由前被门挡掉、会话停在 Creating 直到 15 分钟超时——也就是 OpenDesign 经配对连接根本起不来 |
| test | cds | 两处「会话列表不该开」的断言改为按真实用途放行（同一个「未被 MAP 使用的旁路」前提连错两次，都是反向锁死住缺陷的断言） |
| test | cds | 新增跨服务接线守卫：从 MAP 的会话服务源码扫出它用连接凭据打的每一条「方法 + 路径」，逐条要求白名单表里有显式结论；判据到方法级，因为上一次那条路径上 POST 早就开着、只按路径判看不出 GET 没开 |
| fix | prd-api | 远程设计结果上传成功、随后那次「写完了」的状态落盘失败时不再把预约永久钉住：恢复器按设计拒收「writing + 本代进程」的预约（防止删掉可能还在上传的对象），而这条保护恰好把唯一能收拾残局的动作也挡掉，对象与预约一起留到进程重启；调用方带着「这一次上传确实收工了」的事实放行回收，并留日志 |
| fix | prd-api | 上一轮那个「写入者确实收工了」的出口补到活跃闸上：提交走到那里时 run 还握着租约，活跃闸先一步返回，出口一次都没被走到——修复在真实场景里是空的 |
| test | prd-api | 对应守卫改用「活着且握着租约」的真实 run：上一版用已终态 run，恰好绕开活跃闸，绿得毫无意义（形状 4） |
| test | prd-admin | 补一条用例钉住「2xx 裸对象按 data 收下」：复审据此判本地编辑必然显示格式异常，核对下来引的是 FormData 那一支的兜底，两支行为相反 |
| docs | doc | debt.platform.open-design 记下这条反驳与不逐个改包的理由 |
| fix | prd-api | OpenDesign 远程会话处置补上直接停止这条落点：原先它挂在「登记清理账本」分支的 else if 上，走过完成事件后账本登记一失败（返回 null 或抛错）就再也落不到停止，而恢复器只收有账本的会话，远程容器会一直占到 CDS 生存期上限；会话由本次运行独占（按 run 新建的 SessionContainer），执行器一退出直接停止永远正确 |
| fix | prd-api | 演示稿保存失败一律标成终态：三条返回 false 的出口原本只有一条标了 error，另两条把 run 留在 running 等 15 分钟的陈旧扫描，而本地编辑是同步端点、没有第二个写入方，用户拿到 500 而版本列表挂着一条没人推进的幽灵版本 |
| refactor | prd-api | 保存失败原因收敛成唯一出口（内层只返回「成功 null／失败原因」，外层统一落 error），让「返回 false 必已标 error」由编译器保证，不再依赖三处各自记得照做 |
| test | prd-api | 新增会话处置行为守卫六条（账本登记返回 null／抛错／成功、未完成轮次、已有账本、停止本身抛错）；把写法退回 else if，其中两条当场变红 |
| docs | doc | debt.platform.open-design 记第十六轮复审三条处置：两条按 A 类修掉，本地编辑重试去重判 B 类（需新语义加唯一索引，索引是 DBA 迁移，不在本 PR 展开） |
| security | cds | 会话回查放行收紧到「必须带 clientRequestId」：处理器把缺省查询值当成不过滤，只按路径放行等于把这把连接凭据名下的整份会话清单（clientUser、模型端点、工作区、仓库、容器、运行时元数据）一起开出去，而 MAP 只需要自己刚发起的那一份；上一版只把这件事写进了注释 |
| refactor | cds | 路由规则支持查询串判据（requireQuery）：不点头就当这条规则没命中，落到表尾即拒绝；鉴权入口把 req.query 一并传进判据 |
| test | cds | 跨服务接线守卫升级到「方法 + 路径 + 查询参数名」：从 MAP 源码扫出它实际带的参数（字面量跨行也扫得到），MAP 哪天不再送 clientRequestId、或白名单哪天不再要求它，两头都会红 |
| test | cds | 鉴权入口接线守卫从钉住 `connectionTokenRequiredScope(req.method, req.path)` 这串字面量改为断言「按 method + path + 查询串查表」——多传一个实参就红而代码更对（形状 4a） |
| fix | prd-api | 保存写库抛异常时先回读权威状态再决定降级：这一支的失败是「不确定」不是「没发生」，内容已落库而回执丢在路上也走这里，无条件标 error 会把一条真的完成版本改写掉、用户再也打不开它；回读也读不到就不下结论，留给陈旧扫描兜底 |
| test | prd-api | 新增保存失败处置决策守卫四条（非歧义失败不打库直接标失败、歧义且确已落库按成功、歧义且确未落库标失败、歧义且读不到不猜）；撤掉回读判据其中两条当场变红 |
| fix | prd-api | Redis 就绪探测合并成一次在途调用：PingAsync 不收取消令牌，超时只能让调用方走人、停不掉它，而本仓库量到过 multiplexer 半失活时命令不按 SyncTimeout 抛异常直接挂住；就绪端点每几秒被打一次，一次 Redis 故障会攒出成百条谁也停不掉的在途探测。只在上一次未结束时合并，不缓存结论 |
| fix | prd-api | 已完成的发布收据不再绑站点当前指针：收据是历史事实，站点后来发了新版本把指针顶走不该让重放拿不回它——而恢复器明确不收 completed 意图，顶掉之后这条请求永远 503 说「正在自动恢复」；改判「这条版本自己确实完成过一次发布」，半落地的版本仍然拿不到收据 |
| test | prd-api | 新增就绪探测合并守卫（连打五次只发起一次真实探测、上一次结束后不再合并）与发布收据重放守卫两条（被新版本顶掉仍能重放、半落地版本仍被拒）；三处判据各自撤回后都当场变红 |
| fix | prd-api | 还原不出来的回退当场拒绝，不再悄悄发布一个残缺站点：多文件站点的样式与图片是对象存储里的旁挂对象，只有整包版本存下了它们；只留入口 HTML 的基线在下一次整包发布把旧对象回收之后，回退会得到旧 HTML 配新版本的样式，或指向已删对象 |
| feat | prd-api | 版本记录建档那一刻线上站点的内容形态（CapturedContentShape），回退判据据此判「这条版本自己够不够还原当时的站点」；存量版本无此字段时退让到按站点当前形态判断 |
| test | prd-api | 新增回退保真度守卫六条（整包必可还原、单文件基线可还原、多文件基线不可还原、无形态记录退让、空文件表不算多文件、生成包六件套仍算自包含）与一条接线集成用例（形态真写进库、回退真被拒且不留半条草稿）；撤掉记录与拦截当场变红 |
| test | prd-admin | 补一条用例钉住「刷新后取到同一个 history key 就落回同一命名空间」，直达打开（default）与站内跳转两种条目各测一次：复审判定刷新会把 key 一律变成 default 导致恢复游标丢失，核对 react-router v7 源码后不成立 |
| docs | doc | debt.platform.open-design 记下这条反驳与证据（key 写在 window.history.state，随会话历史条目跨刷新存活） |
| fix | prd-api | 归属目标团队挪到所有还会失败的持久化步骤之后：补偿只认「私有、未发布、未分享」的站点，先盖团队再撞上续租或版本快照失败，补偿就一个候选都找不到并随即清掉清理计划——任务报失败，而站点和它的对象仍挂在用户选的团队里，没有任何东西会来收拾 |
| test | prd-api | 新增顺序守卫：版本快照失败时 SetSharedTeamsAsync 一次都不许被调用（带 companion 断言确保快照确实走到了）；既有那条租约用例补齐快照桩。把归属挪回原位当场变红 |
| fix | prd-api | 运行事实探测失败不再一律说成「请检查系统连接」：CDS 那一版没有运行时目录路由（404）时明说是版本/路由不匹配、该升级 CDS，其余仍按连接问题提示；判据取抛出点带出来的真实状态码，不匹配异常文案 |
| fix | prd-api | CDS 请求失败的异常带上上游真实状态码（UpstreamStatus）：原先状态码只拼进消息字符串，而 HttpStatus 恒为 502，分不出「路由不存在」与「连不上」 |
| fix | prd-api | 运行事实探测的三条失败出口补日志：原先超时与通用失败一个字都不记，用户看到一句笼统提示、运维在服务端也查不到任何线索 |
| test | prd-api | 新增诊断分类守卫三条（404 说版本不匹配、传输失败仍说连接、503 不误判成版本问题）；撤掉分类当场变红 |
| fix | prd-api | 基线按「这个站点这一版」判定，不再按创建者比对：站点主人打开一次版本历史建了基线之后，团队编辑者再发起 AI 微调会在模型跑完之后被拒，任务判失败、草稿存不下来；调用方能不能动这个站点在拿 entry 时已由 CanEditSiteAsync 判过 |
| test | prd-api | 认领基线的拒绝变体把 other-owner 换成 other-site：前者逐字要求「创建者不是调用方就拒」，正是这条缺陷本身（形状 4a）；另加一条协作编辑守卫（主人建基线、队友认领成功），还原创建者比对当场变红 |
| docs | doc | 更正 debt 台账里一处事实错误：IAssetStorage 有 TryDownloadBytesAsync，历史版本存字节不需要新增存储抽象，那条后续工作比当时估的便宜 |
| docs | doc | debt 台账记下「沙箱预览里的 ES 模块取不到票据」：opaque origin 下模块按 CORS + same-origin 凭据取，cookie 一律不带，而预览资源路由以 cookie 为门；当前生成链路被提示词禁止输出任何 script，故无生产路径，判 B 类并写明「放开带脚本产物时必须同批把票据挪进 URL 路径段」 |
| fix | cds | .cds.env 的读取按写入的真实格式解码：env_upsert 用单引号包裹并把内部单引号写成转义序列，read_env_value 却只剥双引号，读回来的是带引号字符的字面量；那些值正是 init 重跑时「回车保持原样」的默认值，会被原样再写一遍，引号从此成为密码、用户名、JWT Secret、根域名本身的一部分，重启后仪表盘登不进去、路由也对不上。双引号那一支保留以读旧版本存量文件 |
| test | cds | 新增 .cds.env 往返行为守卫七条：把 env_upsert 与 read_env_value 从脚本取出在 bash 里真跑一遍，覆盖普通值、含单引号、含双引号、含空格与等号、域名列表，外加双引号与裸值两种存量形态；撤掉解码五条当场变红 |
| fix | prd-api | 补回的来源块按原文顺序插入而非追加到页尾：认领了块1、块3 而漏了块2 时会变成 1,3,2；页上第一个认领的是块3 时更会变成 3,1,2，而 Bind 原样保留这个顺序，正文于是渲染到了下一节标题后面——与该方法自己承诺的「按原文顺序」相反 |
| test | prd-api | 新增两条顺序守卫（漏在中间的块插回中间、排在全部认领块之前的块插到页首）；改回追加两条当场变红 |
| fix | prd-api | 预览票据 cookie 收敛到自己的访问路径：Path=/ 时 15 分钟寿命内签发过的每一张票据都会附在打到本域名的所有请求上，连着翻几十个版本就能把 cookie 与请求头堆到上限，打坏的是与预览无关的普通接口且要等过期才恢复；路由前缀抽成常量，路由声明与 cookie 路径共用一份 |
| fix | prd-admin | 预览正文只交出属于当前键的那一份：发布换掉 siteUrl 的那一次渲染里 state 还装着上一条的正文，调用方拿它闩成 srcDoc；等新键取回来发现走不了 srcDoc，iframe 的 src 与 srcDoc 同时为空，白屏，本该退回的直链再也走不到 |
| test | prd-api | 预览 cookie 断言补 Path 收敛（并断言不是 Path=/） |
| test | prd-admin | 新增 entryForKey 行为守卫四条与一条接线守卫（hook 必须走它，不得直接交出 state 里的上一条）；撤掉判据与接线四条当场变红 |
| fix | prd-api | 换版后的孤儿对象键改由一条共享判据算出（旧文件表里有、新文件表里没有）：原先按「这次删没删 sidecar」分支，单文件站点因为 Files.Count 等于 1 走不进那一支，obsoleteKeys 恒为空，而入口每次发布都会写一个带版本号的新 key，旧入口既不再被站点引用也没进回收队列，于是每发布一次就在对象存储里留下一份永不回收的旧正文；局部换入口时仍被新文件表引用的 sidecar 一律不入队 |
| refactor | prd-api | 重传与整包发布两条路径一并接到同一判据上，行为不变（两者的新 key 都在新版本目录下，本来就不保留旧键），避免三份近似判据各自漂移 |
| test | prd-api | 新增孤儿键守卫八条：单文件换入口、局部换入口保留 sidecar、自包含重写回收全部旁挂、整包换版、两条路径共用同一对象时不得删、空键不入队，外加一条接线守卫（三条发布路径必须都走共享判据）与一条真跑 Mongo 的行为守卫（单文件站点发布后旧入口对象必须真的被删）；改回分支写法后接线守卫与行为守卫当场变红 |
| perf | prd-api | 版本面板的查询补上索引 `idx_hosted_site_revisions_site_created`（`{SiteId:1, CreatedAt:-1}`）：`hosted_site_revisions` 是全局一张表，站点越多它越长，而唯一那条回退幂等索引带 partial filter、第二段也不是 CreatedAt，服务不了这个排序——没有本索引，打开任意站点的版本面板都是整表扫 + 内存排序。索引在 MongoDbContext 与 DBA 清单 `scripts/mongodb-indexes.js` 两处同时登记（前者从不执行，只在后者生效） |
| test | prd-api | 新增一条守卫把查询形状与索引键钉在一起（按 SiteId 过滤、按 CreatedAt 倒序 对应 清单里 `{SiteId:1, CreatedAt:-1}`）；从清单里撤掉该索引，这条与既有的索引清单覆盖守卫双双变红 |
| fix | prd-admin | 网页托管的百宝箱条目补回 `wip: true`：两份计划都写着没验收（设计生成的产物质量与最终盲验未通过、OpenDesign 六步全部未验收），按 navigation-registry 的注册流程，百宝箱工具默认带 wip，规则 #8 真人验收通过后才删。注释里写清了它管得到什么——`ToolboxItem.wip` 目前没有任何渲染方，是登记账不是用户可见标记；真正渲染「施工中」的 navRegistry 那条 `/web-pages` 是主干既有的正式导航项，本 PR 不动它 |
| docs | prd-agent | 台账更正一条自己的错判：上一轮把补 wip 的理由写成「不挂标等于替用户宣布能用」，查清后不成立（该字段无渲染方）；同时记下 `ToolboxItem.wip` 是只建了一半的链路（形状 2），十几条内置工具挂着它却渲染不出来 |
| test | prd-admin | 新增一条 wip 守卫并在注释里写明何时可以连同字段一起删；撤掉 `wip: true` 当场变红 |
| chore | prd-agent | 本 PR 的 11 份 changelog 碎片按日期收敛成 6 份（同一天的合成一份）：assemble 脚本按文件名里的日期分组，同日多份与一份产出完全一致，已用 dry-run 逐字比对证明；跨日期不合并，否则 13 天的条目会被塞进同一天，更新中心按天渲染会显示错误日期 |
| fix | prd-admin | 网页生成与网页修改的两条 SSE 进度流改走 `buildApiUrl`：`connectSse` 内部是裸 fetch、不套 API 基址，配了 `VITE_API_BASE_URL`（前后端分开部署）时相对路径会打到前端自己身上、拿回一坨 HTML，前端立刻显示「进度连接中断」而服务端任务照跑——用户看到一次并不存在的失败。同文件的提问流早有同样的注释与写法，这两条是漏掉的 |
| test | prd-admin | 新增三条真跑 fetch 的行为守卫（配了基址时两条流都打到该源、带续传游标、不配基址时仍是同源相对路径）；撤掉 buildApiUrl 两条当场变红 |
| fix | prd-api | 来源块补齐后的页在 `done` 之前按 page 事件重发一遍：补齐此前只改落库那一份，前端手里仍是补齐前的 `sourceBlockIds`，点「确认，生成 PPT」提交的就是那一份，与库里已修好的计划对不上，直接吃 422 `source_plan_binding_mismatch`——流程看着一路正常，直到最后一步才炸。前端的 page 处理本来就按下标覆盖，重发即替换，不需要新事件类型；只重发补齐真正动过的页，没动的页不重发（免得前端对着没变的内容闪一下） |
| fix | prd-api | 同一个来源块被放到两页现在判错：`covered.Add` 的返回值此前被丢掉，只统计不判断，于是两页各自拿到同一份权威原文渲染两遍，而 `RepairCoverage` 那边是按「一个块归一页」建表的——同一件事两处判据不一致。判严的同时把大纲提示词从「全部ID必须至少覆盖一次」改成「全部ID必须覆盖，且每个ID只能出现在一页」，不能按一份没交代过的契约罚模型 |
| test | prd-api | 新增八条守卫：跨页重复（别名与长 Id 混写也算）、恰好一页仍放行、提示词已交代该契约、补齐差集只报真正变过的页、页内换序也算变过、以及一条接线守卫（重发必须存在、来自实际差集、且排在 done 之前）。三处修复分别撤回，对应守卫当场变红 |
| test | prd-api | 既有的四页四块用例改口径：那份输入同时犯了「跨页重复」与「漏块」两条，四页四块且不许重复时漏块不可能单独出现，故先报重复；纯 incomplete 的覆盖仍在补齐用例里（页数少于块数） |
| fix | prd-api | 预览票据 cookie 的 Path 带上 `Request.PathBase`：cookie 的 Path 是**浏览器看到的那条路径**，子路径部署（API 挂在 `/platform` 之类前缀下）时浏览器打开的是 `/platform/api/...`，写死从根开始等于签了一张永远不会被带上的票，每个文件 404、预览全白——比上一轮收窄之前更糟。同方法里的 redirect 本来就用相对路径、前缀安全，这里补齐同一口径。这是上一轮收窄 Path 时引入的回归 |
| fix | prd-api | 取消预热改为停掉该用户名下**全部**未认领预热，不再只停最新那一个：用户在确认大纲前换几次模型运行配置会留下几个各自独立的未认领预热，只停最新的，旧容器要占着 CDS 主体名额直到八分钟后过期，而默认并发上限只有四个——界面明明已经「取消预热」，真正要转换时却被自己刚才那几次换配置挤到没名额。判据与 `StopUnusedPrewarmAsync` 共用一处（此前一份停全部、一份只停最新） |
| test | prd-api | 新增两条守卫：子路径部署下 cookie Path 必须带前缀（且不退化成不带前缀那一条）、取消必须走共享判据且不得再自己挑最新的一个停；两处修复分别退回都当场变红 |
| fix | cds | 没有密封密钥时，旧版数据迁移凭据的升级改为**推迟**而不是抛错：这个迁移跑在 `initStateService` 里、HTTP 服务起来之前，而唯一能装上密钥的入口（`POST /cds-system/sealed-storage/initialize`）正是那个还没起来的服务——一台存着旧明文、又没配密钥的实例会永远开不了机，连来装钥匙都做不到。推迟不写入任何东西（明文留在它本来就在的地方），并打一条外因在前的告警说清缺什么、下一步做什么；「同时存在明文与密封凭据」仍然抛错（那是状态损坏，装钥匙也修不好） |
| fix | cds | 预览日志脱敏认外部路径前缀：此前只特判 `/_cds`，其余一律要求路径从 `/api/hosted-site-preview-files` 开头。子路径部署下代理收到的是 `/platform/api/...`，判不出来，票据会原样留在记录下来的 URL 里——安全加固恰好漏在它自己要防的那种部署上。改成在任意段边界上匹配受保护路由段，并在脱敏结果里保留前缀 |
| test | cds | 新增五条守卫：无密钥时推迟而非抛错、推迟时一个字节都不写、装上密钥后同一份状态能正常升、明文与密封并存仍抛错、带外部前缀的预览路径必须脱敏；两处修复分别退回都当场变红 |
| test | cds | 两条本 PR 自己写的断言随之改口径：「没有密钥就拒绝加载」改为「照常起得来且明文原样不动」，「/other/api/hosted-site-preview-files/value 属于无关端点」改为「属于带外部前缀的预览路径」 |
| fix | prd-api | 设计模型代理的兜底 catch 在已发响应头时也中断下游：上面两支各自写了一遍「已发头就必须 Abort」，唯独兜底漏了——而它接的恰恰是类型没被点名的那一类，`HttpRequestException` 不派生自 `IOException`，读 HTTP/2 响应体时抛出来就落到这里，只记一条日志就返回，Kestrel 于是把下游收成一次干净的 200 EOF，传输失败被抹平，OpenDesign 读到的是「完整的成功」。调用方自己断开仍然只记一条（终止权在它那边） |
| test | prd-api | 中断守卫补上兜底分支：此前它只截到文件尾，上一支的 Abort 正好让断言判绿，兜底有没有 Abort 根本没被测到——守卫自己漏扫了它要守的那一段；撤掉兜底的 Abort 当场变红 |
| fix | prd-api | HTML PPT 专用任务落库后、`BeginAsync` 建公共账本失败时回滚半成品：这一种谁都不管——恢复流程明确跳过无账本孤儿（它自己的注释写着「归属仍待明确」），过期判定也要先有账本才轮得到，于是它会永远停在 `running`，而调用方连这个 run 都没拿到。只回滚没有账本的那一种（有账本说明身份已建出，删了反而把公共账本变成孤儿），补偿失败不盖住原始异常 |
| test | prd-api | 新增三条守卫：无账本半成品可被回滚（先断言恢复流程确实不收它）、有账本时不得删、以及一条接线守卫（控制器必须在 Begin 失败时调补偿且照样 rethrow）；判据与接线分别退回都当场变红 |
| fix | prd-api | Redis 就绪探测的单飞合并加过期窗口：`PingAsync` 挂死后那条任务永远不完成，之后每一次就绪检查都复用它、都超时——**Redis 恢复了也没人再去 ping**，只能重启 API 才能重新变健康。合并本身成了第二个故障。窗口取依赖超时的十二倍（默认 5 秒即一分钟）：必须显著长于单次超时，否则每次轮询看到的都是「过期」，合并会被打散回一次请求一条；一分钟一条孤儿探测（`PingAsync` 收不了令牌，停不掉是它的性质），比一次请求一条好一个量级 |
| test | prd-api | 新增恢复守卫：第一条永远挂着、窗口内仍合并（不退回一次一条）、熬过窗口后重新发起于是恢复健康，并断言挂死那条仍挂着（只是不再挡路）；撤掉过期窗口当场变红 |
| docs | prd-api | OpenDesign 远程完成路径为什么不产出 `model` 分片，把答案锚在它自己那一行：`ai-model-visibility` 第 2 条要的是网关 `Start` 分片的 Resolution，而 OpenDesign 的模型调用在 CDS 容器内，MAP 结构上收不到那个分片；`workspace.Model` 是请求值不是解析值，容器自报属于推断不是解析。同一 file:line 已被提三次而源码那一行一字未写，答案只活在 PR 描述与台账里 |
| fix | prd-api | 站点删除围栏并进租约判断：内容版本与更新时间只挡得住「内容被改写」，而借用/发布登记只写租约与键数组、**不碰**这两个值，所以「读站点」与「登记键」一交错，删除就会拿着读取时那一刻的清单成功执行——发布方随后把对象传上去，站点没了、键也释放不掉，那批对象没有任何人认领。读取时本来就有一道租约判断，缺的只是「与删除在同一次原子操作里成立」，典型 check-then-act。刻意不改键清单：借用场景里那批键恰恰是源站自己的当前文件，并进删除清单等于在别人读取途中删源文件；租约的语义是让删除等，不是让删除跳过 |
| test | prd-api | 围栏两层守卫：结构层断言渲染出的查询里租约与发布中的键以「或」的形式在场（不带 Integration 标，CI 默认过滤跑得到）；真库层六例覆盖 Mongo 里 null 与「字段整个不存在」的判定差别、租约过期、键已释放但时间戳未回拨、以及存量无字段文档仍可删。撤回围栏条件时两层各变红一条 |
| fix | llmgw | 网关 serving 新增脱敏就绪路由 `/gw/v1/healthz/ready`：CDS 的容器就绪探针是匿名 GET 且把一切 < 500 当就绪，而 compose 此前把就绪声明指向带密钥门的 readyz——401 被当成「就绪」，依赖状态一次都没被评估过，这条声明等价于只探了「HTTP 端口起来了」。新路由与 readyz 判据同源（同一个就绪探针，自带 10 秒缓存与单飞锁，匿名不构成打穿依赖的放大面），区别只在端出多少：只用状态码表态，正文无组件名、耗时与摘要；要明细仍然只能走带密钥的 readyz |
| fix | cds | 凭据轮换不再要求就绪声明等于它的深检路径：同一个 `cds.readiness-path` 被两个要求相反的消费者共用——容器就绪门控要匿名只看状态码，轮换要带密钥读逐组件明细判 gateway-mongo。上一版用 `contract_mismatch` 把两件事焊死，于是就绪声明一改轮换就炸。改为认得的形态按形态取自己那条深检路径；认不得的形态没有深检契约，仍只打它声明的那条到 200 为止 |
| test | cds | 补守卫：serving 声明脱敏就绪路径时，轮换深检仍必须打带密钥的 `/gw/v1/readyz`。原先 `contract_mismatch` 那条分支没有任何测试引用，是个没守卫的分支 |
| test | prd-api | 就绪契约守卫改成断言新不变量：就绪声明必须指向匿名那条且不得指回 readyz；脱敏路由必须存在、必须按状态码表态、且不得端出组件明细；白名单必须精确匹配（退化成前缀放行会让 401 重新冒充就绪）。三处分别撤回都当场变红 |
| fix | cds | `exec_cds.sh init` 的四个凭据键整组原子：本 PR 早前把主干的单次整文件替换（`cat > "$ENV_FILE"`，天然保证四个值是同一组）改成了四次独立 `env_upsert`，而目录写锁是各抢各放——两个 init 并发就能交错出「A 的用户名配 B 的密码与 JWT」这种谁都登不进去的组合。拆出假定已持锁的 `env_upsert_locked`（目录锁不可重入，重入即 fail-closed 死锁），备份与四次写入共用一次取锁，任一步失败即报错退出并保留备份 |
| test | cds | 补两条守卫：`env_upsert_locked` 自己不得碰锁；init 的凭据段全程只取一次锁、四次写入全走已持锁那一版、备份在同一把锁内。把任一次退回自带抢锁的 `env_upsert` 当场变红 |
| style | changelogs | 换掉一条更新记录里的 U+2194：该码点在 Unicode emoji-data 里标为 `Emoji=Yes`，撞 AGENTS.md 第 0 条（最高优先级，全系统禁 emoji），改用「对应」二字 |
| fix | cds | 清理待处理的 OpenDesign 会话改为每次 stop 都真的重试清理：启动恢复一个进程只跑一次，它若因 Docker 还没就绪这类瞬时原因 bootstrap 失败，就把整批 OpenDesign 预约盖成 `resourceCleanupPending`；而 stop 路径在这个标志上提前返回，于是此后每次重试都到不了真正的 `agentWorkspaceSessionRuntime.stop`，恢复又永远不会重跑——响应写着 `retryable: true`，重试却可证明是空转，孤儿容器占着容量直到重启 CDS。删掉提前返回，落到下面本来就完整的清理尝试（真去 stop、用 `has()` 复核、成功清标志回 200、失败才留可重试账本）。`stop()` 走 docker 不依赖 bootstrap 状态，所以 Docker 恢复后这条重试是真能成功的 |
| fix | cds | 幂等回放收紧为「已 stopped **且** 不在清理待处理」：pending 意味着资源没被证明清干净，这时回 200 `idempotentReplay` 等于拿一条谎言把孤儿容器盖过去 |
| test | cds | 两条既有用例在反向锁死这个缺陷，按新契约重写并说明原委：一条的 mock `stop` 是空实现却期待 503（旧代码提前返回、stop 从没被调用过，用例名写着 on stop failure 而 stop failure 从未发生），改成让清理真的失败；另一条最后一行 `expect(stop).not.toHaveBeenCalled()` 字面要求「清理待处理时不许去清」，改为断言无论成败每次 stop 都必须真尝试过清理。新增一条守卫覆盖缺陷本体：瞬时失败后重试 stop 必须就地回收、不必重启 CDS，且清干净之后才配走幂等回放。退回提前返回当场变红 |
