| feat | prd-admin | 分享页新增「下载源文件」：登录访客可把托管页的源 HTML 取回本地，多文件站与 PDF 包装站按形态分档并在按下之前说清会拿到什么 |
| polish | prd-admin | 分享页顶栏改版：四个按钮从各写一套内联亮蓝改为统一 token 胶囊（一个实心主操作 + 其余安静），补 hover/active/focus-visible 与 reduced-motion，颜色全部走双主题 token |
| fix | prd-admin | 分享页首帧不再向托管域名发跨域直链请求：iframe 改为 pending/srcdoc/direct 三态（resolvePreviewSource），等正文期间不给 src，包装站与取不回正文时才转直链——治某些 App 内置浏览器把那次 text/html 请求当成下载、弹「Download：(null)」 |
| fix | prd-admin | 源文件按钮的语义对齐实情：PDF 这类包装站只能在新窗口打开（跨域 a[download] 被忽略、fetch 落盘受制于按域名配的 CORS 白名单），按钮文案改「打开源文件」不再宣称下载；取不到源文件的那一档（视频包装站）直接不渲染按钮，不再让用户点一次才知道不行 |
| fix | prd-admin | 源文件按钮在按下之前就判出「超过服务端 2MB 取回上限」的单文件站（多文件站不按总和拦，入口那份可能很小），并把后端协议口径的报错映射成人话 + 下一步，原始报错降级成附注 |
| fix | prd-admin | 迟到原文的宽限期改为从「直链露出那一刻」起算：原先与 fallback 窗口同值同起点，导致宽限期实际为 0——任何超过 6 秒回来的正文都被判成迟到、不自动换，本该救场的那条路被自己关死 |
| fix | prd-api | SharedSiteInfo 增加 EntrySize（入口文件自己的字节数）：前端此前只能拿 TotalSize 近似、判据只对单文件站成立，多文件站里入口超 2MB 的会漏 |
| fix | prd-admin | 源文件大小判据改用入口文件自身大小，单文件/多文件同一条判据不再有特例；失败提示只在认不出错误时才带原始报错，认得出的不把 HTTP 细节给外部访客 |
