| fix | cds | 分辨率判据从 p90 改为「忽略真实断档后的最大常规间隔」（≤ MAX_RATE_GAP_MS 的间隔降序取第 3 大）。按数量取的分位数最终会被密的那一档吞掉：30 分钟窗口里抽屉开满 15 分钟，5s 间隔就超过 90%，p90 变成 5s，又切出 120 个 15s 的桶、老的三分之二全空——锯齿原样回来 |
| fix | cds | 历史拉取失败的提示不再被 hasPlot 门住。漏掉的是「先成功、后持续失败」那条路：metricSeries 还留着上一次结果、hasPlot 仍为真，于是一条过期曲线顶着「近 30 分钟」的标签无限期挂着，一句提示都没有 |
| test | cds | 新增 4 条守卫（16 分钟后稀疏档仍定分辨率 / 偶发漏采不减半分辨率 / 真实断档不参与定分辨率且仍以 null 呈现 / 有旧图时也提示曲线已停更），均做过红绿闭环 |
