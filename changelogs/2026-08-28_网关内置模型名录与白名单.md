| feat | llmgw | 新增内置已知模型名录（OpenAI / Anthropic / Google / DeepSeek / 通义 / 智谱 / Kimi / 豆包），登记每个模型真实的用途、能不能收图、是不是必须给图；模型标识做归一化（厂商前缀 / 日期快照 / -latest 落到同一条登记） |
| feat | llmgw | 用途解析改为「名录 > 上游声明 > 按名字猜」，并把来源如实透出到界面：名录内是绿标事实，猜出来的是灰标并提示要核对——此前两者长得一模一样，用户没法分辨该不该信 |
| feat | llmgw | 模型白名单落地（方案 B）：名录外模型默认拦在导入这一步，要用得管理员在面板上显式「放行名录外的」，放行了哪几个进审计。拦在入库而不是拦在请求时——请求只会打到池成员，进不了库就进不了池 |
| feat | llmgw | 导入面板默认只勾名录内模型，名录外一行灰掉并说清怎么放行；被拦下的模型与「已存在」分开报，附可执行的下一步 |
| feat | llmgw | Quickstart 试跑按调用类型区分输入：看图类型把「要看的图」提成独立一格（空态也占 88px），没给图时如实说明发的是 1x1 测试图、只能证明链路通 |
| test | llmgw | 名录守卫：能力名必须在规范能力表内、归一化键全局唯一、等价写法落回同一条登记、不做模糊匹配、来源优先级与自洽性逐条断言 |
