| feat | cds | 新增凭据自检端点，把「未授权」拆成有效 / 已吊销 / 从未签发 / 项目前缀不符 / 查不了五类可分辨结论 |
| feat | cds | 自检能识别「项目改过 slug 导致存量凭据前缀对不上」这种鉴权静默跳过的情况，并给出重新签发的指引 |
| test | cds | 补凭据自检判据与路由回归共 21 例，含红绿闭环、与鉴权路径的漂移守卫、不泄密断言与免鉴权接线守卫 |
| docs | cds | 新增一仓多项目与身份层计划看板，登记十五条已定决定与六个阶段的机械验收判据 |
| feat | cds | push 按仓库分发到该仓库下的**全部**项目，不再只认第一个；主结果之外的项目挂在 fanout 上，路由对它们的部署一视同仁派发 |
| feat | cds | 新增项目级作用域判据：作用域取该项目名下全部服务 buildScope 的并集，未声明即全通配（零回归），拿不到改动清单时 fail-open |
| feat | cds | compose 支持服务级 `cds.build-scope` 标签，让没有部署模式的服务也能声明构建输入范围 |
| feat | cds | 自托管项目 compose 声明作用域 `cds/**`，link 之后只有改到 CDS 自身代码的 push 才会重编它 |
| refactor | cds | `findProjectByRepoFullName` 改为委托给新的复数版，两者匹配口径收敛成一份 |
| test | cds | 补作用域判据 17 例、多项目分发 9 例、路由分发 2 例，含「删掉 fanout 循环即变红」的红绿闭环 |
| feat | cds | 新增 `POST /api/preview-dispatch`：由服务端判定「本次改动波及哪些项目、各自有哪些已发布入口」，与 push 分发共用同一份作用域判据 |
| feat | cds | 预览地址输出格式定为 `[项目 · 入口] URL`，项目只有一个入口时收缩成 `[项目] URL`，多项目全部列出、没有主从 |
| feat | cds | 取不到地址时分三种情形说清（与该项目无关 / 还没有这条分支 / 分支在但没有入口），不再压成一句取不到 |
| feat | cds | preview-dispatch 按凭据过滤可见项目：项目级凭据只看得到自己那个项目，别人的项目名与地址一概不出现 |
| feat | cds | cdscli preview-url 改走服务端派定，新增可选 `--changed-since`；老版本 CDS 自动回退旧路径，不破坏既有技能 |
| docs | cds | preview-url 技能同步决策链路与输出格式；cdscli 与 cds 技能版本号同步到 0.15.0 |
| test | cds | 补预览派定判据 8 例与路由 6 例，含可见性隔离与「未波及仍出现在结果里」 |
| feat | cds | 新增身份层三张表（主体 / 用户级凭证 / 项目授权）：钥匙不再等于身份，丢钥匙不再等于丢身份 |
| feat | cds | 用户级凭证「只授权不操作」：可达路由由唯一一张白名单表收口，只能建项目、列项目、为已授权项目签钥 |
| feat | cds | 自愈回落：凭据文件丢了（挪目录 / 换机器 / 新 clone），有授权就当场补发项目级凭据，全程零人工；没授权才走一次页面批准 |
| feat | cds | 判据是「在不在授权名单里」而不是「是不是我建的」：建项目写创建型授权、页面批准写批准型授权，同表同权 |
| feat | cds | 级联撤销：撤掉用户级凭证，它签出的全部下游项目级凭据同时失效；签发次数留痕 |
| feat | cds | 新签发的项目级凭据 30 天、用户级 90 天，均为用一次自动续；存量凭据无到期字段，永不过期，零回归 |
| feat | cds | CDS 系统设置新增「权限总览」页：按主体聚合、退役凭证折叠、超 90 天归档、可撤销可停用，并明示未认领的存量凭证 |
| feat | cds | cdscli 新增 identity save / whoami / heal；用户级凭证存用户目录（0600），跟着人走不跟着仓库走 |
| fix | cds | 修复 auth=disabled 模式下主体上下文不被 stamp 导致身份层路由一律 401（本地端到端跑通时撞出） |
| test | cds | 补身份层判据 18 例与路由 11 例，含级联撤销的红绿闭环与权限边界断言 |
| test | cds | 补身份层零回归守卫 6 例：升级前的 state.json 加载后三张表为空、存量项目级凭证照常鉴权、无到期字段 = 永不过期、总览明说「未认领」；含「把无到期字段当成已过期」的红绿闭环 |
| fix | cds | 凭据自检补上用户级凭证面：生效中的 cdsu_ 曾被答成「从未签发」，同时新增到期与主体停用两种结论，并加接线守卫防下一种凭据面再漏 |
| fix | cds | 删项目连带吊销它的项目授权，权限总览不再留下指向已删项目的授权芯片 |
| fix | cds | 停用主体或撤销项目授权后，其名下项目级凭据立刻鉴权失败（此前最长滞后 30 天，与界面文案不符） |
| fix | cds | 项目级凭据用一次续 30 天，兑现「用即续」；存量无到期日的密钥不受影响 |
| fix | cds | 权限总览页签补自己的内容槽，此前点开是整页空白；并加「页签与内容槽一一对应」守卫 |
| fix | cds | push payload 被截断时作用域判据 fail-open，不再拿不全的改动清单把项目判成未被波及 |
| fix | cds | 接入申请批准时写 approved 项目授权，「批一次，此后丢凭据自愈」才真正成立 |
| fix | cds | 权限总览支持为已有主体补签用户级凭证，不再只能新建主体（新主体没有旧授权，自愈走不通） |
| security | cds | 身份管理门卫对齐 assertNotMachineAgentKey：全权 cdsg_ 机器钥匙不再被当成管理员签凭证、停用主体、增删授权 |
| fix | cds | 预览地址只在分支真的在跑时才给，此前配了预览域名就无条件返回一个打不开的 URL |
| fix | cds | 订正未认领存量凭证的说明文案：存量凭证不会「下次使用时自动认领」，光看被使用认不出背后是谁 |
| security | cds | 身份层签发接口交付的凭据明文不再落进 HTTP 日志：脱敏认 plaintext 字段，并按值形状兜底任何 cds 凭据串 |
| security | cds | cdscli identity save 的明文改走 stdin / 隐藏输入，不再进 argv 与 shell 历史；签发页同步教这条安全路径 |
| fix | cds | 凭据自检认「授权被撤」这一档，不再对着鉴权因授权撤销造成的 401 回答「有效」 |
| fix | cds | 预览地址判据改看「有没有可路由服务在跑」，只跑后台 worker 的分支不再被报成已部署 |
| fix | cds | 权限总览的凭据状态跟着实际能不能用走：主体被停用或授权被撤的凭据不再算进「有效凭证」 |
| security | cds | 用户级凭证列项目时按其项目授权收窄，不再看得到全部项目的仓库地址与配置摘要 |
| fix | cds | 权限总览的用户级凭证同样跟着主体状态走；退役行说得出「主体已停用 / 授权已被撤销」，不再一律写「过期于 —」 |
| fix | cds | 签发页给出的落地命令带上 CDS 主机，新机器上不会在收凭证之前就先失败 |
| test | cds | 加接线守卫：判凭据可用性的地方一律不许传空主体，防同一个洞第五次出现 |
| fix | cds | 凭据自检的节流按转发来的客户端地址分桶，nginx 后面不再把所有调用方并成一个桶（一人打满全员 429） |
