| fix | prd-api | 能力契约收敛为唯一判据，历史别名 image-gen 在读写两侧统一归一，消除模型池「配置可用但候选为 0」的静默故障 |
| feat | prd-api | 路由解析失败拆成十类结构化错误码，管理员日志点名 appCaller/逻辑模型/Offering/失败阶段，用户文案区分「重试有用」与「必须找管理员」 |
| fix | prd-api | 区分模型池为空与池内成员全熔断，两者不再共用同一个失败原因 |
| fix | prd-api | 网关配置面读不到与配置配错分开上报，配置库抖动不再被报成全站模型池报废 |
| feat | llmgw | serving readiness 增加场景能力检查，使用与生产运行时相同的解析判据，配置不兼容时在用户失败前变红 |
| feat | llmgw | 逻辑模型能力增加契约版本与幂等迁移，未知能力不丢弃并逐个点名，新增 /gw/logical-models/capability-audit 发布门禁只读端点 |
| feat | prd-admin | 补齐十类路由失败的用户文案，配置不兼容不再显示成供应商宕机 |
| test | prd-api | 补齐能力兼容、迁移幂等、路由场景、故障隔离、错误分类、正式数据形态回放与接线守卫测试 |
