| feat | cds | 新增身份层三张表（主体 / 用户级凭证 / 项目授权）：钥匙不再等于身份，丢钥匙不再等于丢身份 |
| feat | cds | 用户级凭证「只授权不操作」：可达路由由唯一一张白名单表收口，只能建项目、列项目、为已授权项目签钥 |
| feat | cds | 自愈回落：凭据文件丢了（挪目录 / 换机器 / 新 clone），有授权就当场补发项目级凭据，全程零人工；没授权才走一次页面批准 |
| feat | cds | 判据是「在不在授权名单里」而不是「是不是我建的」：建项目写创建型授权、页面批准写批准型授权，同表同权 |
| feat | cds | 级联撤销：撤掉用户级凭证，它签出的全部下游项目级凭据同时失效；签发次数留痕 |
| feat | cds | 新签发的项目级凭据 30 天、用户级 90 天，均为用一次自动续；存量凭据无到期字段，永不过期，零回归 |
| feat | cds | CDS 系统设置新增「权限总览」页：按主体聚合、退役凭证折叠、超 90 天归档、可撤销可停用，并明示未认领的存量凭证 |
| feat | cds | cdscli 新增 identity save / whoami / heal；用户级凭证存用户目录（0600），跟着人走不跟着仓库走 |
| fix | cds | 修复 auth=disabled 模式下主体上下文不被 stamp 导致身份层路由一律 401（本地端到端跑通时撞出） |
| test | cds | 补身份层判据 18 例与路由 11 例，含级联撤销的红绿闭环与权限边界断言 |
