| feat | cds | 验收报告主页重做（方向 A 结论优先）：新增 `GET /api/reports/overview` 聚合——同一验收目标只计最新版、按根因合并未通过簇、口径冲突单列、发布闸、每日验收连续性、主干合并记录 × 报告元数据算出合并未验 |
| feat | cds | 验收报告主页首屏改为结论头条 + 发布闸 + 结论分布图；未通过与待决表；每日验收连续性与合并覆盖两张图；报告台账按九类前缀分页签、默认折叠被取代版本 |
| test | cds | 新增 acceptance-overview 聚合守卫（标题合同解析、版本折叠、产品失败与验收失败分开、口径冲突、时区落日、合并覆盖匹配） |
| fix | cds | 验收发现的回归：桌面端打开报告后无法返回结论首页（返回按钮带 lg:hidden，同路由点导航不复位选中态），去掉断点限制并补源码守卫 |
| polish | cds | 验收报告主页首屏不再被右列拉高，支撑卡改自适应列宽，支撑卡少时不留大片空白 |
| fix | cds | 验收主页台账展开被取代版本时旧版整批掉进「其他」，overview 补返回 supersededReports 让旧版按标题合同归类 |
| test | cds | 补被取代版本 kind 解析的回归断言（红绿闭环验证过） |
| feat | cds | 结论头条补「根因集中度」水平条形图（按验收对象合并、口径冲突拆红橙两段、点击下钻），顶部不再是纯文字且左列不再留大片空白 |
