| feat | cds | 验收报告主页重做（方向 A 结论优先）：新增 `GET /api/reports/overview` 聚合——同一验收目标只计最新版、按根因合并未通过簇、口径冲突单列、发布闸、每日验收连续性、主干合并记录 × 报告元数据算出合并未验 |
| feat | cds | 验收报告主页首屏改为结论头条 + 发布闸 + 结论分布图；未通过与待决表；每日验收连续性与合并覆盖两张图；报告台账按九类前缀分页签、默认折叠被取代版本 |
| test | cds | 新增 acceptance-overview 聚合守卫（标题合同解析、版本折叠、产品失败与验收失败分开、口径冲突、时区落日、合并覆盖匹配） |
| fix | cds | 验收发现的回归：桌面端打开报告后无法返回结论首页（返回按钮带 lg:hidden，同路由点导航不复位选中态），去掉断点限制并补源码守卫 |
| polish | cds | 验收报告主页首屏不再被右列拉高，支撑卡改自适应列宽，支撑卡少时不留大片空白 |
| fix | cds | 验收主页台账展开被取代版本时旧版整批掉进「其他」，overview 补返回 supersededReports 让旧版按标题合同归类 |
| test | cds | 补被取代版本 kind 解析的回归断言（红绿闭环验证过） |
| feat | cds | 结论头条补「根因集中度」水平条形图（按验收对象合并、口径冲突拆红橙两段、点击下钻），顶部不再是纯文字且左列不再留大片空白 |
| polish | cds | 验收主页版面重排为「单脊柱」：主栏叙述 + 320px 固定侧栏仪表（跨行 sticky），结论区脱掉卡片外框成为唯一主角，支撑句由卡中卡改行内条目，重复的发布闸支撑句不再渲染，台账降为查找工具层 |
| fix | cds | 未通过与待决的簇标签 ×N 数错类：1 份未通过 + 2 份有条件被渲染成「未通过 ×3」，改为按 failCount / conditionalCount 分别计数并在行内标出夹带的有条件份数 |
| feat | cds | 验收首页从需求重推：不选项目时改为跨项目「验收流水线总览」（改动→部署→验收→合并四环 + 环间落差 + 一行一个项目），新增只读接口 `GET /api/reports/pipeline` |
| feat | cds | 新增 acceptance-pipeline 聚合：按 PR 号 / commit / 分支名把报告对齐到改动单元，报出四种漏（合并了没验 / 验了没过还合并 / 部署了没验 / 报告没记它验的是谁），未接 GitHub 的项目单独标出 |
| refactor | cds | 报告与改动的对齐判据抽成唯一一份 `matchesChange`，合并覆盖与流水线共用 |
| polish | cds | 「有条件通过」全站转正为「原则性通过」（UI / 归档门禁 / 报告模板 / SSOT 规范）；归档门禁新旧两种写法都接受，历史周报与 CHANGELOG 保留原话不回溯改写 |
| fix | cds | 拿主实例真实数据核对流水线聚合，发现「报告对不上分支」是喊狼：153 份对不上的报告里 152 份点名的分支根本已被 CDS 回收（现存仅 71 条），删掉这一档漏，改为背景数 staleReports；只保留「报告没记它验的是谁」（三个标识全空）这一条可证的缺口 |
| test | cds | 补三条守卫钉住「对不上不许一股脑喊成漏」，并新增前后端漏点顺序一致性守卫（防判据分裂漂移），两条都红绿闭环验证过 |
| fix | cds | 验收报告正文改存对象存储：元数据在 Mongo、正文在容器本地盘，容器重建后正文全丢而元数据还在，列表 23 份报告点开全是 404。正文改走对象存储（复用已有 R2 SigV4 客户端，无新依赖），本地盘降为读缓存并在未命中时回填；写入顺序改为「先正文后元数据」，上传失败整个归档失败，不再留下点不开的幽灵记录 |
| fix | cds | 未配置对象存储时不许假装持久：元数据记 `storage:'local'`，`/raw` 与 `/download` 的 404 改为说清三种成因（未配对象存储 / 归档于本次改动之前 / 对象确实不在），不再甩一句「内容文件已丢失」 |
| fix | cds | `fetchReportRaw` 抛错时把响应体传成 undefined，服务端给的原因被丢掉，用户看到「服务拒绝了请求，但没有返回可读错误原因」——改为先读错误体再抛 |
| feat | cds | 验收首页补结论层：第一屏改为规则生成的一句判断（挂真实数字、按严重度取第一条成立的、算不出就不出句）+ 支撑点 + 指到具体分支的下一步，漏斗降级为支撑图；内部词换人话（漏/在途/一次都没跑过的验收 → 该管的是这些/在改的分支/没做过的验收类型） |
| rule | platform | 新增强制规则 12「账在哪，货就在哪」+ `.claude/rules/durable-payload-storage.md`：元数据与实体必须同一持久等级，实体默认进对象存储，凭据不全拒绝静默回退本地，测试必须删本地目录模拟容器重建；附全仓扫描台账 |
| test | cds | 补对象存储守卫（9 条）与正文持久性守卫（7 条，用删除本地缓存模拟容器重建），红绿闭环验证过：回退旧行为后 7 条中 5 条变红 |
