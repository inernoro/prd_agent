| feat | prd-admin | 处理中那一屏按设计稿 R4 补齐：页面级标题、三阶段铺开、阶段级剩余预估、音频就绪卡、原文逐句生成卡（无内容时给产物形状的骨架）、底部「立即播放」主按钮 |
| feat | prd-admin | 结果页三块内容改回同屏并置（词云 / 会议纪要 / 待办 / 提问），撤销上一轮的互斥分区——那个改法打断了设计稿「看词云→对照纪要→落到待办」的贯通路径 |
| feat | prd-admin | 词云按设计稿压成三档词频（高频黑底大字 / 中频蓝字 / 低频灰底小字），选中词加描边；新增命中面板抬头「「X」命中 N 句」、「全部」入口与句内关键词高亮 |
| feat | prd-admin | 会议纪要标题右侧补「重新生成」，待办补条数；播放请求走一条两端显式登记的窄通道，附接线守卫 |
| feat | prd-admin | 新增录音链路专用配色皮肤（设计稿原色），作用域生效不影响全站，供决策方与平台主题二选一 |
| chore | prd-admin | 双皮肤棘轮基线更新：播放器硬编码归零、DocBrowser 减少，新增项仅为上述专用深色档皮肤（规则允许的例外） |
| feat | prd-admin | 按第二轮评分单继续补齐：阶段行改「标题→进度条→副行」、进行中阶段标题用强调色、播放缩略图变成黑底可点播放键、原文生成卡独立成白卡并常吊两条骨架、保存音频行补时长（由播放器广播，条目元数据里没有这个字段） |
| feat | prd-admin | 命中面板默认选中最高频词并移到词云下方（设计稿顺序），选中词与搜索词分成两个状态；命中高亮改黄色，与蓝色强调色分工 |
| feat | prd-admin | 播放区滚过一段距离后收成 56px 迷你条（当前句 + 播放键 + 展开），用哨兵元素 + IntersectionObserver，不监听 scroll |
| fix | prd-admin | 纪要不再重复列待办：整段只有勾选项的模块交给待办区渲染，判据是结构而不是标题措辞 |
| fix | prd-admin | 区块标题加 scroll-margin，点进某一区块时不再被吸顶迷你条压住 |
| feat | prd-admin | 词云三档阈值按稿面词频重定（并列头部词同为黑档），命中句改「时间戳独立左列」两列排版，阶段圆环换成带缺口的强调色环 |
| feat | e2e | 取证脚本给一屏装不下的画板补下滚证据：按区块标题逐个滚到位再截，标题即文件名，区块增减自动跟上 |
| fix | prd-admin | 摘要解析：标题之下的连续段落同属一个模块，不再各自成块并自动编一个从正文截出来的标题 |
| fix | prd-admin | MarkdownViewer 加紧凑模式：整篇阅读用的 96px 底部留白嵌进小卡片会变成一屏死空白 |
| feat | prd-admin | 待办补原文出处（几点、谁提出、点击跳原文）与「来自 N 处原文」计数，判据是双字词重合、重合不足就不给出处 |
| feat | prd-admin | 折叠迷你条补播放位置与总时长；收敛强调蓝的使用范围（重新生成转中性、选中词用黑底而不是蓝描边） |
