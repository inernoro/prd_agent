| fix | cds | 指标序列改为服务端对齐：所有容器共享同一条时间轴，缺口用 null 显式表达。此前各容器各自跳过空桶导致长度不一致，前端按下标堆叠算出 NaN，图上出现黑色缺口 |
| fix | cds | 堆叠图取各序列最短长度并逐点兜底，作为长度不齐的第二道闸，宁可少画右边一截也不吐 NaN 路径 |
| fix | cds | 入口卡：主入口移出网格（跨满轨道会让 auto-fit 不折叠空轨道），次要入口按可用宽度自适应分列，不再留半宽孤儿卡或右侧空列 |
| test | cds | 新增 5 条守卫（共享时间轴 / null 缺口 / 堆叠无 NaN / auto-fit / 主入口不在网格），全部做过红绿闭环 |
