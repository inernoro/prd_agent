| feat | cds | 一个仓库现在能绑第二个项目：link 与建项目各加一次显式确认（allowShared / allowSharedRepo），不带确认仍拦住 |
| fix | cds | 删分支 / 关 PR / 极速版镜像完成三条 webhook 事件改为分发到仓库下每个项目，此前只处理第一个且无任何报错 |
| feat | cds | 新增同仓关系判据（repo-sharing.ts）：算出兄弟项目、未声明范围的项目、真正撞在一起的数据库与缓存 |
| feat | cds | 项目接口透出 repoSharing（仅浏览器会话），列表一行小字、项目设置顶部一条、绑仓库时一次打断，三档强度分开 |
| feat | cds | 新增构建范围声明入口：此前 buildScope 只能走 API 改，界面上根本没有地方填 |
| fix | cds | CI 镜像结果缓存的键加上项目：镜像先于 push 到达时，此前第一个项目认领走之后第二个永远停在等待中 |
| fix | cds | 同仓关系的可见判据从「是不是 cookie 会话」改成「是不是机器凭据」：前者在 CDS_AUTH_MODE=disabled 的实例上永远为假，真人打开什么都看不到 |
| polish | cds | 已经声明了范围的项目不再显示「去声明本项目的构建范围」按钮，它照样看得见横幅 |
| polish | cds | 解绑仓库的确认弹窗说清「只影响本项目，同仓的其它项目照常收 push」 |
| feat | cds | 构建范围改为系统推断：按各服务启动命令里的 cd 看出本项目待在哪个目录，横幅直接给建议 + 一键采纳 |
| feat | cds | 划范围对话框从空文本框改成勾选仓库里的真实一级目录，建议项预勾并写明依据，手写路径降为折叠区 |
| fix | cds | bg-primary 上误用 text-primary-ink 导致按钮文字在暗色主题隐形；补守卫禁止这对组合 |
| fix | cds | webhook 路由的停容器 / 墓碑 / 删分支只消费主结果，同仓第二个项目的容器没人停、分支条目没人删 |
| fix | cds | 顶栏粘 Git URL 建项目（主路径）丢掉 repoAlreadyLinked，撞上已绑仓库时静默建出没绑仓库的项目 |
| fix | cds | 范围对话框拿建议当保存基线，接受默认值点保存等于什么都没发生 |
| fix | cds | 仓库改名 / 转移 / 删除、安装移除仓库访问权时只解绑第一个项目，其余留着指向旧名字的死链接 |
| fix | cds | 范围声明在部署模式上时对话框写的是另一个字段，改了盖不掉；改为如实只读并说明改哪儿 |
| fix | cds | 嵌套范围（llmgw/serving）不在一级目录清单里，此前看不见点不掉却照样被保存 |
| fix | cds | 共用仓库的确认没绑定它描述的那个仓库，换选择后确认会绕过新仓库自己的拦截 |
| polish | cds | 项目只划了一半范围时横幅两个分支都不给入口，用户再也进不去范围对话框 |
