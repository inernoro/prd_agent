| feat | prd-api | CDS 验收报告镜像库每小时自动刷新：把库里已经存过的那些报告各刷一遍，每一份刷回它自己那台 CDS（CDS 上新出的报告不会自己冒出来，仍需先手动保存一次） |
| docs | prd-api | debt.knowledge-base 补「CDS 验收报告自动同步」已知边界 |
| fix | prd-api | 换 CDS 源时把旧源的增量水位一起清掉：来源字段无条件改成新源、水位却留着旧源的游标，自动同步会拿 A 的游标去列 B 的报告，B 上更新时间更早的报告被永久跳过且无任何报错 |
| test | prd-api | 「同一个源吗」收敛成唯一判据 + 6 条等价写法用例，另加两条源码守卫（真的清了水位、判断没被抄成第二份），两处均做过红绿闭环 |
| fix | prd-api | 每小时同步的闸改用 `CanRunSharedScheduledWork`：原来走的 `IsAuthoritativeDeployment` 按契约只管通知，分支预览打开「接管全局告警」的逃生阀就会顺带获得对共享库和 CDS 跑周期拉取的权限 |
| test | prd-api | 新增 `CanRunSharedScheduledWork`（软开关只能收紧不能放宽）三条行为用例 + 一条接线守卫，做过红绿闭环 |
| fix | prd-api | 自动同步改成「刷库里已经存过的那些报告」，不再靠记录首次导入范围重放：产品里唯一的导入入口一次只存一份、都进同一个默认库，记范围等于每存一份新的就把整个库钉死在那一份上，之前存的全部不再更新 |
| fix | prd-api | CDS 来源改用独立字段 `CdsReportSourceBaseUrl`，不再复用跨库同步的 `PeerSyncNodeBaseUrl`：同一个库走过 peer-sync 后那个字段会变成对端 MAP 地址，拿它解析 CDS 凭据必然失败、该库每小时被静默跳过 |
| test | prd-api | 判据用例改写成 11 条（含「存三份就刷三份」「重复 id 不刷两遍」「超上限截断且报得出截了多少」），做过红绿闭环 |
| fix | prd-api | 每一份报告刷回它自己那台 CDS：一个人从两台 CDS 各存过报告时都落进同一个默认库，照「库最后一次从哪导入」一刀切会让先存那台的报告要么找不到、要么在编号撞车时被另一台的同名报告覆盖 |
| docs | prd-api | changelog 首行与台账改口：删掉已不存在的「范围重放」说法与已删字段名，按实际实现描述 |
| fix | prd-api | 认领已有条目时连来源一起认（库 + 报告编号 + 哪台 CDS）：报告编号在不同 CDS 上可以重名，只按前两项认领会让 B 拉回来的正文直接盖掉来自 A 的那一条，A 的那份就此消失且无任何报错；没记来源的老条目仍认领得到并顺手盖上来源 |
| fix | prd-api | 老条目没记来源、而已授权的 CDS 连接不止一条时，自动刷新宁可让这一份失败也不挑一条接着拉（挑错就是把 B 的正文写进一条来自 A 的条目，不响）；手动导入维持原样，人当场发起、挑错看得见 |
| fix | prd-api | 自动刷新不再改写库上那四个归跨库同步所有的展示字段：手动点一次顺带改写还说得过去，改成每小时跑之后会把真正的对端同步状态每小时覆盖成「CDS 验收中心」 |
| fix | prd-api | 一份报告导入失败就算这一份失败：导入服务把「正文 404」「图片归一化失败」记进结果而不抛异常，原来只认抛出来的异常，本轮结束那行日志于是永远报成功 |
| test | prd-api | 新增 8 条用例（「没记来源时只有一条连接才不算猜」7 条纯判据 + 4 条接线守卫），四条修复逐条做过红绿闭环 |
| docs | prd-api | debt.knowledge-base 补一条已知边界：老条目在授权了多台 CDS 的环境下，要先在报告页重新保存一次才会自动刷新；「模块」一行的实现符号改成行为描述 |
| fix | prd-api | 报告在 CDS 上被删掉后，按编号去列会正常返回空结果，原来被计成「刷成功」且不打任何日志；现在单立一类、每一份点名说出来，镜像里的旧版本原样保留不自动删 |
| fix | prd-api | 同一台 CDS 换地址重新授权后，条目不再变成永久孤儿：地址对不上时按仓库既有的「同一台 = 同 partner + 同 project」判据找替换连接，导入成功后条目上的地址自愈；老地址必须真对应一条记录在案的连接且带 project，避免退化成随便挑一条 |
| test | prd-api | 新增两条接线守卫并做红绿闭环；其中「列不到要说出来」那条第一版没能变红（条件被另一行的同款表达式替作了证），已收紧成条件与语句绑定断言 |
| fix | prd-api | 老条目没记来源时目标源留空，不再拿库级来源顶替：否则用户按提示重新保存其中一份就把库级来源钉上，下一轮剩下的老条目全被当成「有来源」一股脑刷，绕开「认不出就不猜」那道闸 |
| fix | prd-api | 定点刷新（指定报告/项目）不再改写库级来源、也不判换源：库里有两台 CDS 的报告时，每小时的定点刷新会交替改写它，下一份进来判成「换源了」就清一次水位，而水位同时是跨库同步的「上次同步时间」 |
| fix | prd-api | 自动刷新导入单份报告时不再透传停机信号：导入一份是多步写（存正文、插条目、加计数），优雅重启可能停在中间留下对不上的计数；取消改为只在两份报告之间判 |
| test | prd-api | 新增两条纯判据用例 + 两条接线守卫，四处逐条红绿闭环 |
| fix | cds | 「系统互联」连接凭据在页面代理之外，增加验收报告列表与正文两条只读路由：MAP 镜像 CDS 报告用的就是这把凭据，而报告接口原先只认 CDS 自己的密钥，导致导入（手动与自动）一律 401 |
| refactor | cds | 「这把连接凭据能到达哪些路由」从鉴权代码里写死的前缀判断收敛成一张显式的表，每条声明方法、路径判据、所需授权范围与「为什么给」；建/删报告与整个 CDS 管理面明确不开 |
| test | cds | 新增 13 条用例：该开的两类各自可达、只读不可写、前缀相近的路由不被顺带放行、管理面一条不开，另加三条接线守卫（鉴权入口真的查表、旧的写死判断已移除、scope 不再是字面量），四处红绿闭环 |
| fix | prd-api | 空跑一轮不再把镜像库顶到知识库列表最前面：列表按库的最后修改时间倒序排，而每轮同步无条件更新它，于是一份都没变（甚至一份都没刷到）也会把 CDS 镜像库顶上去，把用户手改过的库挤下去 |
| fix | cds | 连接凭据的「最近用过」改成节流写（5 分钟一次）：每次更新都会把整份状态存一遍，报告只读放开后一轮自动刷新会打出几百个请求，等于把只读定时任务变成每小时几百次整份落盘 |
| fix | prd-api | 一条脏元数据不再掀掉整轮同步：条目的元数据可被通用接口写成任意 BSON，直接按字符串读会抛，而这段在逐份报告的错误处理之外，一条坏数据就让本轮所有库都不刷且每小时复发 |
| test | prd-api | 新增 6 条节流用例（含未来时间自愈、认不出的值不卡死）+ 2 条接线守卫，三处红绿闭环 |
| fix | prd-api | 一台 CDS 连不上时本轮不再挨个去撞它：循环是一份报告一次导入、每次先发列表请求超时 60 秒，一台掉线能让一个 200 份的库串行耗掉三个多小时，后面的库这轮根本轮不到，下一轮又立刻开始，健康的库被永久饿死；判据只认网络层失败，跳过多少份会报出来 |
