| feat | cds | 首页第二屏起改为滚动叙事：一条分支线贯穿、镜头沿线推进，Push / Build / Preview / Observe / Ship 五章各发生一件事（脉冲、容器弹出、域名转绿、集群抬升、收束），替换原来的卡片网格分区 |
| perf | cds | 叙事场景只在进入视口时渲染，标签页隐藏即停，卸载释放 WebGL 资源；文案淡入淡出直接写 DOM 不走 setState |
| test | cds | 新增叙事区接线守卫：五章齐全、顶栏锚点有落点、场景不用 rootMargin、视口内才渲染、卸载即释放、尊重 reduced-motion |
| ops | cds | 冷却 5 条超过 6 小时无人访问且仍占着运行中容器的报错分支（通过调度器休眠，下次访问自动唤醒） |
| polish | cds | 叙事区质感层重做：发光走 emissive + Bloom（HalfFloat）、容器换圆角金属玻璃模块 + 线框 + 发光核、实体管主线、软圆点星尘、RoomEnvironment 反射、ACES 色调映射、SMAA 抗锯齿、镜头轻呼吸 |
| polish | cds | 压掉叙事区背景灰雾：星尘减量减亮换暖色、辉光阈值抬高、曝光与雾密度微调；舞台顶部向 hero 渐隐 |
| fix | cds | 首页叙事区舞台类名与 hero 实况板列撞车（cdsh-stage），大屏上实况板下方多出整屏黑块；改名并加守卫「叙事区类名在 HomePage 只许出现一次」 |
| polish | cds | hero 与叙事区交融：叙事区从蜂窝层下面钻出（负外边距 + 蜂窝层在上、章节文字在上、舞台在下）、顶部过渡拉到半屏、舞台叠同款六边形淡纹；hero 撑满首屏；3D 环境光与辉光半径压向黑 |
| feat | cds | GitHub push 的 head commit 提交信息带 [skip ci] / [ci skip] / [no ci] / [skip actions] / [actions skip] / [skip cds] / [cds skip] 或 skip-checks: true trailer 时不再建预览分支、不排构建（与机器人过滤同层短路；治 myTapd 发版归档分支四天堆 30 条预览的风暴） |
| ops | cds | 清掉 myTapd 项目 23 条残留的 chore-archive-changelogs 一次性预览分支记录 |
| polish | cds | 叙事区去灰：环境光与半球光再压、雾更密、星尘减半、辉光阈值抬高半径收紧、金属模块反射降档、集群分支改深紫、暗角加重、六边形淡纹再淡 |
| perf | cds | 叙事区离屏时真的停掉帧循环（由不带 rootMargin 的 IntersectionObserver 叫醒），reduced-motion 下冻结场景时钟、画完一帧即停只由滚动唤醒，不再有自动播放的动效 |
| perf | cds | 叙事场景改为懒加载：three + postprocessing（约 690 kB）不再进首页主链路，访客第一次滚动才下载并挂载；WebGL 构建推迟到叙事区进入视口，停在 hero 不动的访客既不下载也不建场景 |
| test | cds | 叙事场景的帧循环抽成可注入边界的纯模块，视口门 / reduced-motion / 可见性 / 构建失败 / 释放的契约改为 11 条行为测试（假 rAF、假观察者、假构建），不再靠源码正则守卫 |
| fix | cds | skip-checks: true 只认提交信息结尾的 trailer 块，正文里长得像 trailer 的行不再误判为跳过 |
| fix | cds | 叙事场景 WebGL 构建失败不再静默：原因打进控制台、叙事区根元素带 data-cdsh-scene=live/fallback 机读状态；RoomEnvironment 烘完 PMREM 即释放 |
| fix | cds | 叙事场景构建改为事务性：中途抛错先释放已建的 renderer / GPU 资源 / 合成器再抛出，失败不再漏 WebGL 上下文；skip-checks: true 按 GitHub 文档要求必须是最后一条 trailer |
