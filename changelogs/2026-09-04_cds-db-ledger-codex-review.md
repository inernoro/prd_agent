| fix | cds | 数据台账备份 dump 改两阶段（先落容器 /tmp 再 gzip），mysqldump / pg_dump 半路失败不再被 gzip 的退出码盖成成功、半份文件不再当备份收下（Codex P1） |
| fix | cds | mongo 专用隔离实例的隔离库：备份、数对象、演练打到专用容器而不是克隆来源的共享实例，避免空归档通过演练后把专用实例当有备份删掉（Codex P1） |
| fix | cds | postgres 派生库回写：孤儿库的应用用户改从项目里仍在用该服务的分支解析；解析不到就拒绝回写，不再在 DROP 后丢掉授权让应用连不上（Codex P1） |
| fix | cds | 扫描补录按实例分别判「已知」，同名库在另一台实例上有记录不再让这台实例上的存量库漏录（Codex P2） |
| docs | cds | 债务台账记两条 Codex 扩范围建议：回写窗口内目标库写入围栏、克隆完成后复核部署租约 |
| fix | cds | 演练还原与整库还原改两阶段（gunzip 先落盘再喂客户端），截断或损坏的备份不再被客户端的退出码盖成演练通过（Codex P1） |
| fix | cds | 删项目级联拆除数据台账里保留下来的专用隔离实例（分支已删、快照已没，台账是唯一登记处），台账条目随项目一起清掉，不再永久漏跑带数据卷的容器（Codex P1） |
| fix | cds | 回退后对象数与快照不一致时不再标「已回退」、返回失败并保留重试路径（Codex P1） |
| fix | cds | 扫描补录条目 id 带项目作用域，两个项目同名 infra id 同名库不再互相覆盖（Codex P1）；备份文件名加随机段，同一秒两次备份不再写同一个文件（Codex P2） |
