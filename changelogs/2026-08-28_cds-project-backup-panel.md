| feat | cds | 项目设置新增「周期备份」面板：第一屏一句判断（要不要你管），需要处理的目标展开、正常的和备不了的各收成一行，失败原因收进详情 |
| feat | cds | 新增 `GET /api/projects/:id/backup-health`：按项目返回三个时间戳、每个目标的处境与产物大小、备份文件数，以及备份相关的每日体检结论 |
| fix | cds | 「最近一次成功」从盘上的文件名推，不用这一轮的结果顶替——连续失败三天的目标照样答得出「三天前那份还在」 |
| fix | cds | 落盘的失败原因过一遍脱敏再截尾：原文是 docker exec 的合并输出，会经面板端点回到浏览器 |
| refactor | cds | 备份健康文件名、备份周期、备份判据抽成共用 SSOT（三个消费者：写盘、每日体检、面板），不再各写一份 |
| test | cds | 补面板判据单测、端点路由用例与渲染冒烟；四处破坏各自验红（项目筛选、状态优先级、只读不建目录、失败原因脱敏） |
| docs | doc | 台账新增 E84：面板暂无「立即备份一轮」入口，改完凭据要等下一轮才知道修没修好 |
