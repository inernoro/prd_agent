| refactor | cds | 任务调度页重构为「调度室」：结论条 + 今日调度轴 + 任务/运行流/详情三栏，宽度放开到 fluid |
| feat | cds | 运行记录新增 run.steps[]，逐个动作各留状态、耗时与返回码 |
| fix | cds | 运行记录改为按任务各留 120 条，此前全局 1000 条环形缓冲会让高频任务挤掉日任务的历史 |
| feat | cds | 任务列表接口支持 ?nextRuns=N 投影后续触发时刻，前端不再复制到期判定 |
| refactor | cds | 运行记录从折叠的 details 提升为常驻运行流，带全部/失败/手动过滤与逐步展开 |
| polish | cds | Agent 请求、分支详情、分支拓扑三页宽度放开；清掉拓扑页内层三处 cds-workspace-wide 覆盖 |
| test | cds | 新增 15 条守卫：逐步结果、按任务保留、触发序列，以及结论条/时间轴/健康度的算术 |
| refactor | cds | 清掉 18 个文件里 113 处按钮内的死图标尺寸：调用点写的值一直被 button.tsx 的 [&_svg]:size-* 吃掉 |
| test | cds | 新增守卫钉死零残留，防止「写 14 渲 20」的档位再长回来 |
| fix | cds | 任务调度运行流时钟列不再从长日期截尾（曾显示「05 AM」），改用独立的 HH:MM 格式化并加守卫 |
| fix | cds | 任务调度页首屏不再是空表单：有任务时默认落位到最该看的那一个，点「新建任务」后不再被抢回 |
| feat | cds | 任务调度运行流增加作用域开关，选中低频任务时可一键看回全部任务的运行流 |
