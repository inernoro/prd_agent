---
globs: prd-admin/src/**/*.{ts,tsx},prd-desktop/src/**/*.{ts,tsx}
description: 陌生页面 3 秒内知道做什么：空状态引导 + 首次使用引导 + 操作提示
---

<!-- 本文件由 scripts/sync-cursor-rules.sh 自动生成，请勿直接编辑。
     事实源: .claude/rules/guided-exploration.md
     生成时间: 2026-04-18T18:10:31Z -->

# 陌生页面引导性原则

> 用户进入一个全新页面时，必须在 3 秒内知道「这是什么」和「下一步做什么」。

## 强制规则

1. **空状态不能空**：列表为空时禁止只显示"暂无数据"。必须包含：说明文案 + 主操作按钮 + 可选的引导插图
2. **首次进入有引导**：AI 原生页面（涌现、工作流、Agent 等非传统 CRUD 页面）首次进入时，必须有引导内容告诉用户这个页面能做什么、怎么开始
3. **操作有反馈路标**：每完成一步操作后，界面必须暗示下一步是什么（按钮文案、提示文字、高亮区域）
4. **形势感知**：页面当前状态（空/少量/正常/异常）必须有对应的视觉表达，用户一眼就能判断"现在是什么情况"

## 引导层级

| 层级 | 触发条件 | 表现形式 | 示例 |
|------|---------|---------|------|
| **L1 空状态引导** | 列表为空 | 插图 + 说明 + 主操作 CTA | "还没有涌现树，点击开始探索" |
| **L2 首次使用引导** | 用户从未使用过此功能 | 步骤卡片 / 动画演示 / 示例数据 | "涌现探索器分三步：种下种子 → 探索 → 涌现" |
| **L3 操作提示** | 用户完成某步但不知道下一步 | 高亮提示 / 脉冲动画 / Toast | 节点上的「探索」按钮脉冲提示 |
| **L4 上下文帮助** | 用户停留过久未操作 | 悬浮提示 / 侧边帮助面板 | "试试点击节点上的探索按钮" |

## 设计清单

新建页面时逐条检查：

- [ ] 空状态有引导（说明 + CTA + 可选插图）
- [ ] 页面标题/副标题说明了这个页面是做什么的
- [ ] 用户知道第一步该做什么（主操作按钮醒目）
- [ ] 完成每一步后，界面暗示了下一步（按钮文案变化、新区域高亮）
- [ ] 异常状态有明确提示（加载失败、权限不足、数据为空）
- [ ] AI 原生页面有「怎么玩」的简要说明

## 反面案例

| 错误做法 | 正确做法 |
|---------|---------|
| 空列表只写"暂无数据" | 插图 + "还没有 XX，点击创建第一个" + CTA 按钮 |
| React Flow 画布只有一个孤零零的种子节点 | 种子节点上有脉冲动画的「探索」按钮 + 底部提示"点击探索开始生长" |
| 涌现按钮没有解释 | Tooltip 说明"AI 将组合现有节点，发现新的可能性" |
| 新功能页面无任何说明 | 页面顶部一句话说明 + 可折叠的详细引导 |

## 与其他原则的关系

```
用户进入页面 → 「引导性原则」告诉他做什么
         ↓
用户开始输入 → 「零摩擦原则」让他不发呆
         ↓
用户等待 AI → 「2 秒原则」让他不焦虑
```

三个原则覆盖用户操作的完整生命周期：**认知 → 行动 → 等待**。
