---
globs: prd-admin/src/**/*.{ts,tsx},prd-desktop/src/**/*.{ts,tsx}
description: 输入零摩擦：能上传不让用户手输，不确定就两个都给
---

<!-- 本文件由 scripts/sync-cursor-rules.sh 自动生成，请勿直接编辑。
     事实源: .claude/rules/zero-friction-input.md
     生成时间: 2026-04-18T16:59:04Z -->

# 输入零摩擦原则

> 用户能上传的，绝对不让用户手输。无法确定的，两个都给。

## 强制规则

1. **上传优先**：当输入内容可以从文件中获取时（文档、方案、配置、代码），必须提供文件上传入口，不能只给一个空白输入框
2. **双通道并存**：无法确定用户是想手输还是上传时，**两个都提供**——上传区 + 输入框同时存在，用户自选
3. **禁止空白发呆**：用户面对空白输入框超过 3 秒找不到方向 = 体验缺陷。必须通过以下至少一种方式消除：
   - 文件上传（拖拽 / 点击选择）
   - 预填模板 / 示例内容
   - 快捷选项卡片（从已有数据中选择）

## 判断清单

设计输入表单时，逐条检查：

| 检查项 | 做法 |
|--------|------|
| 这个字段的内容可能已经存在于文件中？ | 加文件上传 |
| 这个字段可以从系统已有数据中选择？ | 加选择器（下拉 / 卡片） |
| 这个字段用户可能不知道该写什么？ | 加模板 / 示例 / placeholder 引导 |
| 以上都不确定？ | 上传 + 手输 + 示例，三个都给 |

## 输入方式优先级

```
选择已有数据（零输入） > 上传文件（低摩擦） > 模板预填（中摩擦） > 空白手输（高摩擦）
```

## 典型场景

| 场景 | 错误做法 | 正确做法 |
|------|---------|---------|
| 涌现树种子内容 | 只有 textarea | 上传文档 + 选择已有文档 + 手输，三通道 |
| PRD 导入 | 让用户粘贴全文 | 拖拽上传 PDF/Word/Markdown |
| 配置导入 | 让用户逐字段填写 | 上传 JSON/YAML + 表单编辑 |
| 提示词编写 | 空白输入框 | 模板选择 + 在模板基础上编辑 |

## 与「2 秒原则」的关系

- **2 秒原则**：用户等待 AI 响应时，屏幕必须有持续变化（解决等待焦虑）
- **零摩擦原则**：用户面对输入时，必须有明确的行动路径（解决输入焦虑）
- 两者共同目标：**用户在任何时刻都不应该盯着屏幕发呆**
