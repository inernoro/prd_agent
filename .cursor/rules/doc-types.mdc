---
globs: doc/**/*.md
description: doc/ 下文档 6 种类型前缀（spec/design/plan/rule/guide/report）
---

<!-- 本文件由 scripts/sync-cursor-rules.sh 自动生成，请勿直接编辑。
     事实源: .claude/rules/doc-types.md
     生成时间: 2026-04-18T16:59:04Z -->


# doc/ 文档类型规则

`doc/` 下所有 `.md` 文件必须属于以下 6 种类型之一，禁止发明新前缀。

| 前缀 | 对应 | 核心问题 | 子类型 |
|------|------|---------|--------|
| `spec.*` | **What** | 做什么 | 产品规格、Agent 文档、用户故事 |
| `design.*` | **How** | 怎么做 | 技术设计、技术分析 |
| `plan.*` | **When** | 什么时候做 | 实施计划 |
| `rule.*` | **Why not** | 为什么不能那样做 | 规范约定、审计报告 |
| `guide.*` | **How-to** | 怎么操作 | 操作指南、备忘录 |
| `report.*` | **What happened** | 做了什么 | 周报 |

## design.* 特别要求

- **管理摘要必填**：第一节必须是管理摘要，30 秒让非技术读者看懂方案全貌
- **受众分层**：前四节（摘要 → 背景 → 目标 → 决策）面向所有读者，禁止代码；技术章节允许代码但需有上下文
- **代码有节制**：全文代码不超过 30%，每段代码前必须有一句话说明，禁止连续代码堆砌
- **故事靠前，设计靠后**：章节顺序为 管理摘要 → 产品定位 → 用户场景 → 核心能力 → 架构 → 数据设计 → 接口设计 → 关联文档 → 风险。人能看懂的在前，接口字段在后
- **永远替换，不留历史**：设计文档不需要版本迭代说明（如"v2.0 变更：xxx"），每次更新直接覆盖全文。版本追踪由 `changelogs/` 碎片和 Git 历史承担
- **按应用归属，不重叠**：一个应用/领域对应一个 design 主文档。子功能可以有独立的详细设计文档，但必须从主文档的「关联设计文档」章节引用，不得重复描述同一功能

详细模板见 `doc-writer` 技能（`/doc`）和 `doc/rule.doc-templates.md`。
